# Instruction-budget suppression list (prompt-hygiene REQ-B1.3).
#
# scripts/check-instructions.sh reads this file as DATA (never evaluated,
# REQ-B1.9). Blank lines and lines beginning with '#' are ignored. Two forms
# exist; each REQUIRES a recorded reason (a reason-less entry of either form is
# an error, REQ-B1.3 / REQ-B1.8):
#
#   exempt|<path>|<reason>
#       Permanent per-file-floor exemption ONLY. It suppresses a file's per-file
#       floor and NEVER the mandatory-at-start or reachable-closure budgets
#       (REQ-B1.3a). Carries a standing rationale.
#
#   pending-diet|<budget>|<target>|Task <N>|<reason>
#       Transitional allowance. The only form that may cover a mandatory-at-start
#       or reachable-closure offender, and only transiently. Removed by that
#       diet task's own PR, and forbidden at closeout (REQ-B1.3b, REQ-D1.4).
#       <budget> is one of: file | start-load | closure
#       <target>  is a file path (file) or a skill name (start-load | closure)
#
# Seeded below: the per-file offenders the initial audit surfaced, each carrying
# a transitional allowance until its diet task lands (Tasks 5/6/7). The
# start-load allowance for /spec-draft is seeded at Task 3, when manifests first
# make start-load computable — not here, where no manifest yet exists.
pending-diet|file|skills/orchestrate/SKILL.md|Task 5|/orchestrate SKILL.md is over the per-file SKILL floor; dieted in Task 5 (the /orchestrate pilot).
pending-diet|file|skills/execute-task/SKILL.md|Task 6|/execute-task SKILL.md is over the per-file SKILL floor; dieted in Task 6.
exempt|doctrine/spec-format.md|Permanent per-file-floor exemption (REQ-B1.3a), Task 7 disposition. spec-format.md is the meta-spec: its authorable-from-alone contract requires it to stay self-contained (a compliant four-file bundle is authorable from this doc alone), and a compliant trim removes only ~99 words against the ~570-word gap to the 4,000 floor without weakening that contract, so it is exempted rather than trimmed. Start-load coupling: spec-format.md is the dominant run-start load for /spec-draft and /spec-kickoff, but a permanent per-file exemption never suppresses the mandatory-at-start or reachable-closure budgets, so it does not strand those dependents — their start-load compliance rests on Task 7.5's point-of-use reclassification of their non-gating doctrine reads, not on shrinking this doc.

# Start-load allowances seeded at Task 3, when manifests first made mandatory-at-start
# computable (REQ-A1.2, REQ-B1.3b). Each start-load offender the manifest computation
# surfaced is carried transiently until its diet lands; a start-load offender has no
# permanent-exemption escape valve (REQ-B1.3a forbids it), so this is the only form
# that can keep the check green while the diet is pending. No reachable-closure offender
# surfaced (every skill's closure is under the 20,000 error threshold), so no closure
# allowance is seeded. Each is removed by its diet task's own PR (Tasks 5/6 shed both
# their per-file and start-load allowances). /spec-kickoff's Task 7 diet sheds its
# per-file allowance, but its start-load is led by spec-format — gating law REQ-C1.2
# forbids deferring — so its start-load allowance is carried to Task 7.5's point-of-use
# reclassification of the non-gating reads. /spec-draft's start-load diet is Task 7.5 too.
pending-diet|start-load|orchestrate|Task 5|/orchestrate mandatory-at-start is over budget (body + run-start doctrine); dieted in Task 5 (the pilot: content moved to rule docs, rare mode branches reclassified to point-of-use).
pending-diet|start-load|execute-task|Task 6|/execute-task mandatory-at-start is over budget (body + run-start doctrine); dieted in Task 6.
pending-diet|start-load|spec-kickoff|Task 7.5|/spec-kickoff mandatory-at-start is over budget. Task 7 dieted the SKILL body (per-file allowance shed) but the start-load is dominated by run-start doctrine led by spec-format (gating law that REQ-C1.2 forbids deferring to point-of-use), so start-load compliance rests on Task 7.5's point-of-use reclassification of the non-gating lens-pass reads (discovery-rigor, autopilot-reflex, validation-rigor); carried here until Task 7.5 lands.
pending-diet|start-load|spec-draft|Task 7.5|/spec-draft mandatory-at-start is over budget purely through run-start doctrine (body under the per-file floor, so invisible to Task 2's pre-manifest audit); dieted in Task 7.5 by point-of-use reclassification.
