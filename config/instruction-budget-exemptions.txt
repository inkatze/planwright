# Instruction-budget suppression list (prompt-hygiene REQ-B1.3).
#
# scripts/check-instructions.sh reads this file as DATA (never evaluated,
# REQ-B1.9). Blank lines and lines beginning with '#' are ignored. Four forms
# exist; each REQUIRES a recorded reason (a reason-less entry of any form is
# an error, REQ-B1.3 / REQ-B1.8):
#
#   exempt|<path>|<reason>
#       Permanent per-file-floor exemption ONLY. It suppresses a file's per-file
#       floor and NEVER the mandatory-at-start or reachable-closure budgets
#       (REQ-B1.3a). Carries a standing rationale.
#
#   pending-diet|<budget>|<target>|Task <N>|<reason>
#       Transitional allowance. The only form that may cover a mandatory-at-start
#       or reachable-closure offender, and only transiently. Removed by that
#       diet task's own PR, and forbidden at closeout (REQ-B1.3b, REQ-D1.4).
#       <budget> is one of: file | start-load | closure
#       <target>  is a file path (file) or a skill name (start-load | closure)
#
#   declared-exception|<surface>|margin=<N>|<reason>  (margin-bearing surfaces)
#   declared-exception|<surface>|<reason>            (use-site: surfaces only)
#       RESERVED PREFIX: on a margin-bearing surface the first pipe-delimited
#       segment after the key is the margin field, so a reason cannot begin with
#       a segment reading `margin=...`. Such a reason is read as a malformed
#       margin and refused. Everything else is reason text, pipes included; put
#       any leading `margin=` wording later in the sentence, or after a pipe.
#       margin=<N> is the surface's headroom margin when the exception was
#       granted. It is a ratchet: the deferral may stand, but a later run
#       whose margin falls BELOW it is a widening and a fail-closed error,
#       answered by a compensating trim or by governed relief, never by
#       editing the number upward. A use-site surface never reaches the
#       headroom check, so it carries no margin and keeps the short form.
#       Standing exception (instruction-headroom D-11, REQ-D1.6) that excuses
#       exactly the warning it names — a below-target warning (whose <surface> is
#       the surface key the warning prints: a file path, or `start-load:<skill>` /
#       `closure:<skill>`) or a use-site warning (via the `use-site:<skill>/<doc>`
#       surface key, REQ-D1.3). It NEVER excuses a floor-breach warning. An entry
#       whose named warning no longer fires is stale — a named cleanup WARNING,
#       not an error (staleness in the protective direction nudges, never blocks).
#
#   raise|<knob>|<value>|<reason>
#       The recorded rationale for a budget raise (instruction-headroom D-12,
#       REQ-A1.4). Required when an effective instruction_budget_*_warn /
#       *_error knob exceeds its shipped core default; <value> must match the
#       effective value. A raise with no matching entry, an absent/unreadable
#       core-default baseline, or a stale entry (its knob at or below the core
#       default, or unknown) is a fail-closed guard error. Floor knobs (*_floor)
#       are protective and outside this rule.
#
# Originally seeded below: the per-file offenders the initial audit surfaced,
# each carrying a transitional allowance until its diet task landed (Tasks
# 5/6/7). Those allowances are now shed; what remains is spec-format.md's
# permanent per-file-floor exemption (Task 7 disposition). The start-load
# allowance for /spec-draft was seeded at Task 3, when manifests first made
# start-load computable — not here, where no manifest yet existed.
exempt|doctrine/spec-format.md|Permanent per-file-floor exemption (REQ-B1.3a), Task 7 disposition. spec-format.md is the meta-spec: its authorable-from-alone contract requires it to stay self-contained (a compliant four-file bundle is authorable from this doc alone), and it sits far enough over the 4,000 per-file error threshold that no compliant trim closes the gap without weakening that contract, so it is exempted rather than trimmed. No word figure is transcribed here — the doc's own audit line prints the honest actual every run, and a copied count goes stale the next time the meta-spec grows (format-grammar Task 5 grew it again; the figure this entry used to carry was ~2,900 words out of date by then). Aggregate coupling: under the capped-charge law (instruction-headroom D-4) every dependent mandatory-at-start and reachable-closure aggregate charges this doc at min(actual, its per-file error threshold) — the doctrine error knob, 4,000 at the shipped default — not its larger actual, so the overage never cascades and never strands a dependent; the doc's own audit line still prints the honest actual beside the charged threshold value.

# Standing below-target exceptions for /orchestrate (instruction-headroom D-11,
# REQ-D1.6). Two additions put the surfaces here. fleet-hardening Task 9 (#271)
# added the observation-carry step to skills/orchestrate/SKILL.md after this
# spec's restoration campaign brought the body to its 2x target; model-allocation
# then added the terminal-state reports and funded them with an in-file trim, so
# it left both surfaces essentially where it found them. Both sit below their 2x
# restoration targets with the safety floors intact. The entries below now DO
# carry a margin figure, and the reasoning that once argued against transcribing
# one is what makes it safe: a copied count rotted because nothing checked it,
# and the guard compares these every run, so a stale number fails the check
# instead of quietly misinforming a reader. The spec-format.md exemption keeps
# the older posture, having no margin to be held to. So this is a below-target
# shortfall, never a floor-breach.
# The diet back to target is deferred to instruction-headroom relief rather than
# trimming either spec's just-landed doctrine here; these exceptions keep the
# closing gate honest about the state without silently deferring gating law (the
# surfaces are
# named, the cause recorded, and a future diet makes each entry stale and
# self-reporting).
declared-exception|skills/orchestrate/SKILL.md|margin=251|Below the 2x per-file restoration target with the per-file floor intact; the margin field above is the figure this entry is held to, and the ratchet is what makes transcribing one safe here where it would not be elsewhere: the guard compares it every run, so a stale number fails the check instead of quietly misinforming a reader. fleet-hardening Task 9 (#271) grew the /orchestrate body with the observation-carry step. model-allocation then added the terminal-state reports to the reconcile (REQ-F1.2, D-11) — the escalation feedback loop had no caller at all until then, and the reconcile is the only pass that observes both of a unit's terminal states — and funded them in the same change per REQ-E1.1's compensating-trim path rather than breaching. Five trims paid for it, each a duplication rather than a cut into doctrine: the skill stopped restating orchestrate-relay.sh's relay/observe internals, which doctrine/inter-orchestrator-coordination.md owns and this skill already cites point-of-use; the unattended-mode paragraph went, its substance already carried by the --unattended flag line and the worktree step's unattended clause (the cron/launchd/CI enumeration is the one nuance not restated verbatim elsewhere); the halt protocol is stated once, in its own section, instead of in both that section and the pre-flight preamble; the --bookkeeping mode bullet points at the section that enumerates its passes instead of previewing them; and the print and in-session bullets became one manual-dispatch bullet, the tmux bullet states the send-keys rule once instead of restating it at every clause, and the subagent bullet took the tighter wording that names both what the shipped worker profile pre-approves and what it denies. The constraint therefore stands unchanged for whatever lands next: relief or its own compensating trim, never a silent breach or a widening of this entry. Diet back to target deferred to instruction-headroom relief; do not trim #271's doctrine or the terminal-state reports here.
declared-exception|closure:orchestrate|margin=1002|Below the 2x closure restoration target with the closure floor intact; the margin field above carries the figure, held honest by the ratchet for the reason the per-file entry above records. Same two causes and the same compensating trim: fleet-hardening Task 9 (#271) grew the /orchestrate body, and model-allocation's terminal-state reports funded themselves in-file. Diet back to target deferred to instruction-headroom relief.

# Standing below-target exception for /spec-kickoff (instruction-headroom D-11,
# REQ-D1.6). skill-rigor Task 5 added the sign-off pre-flip verification step
# (the REQ-B1.2 lint over the brief and edited spec files, the REQ-B1.3 recorded-
# claim re-derivation) and Task 6 added the terminal ready-flip CI gate (REQ-B1.1,
# D-3). Task 5 alone left the body below its 2x per-file restoration target with
# the safety floor intact (a below-target shortfall). Task 6's CI-gate mechanics
# would have breached the safety floor, so per skill-rigor R1 / REQ-E1.1 (relief
# OR a compensating trim in the same change — never a silent breach or an
# exemption edit) Task 6 lands the trim structurally: its ~200 words of gate
# mechanics were RELOCATED out of the skill body into `doctrine/kickoff-
# verification.md` (resolved point-of-use), leaving only a lean reference inline.
# That keeps the safety floor intact — still a below-target shortfall (margin
# ~253 >= floor 250), never a floor-breach. The residual below-target gap is the
# unavoidable inline residue (the config-read knob, the one-line gate reference,
# the manifest + degrade entry); trimming the just-restored (Task 5) or existing
# spec-kickoff doctrine to reabsorb it would risk its meaning, so the diet back
# to the 2x target is deferred to instruction-headroom relief. operator-dialogue
# Task 3 (a further same-file addition — the interaction-discipline instantiation)
# lands the same way: its heavy prose is relocated into
# `doctrine/kickoff-dialogue.md` and procedural flow is compressed to offset the
# lean inline references, so the surface stays below-target with the floor intact.
# The surface is named and the cause recorded, so the closing gate stays honest.
# anchor-integrity Task 7 (the sign-off enumeration cross-check) lands the same
# way and leaves the surface sitting exactly ON its floor, which is why the entry
# below carries the margin it was granted at rather than omitting one: a copied
# number went stale because nothing compared it, and the guard now compares this
# one every run, so a stale figure fails the check rather than misinforming
# quietly — the inverse of the failure the spec-format.md exemption records, and the
# one D-8 names in general.
# anchor-integrity Task 6 (the kickoff terminal re-anchor) is the next addition
# Task 7's entry anticipated, and it brings the compensating trim rather than
# relief. The ritual's mechanics are meta-spec law binding every writer of an
# anchor entry, so they went to doctrine/spec-format.md, whose permanent
# exemption and capped aggregate charge keep them off every dependent's
# start-load and closure budgets; only a pointer lands in the finishing ritual.
# The pointer alone still outweighed the nothing that was left, so it is funded
# in-file as well: the sign-off record step stopped restating the writers rule
# the same sentence already cites spec-format for.
declared-exception|skills/spec-kickoff/SKILL.md|margin=262|Below the 2x per-file restoration target with the per-file floor intact; the margin field above is the figure this entry is held to, and the ratchet is what makes transcribing one safe here where it would not be elsewhere: the guard compares it every run, so a stale number fails the check instead of quietly misinforming a reader. skill-rigor Tasks 5 and 6 added the sign-off pre-flip verification step and the terminal ready-flip CI gate, relocating those gate mechanics into doctrine/kickoff-verification.md per REQ-E1.1's compensating-trim path; operator-dialogue Task 3 then instantiated the interaction disciplines in-band, relocating the discipline-instantiation prose, the shared-understanding approval summary, the plain-language gate framing, and the structured decision/transcript log schema into doctrine/kickoff-dialogue.md and compressing procedural flow to offset the added references, keeping the surface below-target rather than floor-breaching. anchor-integrity Task 7 then added the sign-off enumeration cross-check and funded it with an in-file diet of redundant prose, landing the surface exactly ON its floor. anchor-integrity Task 6 (the kickoff terminal re-anchor) is the next addition that clause anticipated and carries its own compensating trim in the same change (REQ-E1.1): the ritual's mechanics live in doctrine/spec-format.md, whose permanent exemption and capped aggregate charge keep them off every dependent's budgets, so only a pointer lands in the finishing ritual, and the sign-off record step stopped restating the writers rule the same sentence already cites spec-format for. The margin is back above the floor but only barely, so the constraint stands unchanged for whatever lands next: relief or its own compensating trim, never a silent breach or a widening of this entry. Diet back to target deferred to instruction-headroom relief; do not trim the just-restored spec-kickoff doctrine to reabsorb the residual.

# The start-load allowances seeded at Task 3 (when manifests first made
# mandatory-at-start computable, REQ-A1.2, REQ-B1.3b) are all shed: Tasks 5/6
# removed /orchestrate's and /execute-task's in their own diet PRs, and
# Task 7.5's point-of-use reclassification of the non-gating doctrine reads
# brought /spec-kickoff and /spec-draft under the start-load budget, removing
# the last two. No transitional `pending-diet` allowance remains (the REQ-D1.4
# closeout direction); the live directive entries are the permanent spec-format
# per-file exemption above, the two /orchestrate below-target declared
# exceptions (cross-spec headroom collision, fleet-hardening #271), and the
# /spec-kickoff below-target declared exception (skill-rigor Tasks 5 and 6, whose
# required steps spend this spec's own headroom; Task 6 relocated its gate
# mechanics into doctrine/kickoff-verification.md per REQ-E1.1 to stay below-
# target rather than floor-breach — the residual diet awaits instruction-headroom
# relief, R1).

# inception Task 3 (REQ-I1.2, REQ-I1.3) grew doctrine/decision-domains.md by
# eight non-engineering domains and added the artifact-class lens-selection
# reference to discovery-rigor / validation-rigor. The catalog's prose entries
# are its normative home (the yaml is only the machine view), so the growth
# lands where the consumers load it. Every affected surface stays ABOVE its
# headroom floor; only the 2x restoration target is missed, so these are
# below-target exceptions, never floor-breach ones.
declared-exception|start-load:builder|margin=515|Below the 2x start-load restoration target after inception Task 3 added eight non-engineering decision domains to doctrine/decision-domains.md, which /builder loads at run-start; the start-load floor (500) is not breached. The catalog's prose is its normative home and the eight domains are the REQ-I1.2 deliverable, so the load is contract, not slack: the entries were already trimmed to the seed entries' density and the seam list de-linked to hold the floor. Diet back to target deferred to instruction-headroom relief; do not trim the catalog to reabsorb it.
declared-exception|start-load:self-review|margin=566|Below the 2x start-load restoration target after inception Task 3 added the artifact-class lens-selection reference to doctrine/discovery-rigor.md and doctrine/validation-rigor.md, both /self-review run-start docs; the start-load floor (500) is not breached. The surface was already within 5 words of the target before this task (baseline margin 1005), so any REQ-I1.3-conforming addition trips it; the non-code lens SETS live in doctrine/artifact-lenses.md precisely so only the selection reference is charged here. Diet back to target deferred to instruction-headroom relief.
declared-exception|closure:spec-kickoff|margin=1002|Below the 2x closure restoration target with the closure floor intact; the margin field above is the figure this entry is held to, and the ratchet is what makes transcribing one safe here where it would not be elsewhere: the guard compares it every run, so a stale number fails the check instead of quietly misinforming a reader. inception Task 3 grew doctrine/decision-domains.md, discovery-rigor.md, and validation-rigor.md — all three point-of-use docs in /spec-kickoff's manifest (the sign-off gap check and lens pass); same disposition as the sibling entries above, the growth is the REQ-I1.2 / REQ-I1.3 deliverable, already density-trimmed. anchor-integrity Tasks 7 and 6 then grew the /spec-kickoff body, which this aggregate charges too, taking the closure margin down alongside the per-file one. Task 6's mechanics went to doctrine/spec-format.md instead, charged at its per-file error threshold under the capped-charge law (instruction-headroom D-4), so that relocation costs this aggregate nothing and only the pointer is charged here. Diet back to target deferred to instruction-headroom relief.

# Standing below-target exception for /execute-task (instruction-headroom D-11,
# REQ-D1.6). Two specs grew the same convergence step and arrived together in
# this merge, so ONE entry covers both — the surface is the file, not the task,
# and a second entry naming the same surface would suppress nothing extra while
# going stale independently. merge-currency-guard Task 3 (REQ-B1.1, REQ-B1.5,
# D-4) adds the convergence-loop `main`-sync call, its mechanism offloaded to
# scripts/converge-sync-main.sh so the body grows by one instruction rather than
# a paragraph, and the residue already at the floor of what the requirement's
# three clauses can say (run it once per pass at the top; halt on a non-zero
# exit with its reason; the PR still only opens draft). anchor-integrity Task 5
# (D-5, REQ-C1.1-C1.3) adds the act-on-findings re-anchor ritual to that same
# step and to the in-flight-amendment section: the stale-anchor pre-flight and
# the binding of a convergence finding that edits the spec to that ritual. It
# too was minimized structurally — the pre-flight sits in the amendment section
# that already states the expression-only and meaning-class lanes rather than
# being restated beside them, the convergence paragraph is a one-line binding,
# and the general doctrine statement went to doctrine/spec-format.md, whose
# permanent per-file exemption and capped aggregate charge keep it off every
# dependent's start-load and closure budgets. Each task entered its own baseline
# a handful of words above the 2x target, so either addition alone trips the
# below-target warning and the pair widens the shortfall; the per-file floor
# stays intact, so this is a below-target shortfall, never a floor-breach. Only
# the per-file surface is affected — start-load and closure both stay above
# target. What remains, on both counts, is gating law.
declared-exception|skills/execute-task/SKILL.md|margin=251|Below the 2x per-file restoration target after merge-currency-guard Task 3 added the convergence-loop `main`-sync invocation (REQ-B1.1, REQ-B1.5, D-4) and anchor-integrity Task 5 added the act-on-findings re-anchor ritual (D-5, REQ-C1.1-C1.3), both landing on the convergence step and merged together here; the per-file floor (250) is not breached. One entry covers both because the surface is the file. Each was already minimized structurally — the sync mechanism lives in scripts/converge-sync-main.sh so only the one-line call is charged here, and the re-anchor pre-flight sits in the in-flight-amendment section rather than restating the lanes beside it — and each entered its own baseline within a handful of words of target, so no conforming form of either addition holds it. Diet back to target deferred to instruction-headroom relief; do not trim the sync instruction, the re-anchor ritual, or the surrounding convergence law to reabsorb it. Carrying both additions leaves the margin close enough to the floor that the next same-file addition has to bring relief or a compensating trim in the same change (REQ-E1.1) rather than widen this entry again.

# Reachable-closure budget raise for the tower conversation rule doc
# (tower-comms D-13, REQ-I1.3; instruction-headroom D-12, REQ-A1.4). The knob
# lives in .claude/planwright.yml (the repo-tracked layer; the core default in
# config/defaults.yml is the baseline this raise is measured against and stays
# as shipped). The value is the shipped default plus the doc's word count as
# `wc -w` measures it, which is what the guard charges; a later edit to the doc
# that changes its size re-sizes this raise in the same change, or the tower
# closure exception ratchet reports the shortfall.
raise|instruction_budget_closure_error|20449|Raised by the measured size of doctrine/tower-comms.md (449 words at the time of the raise), the conversation rule doc the tower skills read at point of use. Both tower closures (orchestrate and spec-kickoff share the figure) sat exactly on the closure floor before this doc existed, held there by declared exceptions whose margin ratchet turns any further point-of-use load into a fail-closed widening, so the doc could not land at the shipped budget. Why the rungs below a raise did not apply: rung 1, diet, was measured unreachable on the orchestrate closure (the headroom bundle's diet already harvested the duplication, and the two clearest remaining restatement sites yielded a few dozen words against the roughly a thousand the restoration target needs; D-13 records the diet as run and the room as spent again); rung 2, point-of-use reclassification, cannot lower a reachable-closure figure at all, because the closure charges point-of-use docs as well as run-start ones, and this doc is already point-of-use by design; rung 4, exemption, is per-file only and this is an aggregate surface. What this raise does not claim: the charged closure figure is already below real load, because doctrine/spec-format.md is charged at its per-file cap under its permanent exemption while its actual size is far larger, so the raised threshold bounds the charged figure, not the words a tower session loads. The raise is sized to the doc and nothing else: the manifest line and the one-line gist that wire it into a tower skill are words in that skill body, charged to the body and to the closure alike, and are funded by a compensating trim in the same skill body, which its per-file margin ratchet forces regardless; the tower closure exceptions keep their granted margins.
