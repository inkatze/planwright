# Observations log

> **Frozen — 2026-07-11.** This shared observations log is superseded by the
> per-observation fragment substrate introduced by `specs/observation-recording`:
> live observations are individual files under `specs/_observations/entries/`
> and consumed ones move to `specs/_observations/archive/`. Recording now routes
> through `scripts/obs-record.sh`; no new lines are added below. The unconsumed
> lines that remain drain in place through `— consumed-by:` annotations written
> by the legacy arm of `scripts/obs-consume.sh --legacy` as normal mining
> consumes them; the file is retired once it holds no unconsumed lines.

- 2026-06-10 [planwright] Repo ships no doc-grounding tooling (no markdownlint/vale/link checker), so `/polish` on spec-only diffs can never produce Auto-applicable findings; Task 2 (quality guards & CI) could include doc linters to unlock autonomous draining on spec bundles.
- 2026-06-10 [planwright] The critical-path claim in `tasks.md`/the brief was computed by hand and was wrong (corrected 2026-06-10); a tiny script (deps + efforts → longest chain) would make critical-path-first selection (REQ-F1.2) verifiable instead of asserted.
- 2026-06-11 [planwright] Claude Code provides `${CLAUDE_PLUGIN_DATA}` (`~/.claude/plugins/data/<id>/`, persists across plugin updates) — candidate durable home for T13's advisory locks and any framework runtime state in plugin delivery; plugin root is ephemeral per-version.
- 2026-06-11 [planwright] Bash `[a-z]` range globs are locale-dependent (UTF-8 collation admits uppercase); caught by Task 1's resolver test. Every portable script doing charset validation (T5 validator, T6 hooks, T10 gate parser) should pin `LC_ALL=C`; worth a line in the engineering doctrine (T15) or a shared shell prelude.
- 2026-06-11 [planwright] `shfmt` is absent on the dev host while Task 2 plans it in CI; the tool-discovery hook (T6) could flag locally-missing CI tools to close the local-vs-CI gap.
- 2026-06-11 [planwright] The ~/.claude/ writer refresh-copies and never deletes: files removed/renamed in a newer planwright persist as ghosts under the installed namespace. T19 (packaging finalization) should design the upgrade/cleanup story (manifest-of-installed-files or versioned namespace); surfaced by panel review, human chose document-and-defer.
- 2026-06-11 [planwright] REQ-D1.1 does not define fan-out partial-failure handling (a lens sub-agent that dies or returns nothing leaves a silent coverage hole the lens-coverage table cannot show); candidate spec amendment surfaced while migrating Discovery Rigor (T3).
- 2026-06-11 [planwright] REQ-C1.7's resolution ladder is silent on partial answers, contradictions between rungs (brief vs research), and unevaluable rungs; candidate spec amendment surfaced while migrating Finding Categorization (T3).
- 2026-06-11 [planwright] Under REQ-F1.9's interim whole-file anchor, after any tower state move every in-flight worker's recomputed pre-flight anchor disagrees with main's latest anchor entry (observed at T7 pre-flight: T15's dispatch re-anchor landed on main after T7's worktree was cut); a literal halt-on-mismatch implementation in T12/T13 would false-halt; per-file digest comparison (divergence confined to tasks.md placement content) is the workable interim check until Task 4's canonical extraction ships.
- 2026-06-11 [planwright] Concurrent install.sh runs into the same claude dir can interleave copies (mkdir+cp are not atomic); immaterial for the v1 stub but T19's upgrade/cleanup design should consider temp-then-move or an install lock alongside the ghost-file fix.
- 2026-06-11 [planwright] Repo now carries mise.toml (pinned shellcheck/markdownlint-cli2/yamllint/gitleaks + `mise run check`) and a minimal .markdownlint.jsonc (MD013 tables exemption); T2 should reuse these in GitHub Actions (jdx/mise-action keeps local and CI tool versions identical) and owns widening lint targets to specs/ plus the per-guard seeded-violation fixtures.
- 2026-06-11 [planwright] Default-config markdownlint flags ~a dozen MD013 line-length hits in specs/*.md (wide tables aside, a few genuinely long prose lines in test-spec.md); T2's config decision: exempt specs, raise line_length, or wrap at next spec edit.
- 2026-06-11 [planwright] Doctrine gap surfaced by the T1 tooling pass: the builder's core guard catalog (REQ-G1.2) and the engineering doctrine name the quality guards but never the meta-guard that the toolchain running them must be version-pinned/reproducible — tool-grounded discovery silently assumes version reproducibility (different tool versions fire different rules). T15/T16 should add a catalog entry: pin via the ecosystem-native mechanism (devDependencies/Gemfile/uv), with mise as the current de-facto polyglot default; principle-not-mandate per REQ-G1.6, and distinct from REQ-K1.5 (planwright's own runtime stays pin-tool-free). Evidence: two shim failures mid-review on this host until mise.toml landed.
- 2026-06-11 [planwright] markdownlint baseline settled at MD013 line_length 100 + tables/code_blocks exempt (default 80 fought readable prose; tables and command/path blocks cannot wrap). At this baseline specs/*.md carry only 4 hits, all citation/ID-list lines in anchored files (tasks.md 389/409, test-spec.md 62/321); wrap them at the next meaning-bearing spec edit (expression-only ritual already paid then) rather than a whitespace-only PR, then T2 can widen lint:md to specs/.
- 2026-06-11 [planwright] Doctrine candidate (companion to the toolchain-pinning gap): adopting a tool means owning its defaults. REQ-G1.1's "defer to tooling and ecosystem standards" needs the nuance that tool defaults are the tool author's context, not the project's (80-column line length appeared in two tools' defaults and fit neither); at adoption time review the handful of conventions-bearing defaults, record deviations with rationale in tracked config, and accept the rest (no flattening: only conventions-bearing defaults surface as decisions). The builder (T16) should do this when applying guards; concrete instances this repo hit: MD013/yamllint line length 100, MD013 tables/code_blocks exemptions, gitleaks --redact (the leak detector must not leak into logs), shellcheck optional-checks tier left to T2.
- 2026-06-11 [planwright] Selection-policy gap surfaced by the first orchestrated run (human feedback at the merge queue): critical-path-first (REQ-F1.2) dispatched T3/T4/T7/T15 before T2, so the first six PRs never ran under the CI guards T2 exists to provide, even though T2's own Done-when says "all subsequent tasks merge only under green CI" and the tasks.md intro says foundational work "comes first" — intent encoded in prose, not in dependency edges, so the selector cannot see it. Candidate fixes for a delta walkthrough: (a) a guard-infrastructure-first selection rule (tasks whose deliverables gate other tasks' verification outrank critical-path), (b) soft/ordering edges in the task format ("Should-precede:"), or (c) authoring guidance that CI/guard tasks get explicit dependency edges from everything they protect. Remediation applied this run: open PR branches get main merged in after T2 lands so CI validates them pre-merge.
- 2026-06-11 [planwright] Task 2 wired `check:specs` as skip-with-notice until the validator exists; Task 5 must flip it to enforced (executable `scripts/spec-validate.sh`, exit-code contract) in the same PR that ships the validator, or CI keeps "passing" with a skip notice.
- 2026-06-11 [planwright] CI action SHA pins (checkout, mise-action) have no auto-update path; when the builder (T16) grows a dependency-adoption guard, pinned-action freshness is a natural catalog entry.
- 2026-06-11 [planwright] Before public release (REQ-J1.5), re-evaluate fork-PR CI isolation: `ci.yml` runs `mise run check` which executes PR-authored task definitions. Safe while private (read-only token, no fork secrets, trusted collaborators only); revisit the isolated-runner pattern and audit for any privileged CI steps (currently none) at the public-release gate. Belongs on the Task 19 checklist.
- 2026-06-12 [planwright] gate-wiring.md introduces normative mechanisms beyond the REQ-level Gherkin (the `[pending-sign-off]` commit marker, checklist regeneration scope and source of truth, PS-ID stability, ladder-audit row format, pause keep-commits, zone-screen-first order) with no verification home in test-spec; when T11 makes them executable, add wiring-level fixtures or amend the REQ-C1.3/C1.5 test-spec entries (surfaced by /self-review on T7's branch).
- 2026-06-11 [planwright] `scripts/spec-anchor.sh` (canonical anchor form) now ships; the kickoff brief's anchor entries still use the interim whole-file form. `/spec-kickoff` (Task 9) and the emulated orchestrate state-move re-anchors can switch to the canonical form, ending the forced re-anchor on every tasks.md state move.
- 2026-06-12 [planwright] Practice gap surfaced by the SSH-agent outage during the orchestrated run: there was no machine-local environment layer, so session plumbing (SSH_AUTH_SOCK) leaked in from whatever context launched each worker and went stale on reconnects. Adopted: a gitignored repo-root `mise.local.toml` ([env] only, per-machine paths, never secrets or repo content) that every worktree inherits via mise's ancestor-directory config loading — possible because D-37 places worktrees inside the repo; plus `trusted_config_paths` covering the repo prefix so fresh worktrees need no per-worktree `mise trust`. Candidates to encode it: the builder's guard catalog (T16, "local env layer + worktree trust" as a stack-detection entry) and the config-model docs (D-33 distinguishes planwright's own local overrides; the project-env equivalent deserves a sibling mention).
- 2026-06-12 [planwright] Task 8 pinned the spec-worktree directory name as `<spec>-spec` (for branch `planwright/<spec>/spec`) in the /spec-draft skill, because the literal branch suffix `spec` (D-37's `<branch-suffix>` placement reading) collides across specs; spec-format.md's branch/worktree section and D-37 name only the task-branch case, so the meta-spec could state the spec-branch worktree name explicitly — Task 9 (/spec-kickoff) must locate the worktree by the same convention.
- 2026-06-12 [planwright] REQ-B1.3 names the fold-detection scan set as "existing Active/Draft specs" while /spec-draft (Task 8) scans all non-terminal statuses (Draft/Active/Done) so a duplicate of a finished feature triggers the REQ-A3.1 reopen cycle; human-resolved at panel pairing 2026-06-12 as a floor reading (the REQ does not say "only"). At the next meaning-bearing edit to requirements.md, REQ-B1.3 could name the non-terminal set explicitly so the floor reading stops carrying the load.
- 2026-06-12 [planwright] /polish (T11) operationally pinned the loop bounds the Amendment 2 backlog left open: convergence = an iteration with zero new dispositions, safety cap = 10 iterations (pair-flow precedent as rung-3 convention). The backlog item "iteration cap + convergence criterion" now has a shipped answer to canonize or change at the next amendment.
- 2026-06-12 [planwright] /polish's loop ledger has no resume-after-kill semantics (a killed loop restarts at iteration zero with an empty ledger; prior dispositions are only partially reconstructible from [pending-sign-off] commits and the committed audit record). Same class as the "resumable partial brief" backlog item; candidate for the same amendment that pins the cap/convergence criterion.
- 2026-06-12 [planwright] Gate task atoms resolve ids from canonical `### Task <id> —` headings; a non-canonical variant like `### Task 1: title` yields id `1:` and a false unknown-task MALFORMED. Task 5's validator should enforce the canonical heading form so drain-gate task atoms stay reliable (surfaced by Task 10's polish re-probe).
- 2026-06-12 [planwright] Operational protocol distilled from the emulated orchestrator run, for Task 13's implementation (companion to the guard-first-selection and machine-local-env entries): (1) division of labor — the tower owns tasks.md state moves, dispatch, and merged-window cleanup; branch updates and conflict resolution on a task branch belong to the owning worker session, with the tower relaying attributed instructions (worker briefs should include the post-merge self-sync ritual: merge main, union-merge accumulator files, take main's state files, verify zero conflict markers, push). (2) Relay mechanics under the tmux backend — clearly-attributed messages delivered via tmux load-buffer/paste-buffer (send-keys with quoted payloads mangles content); never submit text already sitting in a worker's input line (suggested prompts are indistinguishable from typed text); never answer worker permission prompts. (3) Dispatch-time environment hardening — launch panes via a umask-pinning wrapper (a recovered tmux server spawned panes with umask 0117, breaking every scratch dir the harness created), pre-trust worktree config paths, and verify the SSH agent indirection is alive before any signed state-move commit. (4) The REQ-F1.1 positive-evidence-of-death predicate proved its worth in reverse: a dead tmux socket plus a truncated process listing mimicked dead workers that were in fact alive; the sweep must distinguish lost observability from observed death before orphaning. (5) The tower's standing instructions, maintained as the self-pacing loop's wake prompt, double as the disposable-tower handover document (D-7).
- 2026-06-12 [planwright] spec-validate.sh membership checks (set_in) are O(n*m) grep passes per bundle; measured ~1.4s over specs/ today, acceptable, but worth a single-pass awk rewrite if bundle/REQ counts grow substantially.
- 2026-06-12 [planwright] Decided at panel-pairing handoff (task 5, PR #9): format-version 1 parsers (spec-validate.sh + spec-anchor.sh canonical extraction) are not code-fence aware; a fenced mock heading/bullet at column 0 parses as a real element (reproduced: false duplicate-REQ error). Human-selected resolution: full grammar amendment — fence-aware grammar in the meta-spec extraction definition plus BOTH scripts in lockstep, meaning-class, via /spec-kickoff delta re-walkthrough as a follow-up. Until then bundles must not place column-0 spec-shaped lines inside fences.
- 2026-06-12 [planwright] Two scripts now parse the flat-YAML config independently (check-options-reference.sh walks defaults.yml; tasks-pr-sync.sh sed-extracts stale_lock_threshold from the local override); when T13's /orchestrate needs config reads (dispatch_backend, max_parallel_units, commit_on_state_move), extract a shared scripts/ config-get helper (defaults + local-override precedence per D-33) instead of a third ad-hoc parser.
- 2026-06-12 [planwright] The Amendment 2 backlog item "hook lock-failure path needs a named recovery" now has a shipped interim answer (tasks-pr-sync: busy lock → drop the event, --bookkeeping reconciles; stale lock → break at stale_lock_threshold); canonize or change it at the amendment that pins the D-10 lock semantics for T13.
- 2026-06-12 [planwright] /spec-kickoff (T9) operationally pinned the "resumable partial brief" mechanism the Amendment 2 backlog flagged as having no REQ/design home: per-section `Signed off:` lines are the resume key (resume at the first unsigned section), and the anchor-written-last rule makes a killed sign-off fail closed as absent-anchor. A shipped answer to canonize (or strike from Task 9's Done-when) at the next amendment, same class as /polish's loop-bound pinning.
- 2026-06-12 [planwright] drain-gates.sh shipped fence-aware gate/task-id parsing (Task 10 self-review) ahead of the decided fence-grammar amendment; when the amendment lands, fold the gate evaluator's fence rule (column-0 fences toggle illustration mode) into the same meta-spec grammar definition so all three parsers cite one normative rule.
- 2026-06-12 [planwright] Self-review of T9 (tests lens) found three /spec-kickoff behaviors with thin test-spec homes: pre-flight amendment-mode selection (REQ-A3.3 [manual] covers the ritual, not the routing), the skill-written expression-only anchor entry (REQ-F1.10 fixtures verify entry validity, not writer production), and the gap-check degradation when the decision-domains catalog is absent (REQ-G1.4 assumes the catalog exists). Test-spec additions are meaning-class; proposed for the pending fence-grammar delta re-walkthrough rather than this task PR.
- 2026-06-12 [planwright] Anchor-command portability gap: the sanctioned canonical form recorded in anchor entries is the repo-relative `scripts/spec-anchor.sh <spec-dir>`, but adopter repos consuming planwright as a plugin have no scripts/ at the repo root, so recorded entries are not recomputable there as written. T12's gate consumer and T19's packaging should pin a resolution-path-aware command form (or writer-installed scripts) before adopters anchor.
- 2026-06-12 [planwright] Orchestration state-move corruption observed in tasks.md (surfaced by Copilot on PR #16): the dispatch commit 572cb29 appended Task 14's `Status/Last activity/Dispatch` metadata onto Task 4's Completed block (Task 14's real block is intact in In-progress), so Task 4 reads as "implementing" with a duplicated dispatch. Pre-existing, out-of-scope for the Task 17 validator PR; left for the orchestrator `--bookkeeping` reconcile. Pattern worth hardening in T13: a tasks.md section-move that targets the wrong block / fails to remove the source leaves duplicate dispatch metadata — the reconcile sweep (or a validator lint for >1 Status line per task block) should detect and repair it.
- 2026-06-12 [planwright] /orchestrate bundled dispatch (tasks 12/14/17, commit 572cb29) appended Task 14's In-progress Status/Last-activity/Dispatch annotations under Task 4's Completed block rather than Task 14's own block; still present in main's canonical tasks.md (a hand reconciliation on the task-14 branch was superseded when the branch re-synced main's tasks.md, which orchestration owns). T13's dispatch state-move logic should anchor annotation insertion to the dispatched task's heading (not append at section/file position) so multi-task bundled dispatch annotates each block correctly, and the reconcile sweep should clear the existing artifact. Anchor-safe (state-only), so it never surfaced via the freshness gate.
- 2026-06-12 [planwright] Task 13 shipped scripts/orchestrate-lock.sh and scripts/config-get.sh; the advisory-lock acquire/stale-break and the flat-config threshold parse now exist in two places — orchestrate-lock.sh (+ config-get.sh) and tasks-pr-sync.sh's inline `mkdir`/`sed` lock block. They share the lock path and mkdir protocol (risk row 18 satisfied: the two writers exclude each other), but two implementations can still drift; a follow-up should refactor the hook's inline lock+threshold onto orchestrate-lock.sh/config-get.sh so there is one lock primitive and one config reader.
- 2026-06-12 [planwright] REQ-F1.2's critical-path selection is now scripted (scripts/orchestrate-select.sh: ready-set + effort-weighted longest dependent chain), but the guard-infrastructure-first overlay (observation 2026-06-11) stays a prose judgment call in the /orchestrate skill — the script cannot see "this CI/guard task should precede everything it protects" without a soft-ordering edge in the task format. Candidate to encode at the next spec amendment (a `Should-precede:` field or a guard-task marker the selector reads).
- 2026-06-12 [planwright] scripts/orchestrate-select.sh (Task 13) parses tasks.md section/task headings at column 0 and is NOT fence-aware, like the other format-version-1 parsers before drain-gates.sh added fence handling; it relies on the interim "no column-0 spec-shaped lines inside fences" convention. When the decided fence-grammar amendment lands, fold the selector's heading scan into the same normative rule (joins spec-validate.sh + spec-anchor.sh on that list).
- 2026-06-15 [planwright] tasks.md section-placement ledger drifted badly during the multi-day signing outage: nine merged tasks (T5,T6,T8–T12,T14,T15) were left mislabeled in Forward plan and T13/T16 sat in Completed, while the spec content/anchor stayed correct (placement is anchor-excluded, so the drift was invisible to the gate). Root cause: git auto-merge interleaving the live tasks-pr-sync hook's section moves with stale PR-branch snapshots during reconcile pulls; small-hunk conflict resolution let the rest mis-sort silently. Recovered by regenerating all section placements from PR-state ground truth (gh pr list merged/open). HARDENING for T13's /orchestrate and the loop: on ANY tasks.md merge conflict, do not trust ours/theirs/union — regenerate Completed/In-progress/Forward-plan placement from the authoritative PR/merge state (merged→Completed, open-PR→In-progress, deps-unmet→Forward plan), then verify with the validator. The tasks-pr-sync hook (T6) should arguably be the single writer of section placement and be idempotent/merge-safe; consider making orchestrate's reconcile call a "resync from gh" routine rather than hand-editing sections.
- 2026-06-15 [planwright] install.sh emits no changed/unchanged signal, so consumers cannot report idempotent change state from its output. Surfaced by the dotfiles Ansible integration (roles/osx/tasks/osx.yml): its `changed_when` originally string-matched 'wrote'/'installed', tokens the writer never prints, so the task always reported `ok` even on a real first install; the dotfiles side was fixed to `changed_when: false` (honest, but it can no longer distinguish a real write from a no-op). True idempotent reporting needs the writer to emit a machine-readable change count (e.g. `wrote N file(s)`) and skip unchanged files — candidate for Task 19 packaging (the ~/.claude writer's idempotence + change-signal contract). Recorded by the orchestrator on behalf of the dotfiles /panel-pairing run (J1 resolved consumer-side as changed_when: false).
- 2026-06-15 [planwright] Self-hosting rigor gap: the install.sh change-signal defect (prior entry) was caught only by an external consumer's review (the dotfiles /panel-pairing run), not by planwright applying its own Discovery Rigor / guard catalog to itself. planwright should catch violations of its own patterns and rules regardless of who authored the change (Diego, 2026-06-15). The T16 builder dogfood loop and a self-review of planwright's own repo should surface own-contract issues like "a writer/installer emits no idempotent change signal"; consider an installer/consumer-contract lens in Discovery Rigor and/or a guard-catalog check, so good-engineering defects are caught internally rather than only by downstream integrators.
- 2026-06-15 [planwright] Dispatch-isolation decision (Diego, 2026-06-15): make fresh-session-per-STEP the DEFAULT tmux-backend behavior — implementation, then each review skill, each runs in its own fresh session (rehydrating durable state via /resume) instead of one long worker session that inherits prior-step context; the other backends (subagent, print, in-session) should approximate this isolation as closely as their substrate allows. Rationale: independent (uncontaminated) review perspective, bounded context, and consistency with the disposable-tower philosophy (D-7/D-8/D-38). Needs a D-38 design amendment, a config knob (e.g. dispatch_isolation: per-step), and /execute-task handing off review phases to fresh /resume-seeded sessions; route through the spec pipeline (amendment or follow-on spec), not a raw skill edit.
- 2026-06-15 [planwright] spec-format.md's `test-spec.md` section shows entries as bare H3 (`### REQ-<id> …`) directly after the header/intro, but markdownlint MD001 (heading-increment) requires an intervening H2, so an H1→H3 jump errors. Both the bootstrap and customization-overlay bundles satisfy the linter by grouping test-spec entries under `## REQ-<Group> — <theme>` H2 headings (mirroring requirements.md), but the meta-spec never states this — a fresh author following the format example literally writes a bundle that fails `mise run check`. Candidate meta-spec fix: have the `test-spec.md` section show/require the `## REQ-<Group>` grouping (or document the MD001 exemption) so the format example and the doc-lint guard agree. Surfaced while drafting specs/customization-overlay (test-spec.md tripped MD001 on first lint).
- 2026-06-16 [planwright] Review/disposition asymmetry: a single discovery+validation pass tends to validate the KEEP set but rubber-stamp the DECLINE set, and self-prune. Diego observes (2026-06-16) that explicitly asking to "re-validate all findings" reliably produces better results — because the second pass is adversarial in BOTH directions: it tries to falsify each kept finding AND to resurrect each declined one. In the customization-overlay kickoff lens pass this flipped one false-positive keep (F7, a "vacuous test" that already had a positive assertion) and upgraded three declines to valid light fixes (F9 absent-namespace degrade, F10 supersede-target edge, F11 doctrine-determinism note). validation-rigor already mandates "three independent passes per finding" and discovery-rigor has a "self-critique pass," but neither forces the pass to run SYMMETRICALLY over the decline set or ADVERSARIALLY over the keep set, so it only fires on human request. Candidate doctrine amendment (general rigor capability, not personal style — belongs in core per D-10's own boundary rule): make the adversarial re-validation over both keep and decline sets a required, automatic step in validation-rigor (and cite it from the skills that disposition findings: /spec-kickoff lens pass, /self-review, /polish, /panel-*). Surfaced during the customization-overlay kickoff.
- 2026-06-16 [planwright] Validation thoroughness gap (Diego, 2026-06-16), companion to the prior review/disposition-asymmetry entry: validation-rigor treats integration/end-to-end reproduction as "angle 3, when relevant," secondary to a targeted unit test. Diego wants confirmation of a finding AND of a fix pushed to the whole-system level when possible and applicable — actually reproduce the failure and run the assembled system, via the mechanism that fits the surface (CLI: invoke the real command and observe outputs/exit codes/side effects; Web UI: browser automation such as Playwright; Desktop: UI automation) — rather than stopping at a unit test of an isolated piece, because behavior changes when components are chained and the assembled run catches what fragment-level tests miss (keep the machine advantages, but don't lose human-style end-to-end rigor; "end-to-end" is surface-relative, not Playwright-specific). Additive, not a replacement: unit tests still guard regressions. Candidate doctrine amendment (general rigor capability, not personal style — core per D-10): elevate validation-rigor's solution-validation angle 3 to a preferred whole-system reproduction step when the system is runnable (with /run, /verify, and the surface-appropriate driver), require an explicit `not applicable because <reason>` when it isn't, and cite it from the disposition skills alongside the adversarial-revalidation amendment. Surfaced during the customization-overlay kickoff (does not apply to this bundle — pure shell resolvers, no assembled surface).
- 2026-06-16 [planwright] Re-anchor gap in the act-on-findings skills: /panel-review and /panel-pairing amended an Active spec bundle post-activation (specs/customization-overlay: D-3 writer-mode manifest source, D-4 doctrine.local/catalogs.local collision split, REQ-E1.4 reword, four added REQ→task citations, brief cross-ref fixes across commits 1c33c87/610387a/49de44c/31cb0da/d8a8c51) and committed the edits, but never re-anchored the kickoff brief — leaving the freshness gate stale (brief anchor 8ad2842 vs content 433def6b) until a /spec-kickoff delta re-walkthrough reconciled it; /orchestrate would have halted on the mismatch. The meta-spec amendment ritual requires pre-merge corrections on the spec's own PR to carry "a changelog entry plus a recorded re-sign-off" (re-anchor), and only /spec-kickoff is the sanctioned writer of anchor entries, so the panel-* (and /self-review) skills have no path to satisfy that for spec-bundle edits. Candidate: have the act-on-findings skills refuse to apply edits to an Active spec *bundle* and route to /spec-kickoff, or at minimum detect a stale anchor at pre-flight and surface it. Surfaced during the customization-overlay gate-wiring amendment.
- 2026-06-16 [planwright] Re-anchor gap now confirmed in the copilot-* act-on-findings skills too (extends the prior entry, which named only /panel-review, /panel-pairing, /self-review): a /copilot-review/-pairing iteration committed an expression-only D-11 wording fix to the Active customization-overlay bundle (commit fa4410e, "clarify D-11/REQ-B1.7 protected-set mirror wording") without a changelog entry or a brief re-anchor, leaving the freshness gate stale (brief 7db415d9 vs content efc0a3dd) until this /spec-kickoff delta re-walkthrough reconciled it (anchor 69b89ff). Reinforces that the gap is systemic across EVERY skill that edits a spec bundle post-activation, not just the panel family — strengthens the candidate fix: act-on-findings skills should refuse spec-bundle edits to an Active spec and route to /spec-kickoff, or at minimum detect+surface a stale anchor at pre-flight. Surfaced during the customization-overlay expression-only re-anchor.
- 2026-06-15 [planwright] Multi-install coexistence gap: the plugin manifest hardcodes `"name": "planwright"` (.claude-plugin/plugin.json), so two copies installed the same way (the public plugin + a divergent work fork) collide in the `~/.claude/` plugin namespace — same skill names (`/orchestrate`, `/spec-draft`, …), scripts, hook registrations, and shared state dirs (`~/.claude/inbox/`). Different repo/remote names do NOT isolate the installed artifacts. T19 packaging should make coexistence a first-class case via either (a) a renamespaceable install — parameterize the plugin name at install time and keep all scripts/hooks/state plugin-relative/plugin-scoped (foundation exists: rule-doc resolution is already plugin-relative, D-24/Task 1) so Claude Code namespaces skills as `plugin:skill` and gives each install its own dir; or (b) a documented project-scoped install — the work fork lives in the work repo's `.claude/` rather than as a second global plugin, so it only activates inside that repo (zero global-namespace collision). Proposed T19 acceptance criterion: a renamed second install coexists with the public plugin without `~/.claude/` collisions, OR the project-scoped install path is documented. Cross-links the customization-overlay spec: its adopter-overlay location must be plugin-namespace-aware so two installs don't read/write one shared adopter overlay. (Diego + orchestrator, 2026-06-15.)
- 2026-06-16 [planwright] Anchor-drift guard gap: an expression-only edit to *anchored* spec content made outside a `/spec-kickoff` or `/orchestrate` flow (here, manual citation-fix commit d04bcb4 reworded the requirements.md changelog + Sources after the 2026-06-16 delta re-walkthrough signed off at `74b3cb13…`) leaves the kickoff brief's last re-anchor entry stale (content now anchors to `e0ce72d4…`) with no automated guard — nothing flags brief↔content anchor drift until the next `/orchestrate` or `/execute-task` freshness gate false-halts and demands a re-walkthrough for a pure prose fix. REQ-F1.10 already sanctions a self-re-anchor entry as the remedy, but it relies on the editor remembering to append one. Candidate hardening: a CI/pre-commit lint asserting the brief's last `Anchor:` equals `scripts/spec-anchor.sh <spec-dir>` for every Active bundle (so out-of-flow anchored edits fail loudly at commit time, not at the next dispatch), and/or a `/self-review` anchor-drift lens. Surfaced by `/self-review` on the bootstrap validation-rigor delta branch.
- 2026-06-16 [planwright] Expression-only edits skip their mandatory dated Changelog entry too: the same out-of-flow commit d04bcb4 reworded anchored spec content but added no dated Changelog line, which the amendment ritual (`spec-format` → expression-only: "fixed in place with a mandatory dated Changelog entry") requires and which the self-re-anchor entry must cite. The `/spec-kickoff` amendment had to backfill the Changelog line before re-anchoring. The anchor-drift-guard candidate hardening above should cover both facets: an out-of-flow anchored edit should fail loudly if (a) the brief's last `Anchor:` is stale OR (b) the touched file lacks a Changelog entry dated to the edit. Surfaced by the 2026-06-16 `/spec-kickoff specs/bootstrap amendment` re-anchor run.
- 2026-06-16 [planwright] Validator lacks a changelog-named task-retirement path (parity gap with REQ supersession): `scripts/spec-validate.sh`'s task stable-ID check (the `Task <id> renumbered or removed since <baseline>` error) rejects removing any `### Task` block present on the baseline, with NO escape — whereas the REQ-ID check accepts a removal when a dated `## Changelog` entry names the id (the supersede path). Surfaced during the bootstrap T18 re-sequencing delta re-walkthrough: deferring/retiring Task 18 (it becomes an organically-satisfied release gate, no longer dispatched) could not collapse the task to a `## Deferred` bullet — the block had to be retained in `## Deferred` with the organic state in a `Status` annotation. That is arguably the correct never-delete outcome, but the asymmetry means a deferred-task block lives in a section the format documents as holding "deferral bullets," and there is no sanctioned way to express "this task is intentionally retired/organic" except an annotation. Candidate: either (a) document that a retained task block in `## Deferred` is the sanctioned representation for a deferred/organic task (and have the format's Deferred-section prose say so), or (b) give the task check a changelog-named retirement escape mirroring REQ supersession. Surfaced by the 2026-06-16 `/spec-kickoff specs/bootstrap` T18-resequence amendment.
- 2026-06-16 [planwright] `orchestrate-select.sh` critical-path weighting counts deferred tasks as future work: the `terminal` flag in the `crit()` reverse-edge build excludes only `Completed` and `Out of scope`, so a `## Deferred` task that is never dispatched and never reaches `Completed` (e.g. the organically-satisfied Task 18, which retains `Dependencies: 13,14,16,17`) permanently contributes its effort to the downstream critical-path weight of its upstream dependencies. This diverges from the human-readable critical-path note, which the T18 re-sequencing redrew to route the *dispatchable* path through T16 and exclude the deferred gate. Practical impact is near-zero for bootstrap now (the only remaining Forward-plan tasks, T19/T20, don't feed T18), and a deferred task's upstream deps are arguably still real work, so this may be working as intended — but the selector's notion of "remaining critical path" and the spec's notion of "dispatchable critical path" no longer agree once a never-completing Deferred task carries live dependencies. Candidate: treat `Deferred` (and possibly `Awaiting input`) as terminal for `crit()`, or document that deferred-with-deps tasks intentionally weight their predecessors. Surfaced by `/self-review` on the bootstrap T18 re-sequence branch.
- 2026-06-16 [planwright] Research-grounding under-fires for human-facing/didactic design decisions: drafting specs/spec-comprehension (the `/spec-walkthrough` comprehension aid) needed heavy prior-art + cognitive-science research (high-stakes cold-read practice, dual-coding/cognitive-load/testing/self-explanation effects, the spec-tool rendering landscape, cross-audience plain-language), but research-rigor's design-phase triggers only fired because the human asked — there is no decision-domains catalog entry whose trigger is "the feature introduces a human-facing comprehension/presentation/UX surface." Candidate: add a "human-comprehension / information-UX / human-factors" domain to the decision-domains catalog (Trigger: a feature that renders, presents, or visualizes information for human consumption, or introduces a new human-facing presentation surface; Considerations: prior art in high-stakes fields, the cognitive science of comprehension, audience/precision tradeoffs, accessibility; Disposition: research per research-rigor before recommending, escalate when the presentation IS the product) so `/spec-draft`'s design phase and `/spec-kickoff`'s gap-check surface it automatically. General capability (core catalog), not personal style. Surfaced during specs/spec-comprehension drafting (2026-06-16).
- 2026-06-16 [planwright] Discovery-Rigor's canonical lens set is code-oriented, so the `/spec-kickoff` sign-off lens fan-out over a *spec/doc* artifact over-fires: the performance, concurrency, and code-security lenses returned mostly premature/out-of-threat-model findings (size budgets, write-collision/TOCTOU, scalability) that don't fit a single-user read-only doc, while the genuinely high-value finding (no HTML-escaping requirement for content rendered into an HTML artifact — REQ-E1.7) came from the security lens reframed for the artifact. Most fan-out cost went to declining code-shaped findings. Candidate: a spec-artifact lens profile for discovery-rigor (or a note in the spec-kickoff lens-pass step) that retargets/drops the code-only lenses and adds spec-native lenses (rendered-content injection, citation/coverage integrity, dead verification paths, decision-domain gaps) so the pass spends its budget where spec bugs actually live. Surfaced during the specs/spec-comprehension first-activation lens pass (2026-06-16).
- 2026-06-16 [planwright] Lens-pass requirement extensions land without a paired test-spec update, and the dead-path check is presence-based so it misses partial-coverage gaps: the specs/spec-comprehension first-activation sign-off applied brief findings F2 (REQ-E1.3 extended to cover a present-but-failing renderer) and F3 (REQ-A1.5 extended to cover a charset-valid scope selector resolving to nothing) to the *requirements* but never mirrored either into `test-spec.md`. Both REQs already had a `### REQ-x` entry, so the verification-approach dead-path check ("REQs whose named verification cannot actually run") passed them — it asserts entry *presence*, not coverage *completeness* against the (now-extended) requirement text. The two partial-coverage dead paths escaped sign-off and were caught later by a `/copilot-pairing` review, then reconciled in the 2026-06-16 delta re-walkthrough (test-spec synced, anchor 613e19c). Candidate: (a) the lens-pass disposition step should treat "apply a requirement addition/extension" as also requiring a paired test-spec edit in the same disposition (a requirement-change-without-verification-change is itself a finding), and/or (b) strengthen the dead-path check from presence-based to coverage-based — flag a `[test]` REQ whose test-spec entry does not mention each distinct behavioral clause the requirement enumerates. Surfaced during the 2026-06-16 `/spec-kickoff specs/spec-comprehension` delta re-walkthrough.
- 2026-06-16 [planwright] `/orchestrate`'s worktree invariant (D-37: "never shell out to `git worktree`; use the native mechanism") is not mechanically satisfiable for the established placement contract. The contract is dir `.claude/worktrees/<suffix>` paired with branch `planwright/<spec>/task-<id>` (slashed), but `claude --worktree <name>` couples the worktree dir name and the new branch name to one token — it cannot produce a suffix-named dir on a differently-named slashed branch. Every existing task worktree (`task-N` dir on `planwright/bootstrap/task-N`) evidences `git worktree add -b <branch> <dir> main`-style creation, and the T20 dispatch used the same to honor the contract. Candidate: either (a) the skill documents `git worktree add` as the sanctioned tower-side creation for the suffix/slashed-branch contract (relaxing the literal D-37 wording, which reads as a portability guard against ad-hoc worktree layouts rather than a ban on the one deterministic form that hits the contract), or (b) a helper script wraps the native mechanism + branch rename so the invariant stays literally true. Surfaced by the orchestrator dispatching bootstrap Task 20.
- 2026-06-16 [planwright] Bi-directional re-validation scoping interacts implicitly with the existing three-pass scoping in the act-on-findings skills: validation-rigor's new adversarial pass (D-46 / REQ-D1.8) says a skill that *scopes* it must declare the scoping, but the skills that already declare three-pass scoping (e.g. /self-review's Validation section: full three passes on applied findings, spot-check on declines) say nothing about the bi-directional pass, so by the doc's default-full-application rule they fully refute the keep set AND fully resurrect every decline, including declines they only spot-checked forward. That asymmetry (under-invest forward, fully resurrect backward) is plausibly intended but undeclared; a future pass should decide whether the act-on-findings skills declare explicit bi-directional scoping or rely on the default. Surfaced by /self-review on the bootstrap validation-rigor delta branch.
- 2026-06-16 [planwright] Onboarding docs document the "supply project-specific tooling/rigor without editing core" seam PROVISIONALLY (forward-reference to specs/customization-overlay/, which is Active but 0 tasks built as of T19). docs/getting-started.md §4 describes the four-layer overlay model by reference and the config-override + tool-discovery layers that exist today, with an explicit "not yet shipped" callout. Touch-up needed: once the customization-overlay spec lands, revise getting-started.md §4 to describe the SHIPPED overlay mechanism (overlay locations, per-kind merge contract) instead of forward-referencing it, and drop the "not yet shipped" blockquote. Surfaced during bootstrap Task 19 (packaging finalization), per Diego's explicit overlay-seam sequencing decision this run (document by reference, don't block on the overlay).
- 2026-06-16 [planwright] The machine-local overlay layer root (Task 2) resolves to `<repo>/.claude`, the same dir as the repo-tracked layer, distinguished by `.local`-suffixed names (D-4): config `planwright.local.yml`, doctrine `doctrine.local/`, catalog `catalogs.local/`. `.gitignore` covers `.claude/*.local.yml` (the config file) but NOT the `doctrine.local/` and `catalogs.local/` directories Tasks 4/5 will introduce — a glob on `*.local.yml` does not match directories. Candidate: when Task 4 (doctrine overlay) and Task 5 (catalog overlay) land those machine-local dirs, add `.claude/doctrine.local/` and `.claude/catalogs.local/` to `.gitignore` so the gitignored-per-machine guarantee (REQ-A1.6) holds for all three kinds, not just config. Out of scope for Task 2 (the root primitive doesn't create the dirs). Surfaced during Task 2 execution.
- 2026-06-16 [planwright] The echo-discipline pattern (strip non-printables from untrusted content before echoing, so a hostile value cannot reach the terminal raw) is re-implemented per script with no shared helper: `scripts/spec-validate.sh` carries `emit_error` (`tr -d '\000-\037\177'`) and `first_header` (`gsub(/[^[:print:]]/, "")`), and `scripts/spec-walkthrough.sh` (Task 1, specs/spec-comprehension) added `sanitize_echo` (`tr -d '\000-\037\177'`) for the same reason. Three near-identical implementations of one security-posture rule means a future tightening (e.g. also stripping the C1 range, or unifying on the awk vs tr approach) has to be applied in N places, and a new script can silently omit it. Candidate: a tiny shared `scripts/lib`-style sanitizer (or a documented one-liner in security-posture the scripts copy verbatim with a back-reference) so the rule has one definition. Surfaced during specs/spec-comprehension Task 1 `/polish` convergence (2026-06-16). **Resolved 2026-07-02:** PR #108 extracted the canonical sanitizer to scripts/echo-safety.sh (sourced by spec-validate.sh's emit_error and spec-walkthrough.sh's sanitize_echo; first_header's in-awk gsub form carries a back-reference), giving the echo-discipline rule one definition.
- 2026-06-16 [planwright] `/self-review`'s base-capture contract (`git fetch origin` then `git diff origin/main...HEAD`) inflates a worker's diff in the orchestration model: `/orchestrate` commits dispatch bookkeeping (task moves, status annotations) to the local default branch without pushing, so when a worker branch is cut from that local `main`, `origin/main...HEAD` pulls in every sibling task's dispatch commit (here task-1's diff vs `origin/main` showed task-2 and spec-comprehension-task-1 dispatch edits to two unrelated `tasks.md` files). The worker's PR targets local `main`, so GitHub shows the correct scope, but the local self-review base does not match the PR base, and the inflated diff drags out-of-scope files into the review surface. Candidate: either (a) the pre-flight prefers the PR's actual base when a PR exists (`gh pr view --json baseRefName`) over the hardcoded `origin/main`, or (b) it diffs against the local tracking branch's merge-base with the configured default branch rather than `origin/main` when the local default is ahead of origin. Surfaced during customization-overlay Task 1 self-review (base discrepancy: `origin/main` lagged local `main` by 4 unpushed dispatch commits).
- 2026-06-17 [planwright] An `/orchestrate` task branch can carry a *different* spec's dispatch commit as an ancestor. `planwright/customization-overlay/task-2` was cut from a local `main` that held two unpushed `chore(orchestrate): dispatch` commits — `0fd08b4` (spec-comprehension Task 1, touching `specs/spec-comprehension/tasks.md`) below `0fe1218` (customization-overlay Task 2). The task-2 branch diff against `origin/main` therefore includes the spec-comprehension Task-1 "In progress" flip, and merging the task-2 PR would land that unrelated spec's bookkeeping on `main`. The record is accurate (Task 1 is in progress) and reconciles naturally (whichever PR merges first carries both dispatches; the other no-ops), so it is not a correctness bug — but it muddies the task-2 PR diff with a foreign spec's tasks.md change a reviewer must mentally exclude. (Closely related to the 2026-06-16 base-capture-inflation entry above, surfaced independently during Task 1.) Candidate: have `/orchestrate` push each dispatch commit to `origin/main` before cutting the worktree branch (so sibling dispatches never ride along as ancestors), or cut task worktrees from `origin/main` rather than a dispatch-dirtied local `main`. Cannot be fixed on the task-2 branch without history rewrite (forbidden) or a churny revert+re-revert. Surfaced during `/self-review` of customization-overlay Task 2.
- 2026-06-17 [planwright] skill-drift(self-review): when `/self-review` (and any doctrine-resolving skill) runs **inside the planwright repo itself**, `resolve-rule-doc.sh` resolves doctrine to the writer-install copy under `~/.claude/planwright/doctrine/` (PLANWRIGHT_ROOT and CLAUDE_PLUGIN_ROOT are both unset, so the writer arm wins), which can be **stale relative to the repo's own `doctrine/`** — the source of truth under development. This run resolved an installed `validation-rigor.md` lacking REQ-D1.8 (adversarial bi-directional re-validation: refute the keep set, resurrect the decline set) and REQ-D1.9 (surface-relative whole-system end-to-end reproduction), both present in the repo copy; `gate-wiring.md` also differed (cosmetically). Consequence: the skill's Validation section frames rigor as "the three validation passes," which now lags the repo doctrine's added bi-directional pass. Both obligations were honored manually this pass once the divergence was caught, but a self-hosting review should not depend on catching it. Candidate: when the repo being reviewed contains a `doctrine/` dir (planwright reviewing planwright), prefer it — e.g. the skill exports `PLANWRIGHT_ROOT=<repo>` at pre-flight when `<repo>/doctrine/` exists, or the resolver gains a repo-doctrine arm above the writer arm. Surfaced during `/self-review` Maintenance step, customization-overlay Task 2.
- 2026-06-17 [planwright] The `unset CDPATH` house pattern (a user CDPATH makes `cd <dir>` echo its destination into a `$(cd <dir> && pwd -P)` substitution, corrupting derived paths) is enforced only by convention across the `scripts/` tree: nine scripts carry it (`install.sh`, `config-get.sh`, `check-commit-msgs.sh`, `release-checklist.sh`, `builder-guards.sh`, `classify-ci-failure.sh`, `orchestrate-select.sh`, `check-plugin-schema.sh`, and now `spec-walkthrough.sh`) but nothing mechanical flags a new script that uses `cd` in a command substitution without it. `spec-walkthrough.sh` (Task 1) shipped through `/polish` convergence missing it — its path-containment check spuriously refused a valid bundle under the common `CDPATH=.` — and the test suite's own global `unset CDPATH` actively *masked* the gap (every sibling test harness unsets it too, so a missing-unset regression is invisible unless a test sets CDPATH in the child environment explicitly). Candidate: (a) a `lint:shell`-adjacent guard (custom grep/shellcheck-wrapper) that flags `$(cd ...` / `` `cd ...` `` in a script lacking a top-level `unset CDPATH`, and/or (b) a documented test convention that any script resolving paths via `cd`-substitution gets one regression test running it under `CDPATH=.` rather than relying on the harness-wide unset. Surfaced during `/self-review` of specs/spec-comprehension Task 1 (2026-06-17).
- 2026-06-17 [planwright] The decision-domains catalog has no entry for LLM / model-output quality and evaluation gates (non-deterministic model output, golden-dataset scoring, "qualified" eval sets, narrative correctness). A recent work-fork AI-feature kickoff leaned heavily on this domain (a golden-dataset-score gate, qualified-response sets, acceptance-criteria-by-criteria live reads) with no catalog trigger to consult. Candidate new decision domain as planwright handles more AI features; sibling to the "human-comprehension / information-UX" domain candidate logged 2026-06-16. Surfaced in a work fork; relocated here for mining.
- 2026-06-17 [planwright] Spike→production kickoffs inherit operational/infra assumptions that may not hold: a recent work-fork production spec succeeding a spike assumed percentage-ramp feature flags backed by a dynamic flag service that does not exist, carrying a phantom flag-service dependency. Candidate: a /spec-kickoff step (or a kickoff-brief risk-register prompt) that, when a spec cites a predecessor spike, explicitly re-validates each inherited operational/infra dependency against current reality before sign-off — not just inherited findings. Surfaced in a work fork; relocated here for mining.
- 2026-06-29 [planwright] (kickoff-lifecycle Task 7 heads-up) The five→six-status audit Task 7 owns ("any core status-printing path audited for the five→six status set") has at least four concrete sites beyond `/spec-walkthrough`'s framing, all confirmed missing `Ready` at this commit: `scripts/drain-gates.sh:154` (lowercase status case-arm `draft | active | done | retired | superseded`) and `:228-229` (the AWK `VALID["..."]` validity set) — a `Ready` spec's gates parse as unrecognized/invalid there, a *behavioral* gap not just framing; `scripts/spec-assemble.sh:835-847` (no `Ready)` stage-framing arm, falls through to generic text); and the status-loop fixtures `tests/test-spec-walkthrough.sh:192` and `tests/test-spec-assemble.sh:675` (loops omit `Ready`). Task 7's deliverables name the walkthrough renderer explicitly but NOT `drain-gates.sh`, so the gate evaluator's gap risks falling through the cracks — fold it into Task 7's audit (or a dedicated task) so `Ready` is recognized everywhere a status is parsed, not only where it is rendered. Surfaced by `/panel-pairing` (gemini backend) during Task 2 (validator) execution; correctly out of scope for Task 2, which is validator-only.
- 2026-06-17 [planwright] A spec enumerating a "frozen corpus" / authorized anchor set should cross-check the enumeration at draft/kickoff against (a) the actual fixture directories and (b) the per-task bound anchors. In a recent work-fork spec the enumeration omitted real fixtures and several task-bound anchors sat in no authorized set; the drift was caught only by a later panel review, not at kickoff. Candidate /spec-draft or validator check: every anchor a task binds must appear in an authorized (REQ-B1.5-style) set, and enumerated corpora should reconcile with the fixture directories they claim to freeze. Clusters with the anchor-drift-guard and brief↔content-drift candidates logged 2026-06-16. Surfaced in a work fork; relocated here for mining.
- 2026-06-17 [planwright] The observations accumulator is single-repo by construction: the `accumulator-taxonomy` doctrine gives the log exactly one canonical reader (`/spec-draft` in the same repo) and the `[<repo>]` tag records provenance (where surfaced), not a routing target (who should act). So a planwright-about observation surfaced in a downstream/work repo never reaches planwright's log without a manual relocate-and-neutralize (as the four entries above required on 2026-06-17). Candidate: make the accumulator multi-target and multi-source, extending accumulator-taxonomy rather than replacing it. Three sub-problems, distinct shapes: (1) target classification within a repo — redefine the `[<tag>]` to mean target/owner (what it is about), keeping provenance in the trailing "Surfaced…" sentence; zero-infra but a doctrine change that should ride a bootstrap amendment, not a freehand edit. (2) cross-repo routing across one author's fleet — a machine-local fan-in inbox (a stable `~/.claude/planwright/observations-inbox/`, the machine-local-env doctrine applied to observations) that every repo's `/spec-draft` mines in addition to its own log, plus a route step on `/drain` that emits foreign-target entries to the inbox, neutralizing work-internal identifiers on the boundary. (3) public-adopter feedback (repos the author cannot read) — opt-in, consent-gated submission only (never telemetry): a step that neutralizes a planwright-target entry and opens a pre-filled GitHub issue via `gh` for the adopter to review before sending, with planwright's own `/spec-draft` reading labeled issues as another class-3 seed source (fan-in via GitHub). Cross-cutting invariant on every boundary crossing: neutralize internal identifiers (REQ-D1.6) and never auto-send to a public surface without human consent. Surfaced 2026-06-17 while relocating the four work-fork pipeline observations above; Diego scoped the design and chose to log it (the cheap target-tag convention deferred to a bootstrap amendment) rather than draft a spec now.
- 2026-06-17 [planwright] The spec-format parse grammar (the `^- **REQ-<Group><N>.<M>**` requirement bullet, the `^### D-<n>:` decision heading, the `^### Task <id> —` task heading, the H2 section-as-state-label rule, and the `Dependencies:`/`Citations:` token extraction) is now re-implemented in a third script: `scripts/spec-validate.sh` (parse_requirements / parse_design / parse_tasks), `scripts/orchestrate-select.sh` (the task/deps/effort awk), and now `scripts/spec-model.sh` (specs/spec-comprehension Task 2, the bundle reader model). Three independent encodings of one grammar means a meta-spec format change (a new field, a heading-shape tweak) must be mirrored in N places, and a divergence would let the model and the validator disagree about what a bundle contains — exactly the drift the content anchor exists to prevent. Task 2 reused the grammar by mirroring the regexes (with a back-reference comment) rather than extracting a shared library, to avoid restructuring two load-bearing scripts inside a view-substrate task. Candidate: a shared `scripts/lib`-style spec-parse module (POSIX-sh-sourceable awk fragments, or one `spec-parse.sh` emitting the tagged record stream the validator/selector/model all consume) so the grammar has one definition; pairs with the 2026-06-16 echo-discipline-duplication observation (same shared-lib gap, different rule). Surfaced during specs/spec-comprehension Task 2 implementation (2026-06-17).
- 2026-06-17 [planwright] REQ-C1.7 names three normative-token classes — modals (MUST/SHALL/SHALL NOT), thresholds, and enumerated states — that the plain-language layer must preserve verbatim and mark toggle-anchored. The translation layer (specs/spec-comprehension Task 3, `scripts/spec-translate.sh`) mechanically detects and marks the modals and comparator-governed thresholds (`≤ 64`), but has no detector for the open-ended "enumerated state" class (e.g. a status set like Draft/Active/Done, or a `present|absent` pair): there is no syntactic signature distinguishing an enumerated-state token from ordinary prose without a spec-level definition. Verbatim survival still holds for enumerated states (the conservative scrub never softens them), so this is a *marking* gap, not a preservation gap — but an unmarked enumerated state will not get the reveal toggle's source-mapping affordance. Candidate: a meta-spec definition of what constitutes a mechanically-detectable enumerated-state token (an explicit lexicon, a markup convention in the source bundle, or a per-spec declared set), after which the translator and Task 11's coverage gate can assert marking. Surfaced during specs/spec-comprehension Task 3 implementation (2026-06-17).
- 2026-06-17 [planwright] [G1] `scripts/spec-translate.sh` `emit_norm` numbers NORM ordinals per-class (all RFC-2119 modals first, then all comparator thresholds) rather than in true document source order: a threshold preceding a modal receives the higher ordinal. The docstring↔behavior discrepancy this entry originally also flagged is now resolved — the docstring documents the per-class "modals-before-thresholds" numbering (corrected in `f05d95e`). Open item, deferred (panel-pairing handoff, Task 3): the ordinal's only consumers are Tasks 4/8 (the reveal toggle / source-mapping render), which do not exist yet, so whether global source order matters vs. stable per-ref marks is still undecided. Candidate when Tasks 4/8 land: unify the two scans into one source-order pass if global order proves to matter. Surfaced during specs/spec-comprehension Task 3 panel-pairing (2026-06-17).
- 2026-06-17 [planwright] [N2] `scripts/spec-translate.sh` `scrub()` tidy regex (`\([ \t]*[\/,;.&-]*[ \t]*\)`) collapses only a single separator run, so a parenthetical of 3+ identifiers left empty after id removal leaves residue in the plain column (`(D-1, D-2, D-3)`→`(,, )`). Confirmed *not* exposed on the real bundles: multi-id citations live in `(Cites: …)` annotations the model extracts as REQCITE/TASKCITE/DECCITE, not in the requirement/decision/task body text the scrub renders (0 residue over spec-comprehension and bootstrap). Deferred (panel-pairing handoff, Task 3). Candidate: generalize the tidy regex to collapse repeated separator/space runs, with a content-preservation guard so a parenthetical carrying non-id content survives. Surfaced during specs/spec-comprehension Task 3 panel-pairing (2026-06-17).
- 2026-06-17 [planwright] `specs/spec-comprehension/design.md:35` miscites the reveal seam/toggle as `(D-3 / REQ-D1.3)` inside D-2's own decision body, but the reveal toggle is governed by **D-2** (the lossless-layered-view decision whose body literally describes "the reveal toggle … is the seam between the readable surface and the precise substrate"), not D-3 (the independence-flow decision: render → unaided read → teach-back, which owns REQ-D1.1/REQ-D1.2). REQ-D1.3 itself self-cites `(Cites: D-2, REQ-C1.7)`, confirming D-2 is the governing decision. The miscite originated in the kickoff sign-off (PR #26, commit `9727ad5`) and was faithfully copied into `scripts/spec-translate.sh:17` during Task 3; the script copy was corrected to `(D-2/REQ-D1.3)` in this PR (#38), but `design.md` is a contract artifact outside this PR's diff and its correction needs a separate human-gated spec amendment (a `/spec-kickoff` delta or amendment pass). Candidate: amend `design.md:35` to `(D-2 / REQ-D1.3)` so the design body agrees with REQ-D1.3's own Cites and with the corrected script header. Surfaced during specs/spec-comprehension Task 3 `/copilot-pairing` (2026-06-17), Copilot thread on `scripts/spec-translate.sh:17`.
- 2026-06-17 [planwright] The spec-at-a-glance one-pager (specs/spec-comprehension Task 4, `scripts/spec-onepager.sh`) selects REQ-C1.2's "killer items" by a single heuristic — inbound citation/dependency-edge count (`TASKCITE`+`REQCITE` targeting a requirement) as the load-bearing proxy. It surfaces the right claims on the real bundles (spec-comprehension's top three are the reveal toggle, the scope/blast-radius selector, and the precision guardrail), and the *quality* of the pick is REQ-C1.2's `[manual]` verification half — but inbound-reference count is one signal among several a richer ranker could weigh (strength of the normative modal: SHALL NOT / MUST NOT prohibitions are "catastrophic if wrong"; outbound fan-out; a spec-declared killer-item marker). This sits in the still-uncatalogued human-comprehension / information-UX decision domain (the catalog gap logged 2026-06-16), so the heuristic is a declared within-task scoping call, not an escalation. Candidate: a meta-spec notion of how a bundle nominates its own load-bearing claims (an explicit marker the author sets, or a documented multi-signal ranker), after which the one-pager and Task 11's coverage gate can assert killer-item selection rather than trusting a single proxy. Surfaced during specs/spec-comprehension Task 4 implementation (2026-06-17).
- 2026-06-17 [planwright] The spec-format validator (`scripts/spec-validate.sh`) accepts a live requirement that carries a `*(Cites: …)*` annotation but no normative prose (e.g. `- **REQ-A1.1** *(Cites: D-1.)*`): it checks for the citation annotation but not for non-empty requirement text, so the bundle passes with 0 errors. Downstream, `scripts/spec-model.sh` emits that requirement with an empty text field and the one-pager (`scripts/spec-onepager.sh`) renders it as a claim with empty plain and source columns — and, because the citation gives it an inbound-edge score ≥ 1, can even foreground it as a "killer" item (a blank, load-bearing-looking claim). This is upstream of Task 4 (a validator/model gap, not a one-pager defect: the one-pager's pass-through is faithfully lossless — empty in, empty out). Candidate: have the validator require non-empty requirement prose (REQ-A group / spec-format) so a prose-less-but-cited requirement is a clean error rather than a blank downstream claim. Surfaced during specs/spec-comprehension Task 4 `/self-review` (2026-06-17).
- 2026-06-17 [planwright] customization-overlay Task 5 (catalog-overlay resolution) surfaced a spec-vs-reality gap: REQ-B1.3 / D-5 assume the decision-domains catalog is structurally mergeable like the guard catalog, but it shipped as prose only (`doctrine/decision-domains.md`, consumed by LLM-driven skills via `resolve-rule-doc.sh`) with no entry ids for `supersede-by-id` to bind to. Resolved at execution by a human fork decision (Diego, 2026-06-17): create `config/decision-domains.yaml` as the core-seed machine view (the ten domains keyed by stable id), mirroring the guard catalog's doc/yaml split, with the prose doc staying normative. The spec text (REQ-B1.3 / D-5, and Task 5's done-when) still describes the catalog as if it were already structured; aligning it to the chosen model is a pending `/spec-kickoff` delta amendment (Diego's action), recorded in the kickoff brief's Task 5 research/decision log. Candidate seed: when a spec treats two artifacts as the same kind (here "growable catalog"), draft/kickoff should verify each actually has the kind's machine shape, not just the conceptual one. Surfaced during customization-overlay Task 5 implementation.
- 2026-06-17 [planwright] The HTML assembly layer (specs/spec-comprehension Task 6, `scripts/spec-assemble.sh`) runs the model→translate chain twice over the same bundle — once through `scripts/spec-onepager.sh` and once through `scripts/spec-teachback.sh`, each invoked in `<spec-dir>` mode so each re-runs the upstream chain itself. Correct and fine for the small real-bundle corpus, but redundant: a single shared `spec-translate.sh <dir>` pass piped into both views (the views' stdin mode) would halve the upstream work. Deferred — the teach-back view needs the bundle's requirement-group headings for section labels, which it reads from `requirements.md` in dir mode and which are absent from a bare translate stream, so a shared-pass refactor must also thread the GROUPTITLE records through. Candidate when the view set grows (Tasks 7/8/9 add more dir-mode re-runs): a single upstream pass feeding every view, with the group titles prefixed once. Surfaced during specs/spec-comprehension Task 6 implementation (2026-06-17).
- 2026-06-17 [planwright] D-7 fidelity gap (specs/spec-comprehension Task 6, `scripts/spec-assemble.sh`): D-7 / REQ-E1.6 chose styling built from "primitives (Tailwind CSS plus DaisyUI)" and explicitly rejected "custom hand-crafted components", but the shipped stylesheet is original, hand-authored CSS that only draws on those projects' design language (utility/component conventions, color tokens) and bundles none of their code. Surfaced during Task 6 `/self-review`; the original artifact also carried a verbatim `Copyright (c) Tailwind Labs Inc. and DaisyUI contributors` MIT block that inaccurately claimed their copyright over planwright's own CSS. Resolved at review by a human fork decision (Diego, 2026-06-17): keep the hand-authored CSS, soften the artifact footer + comments to an inspiration credit (no verbatim third-party copyright), commit `[pending-sign-off]`. The D-7-vs-implementation gap itself is deferred, not closed. Candidate: either extract a real Tailwind/DaisyUI MIT subset to match D-7's letter, or amend D-7 via `/spec-kickoff` to record that token-level inspiration (original CSS in their design language) was the chosen implementation. Surfaced during specs/spec-comprehension Task 6 `/self-review` (2026-06-17).
- 2026-06-17 [planwright] customization-overlay Task 6 (`review_sequence` knob): the bad-name by-layer policy in `scripts/resolve-review-sequence.sh` degrades a malformed winning overlay value to the **core default** rather than to a strict next-lower layer, because `config-get` exposes only the merged winning value (via `--explain`), not per-layer values — so a precise multi-overlay semantic cascade (degrade machine-local→repo-tracked→adopter→core, honoring an intermediate valid layer) is not implementable without re-reading layers, which REQ-D1.1 forbids skills from doing. Degrade-to-core is always behavior-preserving and is the true next-lower layer in the dominant single-overlay case; the limitation is documented in the script header and locked by a characterization test. Candidate: a `config-get` mode that returns per-layer values (or resolves a key excluding the top-N layers) would let list-valued knobs with semantic validation do a faithful per-layer cascade. Surfaced during customization-overlay Task 6 implementation.
- 2026-06-17 [planwright] customization-overlay Task 7 (adopter docs) surfaced a gitignore-vs-trust-class gap in already-merged T2/T4/T5 infra: the machine-local overlay layer (D-1/D-4) is defined as the gitignored, per-machine, uncommitted trust class, but planwright's own `.gitignore` denylist ignores only the machine-local *config file* by pattern (`.claude/*.local.yml`, `.claude/*.local.json`) — those patterns match files, not directories. So a machine-local *doctrine* overlay (`.claude/doctrine.local/<name>.md`) or *catalog* overlay (`.claude/catalogs.local/<name>.yaml`) placed in this repo is NOT ignored (`git check-ignore` confirms: tracked, would be committed), contradicting the machine-local = uncommitted trust class and the secret-warning premise (REQ-E1.1, R9). Task 7 handled it in-scope by documenting the adopter action (overlays.md §6 "Keep your machine-local overlays actually uncommitted" instructs adding `.claude/doctrine.local/` and `.claude/catalogs.local/` to the repo's own `.gitignore`). Candidate: tighten planwright's own `.gitignore` to cover the machine-local overlay directories (e.g. `.claude/*.local/` or explicit `.claude/doctrine.local/` + `.claude/catalogs.local/`) so the trust class holds by default for dogfooding, and consider whether the resolvers should warn when a machine-local overlay directory is tracked. Out of Task 7's docs scope (touches `.gitignore`, a shared committed file); needs a human-gated follow-up. Surfaced during customization-overlay Task 7 /polish convergence (2026-06-17). **Resolved 2026-06-17:** PR #53 added `.claude/*.local/` to planwright's `.gitignore`, covering the machine-local overlay directories (`.claude/doctrine.local/`, `.claude/catalogs.local/`) so the trust class holds by default for dogfooding; overlays.md §6's guidance was reconciled to match in PR #52 (the adopter action stays, since a repo adopting planwright as a plugin does not inherit its `.gitignore`).
- 2026-06-17 [planwright] The HTML escaper `esc()` (the `gsub` chain over `& < > " '`) is now defined identically in three awk programs inside `scripts/spec-assemble.sh` — `onepager_prog` and `teachback_prog` (Task 6) and now `decisionmap_prog` (specs/spec-comprehension Task 8, which added the third byte-identical copy). The awk programs are deliberately self-contained strings, each fed to its own `awk` invocation, so the duplication is an artifact of that structure rather than a defect; review-mode Refactor Instinct deferred consolidation (low-value pre-existing duplication, no behavior change, shellcheck/shfmt clean, security escaping already test-covered via the `&lt;b&gt;bold&lt;/b&gt;` assertion). Diego's disposition (panel-pairing handoff, Task 8, 2026-06-17): do not touch the pre-existing Task 6 copies on the Task 8 branch; consolidate AFTER both Task 7 (the dependency-graph view, which also extends `spec-assemble.sh`) and Task 8 merge, so a single shared `esc()` awk function string can be hoisted across all view programs (onepager / decisionmap / teachback / graph) in one pass. Clusters with the 2026-06-16 echo-discipline-duplication and the 2026-06-17 spec-parse-grammar-triplication shared-lib gaps (same "one rule, N copies across scripts" shape), and with the 2026-06-17 spec-assemble double model→translate-chain entry (same script, same "defer the cross-view refactor until the view set stops growing" timing). Surfaced during specs/spec-comprehension Task 8 `/panel-pairing` (gemini backend, 2026-06-17). **Resolved 2026-07-02:** PR #108 hoisted the (by then six) inline esc() copies to one shared awk function definition across all view programs in spec-assemble.sh.
- 2026-06-17 [planwright] specs/spec-comprehension Task 7 (`scripts/spec-graph.sh`) realizes the upstream-redundancy growth the 2026-06-17 Task 6 observation anticipated, and adds a distinct angle: the graph view runs the bundle model **and** a second per-render upstream subprocess — `scripts/orchestrate-select.sh --critical-path` (the reused critical-path computation, D-6) — so a future shared-upstream-pass refactor should cache both the parsed model and the critical-path result, not only the translate stream. Correct and fine for the small real-bundle corpus (the selector re-parses the same `tasks.md` the model already read). Candidate when the view set or bundle size grows: a single resolved model object, with the critical path computed once, threaded into every view. Surfaced during specs/spec-comprehension Task 7 implementation.
- 2026-06-18 [planwright] specs/spec-comprehension Task 9 added two more byte-identical copies of the `esc()` awk escaper to `scripts/spec-assemble.sh` (`blast_prog` for the decision blast-radius view and `verify_prog` for the file:test-spec verification view), bringing the in-file count to six (onepager / decisionmap / teachback / graph / blast / verify). This extends — does not change — the deferred consolidation Diego dispositioned in the 2026-06-17 Task 8 observation ("hoist a single shared esc() awk function string across all view programs after T7 and T8 merge"). T7 (#56) and T8 (#55) are now merged, so the consolidation precondition is met; the hoist should now cover all six copies in one pass. Kept duplicated on the Task 9 branch to stay within the unit's scope (the consolidation touches every view program, not just the two new ones). Surfaced during specs/spec-comprehension Task 9 implementation. **Resolved 2026-07-02:** PR #108 collapsed all six esc() copies to a single hoisted awk function definition.
- 2026-06-18 [planwright] `tests/test-resolve-overlay-root.sh` is not hermetic: its "no adopter arm yields empty root" case asserts the resolver returns an empty root when no adopter overlay is configured, but the resolver probes real filesystem locations and, on a machine where planwright itself is installed at `~/.claude/planwright/...`, finds a real overlay dir and returns it — so the test fails locally on a dogfooding dev machine while passing on a clean CI runner (the failure observed during Task 9 convergence: expected '', got `~/.claude/planwright/planwright/overlay`). Candidate: make the test hermetic by pointing HOME/the probe roots at a temp dir (or otherwise stubbing the install-location probe) so the "no overlay" arm is genuinely overlay-free regardless of the host. Pre-existing and unrelated to Task 9 (no overlay files were touched); surfaced during specs/spec-comprehension Task 9 `mise run check`.
- 2026-06-18 [planwright] specs/spec-comprehension Task 9 introduces a third place that parses the `/spec-walkthrough` scope grammar (`whole | file:<name> | reqs:<GROUP> | decisions | tasks | decision:<id>`): `scripts/spec-walkthrough.sh` (charset-validate + human label for the load report, from Task 1), `scripts/spec-scope.sh` (the model-stream filter keep-sets), and `scripts/spec-assemble.sh` (section-flag selection + scope label). Each does a genuinely different job, so the duplication is the case-arm structure, not whole logic — but a future grammar change (a new selector, a renamed form) must touch all three in lockstep, and only `spec-scope`/`spec-assemble` fail closed on a bad selector (the scaffold pre-validates). Same "one rule, N copies across scripts" shape as the 2026-06-16 echo-discipline and 2026-06-17 spec-parse-grammar-triplication observations. Candidate: a single shared scope-classifier (kind + components) the three callers consume, or at least a shared selector-validation helper. Deferred (touches three load-bearing scripts; out of Task 9's scope). Surfaced during specs/spec-comprehension Task 9 /polish convergence.
- 2026-06-18 [planwright] `tests/test-resolve-overlay-root.sh` is non-hermetic with respect to `$HOME`: its `base` helper unsets the planwright/CLAUDE env vars (`PLANWRIGHT_ROOT`, `CLAUDE_PLUGIN_ROOT`, `CLAUDE_DIR`, `PLANWRIGHT_ADOPTER_OVERLAY`, `CLAUDE_PLUGIN_DATA`, `PLANWRIGHT_REPO_ROOT`) but NOT `HOME`, so the adopter "writer arm" (`<claude-dir>/planwright/<name>/overlay`, where `<claude-dir>` falls back to `$HOME/.claude`) resolves a real path on any machine where planwright is installed at `~/.claude/planwright/` — the "no adopter arm derivable degrades / yields empty root" case (test lines 225-227) then fails with the developer's real path (`~/.claude/planwright/planwright/overlay`) instead of empty. The failure is local-environment-only: GitHub Linux CI runs with a clean `$HOME` that lacks that install, so the merge gate passes (confirmed: running the suite under a scrubbed `HOME` passes all cases). Candidate: add `-u HOME` (and set a temp `CLAUDE_DIR`/`HOME` under `$tmp`) in the `base` helper so the adopter writer arm cannot leak the host install, making the suite hermetic for local `mise run check` runs. Belongs to the overlay-resolution work (PR #29, a different spec), not spec-comprehension. Surfaced during specs/spec-comprehension Task 10 `/execute-task` CI run (2026-06-18).
- 2026-06-18 [planwright] The `mise run test` runner is fail-fast (`for t in tests/*.sh; do /bin/bash "$t" || exit 1; done`), so a single failing suite aborts the loop and silently hides every alphabetically-later suite locally. Combined with the non-hermetic `$HOME` leak in `tests/test-resolve-overlay-root.sh` (logged above, same date), this means `mise run test` on a developer machine with planwright installed at `~/.claude/planwright/` bails at `test-resolve-overlay-root.sh` and never runs `test-*` suites that sort after it (e.g. `test-sibling-touchpoints.sh`) — the suites pass when run directly and on CI's clean `$HOME`, but local `mise run check` reports only the first failure and gives no signal about the rest. Distinct from the hermeticity fix candidate: even with hermetic tests, one genuinely-failing or flaky suite masks all later ones locally, weakening local/CI feedback parity. Candidate: run all suites and aggregate (collect failures, exit nonzero at the end) rather than fail-fast, so a single failure does not blind the developer to the rest of the suite. Surfaced during specs/spec-comprehension Task 10 `/self-review` (2026-06-18).
- 2026-06-22 [planwright] `config/worker-settings.json`'s `git push` deny block is a security guardrail whose correctness rests entirely on manual reasoning about Claude Code's permission-glob matcher (literal-substring globs, deny-before-allow, per-subcommand compound parsing), with no automated coverage. The publish-prep pass (and the prior panel-pairing pass) found a real main-push evasion (`git push origin HEAD:refs/heads/main`, the fully-qualified refspec) that every structural check — `lint:json`, `check:specs`, `scan:secrets` — passed clean, because none of them model the matcher. The deny list is documented as best-effort against exotic spellings, which makes the gap a permanent regression risk: a future edit can silently re-open an evasion with all tooling green. Candidate: a tiny fixture-driven test that, for a table of `git push` invocations (force forms, `+refspec`, every `main` spelling, legit feature-branch pushes), asserts expected deny/allow against a documented re-implementation of the matcher's literal-substring rule — or, more robustly, a pre-push git hook that rejects any update to `refs/heads/main` independent of refspec spelling. Surfaced during pre-public publish-prep `/self-review` (2026-06-22).
- 2026-06-22 [planwright] `scan:secrets` (gitleaks) does not flag re-introduced purged work-repo / personal identifiers, because a repo name is not a credential pattern. This pass caught a real re-leak by manual review: the `reference/` purge note in `specs/bootstrap/tasks.md` spelled out the very work-repo identifier the purge (D-27, REQ-J1.4) removed from history, re-publishing it in a tracked file even though `origin/main` was clean of it. There is no automated guard that identifiers deliberately purged from history stay purged on later branches. Candidate: a publish-prep check (custom gitleaks rule or a `check:hygiene` task) seeded with the known-purged identifiers that fails CI if any reappears in the tree, so re-leaks are caught by tooling rather than relying on a reviewer noticing. Surfaced during pre-public publish-prep `/self-review` (2026-06-22).
- 2026-06-23 [planwright] `scripts/check-doc-links.sh` skips pure-fragment (`#anchor`) targets by design (its header documents this), so in-page anchor links in README/doctrine/docs are not validated by CI: a renamed or removed heading silently breaks any `[text](#anchor)` pointing at it with all of `check:links`/`lint:md` green. This branch's README now relies on two such anchors (`#delivery-modes` and the newly added `#the-pilot-in-command-model`); both currently resolve (verified manually), but neither is guarded. Candidate: extend `check-doc-links.sh` to resolve same-file `#fragment` targets against the file's own GitHub-slugged headings (lowercase, spaces→hyphens, punctuation dropped), so a broken intra-doc anchor fails CI like a broken file link already does. Surfaced during the chore/discoverability-metadata `/self-review` (2026-06-23). **Resolved 2026-06-23:** `scripts/check-doc-links.sh` now verifies `#fragment` anchors (same-page `#sec` and `file.md#sec`) against the target's GitHub-slugged ATX headings (lowercase; drop chars outside `[a-z0-9 _-]`; spaces→hyphens; trim leading/trailing hyphens; internal repeats kept), failing CI on a broken anchor like a broken file link; `tests/test-check-doc-links.sh` gains valid/broken same-page and cross-file anchor cases plus the em-dash double-hyphen slug case (chore/audit-followups).
- 2026-06-23 [planwright] Claude Code's dynamic *workflows* (multi-agent orchestration) and the `ultracode` setting are GA but **user-initiated only** — there is no API for a plugin/skill to invoke a workflow, and workflows are not a declarable plugin dependency (verified against code.claude.com/docs/en/workflows). So planwright cannot build its runtime on workflows; this concretely vindicates the "Claude Code primitives only, no second agent framework" principle (D-14): the portable multi-agent primitives are subagents (the Agent tool), hooks, agent-teams, and skills — exactly what planwright already uses. Workflows also cannot pause mid-run for human input (only permission prompts pause them; sign-off needs separate runs), which independently disqualifies them for planwright's human-in-the-loop spine (sign-off, hard pauses, merge). planwright can still borrow the *patterns* (fan-out, pipeline, loop-until-dry, adversarial verify, judge panels) implemented on subagents, and an operator can run a planwright session under `ultracode` to get workflow help for ad-hoc, human-initiated tasks between the gates (e.g. a pre-public audit). Surfaced 2026-06-23 while evaluating whether planwright could adopt dynamic workflows for its runtime; Diego scoped it.
- 2026-06-23 [planwright] The same Claude-Code permission-matcher fragility logged for the `git push` denies (2026-06-22, above) applies to `config/worker-settings.json`'s commit-flag denies: `git commit --amend:*` / `--squash:*` / `--fixup:*` only match when the flag immediately follows `git commit`, so `git commit -a --amend --no-edit` or `git commit -a --squash HEAD` (flag placed after another arg) evades all three — the same flag-position evasion the push denies were specifically hardened against with a leading wildcard (`git push *--force*`). The squash/fixup denies (added on chore/audit-followups) close the immediate-flag hole and bring the config in line with the `_about`'s "amend / squash / rebase" claim, so this is a residual best-effort gap, not a regression. The obvious symmetric fix (`git commit *--amend*` etc.) is unsafe here: a leading wildcard would false-positive on legitimate commit messages that contain the flag literally (`git commit -m "fix --amend handling"`), which is why the commit denies can't simply mirror the push pattern. Candidate: the same robust fix the push-matcher note recommends, a git hook — a pre-commit hook that rejects amend/squash/fixup intent regardless of flag spelling or position — rather than more deny globs. Surfaced during chore/audit-followups `/self-review` (2026-06-23).
- 2026-06-26 [planwright] orchestration-concurrency kickoff: the level-triggered full-recompute derivation (D-1) has no documented degradation point or cost bound. A reconcile re-derives every task's state (potentially a `gh` call per task, branch-reachability walks, full `tasks.md` rewrite) on each run; this is acceptable and deliberate at current spec sizes, but the spec quantifies no cost ceiling, batching, or incremental strategy. The `orchestration-fleet` sibling spec (which owns scale) should quantify a cost bound and decide `gh`-batching / incremental-derivation when it addresses scaling, so the deliberate O(tasks) choice does not silently become a bottleneck. Surfaced during the orchestration-concurrency `/spec-kickoff` lens pass (2026-06-26).
- 2026-06-26 [planwright] skill-gap(spec-kickoff): when the sign-off §8 lens pass mints a new REQ or re-scopes an existing one, earlier as-walked brief sections and dependent spec-file references can be left stale. The orchestration-concurrency post-sign-off review gauntlet (panel-pairing → self-review → copilot-pairing) found three such stragglers, all from this root cause: (1) Task 7 still framed as the pre-rescope "snapshot-vs-live-truth drift" guard after REQ-E1.1 was re-scoped to structural-corruption (panel-pairing); (2) brief §3/§5 group/REQ counts stale after REQ-F was minted in the lens pass (self-review, left as as-walked per human call); (3) REQ-F1.1 pinned the spec-id regex but left the task-id grammar as prose, and Task 8 mislabeled the squash caveat's risk-ID (copilot-pairing). Candidate: after the sign-off lens pass mints/re-scopes any REQ, the flow should sweep the whole bundle + earlier brief sections for now-stale references (counts, cross-refs, dependent task/test wording, risk-IDs) before computing the anchor — turning three review-cycle amendments into zero. Surfaced during the orchestration-concurrency /spec-kickoff + review gauntlet (2026-06-26).
- 2026-06-26 [planwright] doctrine direction (being drafted as the `kickoff-lifecycle` spec, 2026-06-26): "kicked off" should be distinct from "started in earnest". (a) Add a `Ready` spec status — Draft → Ready → Active → Done — where /spec-kickoff sign-off flips Draft→Ready (signed off, executable, no work started) and the first /orchestrate task dispatch flips Ready→Active; "Active" today misleadingly means "approved but unstarted". (b) /spec-kickoff (and a gauntlet over a kickoff PR) marks the spec PR ready, not draft, on clean completion; merge stays the human's (never auto-merge). Touches: spec-format status table + transitions, the status-aware validator (Ready+Active both errors-block), orchestration gates ("never act on a non-Active spec" → "Ready or Active"), the D-44 two-key model, inbox/dashboard states; migrate signed-off-but-unstarted Active specs → Ready. In orchestration-concurrency's derived-state model, Active-vs-Ready is derivable. Origin: Diego, this session.
- 2026-06-27 [planwright] tooling-gap(spec-validate): the status-aware validator passes a bundle that cites a cross-spec decision/REQ id with no resolvable definition in-bundle and no `bootstrap`/`<sibling-spec>` qualifier (e.g. a bare `(D-45)` in `kickoff-lifecycle/design.md` that actually means `bootstrap D-45`). doc-links + spec-validate both stayed green because the bare token is neither a broken markdown anchor nor a malformed in-bundle id. A reader of a spec that defines only D-1..D-8 will hunt for a local D-45 and fail. Candidate: have spec-validate warn on a `D-NN`/`REQ-XN.N` token whose number is outside the bundle's own defined range and which carries no `bootstrap`/sibling-spec qualifier on the same line or in the enclosing block. Surfaced during `/polish` over the kickoff-lifecycle spec branch (2026-06-27).
- 2026-06-27 [planwright] tooling-gap(spec-validate), sharper variant of the prior entry: a cross-spec citation can be namespace-*qualified* yet point to a real-but-unrelated decision in the named bundle, which no range/qualifier check catches because the token resolves. `kickoff-lifecycle` cited the `review_sequence` mechanism as `bootstrap D-6 / REQ-D1.3`, but bootstrap D-6 is "No repo-class in v1" and bootstrap REQ-D1.3 is the Refactor-Instinct doc requirement; the mechanism is `customization-overlay D-6 / REQ-D1.3`. spec-validate + doc-links both stayed green. A purely structural validator cannot detect semantic mis-attribution; candidate mitigations are a cross-spec citation index (assert each `<spec> D-NN`/`REQ` cite's target heading text is topically consistent) or treating cross-spec citations as a manual review-lens checklist item. Surfaced + fixed during `/self-review` over the kickoff-lifecycle spec branch (2026-06-27).
- 2026-06-27 [planwright] skill-gap(self-review): the kickoff-brief detection's fall-through arm ("a single Active spec with a sibling kickoff-brief.md") can bind a *spec-authoring* branch (`planwright/<spec>/spec`, which the `task-<ids>` branch arm does not match) to an unrelated Active spec's brief. On the kickoff-lifecycle spec branch the only Active spec is `orchestration-concurrency`, so brief-alignment for any Agent-resolvable finding would have been checked against the wrong contract. Harmless here (no Agent-resolvable candidates arose, and kickoff-lifecycle is itself Draft with no brief), but a latent mis-binding. Candidate: when the branch names a spec that has no brief of its own, treat the brief as absent (Agent-resolvable unavailable) rather than falling through to an unrelated single-Active-spec brief. Surfaced during `/self-review` over the kickoff-lifecycle spec branch (2026-06-27).
- 2026-06-27 [planwright] doctrine-coupling gap surfaced during /spec-kickoff lens pass over kickoff-lifecycle: a spec that supersedes a bootstrap PR-flow/permission decision in *doctrine prose* (here bootstrap D-26 "all framework PRs are drafts") can miss that decision's *runtime enforcement* in `config/worker-settings.json`. kickoff-lifecycle's Task 4 marks the spec PR ready via `gh pr ready`, which sits in the worker-settings `deny` list (grouped with `gh pr merge`/force-push as human-only ops) — the runtime teeth of D-26. The lens pass caught it and folded the deny→allow move into Task 4. Candidate: when a spec's design supersedes/narrows a bootstrap decision that has a worker-settings or hook enforcement counterpart, the kickoff lens pass (or spec-draft) should check `config/worker-settings.json` and the hooks for the runtime half, not just the doctrine prose. Surfaced during /spec-kickoff over the kickoff-lifecycle spec branch (2026-06-27).
- 2026-06-28 [planwright] orchestration-concurrency Task 1 established two cross-task contracts the later tasks must honor. (1) Runtime-marker home: `orchestrate-state.sh` reads per-task markers from `$PLANWRIGHT_ORCH_STATE_DIR` (override) else `<spec-dir>/.orchestrate/markers/<id>`, the repo-local default consistent with today's advisory lock at `<spec-dir>/.orchestrate.lock`. D-3 names `${CLAUDE_PLUGIN_DATA}` as the marker home "under plugin delivery, with the writer-mode fallback the resolvers use" — a different (per-machine, cross-checkout) home than the repo-local default. Task 3 (marker writer) and Task 6 (unified lock primitive) must resolve the SAME path the reader uses and should reconcile the plugin-data-vs-repo-local home when T6 unifies the lock; the override keeps relocation a one-line change. (2) Marker timestamp format: the marker file content is epoch seconds (the staleness basis), not mtime; T3 must write that. Surfaced during orchestration-concurrency /execute-task Task 1 (2026-06-28).
- 2026-06-28 [planwright] orchestration-concurrency Task 1's derivation matches single-task branches `planwright/<spec>/task-<id>`; a cohesion-bundle branch (`planwright/<spec>/task-<a>-<b>`) is not yet matched to its member tasks for branch-reachability/commits evidence (bundle members still complete via the per-id `Planwright-Task` trailer, which a bundled commit carries one-of per task per D-2/Task 2). If bundle dispatch (an /orchestrate concern) becomes common, the engine's branch lookup should also recognize a member id inside a bundle branch name. Surfaced during orchestration-concurrency /execute-task Task 1 (2026-06-28).
- 2026-06-28 [planwright] orchestration-concurrency Task 7's `check-ledger` guard wires into the `mise run check` aggregate (the repo's CI and documented local pre-commit equivalent). REQ-E1.3 / Task 7 say "wired into the project's CI / pre-commit checks," but this repo ships NO separate pre-commit framework (no `lefthook.yml`, `.pre-commit-config.yaml`, or git-hook installer; `hooks/hooks.json` is the Claude-Code plugin registry, not git hooks). The "/" reads as inclusive-or and the aggregate satisfies both, but the spec prose implies a pre-commit surface that does not exist here. Candidate: a future spec amendment could either drop the "/ pre-commit" phrasing for this repo or add a real git pre-commit hook (a long-standing wish already logged 2026-06-16 / 2026-06-23 re: anchor-drift and amend/squash denies). Surfaced during orchestration-concurrency /execute-task Task 7 convergence (2026-06-28).
- 2026-06-28 [planwright] skill-drift(execute-task): the meta-spec six-status lifecycle (kickoff-lifecycle #80) added a `Ready` state to `doctrine/spec-format.md` between `Draft` and `Active` (`/spec-kickoff` flips Draft→Ready, stored+human-gated; Ready→Active is DERIVED on the first task to start, D-44/D-2/D-3). `/execute-task` pre-flight step 4 and its stop-conditions table still speak only of Draft→Active ("Not Active: halt; suggest /spec-kickoff when Draft"). Two gaps: (1) the remedy for a `Ready` spec is "the human merges the spec PR / the first task starts" (Ready→Active derivation), NOT /spec-kickoff (already done); the skill's message would misdirect. (2) Possible chicken-and-egg: if Ready→Active is derived "on the first task to start" but execute-task refuses to start a non-Active spec, the first dispatch could halt a freshly-merged Ready spec. orchestration-concurrency is already Active so this run was unaffected. Likely already in kickoff-lifecycle's skill-update task scope; logged for the drain reader to fold. Surfaced during /execute-task Task 7 maintenance pass (2026-06-28).
- 2026-06-28 [planwright] consistency-gap(spec-model/spec-graph): `scripts/orchestrate-select.sh` was fixed to parse prose `**Dependencies:**` lines (drop the parenthetical + trailing cross-spec clause, tolerate a trailing period), but the sibling parser `scripts/spec-model.sh` (lines 309-317) still uses the *pre-fix* logic (`gsub(/[^0-9.]+/," ")` then a bare-id match, no paren-drop, no trailing-period strip). On the real `kickoff-lifecycle` bundle `spec-model` therefore emits phantom edges `TASKDEP 3 1.8` / `3 9` (from `REQ-A1.8 / D-9` inside Task 3's paren) and silently drops every trailing-period dep (`2->1`, `4->3`, `5->1`, `7->1`, and `8->6`). `scripts/spec-graph.sh` draws the graph nodes/edges from `spec-model` while REUSING the now-fixed `orchestrate-select --critical-path` for the highlighted path (D-6), so the drawn edges can contradict the highlighted critical path and reference non-existent task nodes (1.8, 9) — undercutting REQ-C1.3's "the highlighted path cannot disagree with the orchestrator's source of truth". Candidate fixes: (a) mirror the prose-dep parse fix in `spec-model.sh`; or (b) reconcile per D-6 so the graph derives edges from one shared parser rather than `spec-model` recomputing them. Deferred (human chose document-and-defer, kept the PR scoped to the selector). Surfaced during `/panel-pairing` over the fix/orchestrate-select-prose-deps branch (2026-06-28).
- 2026-06-28 [planwright] orchestration-concurrency REQ-B1.1 / finding ④ states the "sole writer" property covers BOTH `/orchestrate` and `/execute-task` ("`/orchestrate` and `/execute-task` SHALL write no section placement"). Task 3 removed `/orchestrate`'s dispatch-time `tasks.md` section move, but no task in the bundle reworks `/execute-task`'s pre-flight step 10, which still relocates the unit Forward plan → In progress and commits it under `commit_on_state_move` — a section-placement write. The REQ-B1.1 design-level sole-writer trace is owned by T8 (a verification, not an implementation), so as written T8's trace would find `/execute-task` step 10 still writing placement. Either a task must rework `/execute-task` step 10 to stop the placement write (deriving In-progress from the marker `/orchestrate` already drops, like `/orchestrate` now does) and drop its `commit_on_state_move` use, or T8's trace must explicitly scope to `/orchestrate` + the reconcile and treat `/execute-task` step 10 as a separate fast-follow. Surfaced during orchestration-concurrency /execute-task Task 3 (2026-06-28).
- 2026-06-29 [planwright] skill-drift(spec-kickoff): the installed `/spec-kickoff` SKILL.md still says it "flips Draft to Active" / "Draft→Active" (lines 6, 28, 90, 138, and the sign-off step-3 prose), but `doctrine/spec-format.md` (the meta-spec, kickoff-lifecycle D-1/D-2) now defines a six-status lifecycle where `/spec-kickoff` flips **Draft→Ready** (stored, human-gated) and **Ready→Active is DERIVED** by the first task to start (orchestration-concurrency's reconcile writer), never by sign-off. This run followed the doctrine (flipped to Ready, validated under Ready enforcement) and noted the divergence in the brief. Same root as the already-logged `skill-drift(execute-task)` (2026-06-28): the kickoff-lifecycle six-status change has not been folded into the spec-kickoff/execute-task skill bodies. Likely already in kickoff-lifecycle's skill-update task scope; logged for the drain reader to fold both skills together. Surfaced during the orchestration-fleet `/spec-kickoff` maintenance pass (2026-06-29).
- 2026-06-29 [planwright] The derivation engine (`orchestrate-state.sh`, Task 1) parses `Dependencies:` lines more weakly than `orchestrate-select.sh`: it splits on commas/whitespace and grammar-validates each token, so prose forms PR #78 taught the selector to read — a trailing period ("Task 1."), a parenthetical qualifier, a cross-spec clause — are flagged `malformed-deps` and dropped, making the engine derive `ready` for a task whose prose deps are not actually met (it treats it as having fewer/no deps). Task 5 sidesteps this by consuming only the engine's evidence-based `completed`/`in-progress` states and recomputing readiness locally with the selector's prose-aware parser. The weakness stays latent for any future consumer that branches on the engine's `ready`/`blocked` over prose-deps bundles (e.g. specs/kickoff-lifecycle); the current consumers do not inherit it: the reconcile (T4, `tasks-pr-sync.sh`) reads the engine but collapses `ready`/`blocked` (and anything other than `completed`/`in-progress`) to `## Forward plan`, so the distinction never affects placement, and the ledger guard (T7, `check-ledger.sh`) is snapshot-only and never calls the engine. Candidate fix: share one dependency-line parser between the engine and the selector (or port the selector's greedy paren-strip + trailing-dot tolerance into the engine).
- 2026-06-29 [planwright] reader-staleness(execute-task): pre-flight step 8 ("Confirm every listed dependency sits in `## Completed`") gates execution on the committed `tasks.md` snapshot, which under the derived-state model (D-1/D-3) intentionally lags live truth. Executing Task 8 directly hit this: deps 1, 3, 4 were all merged (PRs #82/#84/#89) yet the snapshot still showed them outside `## Completed`, so a literal step-8 read would have FALSE-HALTED a ready task. This is the reader-side analog of T5 (selection reads live truth, REQ-B1.2): the dispatch selector now reads the live derivation, but `/execute-task`'s own dependency re-check still reads the snapshot. Candidate: have step 8 confirm dependency completion against the live derivation (`scripts/orchestrate-state.sh`), falling back to the snapshot only when the engine is unavailable, mirroring T5. Out of scope for Task 8 (REQ-B1.1 is sole-*writer* scope, not reader-correctness; no Done-when/test line covers step 8). Surfaced during orchestration-concurrency /execute-task Task 8 (2026-06-29).
- 2026-06-29 [planwright] gate-coverage gap(kickoff-lifecycle): Task 5 updates `/orchestrate`'s pre-flight gate to accept `Ready` or `Active`, but the dispatch target `/execute-task` (skills/execute-task/SKILL.md:92, and its invariant :392) still gates on "Verify the spec is Active / status is not Active". The kickoff-lifecycle requirements/design name only `/orchestrate` (REQ-C1.1) in the recognition-only set (Tasks 2/5/7) and never mention `/execute-task`. Inert today under D-9 (no producer ships a `Ready` bundle until Task 3, gated behind Task 6), so no live bug. But once the behavioral path lands, `/orchestrate` will dispatch a `Ready` spec whose worktree `Status:` header is still `Ready` (the Ready->Active reconcile is the single writer on main, Task 6), and `/execute-task`'s status gate would refuse the very spec just dispatched. Candidate fixes: add `/execute-task` to the recognition-only gate set (accept Ready or Active), or document the intended sequencing that guarantees Active before `/execute-task`'s step-4 read. Surfaced during /polish over kickoff-lifecycle Task 5 (2026-06-29).
- 2026-06-29 [planwright] anchor-blocker(kickoff-lifecycle): Task 6 makes the reconcile (`tasks-pr-sync.sh`) the sole writer of the derived bundle `**Status:**` header (Ready↔Active↔Done), but the bundle Status header IS part of the content anchor — `scripts/spec-anchor.sh` hashes requirements.md/design.md/test-spec.md WHOLE, so the `**Status:**` line rides in the anchor (reproduced: flipping Active→Ready changes the anchor `440999874…`→`fc9ee6d5…`). Once Task 3 (the Draft→Ready producer) ships, the reconcile's Ready→Active flip during execution will change the anchor mid-flight and trip the next dispatch's freshness gate (REQ-C1.3), which the reconcile (non-interactive, not a sanctioned anchor writer) cannot re-anchor. The fix — exclude the bundle `**Status:**` header from the anchor across all four files — is consistent with D-3 ("derived content, like tasks.md section placement", which is anchor-excluded) but the brief never provisioned it, and it has cross-spec blast radius: changing `spec-anchor.sh` reshapes EVERY recorded anchor, breaking the freshness gate for every in-flight spec (kickoff-lifecycle self-heals via an expression-only re-anchor; orchestration-concurrency needs a coordinated re-anchor). Today this is inert (D-9: no bundle is Ready until Task 3; every currently-Active spec genuinely derives Active so the writer no-ops). REQUIRED FOLLOW-UP before Task 3 lands: (a) extend `spec-anchor.sh` + the `spec-format.md` content-anchor section to exclude the bundle Status header, and (b) provision a re-anchor migration (likely fold into Task 8 or a new task) covering live sibling specs. Human chose "scope Task 6 to the writer" and defer this. Surfaced during /execute-task Task 6 pre-flight (2026-06-29).
- 2026-06-29 [planwright] brief-drift(kickoff-lifecycle): Task 6 / PR #92 legitimately refined anchored spec meaning during execution — a normative Done-mirror-completion clause added to REQ-A1.5 and a matching D-3 refinement paragraph. That is correct work, but it moved the bundle's content anchor (`440999874…`→`496b6220…`) while the kickoff brief still carried the old anchor, so the execution freshness gate (REQ-F1.9/C1.3) halted the next ready task (Task 3) with no bypass until a human-driven delta re-walkthrough re-anchored the brief (this run, anchor →`ec8581af…`). The expression-only self-re-anchor lane an execution skill may write (REQ-F1.10) cannot absorb this because the edit is meaning-class. Recurring (second re-anchor on this bundle). Candidate: have `/execute-task` detect when a task's diff touches anchored REQ/design *meaning* (not just tasks.md state) and either (a) flag the brief as needing a `/spec-kickoff` delta re-walk in the PR/handoff so the re-anchor is scheduled rather than discovered at the next dispatch halt, or (b) route execution-time meaning refinements back through a scoped kickoff amendment by construction. Surfaced during the `/spec-kickoff` delta re-anchor of kickoff-lifecycle (2026-06-29).
- 2026-06-30 [planwright] doctrine-gap(supersede-ritual): the supersede-pointer ritual (kickoff-lifecycle REQ-A1.7 / requirements.md:163-167) reconciles a superseded bootstrap *decision* by annotating that `design.md` decision with `Superseded-by:` and never reopening the Done bootstrap bundle. But the same Done bundle's *descriptive prose* in other files goes stale and un-annotated: `specs/bootstrap/test-spec.md:127` (REQ-B2.4 manual entry) still says `/spec-kickoff` "flips the spec Active" (now Draft→Ready, Task 3) and "never marks the PR ready" (now marks the spec PR ready, Task 4). The D-26 supersede pointer captures the correction, and the never-reopen-Done principle correctly forbids editing the prose — so a reader of bootstrap's test-spec/requirements prose alone sees superseded behavior with no in-file pointer to the correction. Candidate: extend the ritual so a superseded decision also drops a lightweight pointer/annotation at the stale descriptive-prose sites in sibling Done-bundle files (or define that test-spec/requirements prose is authoritative-as-of-bundle-status and readers must consult supersede pointers on the decisions). Surfaced during /self-review over kickoff-lifecycle Task 4 (2026-06-30); the stale-prose finding was declined here per the never-reopen-Done constraint.
- 2026-06-30 [planwright] doc-invariant-orphan(kickoff-lifecycle) — RESOLVED in Task 8: the top-level hard-invariant prose "never act on a non-Active spec" appeared verbatim at `README.md:111` and `docs/getting-started.md:162`; requirements.md:53-55 (In scope) mandates "never act on a spec that is not Ready or Active". My first /self-review pass declined this on a false premise (that Task 5, the `/orchestrate` Ready-or-Active gate, was unshipped). Diego corrected it: Task 5 (PR #86) IS merged on origin/main — the orchestrate skill now reads "Verify the spec is Ready or Active … superseding the bootstrap 'non-Active' refusal" (skills/orchestrate/SKILL.md:110) — so the wording was stale NOW, not coupled to unshipped behavior, and squarely in Task 8's docs-reconcile scope. Fixed both lines to the mandated phrasing in this Task 8 PR (#99); repo-wide grep confirmed these were the only two live-doc occurrences of the invariant (no stragglers in README/docs/skills/doctrine). Lesson: derive task-completion state from origin/main, not the worktree's stale `tasks.md` Forward-plan section. Surfaced and resolved during /self-review over kickoff-lifecycle Task 8 (2026-06-30).
- 2026-06-30 [planwright] brief-inference-gap(kickoff-lifecycle): the no-arg brief/spec inference *fallback* in `/self-review` (skills/self-review/SKILL.md:110, "a single Active spec ... with `Status: Active`") and `/execute-task` (skills/execute-task/SKILL.md:86, the current-checkout fallback) selects only `Status: Active`, not `Ready`. Distinct from the already-logged hard-gate gap (2026-06-29 gate-coverage gap, which is `/execute-task`'s step-4 refusal): this is the *selection* fallback used when no spec arg / convention branch is given. Under the six-status model a `Ready` bundle (signed off, validated, zero tasks started) is equally a candidate, and during pre-PR convergence a freshly-dispatched bundle can still read `Ready` on the worktree (the Ready→Active reconcile is the single writer, runs on PR events). The two execution skills are mutually consistent today (both say Active), so a one-sided edit would create drift — fix them together. Out of scope for Task 8 (REQ-A1.7 is the Status-migration sweep; these inference fallbacks are not named in its Done-when and my diff did not touch them). Candidate: fold into Task 7 (downstream status surfaces recognize Ready) or a follow-up — accept `Ready or Active` in both fallbacks. Surfaced during /polish over kickoff-lifecycle Task 8 (2026-06-30).
- 2026-07-01 [planwright] enhancement(reconcile-completion-mapping): `orchestrate-state.sh` maps a merged PR to a task **only** by exact head-ref match (`gh_state_for` keys on `planwright/<spec>/task-<id>`, line 381). Work merged from a non-convention branch — e.g. running `/execute-task` on your own feature branch (`diego/steai-834`) — produces no head-ref match, so the merged-PR signal contributes nothing and completion falls back entirely to the `Planwright-Task` trailer. This is by design (the trailer is the intended cross-flow anchor for work with no convention branch), and the whole-message trailer scan (fix/reconcile-trailer-whole-message-scan, this PR) makes that fallback robust to squash relocation. But it means the `gh` completion path is silently unavailable for the common "execute-task on my own branch" flow, and the trailer becomes the *sole* anchor there — a single point of failure if the trailer is ever missing/mistyped (risk R1). Candidate enhancement: also map a merged PR to its task by reading the `Planwright-Task` trailer in the PR's merge/squash commit (already fetched shape via `gh pr view --json mergeCommit`), so completion derives from the merged PR regardless of head-ref naming, restoring belt-and-suspenders for non-convention branches. Surfaced diagnosing grammar-backed-explain Task 1 mis-derivation in paycalc-services (PR #639 squash-merged from `diego/steai-834`: head-ref mapping missed it AND the trailer landed mid-body) (2026-07-01).
- 2026-07-01 [planwright] stale-done-prose(orchestration-concurrency): the whole-message-scan fix (fix/reconcile-trailer-whole-message-scan) reconciled every *live* doc that described the derivation reading the trailer via git's footer-only `%(trailers)` parser (docs/conventions.md, docs/orchestration-state.md, skills/execute-task/SKILL.md, scripts/planwright-commit-trailers.sh), but the D-2 decision prose in the **Done** `specs/orchestration-concurrency/design.md:63-64` still states the trailer "is parsed via git's native trailer support (`git interpret-trailers` / `git log --format='%(trailers:key=Planwright-Task)'`)" — now inaccurate (the reader scans `%B` whole-message). Left unedited per the never-reopen-Done constraint; declined in this /self-review. This is a concrete second instance of the 2026-06-30 supersede-ritual(doctrine-gap) class above (a superseded Done *decision* whose descriptive prose goes stale with no in-file pointer). Candidate: when a later change supersedes a Done decision's mechanism, drop a lightweight `Superseded-by:` / "reader later changed, see docs/orchestration-state.md#risk-r2" pointer on D-2 rather than editing the prose. Surfaced during /self-review over fix/reconcile-trailer-whole-message-scan (2026-07-01).
- 2026-07-01 [planwright] parser-parity-straggler(orchestrate-deps): fix/orchestrate-deps-parse ports the trailing-period tolerance into the derivation engine (`orchestrate-state.sh`), closing the fail-open half of the 2026-06-29 engine-vs-selector parity gap (a SINGLE-dependency line like `Dependencies: Task 1.` previously dropped its only id and derived `ready` before the prerequisite completed). But a THIRD dependency parser — `spec-model.sh:313-316` (the shared spec model behind spec-graph / spec-validate / spec-walkthrough) — still has the same trailing-period drop: it does `gsub(/[^0-9.]+/, " ", s)` (which KEEPS the period) then grammar-checks `^[0-9]+(\.[0-9]+)?$` WITHOUT stripping, so `Task 1.` / `1.` / `1.2.` emit no TASKDEP edge (reproduced: TASKDEP absent with the trailing period, present without it). Lower stake than the engine (spec-model feeds analysis/graph/validation, not ready-gating dispatch), so left outside this narrowly-scoped fix, but it means the dependency graph the model reports can silently diverge from what orchestration actually enforces. The engine's OTHER remaining differences vs the selector (parenthetical / cross-spec clauses, `orchestrate-select.sh:189`) stay fail-SAFE — the id is still extracted and the line is flagged `malformed-deps`, so they are noisier, not incorrect on readiness. Candidate: the 2026-06-29 "share one dependency-line parser between the engine and the selector" fix should also cover spec-model, or port the trailing-dot strip (and paren-strip) there. Surfaced during /polish over fix/orchestrate-deps-parse (2026-07-01).
- 2026-07-01 [planwright] parser-approach-divergence(spec-model-deps): fix/spec-model-deps-period closes the 2026-06-28 #117 / 2026-07-01 #133 spec-model parity gap (trailing-period drop + the `REQ-A1.8 / D-9` substring phantom) by switching `scripts/spec-model.sh:309-336` to tokenize-then-validate (split on `[,;]` and whitespace, strip a trailing run of periods per token, then grammar-check `^[0-9]+(\.[0-9]+)?$`) rather than porting the selector's greedy paren-strip (`orchestrate-select.sh:189` `sub(/\(.*/, "")`). Net-correct for the logged cases (verified end-to-end: `REQ-A1.8 / D-9` no longer scrapes `1.8`/`9`; `Task 1.` keeps its edge; a `Task 1; Task 4` list keeps both edges). But because it does NOT drop parentheticals, a bare id-shaped number standing alone inside a parenthetical is still scraped as a phantom edge the selector does not produce: `Dependencies: Task 2 (also unblocks 3 later)` → spec-model emits `TASKDEP 5->2` AND `5->3`, while `orchestrate-select --critical-path` yields only `2->5` (reproduced). So the drawn graph can still diverge from the highlighted selector path (D-6 / REQ-C1.3), now only for bare-number-in-paren clauses rather than the broader old class — a narrower residual of #117, not a full close. Separately, the graph now splits deps on `;` but the derivation engine `orchestrate-state.sh:404` still splits only on `,` (`tr ',' ' '`), so `Task 1; Task 4` draws both edges in the graph yet the engine flags it `malformed-deps` and drops the `Task 1` edge (reproduced) — a concrete instance of the 2026-06-29 #123 engine-weakness. Reinforces the standing candidate: extract ONE shared dependency-line parser for spec-model / selector / engine (paren-strip + trailing-dot + a single agreed separator set) so all three agree, rather than three approaches converging only pairwise. Surfaced during /polish over fix/spec-model-deps-period (2026-07-01).
- 2026-07-02 [planwright] release-pending comparator (autopilot-reflex REQ-D1.8) measures bump-vs-tag only; a commits-since-last-tag extension would surface silent release-please detection failure (kickoff risk row 2, accepted-risk follow-up).
- 2026-07-02 [planwright] kickoff-verification-gap(sign-off-rigor): the `/spec-kickoff` lens pass flipped `specs/autopilot-reflex` to Ready while two verifiable defects were live in the bundle: (a) `lint:md` (a CI-gated `mise run check` task, markdownlint MD018/MD012) was RED on the kickoff-brief itself (a `#7)`-at-line-start false heading + trailing blanks), and (b) the brief §4 recorded cross-check "every D-ID cited by >=1 REQ and >=1 task" was not actually true — D-12 (armed mode) is cited by Task 10 but by no REQ (reproduced: D-IDs cited by REQs are D-1..D-11,D-13; D-12 absent). Both slipped because kickoff verifies the spec validator (`check:specs`, which passed 0/0) but neither runs the repo's markdown lint over the brief it just wrote nor re-derives the cross-check claims it records as evidence — it trusts the lens-pass narrative. Candidate: `/spec-kickoff` should, before the Draft->Ready flip, (1) run the repo's markdown lint over the brief + edited spec files and block the flip on errors, and (2) mechanically verify any "every X cited by >=1 Y" style cross-check it writes into the sign-off record rather than asserting it in prose. Surfaced during /panel-pairing over specs/autopilot-reflex (2026-07-02).
- 2026-07-02 [planwright] kickoff-verification-gap(numeric-claims): extends the 2026-07-02 kickoff-verification-gap(sign-off-rigor) point above. That entry's (2) covers only "every X cited by >=1 Y" cross-checks; a later /self-review over the same `specs/autopilot-reflex` Ready bundle found the gap is broader — the pre-Ready sign-off also records *numeric* claims it never re-derives, and two slipped through: the brief §5 `[test]` coverage sub-count read 17 when the actual test-tagged count is 18 (REQ-A1.1's `[test]` half uncounted; the prior panel-pairing amendment fixed the 30->36 total but not this sibling sub-count), and brief §2 asserted a stale `main is at 0.2.2 / tag v0.2.1` version figure that the same section already corrected to v0.2.6 (an earlier stale-fetch misread). Both are mechanically checkable at sign-off and both were signed off as fact. Candidate: broaden the kickoff pre-Ready verification so it re-derives the numeric claims it records — coverage sub-counts (per-tag tallies over `test-spec.md`), REQ/D-ID/task/edit counts, and any pinned version/tag figures — and blocks the flip on a mismatch, not just the "every X cited by >=1 Y" cross-checks. Surfaced during /self-review over specs/autopilot-reflex (2026-07-02).
- 2026-07-02 [planwright] echo-consolidation-partial(esc-consolidation): the echo-discipline consolidation (chore/esc-consolidation) extracted the canonical `sanitize_printable` into `scripts/echo-safety.sh` and migrated the two command-tier callers (spec-validate.sh `emit_error`, spec-walkthrough.sh `sanitize_echo`), but two component-tier framework scripts still carry byte-identical inline `tr -d '\000-\037\177'` `sanitize_echo` copies: `scripts/spec-assemble.sh:87` (justified — it is the documented "self-contained, dependency-free" HTML layer and is copied into an isolate scaffold by tests, so sourcing a sibling would break that property, exactly the break that forced test-spec-walkthrough.sh:548 to stage echo-safety.sh) and `scripts/spec-scope.sh:65` (no self-containment rationale; it already runs as a subprocess of spec-assemble via `$here/spec-scope.sh`, so it could source `$here/echo-safety.sh` the same way the migrated scripts do). The new doctrine line (doctrine/security-posture.md, "sourced by the framework scripts") and the echo-safety.sh header thus slightly overstate adoption (2 of 4 shell callers source the canonical helper; the awk `gsub(/[^[:print:]]/,"")` form in spec-model/spec-validate/spec-walkthrough header parsers is separately and intentionally left in place). Candidate: migrate spec-scope.sh in a follow-up (and decide explicitly whether spec-assemble stays self-contained or joins), then tighten the doctrine wording to name the deliberate exclusion. Surfaced during /self-review over chore/esc-consolidation (2026-07-02). **Wording tightened 2026-07-02:** PR #108 reworded the doctrine line (doctrine/security-posture.md) and the echo-safety.sh header to name the partial adoption (2 of 4 shell callers source the canonical helper; spec-assemble self-contained, spec-scope deferred) instead of implying all framework scripts source it; only the spec-scope.sh code migration remains the deferred follow-up.
- 2026-06-28 [planwright] guard gap (pending-sign-off marker placement → unfixable CI red): doctrine (`gate-wiring.md`, `/polish`, `/self-review`) says a Needs-sign-off commit subject *ends with* the `[pending-sign-off]` marker (suffix), which stays conventional because `check-commit-msgs.sh`'s regex absorbs the trailing text via `.*`. A worker on kickoff-lifecycle Task 1 instead wrote it as a *prefix* (`[pending-sign-off] docs(spec-format): …`), which fails the conventional-commit regex; CI `[lint:commits]` went red on PR #80. Because the commit is pushed and the no-rewrite invariant (REQ-J1.4) forbids amend/rebase/force-push for every agent, and `[lint:commits]` scans the whole `base..head` range (a `git revert` leaves the bad subject in-range), the failure is *unfixable by any agent* — it requires a human history rewrite. Local `mise run check` passed because its `[lint:commits]` range differs from CI's PR base..head. Candidate guards: (a) a commit-msg hook (or `check-commit-msgs.sh` mode) that rejects a *leading* `[pending-sign-off]` and tells the author to suffix it, caught pre-push; (b) the skills show an explicit suffix-form example next to the rule; (c) `check-commit-msgs.sh` could accept the marker as a recognized leading token. Surfaced reconciling PR #80 after dispatching kickoff-lifecycle Task 1 (2026-06-28). Reconciled 2026-07-02 from the local orchestrator ledger; not previously on main.
- 2026-07-02 [planwright] unchecked-doctrine-index(doctrine-readme): `doctrine/README.md` is the canonical doctrine index — a table row plus a "How the docs relate" narrative line per `doctrine/*.md` — but no check enforces that every doctrine doc is registered there. `check-doc-links.sh` only verifies that links present in the prose *resolve*; it never asserts the inverse (every `doctrine/*.md` appears in the index), and `check-options-reference.sh` (D-43) covers the options reference, not doctrine. Concrete evidence: Task 8 added `doctrine/autonomous-safe-decision.md` and the first commit shipped it *unregistered*; the omission was caught only by manual inspection during /polish convergence, not by `mise run check`. So a new rule doc can land resolvable-but-undiscoverable (absent from the index a reader/skill navigates), and the gap is silent. Candidate: a small `check:doctrine-index` guard (mirror of check-options-reference) asserting `doctrine/*.md` minus README.md each have a table row in README.md, wired into the check aggregate; or fold the assertion into check-doc-links as an inverse pass. Surfaced during /execute-task over orchestration-fleet Task 8 (2026-07-02).
- 2026-07-02 [planwright] duplication(backend-advertisement-registry): the shipped-backend advertised capability sets are stated twice — the prose table in `doctrine/backend-capability-contract.md` (Task 1) and the hardcoded `caps_for()` in `scripts/orchestrate-backends.sh` (Task 2, this PR) — with only a "keep in lockstep" comment and no automated drift guard, so a future edit to one can silently diverge from the other (the contract table is the source of truth). Candidate: a test that parses the doc table and asserts `caps_for()` matches it, or a single machine-readable advertisement source that both the doc and the script derive from. Surfaced during /execute-task over orchestration-fleet Task 2 (2026-07-02).
- 2026-07-02 [planwright] shared-helper-candidate(plugin-data-namespace): Task 9's `scripts/fleet-state.sh` resolves the cross-spec fleet home through the same `${CLAUDE_PLUGIN_DATA}` → writer-mode (`<claude-dir>/planwright/<manifest-name>/…`) chain that `scripts/resolve-overlay-root.sh` (adopter arm, lines ~228-283) already implements, so the dependency-free top-level-`"name"` manifest reader (the brace-depth-tracking awk) and `valid_identifier` are now duplicated byte-for-byte across the two scripts. This is on-contract, not drift: D-11 says fleet state uses "the writer-mode fallback the overlay resolvers use", and the two leaves differ (`/overlay` vs `/fleet`) plus the override env var differs (`PLANWRIGHT_ADOPTER_OVERLAY` vs `PLANWRIGHT_FLEET_STATE_DIR`), so a straight call into resolve-overlay-root would have coupled on the `/overlay` suffix and mis-handled the override arm. Candidate: extract a shared "plugin-data namespace root" primitive (returns the per-plugin namespace dir — `$CLAUDE_PLUGIN_DATA` in plugin mode, `<claude-dir>/planwright/<name>` in writer mode — with the manifest-name read + charset gate in one place), that both the overlay resolver and fleet-state.sh append their own leaf onto. Keeps the namespace/manifest logic single-sourced (the REQ-D1.1 "no second implementation" spirit) without coupling the two consumers' leaves or override knobs. Surfaced implementing orchestration-fleet Task 9 (2026-07-02).
- 2026-07-02 [planwright] shared-lock-stale-break-race(orchestrate-lock,fleet-state): the mkdir-based advisory lock's stale-break path (`scripts/orchestrate-lock.sh:146-159` and the mirrored `scripts/fleet-state.sh` `try_acquire`) is a non-atomic check-then-remove: `find -mmin +N` decides the lock is stale, then `rm -rf` + `mkdir` re-acquires. If ≥2 holders race the break of the SAME genuinely-stale lock, one can remove/replace the other's freshly-created lock and both `mkdir`-succeed — two holders at once on the crash-recovery path. Reproduced deterministically against fleet-state.sh (12-way race, ~38/40 rounds double-hold on the naive form; a `mv`-aside variant and a `find -mmin -exec rmdir` variant each only NARROW the window — the residual is find's own stat→exec / mv's operate-on-current gap — they do not close it). The NORMAL contention path (no stale lock) is fully serialized in both scripts and is unaffected. Trigger is narrow: a holder must be SIGKILLed at the instant it holds the ms-long lock and never release (so the lock ages past `stale_lock_threshold`, default 15m), AND ≥2 towers must break simultaneously. Correct closure needs a different lock discipline for the whole family (owner token written into the lock dir + ownership re-verified across the critical section, or a rename-claim with post-acquire ownership confirmation), applied to BOTH scripts together so they stay uniform. Task 9 ships the fleet lock at sibling parity (rm+mkdir) with the limitation documented in-code rather than diverging unilaterally with a half-fix. Candidate: a spec/design pass on a hardened planwright advisory-lock primitive shared by orchestrate-lock.sh and fleet-state.sh. Surfaced during /polish adversarial review of orchestration-fleet Task 9 (2026-07-02).
- 2026-07-02 [planwright] latent-parent-dir-edge(fleet-state): `scripts/fleet-state.sh`'s `mkdir_failure_kind` (line ~201) and `atomic_write` (line ~296) derive a file's parent dir via the in-shell `${var%/*}` expansion, which correctly yields the parent for every multi-segment path but returns the empty string for a single-leading-slash top-level path (`/.fleet.lock` → `""`). This is NON-REPRODUCIBLE in any realistic deployment: the fleet home always resolves to a multi-segment path (`$CLAUDE_PLUGIN_DATA/fleet`, `<claude-dir>/planwright/<name>/fleet`, or a ≥1-segment override), never `/`; and even in the pathological `PLANWRIGHT_FLEET_STATE_DIR=/` case the resulting `mkdir_failure_kind` → "error" → fail-closed exit is the CORRECT outcome for a non-root user (who cannot write `/`). The classification is only *wrong* (abort-instead-of-retry under benign contention) for a ROOT user with the home set to `/` — a triply-pathological config planwright never runs in. Two separate backend review passes flagged the `${var%/*}` construct (once mistakenly on the `.` case, which works; once correctly on the `/` edge). Decision: left as-is (correct for all realistic configs; changing working code for a non-reproducible edge fails the review-mode Refactor-Instinct bar). Candidate if ever hardened: switch both to `dirname "$var"` (already a script dependency via `dirname "$0"`), which is robust for all path shapes and would quiet the recurring flag. Surfaced during /panel-pairing (gemini backend) over orchestration-fleet Task 9 (2026-07-02). RESOLVED (2026-07-02, PR #114 /copilot-pairing): Copilot became the THIRD reviewer to flag the `${var%/*}` construct, at which point the recurring-noise cost outweighed the "don't churn working code for a non-reproducible edge" bar; Diego chose to apply the documented `dirname` hardening to both helpers rather than dismiss a fourth time. Behavior is identical for every reachable (multi-segment) path; the change only makes the unreachable single-leading-slash edge correct too.
- 2026-07-02 [planwright] fleet-bound-slot-leak(fleet-state,task-6): `scripts/fleet-state.sh`'s `bound-incr`/`bound-decr` are not a crash-atomic pair — `bound-incr` commits the counter increment and consumes a slot, but if the tower that acquired it is SIGKILLed before it ever runs `bound-decr`, that slot is never released and `$root/concurrency` drifts monotonically upward across the fleet's lifetime, so the fleet progressively under-dispatches until the counter is reset. This is inherent to a plain counter without crash reconciliation and is out of scope for Task 9 (which ships the atomic check-and-increment MECHANISM; the bound VALUE and policy are Task 6's, per the fleet-state.sh header). Seed for Task 6 (fleet-bound accounting): the bound policy should reconcile the counter against the live worker/scope registry (the registry `register` records are the ground truth for which towers actually exist) rather than trusting the free-running counter, or expose a periodic reconcile that floors the counter to the registry's live count — otherwise a crashed tower permanently narrows the effective bound. Surfaced during /self-review over orchestration-fleet Task 9 (2026-07-02).
- 2026-07-02 [planwright] test-helper-scoping(test-fleet-state): `tests/test-fleet-state.sh` defines its per-home helpers (`fenv`, `lenv`, `henv`) inside individual test cases and redefines `fenv` across sections (once for the override-home block, once for the bound-home block), so a case reorder or disable could leave a later case calling a stale/undefined helper — brittle, though harmless today. Pre-existing structure (not introduced or worsened by the Task 9 panel-pairing pass, whose added cases use inline `env` calls), so it was left out of scope under the review-mode Refactor-Instinct bar. Candidate: hoist `run_root`/`fenv`/`lenv`/`henv` to top-level scope (after the `trap`), each taking the home dir explicitly, so every case is self-contained and reorder-safe. Surfaced during /panel-pairing (gemini backend) over orchestration-fleet Task 9 (2026-07-02).
- 2026-07-02 [planwright] cross-spec-amendment-due(bootstrap-D-38): orchestration-fleet Task 4 (this PR) ships the `dispatch_isolation` knob defaulting to `per-step` (D-5, REQ-C1.3), flipping the historical default away from the per-unit behavior bootstrap D-38 assumes — D-38's prose "isolates context per unit (the real pain)" and "isolated context + native worktree per unit" now describes a non-default mode. This is the required cross-spec bootstrap D-38 amendment the brief tracks as risk row R3 ("Task 4 carries the amendment; tracked cross-spec"). bootstrap is a **Done** bundle (never-reopen), so per the supersede-pointer ritual (kickoff-lifecycle REQ-A1.7) the amendment is a `Superseded-by: orchestration-fleet D-5` pointer on bootstrap design.md D-38 (naming that per-unit is now a supported non-default mode), not an in-place prose edit — and it is a concrete new instance of the 2026-06-30 supersede-ritual(doctrine-gap) / 2026-07-01 stale-done-prose class (stale descriptive prose in a Done bundle with no in-file pointer, e.g. bootstrap D-38's per-unit framing). Carried as this surfacing + risk row R3 rather than a silent sibling-spec edit from an execution task (editing a Done sibling's contract is out of Task 4's scope). Candidate: drop the `Superseded-by:` pointer on bootstrap D-38 (and audit bootstrap prose sites that assume per-unit) via a scoped amendment or the drain loop. Surfaced during /execute-task over orchestration-fleet Task 4 (2026-07-02).
- 2026-07-02 [planwright] unqualified-cross-spec-req-citations(options-reference-config-rows): the `docs/options-reference.md` config rows and the resolver-script header comments cite "REQ-E1.4 by-layer" (malformed-value policy) and "REQ-D1.1" (config resolved through config-get) unqualified, but those are **customization-overlay** REQ-E1.4 / REQ-D1.1 (`specs/customization-overlay/requirements.md`), whose numbers collide with orchestration-fleet's own REQ-E1.4 (attention/notification capability) and REQ-D1.1 (meta-orchestration). The `dispatch_isolation` and `review_sequence` rows established the unqualified convention; the new `context_budget_threshold` row (Task 5) mirrors it faithfully, so this is inherited, not new drift. A reader cannot tell which spec's REQ is meant without prior knowledge. Candidate: qualify these cross-spec citations (e.g. "customization-overlay REQ-E1.4") across the options-reference config rows and the resolve-*.sh headers in a scoped docs pass, or adopt a spec-prefixing convention for cross-spec REQ references in shared docs. Surfaced during /execute-task /polish convergence over orchestration-fleet Task 5 (2026-07-02).
- 2026-07-03 [planwright] dual-registry-naming(fleet-state,fleet-attention): orchestration-fleet now has TWO "worker/scope registry" surfaces under the same cross-spec home — Task 9's append-only `scripts/fleet-state.sh` `registry` (durable membership + the concurrency lock/counter substrate) and Task 12's per-worker current-state store `scripts/fleet-attention.sh` (`<fleet-home>/attention/state`, the live-awareness layer the renderer/queue read). D-13's prose calls the thing the attention surface reads "the worker/scope registry", which reads as Task 9's registry, but the state dimension (working/awaiting-input/pr-ready/merged/done) and the structured decision live only in Task 12's store; Task 12 consumes Task 9's `root`/`lock` but not its `registry` file. Harmless today (the two are cleanly separated: membership+concurrency vs awareness), but a reader could conflate them. Candidate: a doc/design clarification naming the two layers distinctly (e.g. "coordination registry" vs "attention/awareness store"), or a Task 10/Task 6 pass that decides whether the meta-tower's membership view should read one or both. Surfaced during /execute-task over orchestration-fleet Task 12 (2026-07-03).
- 2026-07-03 [planwright] resolver-echo-discipline-gap(resolve-*.sh family): the config-knob resolvers echo the raw config-derived value into their malformed-value diagnostics without sanitizing C0/DEL. `scripts/resolve-notification-channel.sh` (Task 12) was fixed to source `echo-safety.sh` and wrap `$value`/`$core_value` in `sanitize_printable` (config-get strips comment/quote/whitespace but NOT control bytes, so a hostile `notification_channel: "<ESC>]0;pwned<BEL>"` in a repo-tracked/adopter/machine-local overlay would otherwise reach the operator's terminal raw — the exact terminal-escape-injection class security-posture's echo discipline forbids). The SIBLINGS `scripts/resolve-context-budget-threshold.sh` and `scripts/resolve-dispatch-isolation.sh` share the same raw-echo pattern verbatim (they were the template Task 12 copied) and were NOT touched (out of Task 12's scope). Candidate: a scoped family-wide pass applying the same echo-safety sourcing + sanitize wraps to every resolve-*.sh malformed-value diagnostic, so the discipline is uniform. Surfaced during /execute-task /polish convergence (security lens fan-out) over orchestration-fleet Task 12 (2026-07-03).
- 2026-07-03 [planwright] stale-sibling-header-claim(fleet-state): `scripts/fleet-state.sh:6-7` (Task 9, a merged/out-of-scope file) describes its own append-only `registry` as "the worker/scope registry the attention surface reads (Task 12)", but the shipped Task 12 attention surface (`scripts/fleet-attention.sh` render/queue) reads its OWN per-worker state store at `<fleet-home>/attention/state` and consumes only fleet-state's `root`/`lock`/`unlock`, never its `registry`. The header's Task 12 forward-reference is now inaccurate. Related to the 2026-07-03 dual-registry-naming note. Candidate: correct the fleet-state.sh header's Task 12 reference (and/or adopt the "coordination registry" vs "attention/awareness store" naming) in a scoped docs pass. Surfaced during /polish convergence (cross-file lens) over orchestration-fleet Task 12 (2026-07-03).
- 2026-07-03 [planwright] c1-control-bytes-unstripped(echo-safety sanitizer family): the shared `scripts/echo-safety.sh` `sanitize_printable` strips only C0 + DEL (`\000-\037\177`), leaving the C1 range (`\200-\237` — notably 0x9B CSI, 0x9D OSC, 0x90 DCS) intact. Every caller that relies on it as the sole terminal-injection defense inherits the gap: `scripts/fleet-attention.sh` `valid_text` ACCEPTS a decision/summary containing C1 bytes (they survive the sanitizer equality check), and `render`/`queue`/the `notify` summary re-emit them; the `resolve-*.sh` malformed-value diagnostics likewise. On a terminal/multiplexer honoring 8-bit C1 controls (some xterm/non-UTF-8 configs) this injects SGR/OSC sequences — the escape-injection class the guards claim to close; inert on a UTF-8 terminal that treats 0x9B as an invalid byte, so exploitability is terminal-dependent. Doctrine deliberately scopes the sanitizer to C0+DEL, so this is a repo-wide policy question, not a Task-12 defect (declined on-branch as out of scope). Candidate: decide whether the echo-discipline contract should also reject/strip `\200-\237` (in the shared sanitizer, and/or in `valid_text`), then apply family-wide. Surfaced during /self-review (security lens fan-out) over orchestration-fleet Task 12 (2026-07-03).
- 2026-07-03 [planwright] wrong-req-number-graceful-degrade(resolve-*.sh siblings): the three sibling resolvers `scripts/resolve-context-budget-threshold.sh`, `scripts/resolve-dispatch-isolation.sh`, and `scripts/resolve-review-sequence.sh` cite **REQ-K1.6** for their broken/partial-install graceful-degrade fallback, but bootstrap REQ-K1.6 is "target GitHub via the `gh` CLI for PR operations" — the graceful-degradation requirement is **REQ-K1.7** ("Skills SHALL degrade gracefully on missing prerequisites"). The new `scripts/resolve-notification-channel.sh` (Task 12) cites K1.7 correctly, so the resolver family now disagrees on the number and the three older siblings carry the wrong one. Distinct from the 2026-07-02 unqualified-cross-spec-req-citations note (that is about *qualification* of E1.4/D1.1; this is a plain wrong number). The fix touches out-of-diff siblings, so it was left out of Task 12's scope. Candidate: correct the three siblings' K1.6→K1.7 citation in a scoped docs pass. Surfaced during /self-review (tests/cross-file lens) over orchestration-fleet Task 12 (2026-07-03).
- 2026-07-03 [planwright] notify-summary-uncapped(fleet-attention): `scripts/fleet-attention.sh` `notify` sanitizes its summary to a control-free line but, unlike the decision fields (`valid_text` caps at 512 bytes), applies no length bound before appending it under lock to the editor-toast file or handing it to a platform push. Low severity (caller-assembled operational one-liner, local single-user toast, lock-serialized — no meaningful threat), declined under proportionality. Noted for symmetry with the capped decision fields. Candidate: apply the same 512-byte cap to the notify summary if a bound is wanted. Surfaced during /self-review (security lens) over orchestration-fleet Task 12 (2026-07-03).
- 2026-07-03 [planwright] spec-dir-path-echo-discipline-gap(orchestrate-degrade,orchestrate-marker): the untrusted-ish spec-dir path argument reaches the operator's terminal raw in error diagnostics (`orchestrate-degrade.sh` "no such spec dir"/"cannot create state dir"/record-abort paths; `orchestrate-marker.sh:75` has the identical raw `$spec_dir` echo). Backend-name and task-id tokens in the same scripts ARE routed through `sanitize_printable`; the spec-dir path is not. Low severity (the spec-dir is derived by /orchestrate from a validated spec id), and fixing only orchestrate-degrade.sh would diverge from the sibling marker's shipped pattern, so declined on-branch for parity. Candidate: a scoped family-wide pass sanitizing spec-dir paths in orchestrate-*.sh diagnostics. Surfaced during /polish convergence (security lens) over orchestration-fleet Task 3 (2026-07-03).
- 2026-07-03 [planwright] script-bare-cross-spec-req-citations(orchestrate-degrade + siblings): `scripts/orchestrate-degrade.sh` (and its test) cite `REQ-F1.1` (orchestration-concurrency), `REQ-J1.1`/`REQ-K1.5` (bootstrap) UNQUALIFIED. design.md:9 mandates namespace-qualifying foreign REQ IDs, but that rule is written for spec docs; the sibling scripts (`orchestrate-marker.sh` cites `REQ-F1.1` bare, `orchestrate-backends.sh` cites `REQ-K1.5` bare) and the `/orchestrate` SKILL.md (cites `REQ-F1.1`/`REQ-J1.1` bare pervasively) establish a bare-citation convention for these cluster REQs, and no doc-link check flags them. Qualifying only the new files would create local inconsistency, so declined on-branch. `REQ-F1.1` is additionally ambiguous (defined with different meanings in bootstrap/orchestration-concurrency/spec-comprehension/autopilot-reflex). Candidate: a policy decision on whether the foreign-ID qualification rule extends to scripts/skills, then a uniform pass. Surfaced during /polish convergence (consistency lens) over orchestration-fleet Task 3 (2026-07-03).
- 2026-07-03 [planwright] double-derivation-per-spec(orchestrate-meta-select,task-6): the meta-tower selector `scripts/orchestrate-meta-select.sh` runs the live derivation twice per supervised spec per step — once directly (`orchestrate-state.sh`, to sum fleet-wide in-flight units for the bound) and once inside `orchestrate-select.sh` (which re-derives the same state to pick the spec's ready unit). Each derivation shells out to git (and optionally `gh`), so a fleet of N specs costs 2N derivations per meta step. It is correct and, at the multi-second metronome cadence with small N, negligible; but it duplicates the expensive work rather than deriving once and reusing. This is a deliberate composition choice (reusing the single-spec primitives unchanged instead of duplicating their dependency-parse / critical-path logic into the meta layer). Candidate: give `orchestrate-select.sh` an opt-in mode that also emits the spec's in-flight count (or a combined "derive-and-pick" primitive both the single tower and the meta selector consume), so the meta step derives each spec once. Surfaced implementing orchestration-fleet Task 6 (2026-07-03).
- 2026-07-03 [planwright] fixed-relay-buffer-name(orchestrate-relay,task-7): `scripts/orchestrate-relay.sh` `relay-command` emits a buffer-paste using a single fixed tmux buffer name `planwright-relay` (`load-buffer -b planwright-relay -` then `paste-buffer -b planwright-relay -t <handle> -d`). tmux paste buffers are tmux-server-global, so two relays issued concurrently against the same server (the meta-tower scenario, where a supervising tower and a subordinate tower could both relay in the same server) could interleave load/paste on the shared name and deliver a crossed payload. Real risk is low — a single tower issues relays serially and the load→paste window is tiny — so it was not fixed on-branch under proportionality (Task 7's contract is the security boundary: attribution, handle validation, message-as-data, no send-keys, all delivered). Candidate: derive a per-relay-unique buffer name (e.g. incorporate a sanitized handle or an invocation nonce) so concurrent relays never share a buffer. Surfaced during /execute-task /polish convergence (concurrency lens) over orchestration-fleet Task 7 (2026-07-03).
- 2026-07-03 [planwright] fleet-bound-slot-leak reaffirmed + in-flight-model clarified(fleet-state,orchestrate-meta-select): a /panel-pairing concurrency-lens fan-out over Task 7 independently rediscovered the 2026-07-02 fleet-bound-slot-leak(fleet-state,task-6) entry above (bound-incr/bound-decr is not a crash-atomic pair; a SIGKILLed holder leaks its slot forever, wedging the fleet on phantom slots) and, as its companion, the ambiguity that `orchestrate-meta-select.sh` decides fleet in-flight purely from the live git derivation and never reads the `bound-incr` counter even though the fleet-state.sh header framed that counter as the fleet-bound reservation. Diego's disposition (2026-07-03): (1) DEFER the slot leak to the shared lock-family owner-token redesign already tracked by shared-lock-stale-break-race(orchestrate-lock,fleet-state) — not a piecemeal per-counter fix — so it stays uniform across the lock family; (2) the in-flight-model ambiguity was resolved IN THIS PR as a doc-only clarification (both the orchestrate-meta-select.sh and fleet-state.sh headers now state the live git derivation is the authoritative, self-healing in-flight source of truth and the counter is a separate, non-self-healing same-instant reservation primitive that the selector does not read). The slot-leak reconcile itself remains the open follow-up (Task 6 registry-reconcile per the 2026-07-02 entry, or the lock-family redesign per the stale-break entry). Surfaced during /panel-pairing (gemini backend + independent lens fan-out) over orchestration-fleet Task 7 (2026-07-03).
- 2026-07-03 [planwright] decision-writes-in-stale-break-window(fleet-state lock): the Task 9 advisory lock's documented stale-break recovery admits a short double-hold window (two acquirers both `mkdir`-succeeding on a genuinely stale lock, `scripts/fleet-state.sh`), and the Task 10 `--fleet` wiring now routes decision-queue `decide` writes through it — so a lost update in that window is no longer only a heartbeat/registry row but a silently dropped human decision on the attention surface (`tasks.md` keeps the durable entry; the queue misses it until the level-triggered mirror re-issues it). Pre-existing, documented primitive tradeoff, not worsened in code by Task 10; declined on-branch. Candidate: assess whether the window warrants a stronger break protocol (e.g. break-token compare-and-swap) now that the queue is an actionable surface, or document the mirror-repairs-it posture as the accepted answer. Surfaced during /self-review (concurrency lens fan-out) over orchestration-fleet Task 10 (2026-07-03).
- 2026-07-03 [planwright] present-tests-unrecorded-in-test-spec(orchestration-fleet): the shipped automated `present` tests (tests 19–26 in `tests/test-orchestrate-backends.sh`, exercising REQ-E1.2/E1.5 surface behavior and the D-13 `--surface-provided` deferral) exceed the test-spec's pinned verification floor — REQ-E1.2 is `[manual + design-level]`, REQ-E1.5 `[design-level + manual]`, and no `[test]` entry names them. Not drift (the floor is met and exceeded), but the test-spec undercounts real CI coverage; adding `[test]` lines is a meaning-class spec amendment reserved to the kickoff owner, so it was declined on-branch. Candidate: fold the tests into the REQ-E1.2/E1.5 (or REQ-B1.2) test-spec entries at the next kickoff-owner delta re-walkthrough. Surfaced during /self-review (tests lens fan-out) over orchestration-fleet Task 10 (2026-07-03).
- 2026-07-03 [planwright] skill-drift(self-review): the pre-flight kickoff-brief detection's second rung looks for "a single Active spec (`specs/*/requirements.md` with `Status: Active`) with a sibling kickoff-brief.md", but under the spec-format six-status lifecycle a signed-off spec sits at **Ready** until the first dispatched task derives Active — so a Ready spec with a signed brief is invisible to that rung and the pass would wrongly proceed brief-less (disabling the Agent-resolvable bucket). Masked whenever the branch-convention rung matches first (as in this run on planwright/orchestration-fleet/task-10). Candidate: widen the rung to `Status: Ready|Active`. Same drift family as the dotfiles execute-task Active-only gate. Surfaced by /self-review's Maintenance step over orchestration-fleet Task 10 (2026-07-03).
- 2026-07-04 [planwright] derived-active-flip-stales-anchor(spec-format,tasks-pr-sync): the derived Ready→Active status flip (kickoff-lifecycle D-2, written by the sole-writer reconcile) changes the bundle content anchor, because the manifest digests requirements.md/design.md/test-spec.md as whole files and those headers carry `**Status:**` — so the first dispatch after sign-off (and the later Active→Done flip) stales every subsequent freshness-gate check until someone writes an expression-only re-anchor, even though the flip is machine-derived state, not meaning. Hit live on the orchestration-fleet Task 11 dispatch (this run wrote the re-anchor entry by hand). Candidate: exclude the `**Status:**`/`**Last reviewed:**` header lines from the three whole-file digests (mirroring the tasks.md state-annotation exclusion, "state moves never trip the gate while meaning edits always do"), or have tasks-pr-sync append the marked expression-only re-anchor entry itself when its status flip moves the anchor. Surfaced during /execute-task pre-flight/state-move over orchestration-fleet Task 11 (2026-07-04).
- 2026-07-04 [planwright] fleet-knobs-table-defaults-untethered(docs): the docs/fleet.md capability-vs-style knobs table restates six config defaults (subagent, per-step, 50, 3, 3, none) with no machine check tying them to config/defaults.yml — check-options-reference.sh scopes only defaults.yml ↔ options-reference.md, so a future default change silently diverges the fleet.md table (all six match today). Candidate: widen check-options-reference.sh (or a sibling check) to cover fleet.md's table defaults, or drop the literal values from the table in favor of the options-reference pointer. Surfaced during /self-review (tests lens fan-out) over orchestration-fleet Task 11 (2026-07-04).
- 2026-07-07 [planwright] write-only-accumulator([manual] test-spec tag): a `[manual]` test-spec entry defers verification to "the next real emission" with no named reader and no drain ritual — a write-only deferral, exactly what accumulator-taxonomy's "every accumulator has a named reader and a drain ritual" invariant forbids. Observations have the drain pass; GATE deferrals have the gate evaluator; `[manual]` verification obligations have neither, so a prose contract can rot undetected if no one remembers to inspect the next PR body / drafted bundle. Candidate: extend the `/drain` pass (which already surfaces observations + GATE deferrals) to count, per spec, the `[manual]` entries whose next-emission verification has not been exercised, making un-swept manual verifications visible the way unmined observations are. Surfaced at specs/output-hygiene kickoff §7 (row 1); recorded as a doctrine-level fix rather than patched spec-locally (which would mis-level a systemic gap).
- 2026-07-07 [planwright] validator-blind-spot(spec-validate decision structure): `spec-validate.sh`'s D-ID structure check keys on `^### D-[0-9]+:` (H3) and `^\*\*Decision:\*\*` (colon). A `design.md` that uses `## D-N` (H2) headings and period field labels (`**Decision.**`) passes 0/0 — the regex never matches the decisions, so the Decision/Alternatives/Chosen presence check runs over zero blocks and the non-conformant bundle validates clean. Discovered because specs/output-hygiene's own `design.md` shipped H2+period from `/spec-draft` and validated green until the kickoff lens pass caught it by eye. Candidate: flag any decision-shaped heading that is not `### D-<n>:` (e.g. `## D-N`, or `### D-N` without the colon-title) and any period-labelled Decision/Alternatives/Chosen field as a malformed-decision error, so the structure rule is enforced rather than silently skipped. Surfaced at specs/output-hygiene kickoff §8 lens pass (Cluster 3.2).
- 2026-07-07 [planwright] decision-domains-catalog-gap(human-comprehension/information-UX): the catalog has no entry for the human-comprehension / information-UX domain (artifacts generated for a human reader — PR bodies, reports, rendered views), yet specs/output-hygiene's D-2 (and the spec's whole premise) decides squarely in it, and D-2 flags the gap explicitly. Candidate: add a domain to `doctrine/decision-domains.md` + `catalogs/decision-domains.yaml` — trigger: a generated artifact a human reads (PR body, report, rendered view); considerations: above-the-fold summary, progressive disclosure, no machine-formatting-for-the-writer (hard-wrap, flat dumps); disposition: convention proceeds, a novel human-read surface escalates. Surfaced at specs/output-hygiene kickoff (D-2); routed here for the catalog's own amendment rather than decided in-bundle.
- 2026-07-07 [planwright] skill-drift(spec-kickoff): the personal `~/.claude/skills/spec-kickoff` copy (dated 2026-06-18) still names the pre-kickoff-lifecycle five-status model — sign-off flips Draft→Active and leaves a draft-only PR ("the PR is always a draft; the draft→ready flip and the merge are the human's") — but planwright 0.2.6 doctrine (`spec-format.md` status lifecycle) is the six-status model: sign-off flips Draft→**Ready** (Active is derived on the first task to start), and `mark_spec_pr_ready_on_kickoff` (default true) marks the spec PR ready as the terminal step. This specs/output-hygiene run followed current doctrine (the skill subordinates named concepts to the doctrine docs), flipping to Ready. Candidate: re-sync or remove the personal `~/.claude/skills/spec-kickoff` so `/spec-kickoff` resolves to `planwright:spec-kickoff`. Surfaced at specs/output-hygiene kickoff pre-flight + Maintenance step (2026-07-07).
- 2026-07-08 [planwright] skill-drift(spec-draft): the skill's archive-on-consume section still reads "(The accumulator-taxonomy doctrine, when it ships, is the canonical home of this drain ritual; until then this section defines it.)" — accumulator-taxonomy has shipped and names the observations log a class-3 accumulator with `/spec-draft` as canonical reader, but the archive-move mechanics remain skill-locally normative and the doctrine never adopted them. specs/observation-recording (drafted today) supersedes both with the fragment ritual (its Task 5 reconciles skill + doctrine); until it executes, the stale "when it ships" clause misstates where the ritual is defined. Surfaced by /spec-draft maintenance during the observation-recording drafting session.
- 2026-07-08 [planwright] resolve-rule-doc.sh fails inside planwright's own checkout and worktrees unless PLANWRIGHT_ROOT is exported: the fallback chain (PLANWRIGHT_ROOT → CLAUDE_PLUGIN_ROOT → writer root ~/.claude/planwright) never considers "the repo I am standing in IS planwright", so every skill run self-hosted must remember the env var. Candidate fix: append a cwd-walk arm (a directory containing doctrine/spec-format.md + plugin.json) to the chain. Surfaced by the observation-recording /spec-kickoff pre-flight (2026-07-08).
- 2026-07-08 [planwright] The observation-recording kickoff's nine-lens fan-out over a freshly drafted bundle returned 86 raw / 42 valid findings (guard gaps, crash windows, reconciliation-scope misses) — a scale suggesting /spec-draft could run a scoped self-critique or mini lens pass over its own output before handing to /spec-kickoff, shifting cheap catches one stage left. Surfaced during the observation-recording kickoff sign-off (2026-07-08).
- 2026-07-08 [planwright] drain-gates.sh status atoms predate the six-status lifecycle: every Ready spec logs "unrecognized status; status atoms referencing it will not evaluate" (the grammar knows draft/active/done/retired/superseded only), so a status-atom gate on a Ready spec cannot evaluate. Candidate: add `ready` to the status-atom grammar in doctrine/accumulator-taxonomy.md and drain-gates.sh together. Surfaced by the observation-recording /self-review pass running the drain end-to-end (2026-07-08).
- 2026-07-08 [planwright] free-text-gate foot-gun: a gate written as `GATE(when: <prose>)` parses as a structured gate with an unrecognized atom and reports MALFORMED, but /spec-kickoff and /spec-draft produced exactly that on specs/observation-recording while calling it a free-text gate (the doctrine's free-text form is plain prose after `**Gate:**`, no wrapper). Candidate: spec-validate (or the drain report) hints "free-text gates must not use the GATE( wrapper" when the condition matches no atom, and the skill texts state the plain-prose form explicitly. Surfaced by the observation-recording /self-review pass (2026-07-08).
- 2026-07-08 [planwright] /spec-kickoff marked PR #128 ready while its GitHub CI was red (lint:md MD034 on the brief it had just committed): the mark_spec_pr_ready_on_kickoff terminal step verifies no CI state. Candidate: gate the ready-flip on the head SHA's checks being green (same discipline the gauntlet applies), or run the repo's lint locally before the flip. Surfaced by the observation-recording /self-review pass (2026-07-08).
- 2026-07-08 [planwright] spec-format gap: no sanctioned home for a superseded task block. The output-hygiene scope-down had to park Tasks 1-2 (superseded by specs/observation-recording) as whole H3 blocks under `## Out of scope` — a section doctrine defines as holding "exclusion bullets" — because the constraint set (stable IDs never removed, definition fields never deleted, must never dispatch, must not count toward Done) has no better fit; test-spec handling of a superseded REQ group (entries removed, tombstone note) is likewise undocumented. Candidate: spec-format names the pattern (superseded task blocks live in Out of scope with a superseded Status annotation; superseded REQ groups drop their test-spec entries) so future carve-outs don't improvise. Surfaced at the output-hygiene scope-down delta re-walkthrough (2026-07-08).
- 2026-07-08 [planwright] orchestrate concurrency accounting is section-blind: orchestrate-state.sh derives a state for every `### Task` block regardless of section, and the in-flight counts in orchestrate-meta-select.sh / the max_parallel_units cap apply no section filter — so an Out-of-scope (e.g. superseded) task with lingering branch/PR/marker evidence would be counted as a permanent phantom in-flight unit that never clears (selection is section-aware and will never advance it; merge never happens). Hypothetical today: output-hygiene Tasks 1-2 have no such evidence and Out-of-scope tasks are never dispatched, but a stale hand-made branch would trip it. Candidate: exclude Deferred/Out-of-scope tasks from in-flight accounting. Surfaced by the output-hygiene scope-down lens pass (error-handling lens, 2026-07-08).
- 2026-07-09 [planwright] /spec-kickoff applied an agent-authored meaning-class edit mid-walk (the prompt-hygiene §3 Task 7.5 + REQ additions) that introduced a real deadlock (non-exemptible start-load + a downstream-only fix gating every upstream green-check) plus an arithmetic coupling error; the fault was caught only by the terminal sign-off lens pass, after it had propagated through §4–§7. Candidate: run a scoped lens / self-critique over agent-introduced meaning-class edits at the point they are applied during the walk (not only at sign-off), so a self-introduced spec bug is caught before later sections build on it. Complements the 2026-07-08 shift-left observation (there: /spec-draft self-critiquing its output; here: /spec-kickoff self-checking its own mid-walk edits). Surfaced during the prompt-hygiene kickoff sign-off (2026-07-09).
- 2026-07-09 [planwright] output-hygiene Task 5's "the guard-catalog `../skills/` link is the sole real violation" premise (signed 2026-07-06) went stale on 2026-07-07 when the orchestration-fleet layer (v0.3.0) added three `../docs/` links to doctrine (`attention-notification-capability.md`, `backend-capability-contract.md`) — a delivered-dead class identical to `../skills/` (writer-install ships neither `docs/` nor `skills/` as a doctrine sibling; verified against install.sh). Task 5 shipped the strict lint per D-4's decided rule and fixed all four links (the count claim was stale, not a meaning change), but a spec whose known-violation inventory is enumerated by count silently rots as sibling layers grow. Candidate: prefer decided-rule statements over enumerated counts in task deliverables, or have the kickoff freshness gate re-scan the rule's live surface (not just the recorded anchor) when the guarded tree is outside the spec bundle. Surfaced during output-hygiene Task 5 execution (2026-07-09).
- 2026-07-09 [planwright] Stale anchor shipped in a squash: output-hygiene's 2026-07-08 §9 anchor was computed before a same-run post-review fix (the panel-pass reword) was applied, both landed in PR #129's squash, and every dispatch then halted on the freshness gate until a dedicated anchor-reconciliation delta re-walkthrough (2026-07-09). Candidate: any post-sign-off spec edit on the spec PR (review/panel fixes included) must re-trigger the anchor recompute as the final pre-push step, and/or CI verifies the brief's most recent anchor entry matches recomputation of the committed bundle. Complements the 2026-07-08 red-CI ready-flip observation (both are missing terminal-state verification in the kickoff finishing ritual). Surfaced by the output-hygiene anchor reconciliation (2026-07-09).
- 2026-07-09 [planwright] The autopilot-reflex bundle renders the altitude ladder three ways across its own describers: doctrine/autopilot-reflex.md and REQ-A1.1 say "impulse/doctrine ... capability ... mechanism ... local value", tasks.md Task 1 abbreviates to "impulse/capability/mechanism/value" (dropping the "doctrine" synonym and "local"), and kickoff-brief.md §2/glossary say "doctrine / capability / mechanism / local value" (dropping "impulse"). All predate the Task 1 branch and live in signed spec artifacts, so no task-PR fix applies. Candidate: an expression-only spec amendment aligning the ladder's labeling on one canonical rendering. Surfaced by the Task 1 polish pass (cross-file lens, 2026-07-09).
- 2026-07-09 [planwright] Doctrine citation-namespace style is split three ways with no stated convention: README index rows for non-core specs prefix every ID (autonomous-safe-decision, customization-boundary rows) or only the group leader (the four orchestration-fleet rows, now the autopilot-reflex row); in-doc Citations lines range from fully bare for a foreign home spec (customization-boundary.md) to fully prefixed (autonomous-safe-decision.md) to leader-prefixed (autopilot-reflex.md). Each form is locally unambiguous but the inconsistency invites copy-paste drift and misread cross-spec references. Candidate: doctrine/README.md states one convention for index rows and in-doc Citations lines. Surfaced by the Task 1 polish pass (naming + cross-file lenses disagreeing about which convention exists, 2026-07-09).
- 2026-07-09 [planwright] Doctrine cross-link text style is split four ways with no stated convention, the link-text sibling of the citation-namespace split above: doctrine prose links render as title-case ([Proportionality], [Gate Wiring]), filename ([proportionality.md], [engineering-decisions.md]), bare slug ([security-posture] in attention-notification-capability.md, [customization-boundary] in autopilot-reflex.md), or descriptive ([decision-domains catalog], [drain-loop]); some docs mix styles internally (refactor-instinct.md uses [Discovery Rigor] and [proportionality.md]). check:links and markdownlint enforce nothing here, so a reviewer can assert whichever "convention" they first sample. Candidate: fold link-text style into the same doctrine/README.md convention statement the citation-namespace entry proposes. Surfaced by the Task 1 copilot-pairing pass (a reviewer claimed a title-case convention that does not exist, 2026-07-09).
- 2026-07-09 [planwright] A CONFLICTING task PR never fires the pull_request CI trigger, so the "[test] ownership: external CI state is the gate" contract silently never engages: PR #134 was created already conflicting with #131's observations-log append (merged ~40 min earlier), has zero check runs on any SHA, yet the local convergence record reads green. No skill or reconcile step detects mergeable=CONFLICTING plus absent check runs as a blocked external gate. Candidate: /self-review's standalone publish step and /orchestrate --bookkeeping surface that pair as an awaiting-input condition (the worker owns the merge-from-main resolution). Surfaced by the Task 1 self-review pass (2026-07-09).
- 2026-07-09 [planwright] observation-recording Task 1: obs-record.sh enforces a `--scope` grammar (1-64 chars, must open alphanumeric, charset `[A-Za-z0-9._-]`) that no requirement, design decision, or glossary entry defines. REQ-A1.4 fixes the entry form and its `[<scope>]` slot but leaves the scope token uncharacterized, and REQ-D1.1's `^[a-z0-9][a-z0-9-]*$` grammar is the consume-side spec-identifier, not record's scope. Task 2's `check:obs` guard must validate the same body/scope shape, so the writer side and the guard side have no shared source of truth and can silently drift. Candidate: pin the scope grammar into a contract (a REQ or D-decision) so Task 1's writer and Task 2's guard reference one definition. Surfaced by the observation-recording /self-review pass (2026-07-09).
- 2026-07-09 [planwright] release-publish.sh's CI gate treats a release commit with zero GitHub checks (a null statusCheckRollup) as not-green and refuses — a deliberate strict default for planwright's own repo (which always has CI), but it blocks an adopter with no CI from ever publishing via the script. Candidate: a `require_ci` knob (default on) or an adopter-path doc note that CI is a publish prerequisite. Surfaced by the autopilot-reflex Task 4 polish pass (CI-gate GraphQL switch, 2026-07-09).
- 2026-07-09 [planwright] The release scripts' version_file path-access guard refuses absolute paths and `..` traversal components but does not canonicalize, so a repo-relative symlink pointing outside the tree is still read by release-pending.sh (release-publish.sh reads via `git show` and is immune). Proportional today (the path is operator-own config, read-only), but the doctrine's "containment-checked after canonicalization" is only partially met. Candidate: a shared canonicalizing path-containment helper if more framework scripts start taking config-specified filesystem paths. Surfaced by the autopilot-reflex Task 4 polish second-pass (2026-07-09).
- 2026-07-10 [planwright] Task 4 self-review (errors lens): `release_exists()` (release-publish.sh) returns non-zero on ANY `gh release view` failure (network/auth/rate-limit), unlike the ls-remote and CI gates which deliberately fail closed. A transient gh error during resume-detection when the Release genuinely exists flips resume=1 → `gh release create` → server refuses the duplicate (no bad mutation), but the operator sees a misleading "creation failed / re-run to resume" and every outage retry repeats the wasted work. Recommended fix (surfaced as Needs-sign-off, not applied — security-sensitive publish gate): distinguish a gh/query failure (die "could not query") from a genuinely absent Release, matching the other probes' fail-closed posture.
- 2026-07-10 [planwright] Task 4 self-review (errors lens): REQ-D1.3's partial-publish resume is specified and implemented narrowly (resume the CURRENT target's tag when its Release is absent). Two edges beyond that contract: (E2) an origin tag arriving by non-script means is trusted unconditionally on resume — creation gates (CI-green/monotonicity/sync) and signature-verify are skipped; and if the origin tag's commit differs from the locally-recomputed release_sha (main rewritten), the Release attaches to the tag's commit while notes come from release_sha (silent tag/notes mismatch). (E3) a stranded partial publish (tag pushed, Release failed) is unresumable-by-tool once a newer version bumps version_file, since resume only probes the current version's tag. Security cleared both (no privilege gained — needs origin write access). Candidate resume-scope amendment: re-run gates on resume, assert origin-tag SHA == release_sha, and/or scan all release tags for a missing Release.
- 2026-07-10 [planwright] Task 4 self-review (tests lens, high): the gh stub's `api graphql` branch returns CI state purely from GH_CI and ignores its `-f sha=` argument, and no test asserts the sha CI is checked on equals release_sha. REQ-D1.2/E1.4's core claim (CI confirmed on the release-merge SHA, not HEAD) is therefore unverified — a regression that checked `ci_green "$(git rev-parse HEAD)"` would keep every test green. The stub already logs the sha to GH_LOG; add an assertion comparing it to release_sha. Belongs in Task 4's test-spec / a test-hardening follow-up (also uncovered: the CI-query-failure rc=2 path, the `require`+signing-configured success path, and release-pending.sh's own copy of the absolute/`..`/not-found path guards).
- 2026-07-10 [planwright] Task 4 self-review (tests lens): the leading-zero and empty-identifier prerelease rejections are now tested, but the positive prerelease/build precedence surface of rl_version_gt (numeric-vs-alphanumeric ranking, fewer-fields-lower, -rc.1 < -rc.2, build-metadata accept-and-ignore) remains largely untested — `pending` only ever compares release-vs-release, so any prerelease-ordering regression slips through. Candidate test-hardening follow-up alongside the CI-sha assertion above.
- 2026-07-10 [planwright] Task 4 self-review (portability lens, low): release-lib.sh rl_version_gt uses `((10#$x > 10#$y))` (64-bit signed) for numeric SemVer identifiers; an identifier of ~19-20+ digits overflows and mis-ranks. Not a realistic version string (declined this pass), but if arbitrary-length numeric fields ever matter, a length-then-lexical compare for equal-length numeric identifiers is overflow-proof.
- 2026-07-09 [planwright] spec-anchor.sh hashes the whole file for requirements/design/test-spec (only tasks.md extracts definition-content), so their `**Status:**` line rides into the content anchor — contradicting the script's own header ("the Status ... annotations are excluded, so /orchestrate state moves never change the anchor"). Effect: the derived Ready->Active flip (D-2/D-3, written by the reconcile) changes the anchor, so /execute-task's execution freshness gate (REQ-F1.9) false-mismatches for any task dispatched after the first on a multi-task spec whenever it computes the anchor against a checkout carrying the reconcile's (even uncommitted) Active flip. observation-recording Task 2 hit this: committed main and the worktree both matched the recorded anchor b45ea8b, but the primary checkout's uncommitted Ready->Active reconcile computed a different anchor; only reasoning that the anchored *bodies* were byte-identical (Status atoms + section placement aside) unblocked the dispatch. Candidate: exclude the Status line from the per-file digest of all four spec files (definition-content extraction, as tasks.md already does) so the derived Ready<->Active transition is genuinely anchor-invariant. Surfaced during observation-recording /execute-task Task 2 pre-flight (2026-07-09).
- 2026-07-09 [planwright] obs-record.sh's UID-uniqueness check globs `"$_dir"/*-"$1".md` (line 142), which — like the pre-fix check-obs.sh enumeration — never matches a leading-dot name, so a hidden `.<date>-<slug>-<uid>.md` fragment (or a leftover `.obs-record.XXXXXX` temp that survived) is invisible to the writer's own collision check and could let obs-record mint a duplicate `obs:<uid>`. Task 2's check:obs guard was just hardened to enumerate `.*` and reject dot-prefixed names; the writer side still shares the blindspot. Candidate: mirror the `.*`-enumeration fix (or a `find`-based scan) in obs-record.sh's uniqueness scan so writer and guard agree on visibility. Surfaced by the observation-recording Task 2 /self-review pass (2026-07-09).
- 2026-07-09 [planwright] check-obs.sh screens NUL-bearing fragments only reactively: it honors check_content's awk exit code, which defends awks that *abort* on an embedded NUL, but an awk that silently truncates a record at the first NUL (historical mawk) drops post-NUL bytes on the entry line from validation without failing. The sibling drain-gates.sh:197 screens NUL up front (`wc -c` vs `tr -d '\000'`) before trusting awk; check-obs does not, so its "a hand-edited fragment cannot slip past" posture is awk-implementation-dependent for the NUL case. A portable regression test is also blocked by the same split (a NUL test passes on abort-awks, fails on truncate-awks). Candidate: add an explicit up-front NUL screen per fragment (mirroring drain-gates) so the verdict is awk-independent and testable. Surfaced by the observation-recording Task 2 /self-review pass (2026-07-09).
- 2026-07-10 [planwright] Inline-copy drift in the echo-discipline family: the canonical shared display sanitizer scripts/echo-safety.sh (sanitize_printable) already strips C0+DEL+C1 (its C1 coverage was widened in PR #112, closing the 8-bit-CSI 0x9B vector), and its sourced consumers spec-validate.sh + spec-walkthrough.sh inherit that. But several scripts keep their OWN inline copy of the rule instead of sourcing the helper, so the strip set can drift: spec-scope.sh (line 66) and spec-assemble.sh (line 88) still strip C0+DEL only — a follow-up echo-safety.sh's own header already tracks — and check-obs.sh's safe(), just widened to strip C1 for Task 2, is a third inline copy. The storage-path obs-record.sh deliberately preserves C1 as UTF-8 (a distinct, correct posture, not drift). Candidate: fold check-obs.sh's safe() onto sourcing echo-safety.sh and widen the remaining spec-scope/spec-assemble inline copies, so the display-sanitizer rule keeps the single definition PR #108 established. Surfaced during observation-recording Task 2 /copilot-pairing, correcting a self-review-pass misread that echo-safety.sh stripped C0+DEL only (2026-07-10).
- 2026-07-10 [planwright] output-hygiene Task 6 deferred the REQ-D1.4 orchestration-fleet `[[…]]` citation reconcile: the fleet bundle went `Done` (#123, after this spec was signed 2026-07-06 when it was `Ready`), and a `Done` contract is frozen — changing it requires a Done→Draft reopen + scoped `/spec-kickoff` (spec-format), not the expression-only amendment lane the task prescribed. The two live memory links remain at `specs/orchestration-fleet/requirements.md:569` and `design.md:542` (both `[[workflows-not-plugin-invocable]]`). The standing `check:memory-links` guard added by Task 6 skips terminal bundles, so it re-engages and forces the neutralization automatically the moment fleet reopens to Draft. Candidate: fold the fleet `[[…]]` reconcile into the next orchestration-fleet reopen (extend/amend), or drop it if the bundle stays terminal. Surfaced during output-hygiene Task 6 execution (2026-07-10).
- 2026-07-10 [planwright] autopilot-reflex Task 5: release-please's `skip-github-release` (the D-3 PR-only path) leaves the merged release PR labeled `autorelease: pending` forever, because the github-release step that would relabel it to `autorelease: tagged` is the skipped step; release-please then aborts the NEXT cycle's release PR with "There are untagged, merged release PRs outstanding" (googleapis/release-please#1561, #1946, #1406). D-3 assumes the PR-only path works across cycles but the bundle captures no mitigation, so REQ-C1.1 ("release PR maintained automatically") holds only for cycle 1. The coherent fix is for the human-gated publish step (`scripts/release-publish.sh`, Task 4) to clear/relabel `autorelease: pending` after it tags — the exact work release-please's github-release step would do — or a small label-clearing workflow step; either needs a spec amendment (a D-ID/REQ giving the publish step that responsibility). Cross-task design gap. Surfaced by the Task 5 /polish correctness lens (2026-07-10).
- 2026-07-10 [planwright] autopilot-reflex Task 5/6: the release PR is opened by `GITHUB_TOKEN`, and GitHub does not raise workflow runs for `GITHUB_TOKEN`-authored events, so the `ci` `pull_request` run never fires on the release PR. If Task 6 makes the CI / release-pending check a REQUIRED status, the release PR can never satisfy it and the human can never merge it — defeating "merge == release approval" (REQ-C1.4, REQ-E1.x). The resolutions each carry a cost: a PAT / GitHub App token to trigger CI contradicts the kickoff token-posture decision (default GITHUB_TOKEN, no secrets — risk row 3); exempting the release PR from the required check weakens the lock; or the required check must pass by construction on the release PR. This is a Task 6 branch-protection design fork, undecided. Surfaced by the Task 5 /polish correctness lens (2026-07-10).
- 2026-07-10 [planwright] doctrine-gap(tower-role): the orchestrate + fleet doctrine (disposable tower, D-7/D-8) never states that the coordinating tower does NOT author repo/config/content changes itself — it dispatches, monitors, and reconciles, and routes edits to worker tasks. Surfaced when the tower, on a loose request ("MD files are limited to few columns, can we change that?"), directly disabled markdownlint MD013 entirely (reversing the rationale-documented `line_length:100` in `.markdownlint.jsonc`, whose comment even said "Task 2 owns evolving this config") and opened PR #144, instead of (a) treating a decision-reversal as a Needs-human-judgment fork (disable vs raise-to-N vs policy) surfaced to the human, and (b) routing it as a sensible-defaults chore to a worker per the own-your-defaults doctrine (engineering-decisions REQ-G1.1 + the 2026-06-11 tool-defaults observation). Two compounding misses: a feasibility question ("can we change that?") was treated as a directive, and a change that reverses a recorded-rationale decision was treated as a chore rather than a fork. Candidate: add an explicit non-authoring boundary to the orchestrate/fleet doctrine (the tower surfaces + delegates; reversing a recorded-rationale decision is a fork, not a tower action), closing the "just a quick config tweak" rationalization. PR #144 was closed; the MD013 sensible-default change is re-queued as an isolated worker chore. Surfaced during the 2026-07-10 fleet run.
- 2026-07-10 [planwright] observation-recording Task 4: obs-consume's legacy arm refuses (exit 3, not-found) when a frozen-log line is already consumed by a *different* spec, because the line carries no second bare copy to annotate — asymmetric with the fragment arm, which unions a second `Consumed-by:` citation onto an already-archived fragment consumed by a new spec. D-3 defines the fragment cross-spec edge only implicitly and is silent on the legacy cross-spec case; a legacy suffix could carry a second `— consumed-by:` but the design does not mandate it. Accepted as-is per proportionality (two specs mining the same frozen line is rare, and D-5 treats the frozen log as a shrinking low-traffic surface); recorded so a future amendment can decide whether legacy lines should support cross-spec citation union. Surfaced during the Task 4 /polish correctness lens (2026-07-10).
- 2026-07-12 [planwright] autopilot-reflex Task 10: the CI-rollup check (`gh repo view` + the `statusCheckRollup` GraphQL query, refuse on non-SUCCESS / null-NONE) now exists in two places — `scripts/release-publish.sh`'s inline `ci_green` (Task 4) and `scripts/release-arm.sh`'s inline `rl_ci_state` (Task 10). Task 10 kept an inline copy deliberately to leave `release-publish.sh` byte-for-byte unchanged (the "post-merge mode remains the unchanged fallback" Done-when). Candidate follow-up: promote a single `rl_ci_state <sha>` into `scripts/release-lib.sh` (the shared-primitives layer both already source) and have both scripts call it, retiring the duplication in a change that is free to touch publish. Surfaced during Task 10 execution (2026-07-12).
- 2026-07-12 [planwright] autopilot-reflex Task 10: armed mode (scripts/release-arm.sh) ships as a directly-invokable script + a getting-started runbook note, but has no surfacing parallel to the post-merge path's `mise run release` wrapper (Task 7 / REQ-F). A `mise run release-arm <pr>` task was left out of Task 10 scope because surfacing is REQ-F's domain and arm's required PR-number argument doesn't fit the zero-arg `mise run release` pattern cleanly. Candidate follow-up: if arm sees real use, add a `release-arm` mise task (or fold an arm mention into the bookkeeping surface) so the faster path is as discoverable as the fallback. Surfaced during Task 10 /polish convergence (2026-07-12).
- 2026-07-13 [planwright] autopilot-reflex Task 10 gauntlet: publish's CI gate reads the AGGREGATE `statusCheckRollup.state` on the release-merge commit, but the untagged-window lock (`release-window` / `window-lock`, Task 6) is red BY DESIGN on that commit until the tag is published — so the aggregate is red for the entire window and publish's `ci_green` (which requires SUCCESS) would refuse every post-merge publish, both via `release-arm.sh` and the manual `release-publish.sh` path. The Stage-2 self-review hardened ARM to exclude the window lock (reads rollup *contexts*, requires only the quality checks green, at pre-validation and in a fire-time settle-poll), per the Diego decision that the window lock gates merges not the publish. But `release-publish.sh` was left byte-for-byte unchanged (its Done-when), so it still refuses on the red aggregate: **end-to-end armed release is blocked until the matching publish-side exclusion lands** (`fix/publish-ci-gate-window-lock`). The two exclusion implementations (arm's `rl_ci_verdict`, publish's future one) must agree on which check is excluded; the standing `rl_ci_state`→`release-lib.sh` promotion follow-up (2026-07-12 above) is the natural place to share one helper and remove the divergence risk. Surfaced during the Task 10 gauntlet Stage-2 self-review (2026-07-13).
