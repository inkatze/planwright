- 2026-07-19 [planwright] release-hardening Task 3 self-review (tests lens): release-arm.sh fire-time verdict branches are only prose-asserted, not tested — the deliberately-differing behavior (too-many refuses immediately like publish; a query-error retries within the poll budget rather than failing closed one-shot) has no arm test exercising the fire-loop too-many die-path or the rc-2 query-error retry. The arm suite covers too-many and query-failure only at pre-validation. Fail-closed either way (a regression refuses, never mispublishes), so low risk; candidate test-hardening follow-up. rl_ci_state (release-lib.sh) is now the single verdict source, so a fixture-driven fire-loop test is straightforward to add.
