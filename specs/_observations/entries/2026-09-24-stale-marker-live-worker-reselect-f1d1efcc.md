- 2026-09-24 [planwright] orchestrate-meta-select re-offered custom-steps Task 1 for dispatch (2026-09-24) while its stream-json worker was alive and working: the dispatch marker aged past stale_marker_threshold (15m) before the worker's first commit, so the unit derived ready again. The tower caught it by hand; a re-dispatch would have hit fleet-dispatch-worktree, whose in-flight test looks for a marker or a live tmux session, so a stale marker plus a stream-json worker may read as an orphan to GC-adopt. Staleness should also consult backend liveness (fleet-streamjson.sh status running/awaiting-input), or the marker should be refreshed while the worker lives; doc-heavy tasks routinely spend over 15 minutes before a first commit.
