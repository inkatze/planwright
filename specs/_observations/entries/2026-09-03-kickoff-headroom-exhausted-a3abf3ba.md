- 2026-09-03 [repo] skills/spec-kickoff/SKILL.md could not absorb a second contract addition (anchor-integrity task 6, the terminal re-anchor) by in-file trimming at all. Task 7 had already left the surface exactly on its per-file floor, so the pointer alone breached both the per-file and the closure floor. What made it fit was relocation: the ritual's mechanics went to doctrine/spec-format.md, whose permanent exemption and capped aggregate charge keep them off every dependent's budgets, leaving only a pointer inline, and one in-file cut (a restatement of the writers rule the same sentence already cites) funded that pointer. Margins are not transcribed here; the guard's audit line prints them every run and a copied figure rots. The lesson is that trimming has stopped being the lever on this file: several cuts attempted this round removed a distinct rule rather than a repetition and had to be reverted, and the cheap redundancy is spent. Relocation is what the next addition will need too, and it only works while there is a doctrine home that is already over its cap. Seed material for the deferred instruction-headroom relief, which the standing declared exceptions already name as the disposition.
