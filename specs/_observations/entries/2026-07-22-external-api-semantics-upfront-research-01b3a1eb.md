- 2026-07-22 [planwright] The merge-currency-guard guard predicate went through four forms during kickoff, each rewrite forced by a GitHub-merge-semantics flaw a different validation layer caught: native 9-lens fan-out (CLEAN over-denies UNSTABLE) -> adversarial re-check (local is-ancestor false-allows stale flips for non-just-fetched flippers) -> panel-review --nested (mergeStateStatus false-allows behind PRs without branch protection, a critical bug the first two passes both missed). Specs whose core mechanism depends on external-API semantics benefit from an explicit API-semantics research step in /spec-draft, grounded in live API probes and docs, before kickoff, rather than discovering the semantics through successive review rounds; and reinforces running an independent-model panel pass for API-behavior claims.
