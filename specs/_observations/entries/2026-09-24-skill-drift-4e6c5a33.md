- 2026-09-24 [planwright] skill-drift(spec-draft): the pre-flight names the native worktree mechanism as the only way to create the spec worktree, but that tool refuses to create one from inside an existing worktree session (the common case when the session was launched with --worktree); the session had to fall back to git worktree add plus entering by path, which D-37 forbids by wording. The skill needs the enter-by-path arm stated.
