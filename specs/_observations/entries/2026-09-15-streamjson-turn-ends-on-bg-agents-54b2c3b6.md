- 2026-09-15 [planwright] A stream-json-persistent worker that spawns background sub-agents and then waits for them ends its TURN instead of waiting: the turn closes, the sub-agent reports never reach the worker, and fleet-streamjson.sh status reports `completed result=success`. Observed 2026-09-15 on tower-comms task 3, whose review lens fan-out spawned nine agents; the worker had committed and passed the full gate but had done no review and opened no PR, and the success verdict said none of that. The worker process stays alive (recover refuses it as not orphaned), so a steer resumes it, but the tower only noticed because it checked the branch for a PR. Two gaps: the persistent-worker turn boundary is not a task boundary, and `completed result=success` is not evidence the unit finished. A tower should verify unit-completion evidence (PR open, review record) before believing a success result, and the review skills should run their lens fan-out inline under this backend rather than as background agents.
