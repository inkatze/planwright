- 2026-09-02 [planwright] Shell embedded in .github/workflows/*.yml run: blocks is outside every gate the repo runs: mise lint:shell and lint:fmt glob scripts/*.sh tests/*.sh tests/lib/*.sh githooks/*, and there is no actionlint task. release-hardening Task 9 put the release-please guard tri-state (an rc capture plus a case with a fail-closed default arm) into such a block, so the one piece of branching policy in the workflows is unlinted and only reachable by a test that awk-extracts the YAML and re-executes it. Two candidate directions: add actionlint to the check aggregate, or factor guard bodies into scripts/ so the existing shellcheck/shfmt gates cover them and tests can call them directly.
