- 2026-08-27 [planwright] tests/test-fleet-streamjson.sh c12 carries a flaky assertion that fails only under the parallel load of a full `mise run check`. Line 599 checks `[ "$(aenv "$home" queue --count)" = 0 ]` immediately after `wait "$launch12"`, but the queue drain is not synchronous with the worker run ending, so under CPU contention the count is still 1 and the run fails with "c12: queue should be clear after the answer". Two lines earlier the same test polls with `wait_until 100 grep -q ...` for the journal row, so the polling helper is right there and the sibling assertion simply does not use it. Observed twice during fleet-lifecycle-closure Task 2 verification: failed inside a full gate run, passed on immediate isolated re-run, and the same tree had passed a full gate minutes earlier. Not caused by that branch (it touches only scripts/check-hook-contracts.sh and its test). Fix: wrap the queue-count assertion in wait_until, and sweep the file for other bare post-`wait` state assertions with the same shape.
