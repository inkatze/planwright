- 2026-07-19 [planwright] instruction-headroom guard DESIGN QUESTION: a floor knob of 0 (getknob accepts any non-negative integer) silently makes headroom_check a no-op for that class -- below-target is unreachable and floor-breach only fires once the surface is already over its error budget, with no diagnostic. Shipped defaults are all non-zero so nothing shipped is wrong; it only bites under a deliberate overlay, and design.md D-2 frames floors as protective overlay-tunable minimums without settling floor=0. Decide in a follow-up amendment: floor=0 = intended per-class opt-out (document it) vs reject fail-loud like a missing knob vs allow-with-warning. Surfaced by the Task 2 nested Copilot-review adjacent pass.
