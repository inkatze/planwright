- 2026-08-25 [planwright] scripts/spec-model.sh keeps a private header-declaration reader (first_header, an awk index($0, "**<key>:**") == 1 probe) for the bundle Status it renders. It is not header-block-scoped and does not fail closed on a duplicate in-header Status:, so it can read a Status line from body prose. Left as-is in format-grammar Task 8: spec-model.sh sits outside REQ-B1.3 named consumer set, and re-pointing it to spec_parse_header_value is a behavior change (scoping plus fail-closed duplicates), not a re-point. Same class as the check-memory-links.sh / migrate-status-lifecycle.sh stragglers already named in the corpus sweep, but invisible to that sweep because the probe uses index() rather than an anchored bold-declaration pattern.
