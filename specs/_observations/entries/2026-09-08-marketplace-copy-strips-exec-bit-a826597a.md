- 2026-09-08 [planwright] The universal-binary drafting survey (2026-09-08) found several closed-without-visible-fix Claude Code issues (anthropics/claude-code 39954, 40280, 39964, 40086, 20818) reporting that the plugin marketplace copy or update resets file modes to 644, dropping the execute bit. Every hook registration in hooks.json and in the worker and tower settings invokes a script by path under the plugin root, which needs that bit; a stripped bit would make a hook silently fail to run, the same shape as the hook-never-registers observations already recorded. Unverified here: whether the stripping reproduces on the current CLI, and whether any recorded silent-hook incident was this. Candidate: reproduce on a fresh marketplace install; if real, invoke hook scripts through sh explicitly or restore modes from a session-start hook. The universal-binary delivery decision D-9 already assumes the bit is never present after a copy.
