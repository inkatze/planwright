- 2026-08-26 [repo] merge-currency-guard D-4 justifies top-of-iteration main-syncing on the premise that /execute-task re-runs the full CI + review sequence on the fresh head every convergence iteration (design.md rejected-alternative and Chosen-because; kickoff risk row 8 is built on it, and the 2026-07-22 changelog records the CI-runs-post-sync clarifier as a deliberate kickoff decision). /execute-task has no such loop: it runs full CI once, then the review sequence once, linearly, with the Task 3 sync landing after CI. The merged head is still covered by the review sequence's wider-suite runs (validation-rigor pass 2) on any iteration that applies a fix, but the skill's own CI step never re-runs on it, and a review pass that applies no fixes leaves the merged head with no suite run. Meaning-class contract drift surfaced during Task 3 panel review, handed to the spec owner rather than amended from an execution session.
