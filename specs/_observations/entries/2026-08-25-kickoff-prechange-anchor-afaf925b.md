- 2026-08-25 [planwright] anchor-integrity Task 4 closed the pre-change-anchor drift in the two gate consumers (/execute-task pre-flight step 7, /orchestrate in-lock gate) per the 2026-08-24-skill-drift-fbc582f6 routing. A third surface has the same gap: /spec-kickoff step 2 routes EVERY freshness mismatch to a delta re-walkthrough, so a bundle whose anchor predates the header-Status exclusion gets a full re-walk over content nobody edited. It is less costly there than at the gate (spec-kickoff is the sanctioned writer, so the re-walk at least terminates in a valid entry) which is why it was left out of scope, and spec-kickoff is at its per-file budget with a standing declared exception, so the fix needs a word budget like the two gate skills did. anchor-integrity Task 6 already touches that ritual. Surfaced during anchor-integrity Task 4 execution.
