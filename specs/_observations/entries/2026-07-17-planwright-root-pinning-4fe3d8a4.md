- 2026-07-17 [planwright] In the planwright dev repo, skill rule-doc resolution defaults to the installed plugin cache (0.14.1 at the time) instead of repo HEAD (0.17.0) because no layer pins PLANWRIGHT_ROOT; the anchor-integrity kickoff had to export PLANWRIGHT_ROOT=$PWD manually. Candidate fix: a repo-local env pin (e.g. mise env PLANWRIGHT_ROOT={{config_root}}) so dev-repo sessions dogfood HEAD doctrine by default.
