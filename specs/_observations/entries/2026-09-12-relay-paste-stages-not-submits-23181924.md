- 2026-09-12 [planwright] Measured on Claude Code 2.1.270 with a scratch interactive session under tmux: the shipped relay-command paste (attribution header line plus the message body) never submits, because two or more lines land as a placeholder the CLI holds, and once the input box holds multi-line text every later paste sits behind it. A one-line paste submitted in some runs and sat in the box in others at every length tried from 9 to 100 characters, so paste submission is not a contract at all. The relay now pastes exactly one pointer line and the doctrine says a paste is staged, not delivered; fleet-streamjson.sh gained a steer verb that writes the attributed message onto the worker's stdin as a user turn with a receipt row, which is the unattended steer path for the default rung.
