- 2026-07-23 [planwright] The fleet-state.sh register/registry dispatch-record primitive has no live writer: no dispatch path (offload-dispatch.sh, fleet-dispatch-worktree.sh, /orchestrate) registers workers, so fleet-status.sh (Task 7) renders the registry source as absent until dispatch paths write records. Wiring registration into the dispatch seams (notably the print rung, whose workers have no runtime presence and are only visible via dispatch records) is out of Task 7 scope; candidate spec-draft seed.
