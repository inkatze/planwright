- 2026-08-18 [planwright] skill-drift(orchestrate): the SKILL.md worktree section states "planwright never shells out to git worktree" absolutely and names only the native mechanisms (claude --worktree / EnterWorktree / Agent worktree isolation), but scripts/fleet-dispatch-worktree.sh is a sanctioned narrow exception (its own header, D-7 amended 2026-07-20) that runs a single `git worktree add -b` to make the D-36 branch name a deterministic output rather than a post-launch rename. A tower following the skill text literally has no sanctioned way to create the branch for a non-tmux backend; --no-attach create-only mode is the intended path for stream-json-persistent/headless but the skill never names it. Surfaced dispatching guard-coverage Task 4.
