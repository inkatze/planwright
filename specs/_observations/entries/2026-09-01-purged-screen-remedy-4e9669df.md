- 2026-09-01 [planwright] purged-identifier screen on a branch predating the guard: the commit-msg failure says the seed file is missing and points at seed-purged-identifiers.sh, but the actual remedy was merging main (which tracks both the script and the seed). The fail-closed posture is right; the remedy line should detect the branch-behind case and say "bring the branch current" before suggesting re-seeding.
