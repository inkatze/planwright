- 2026-08-25 [planwright] scripts/check-anchor-freshness.sh (anchor-integrity Task 4) collects the dated `## Changelog` bullets its pairing check compares with a private awk that is NOT fence-aware: a `## Changelog` heading or a dated bullet inside a column-0 fence counts as a real entry, so an illustrative changelog block quoted in a spec could satisfy the pairing requirement for an edit that was never actually recorded. Same false-satisfaction shape format-grammar just closed everywhere else: #350 routed the canonical tasks.md extraction and spec-validate.sh’s requirement, decision, task-heading, and v2 ledger grammars through one shared fence-aware lexer in scripts/spec-parse.sh, and the 2026-08-24 spec-format changelog entry records that fenced illustration must neither raise findings of its own nor satisfy a check a real record would. The guard was written on a branch cut before that landed, so its scan is the remaining private parser. Fixing it by hand would add a THIRD fence parser, which is what D-3 consolidated; the fix is to route the changelog scan through the shared lexer once both are on main. Surfaced at the /execute-task Maintenance step during anchor-integrity Task 4.
