- 2026-08-18 [repo] orchestrate(guard-coverage): EnterWorktree fails fleet-wide after the Claude Code auto-upgrade 2.1.233 -> 2.1.234 (self-restart logged in daemon.log at 2026-08-18T17:37:37Z). Both a nested-name attempt (planwright/guard-coverage/task-3) and a flat-name probe (gc-task-3-probe) hit the identical harness error: "WorktreeCreate hook failed: hook succeeded but returned no worktree path (command: echo the path to stdout; http/callback: return hookSpecificOutput.worktreePath)". fleet-worktree-track.sh hook-create (0.31.0, the active installed version per installed_plugins.json) was manually replayed offline with the documented input schema (session_id/cwd/hook_event_name/worktree_path per code.claude.com/docs/en/hooks.md) and behaves correctly (echoes the path, exit 0) both for a realistic nested path and a flat one -- so the script itself is not obviously at fault; the mismatch is between what fleet-worktree-track.sh receives/emits and what 2.1.234s WorktreeCreate decision-control channel now expects. Corroborating: the tower-death signposts surfaced at this session SessionStart (pid 3186598, many specs, one shared session id) line up with the same 17:37:37Z restart, and the last successful nested worktree creation in worktree-bootstrap.log (specs-tower/.claude/worktrees/task-3, inception spec) is timestamped 12:50:17, before the upgrade. Blocks REQ-F1.8/D-37 native worktree creation fleet-wide until root-caused against the 2.1.234 harness contract; no workaround attempted (shelling out to git worktree is a hard invariant, not bypassed). Relevant to fleet-autonomy (WorktreeCreate decision-control) and orchestrate D-37/D-44.
