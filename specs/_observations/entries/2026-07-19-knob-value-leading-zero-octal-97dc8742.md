- 2026-07-19 [planwright] instruction-headroom guard: getknob accepts any all-digit knob value including a leading-zero form (0100), which $(( )) then interprets as OCTAL (0100 -> 64) on both /bin/sh and bash, so a leading-zero overlay knob yields wrong headroom margins/targets in headroom_check and the --audit margin-to-warn/margin-to-error columns. Reproduced end-to-end. Shipped defaults are all leading-zero-free so nothing shipped is affected; only a deliberate leading-zero overlay value bites. Same root as the sibling raise-value-non-integer edge: getknob validates all-digit but does not canonicalize to decimal. A fix needs design thought (normalize/strip leading zeros at getknob, reject leading-zero values, or force base-10), deferred under the Task 2 convergence freeze. Surfaced by the Task 2 nested Copilot-review adjacent pass.
