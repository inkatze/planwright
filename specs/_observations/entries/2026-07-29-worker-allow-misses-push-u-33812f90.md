- 2026-07-29 [planwright] config/worker-settings.json allows Bash(git push origin:*) but NOT the -u spelling: `git push -u origin <branch>` puts -u before the remote, so the allow rule does not match and the FIRST push of every task branch falls through to the permission prompt (found 2026-07-29 by the guard-coverage Task 1 fixture table, recorded there as a residual row). /execute-task documents `git push origin <branch>` "with -u on first push", so the documented happy path is the one that prompts. Not fixed in Task 1: widening an allow list is a permissions widening needing sign-off and is not a Task 1 deliverable. Candidate fixes: add Bash(git push -u origin:*) to the allow list, or have /execute-task use `git push origin <branch>` then `git branch --set-upstream-to`.
