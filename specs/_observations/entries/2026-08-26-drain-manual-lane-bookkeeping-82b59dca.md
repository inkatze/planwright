- 2026-08-26 [planwright] The drain manual-verification lane (format-grammar REQ-E1.6) is surfaced by /drain only. skills/orchestrate/SKILL.md step 3 of --bookkeeping enumerates what that pass relays from the same evaluator (gates, observation staleness, pending release) and does not mention the [manual] inventory, so the scheduled entry point produces the section and drops it. accumulator-taxonomy says both entry points produce the same report. REQ-E1.6 is /drain-scoped and orchestrate/SKILL.md is already over its closure word warn, so this was left alone deliberately; the fix is two lines in that step list if the divergence is judged worth the headroom.
