- 2026-09-03 [planwright] check-githooks.sh fails in every worktree of a checkout whose shared .git/config carries core.hooksPath as an ABSOLUTE path to the tracked githooks/ dir (here /home/<user>/dev/planwright/githooks): the guard compares the string against the literal githooks, so a wiring that resolves to the identical directory reads as unwired and mise run check goes red on an otherwise green tree (Task 7 execution, 2026-09-03). Worktrees share the primary checkout config, so the absolute form was set once on the primary and inherited. Either the check should accept a path that canonicalizes to <toplevel>/githooks, or wire-githooks.sh should normalize an absolute equivalent to the relative form it documents; today the remedy it prints would rewrite the primary checkout config from inside a worker worktree, which a worker must not do.
