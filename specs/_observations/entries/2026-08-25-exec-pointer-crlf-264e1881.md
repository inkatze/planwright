- 2026-08-25 [planwright] The **Execution:** pointer check in scripts/spec-validate.sh (exec_canon comparison) breaks on CRLF checkouts, while parse_tasks_v2 in scripts/spec-parse.sh already trims CR for exactly this reason. Pre-existing on main and byte-identical there; the format-grammar Task 6 panel review confirmed it and deferred it as out of scope for that PR since the diff neither introduced nor worsened it. Fix candidates: add the same CR trim to the exec_canon path, or normalize CR once at read time so every downstream consumer inherits it.
