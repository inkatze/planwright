- 2026-09-02 [planwright] model-allocation REQ-F1.2's feedback loop fires only on ESCALATION evidence (ended above the starting tier, or an applied-escalation count at the threshold), so a unit whose CONFIGURED starting tier is already the ladder top records nothing however long it runs. The live tower run that motivated Task 4 was exactly that shape: a fleet of workers dispatched at the execution key's shipped tier with adaptation off, hence zero escalations, so the loop stays silent about a fleet running entirely at the top tier unexamined. Whether 'started at the top and never examined' deserves its own condition, or belongs to REQ-F1.3's stats path instead, is a REQ-F1.2 amendment question rather than an execution-time call.
