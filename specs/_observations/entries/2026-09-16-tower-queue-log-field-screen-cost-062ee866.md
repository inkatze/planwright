- 2026-09-16 [planwright] tower-comms Task 3 review (performance lens, measured): the log verb screens every field with a printf-tr fork plus two sed execs (redact, json_escape), about 60 ms per eleven-field born line, and every add and every rebuilt item now emits one; has_control already does the control-byte test fork-free and most field values (ids, kinds, dashes) cannot match a redaction rule, so a fork-free screen with a cheap pre-filter would cut a 100-item rebuild's log payment by several seconds; deferred as a follow-up, not a defect.
