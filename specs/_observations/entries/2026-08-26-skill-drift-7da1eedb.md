- 2026-08-26 [planwright] skill-drift(self-review): the Discovery step mandates one read-only sub-agent per canonical lens for any non-trivial diff and permits an inline walk only when the diff is small and narrow. It offers no arm for a host that forbids sub-agent dispatch outright (some operator configurations do), so a pass on a large diff in such a host has to deviate from the stated default with no documented degrade path. Sibling docs model the alternative: decision-domains degrades explicitly (note the skipped check in one line, use engineering judgment). Encountered on format-grammar Task 4, where the ~490-line diff met the fan-out threshold but the session forbade Agent dispatch; the pass declared the deviation in its pass summary and ran the self-critique pass to compensate.
