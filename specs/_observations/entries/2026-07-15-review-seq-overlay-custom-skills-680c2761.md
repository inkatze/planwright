- 2026-07-15 [planwright] review_sequence's nestable predicate only accepts planwright plugin skills (skills-root/<name>/SKILL.md). Diego's gauntlet uses panel-review/copilot-review which are dotfiles commands, categorically ineligible, so the gauntlet cannot be config-wired and must be relayed per-task. Desired: let review_sequence (and convergence) overlay custom skills/commands not shipped by planwright, as long as they resolve on the host (adopter command/skill dirs). Enhancement direction, not a bug.
