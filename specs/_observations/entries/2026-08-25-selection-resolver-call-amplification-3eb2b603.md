- 2026-08-25 [planwright] The allocation_* -> fleet_* fallback chain doubles the knob-resolver invocations on the per-dispatch selection path: each column now costs a general-knob resolution plus, when that is the shipped `unset`, a legacy one, and fleet-resource-select.sh adds a delegate process on top. Per dispatch this is negligible, but the doubling is a permanent property of the D-5 fallback design rather than a transient. model-allocation Task 2 budgets within-boundary memoization for the ADAPTATION boundary; whether that also covers the fallback chain is unexamined. Worth measuring against REQ-F1.3 derivation-latency instrumentation once Task 2 lands, before assuming it is covered.
