- 2026-08-25 [planwright] The lifecycle-closure floor asserts that every rung in the backend capability contract carries a row in the resource-class cross, but nothing enforces it: REQ-A1.5 verifies [design-level], so a rung added to doctrine/backend-capability-contract.md with no row in doctrine/fleet-coordination-floor.md passes CI. The bundle already builds the enforcement shape it would need — Task 11 pins its adversarial matrix with an expected-cell manifest. A small guard comparing the two tables rung column by rung column would turn REQ-A1.5 from a review assertion into a mechanical one, which is what the floor claims to be for. Out of Task 1 scope (doctrine only, no scripts).
