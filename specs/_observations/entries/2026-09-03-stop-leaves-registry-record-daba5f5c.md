- 2026-09-03 [planwright] fleet-lifecycle-closure Task 4: `fleet-streamjson.sh stop` releases the five runtime classes REQ-B1.4 enumerates and leaves the Task 3 dispatch registry record standing, because the requirement says the release set is exactly those classes and the registry is not among them. That is the right reading of the contract, but obs:7bc2494a already flagged registry deregistration as one of the things a bare kill skips, and a stop is now the sanctioned kill. The consequence: after a stop, fleet-status.sh renders a registry record for a worker with no process, no locks, and no attention row. Task 3 specified the registry as self-healing on the next scan and Task 8 wires the periodic worktree/registry reconcile, so the gap is probably already owned - but nothing in Task 4 or Task 8 states which one closes it, and the two tasks were written against different framings of the same record. Worth resolving explicitly when Task 8 is picked up rather than discovering it as a stale-inventory bug.
