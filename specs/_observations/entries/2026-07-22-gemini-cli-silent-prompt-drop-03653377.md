- 2026-07-22 [panel-review] gemini CLI 0.46.0 silently produces an empty response (exit 0, `.response` empty, `.stats.models` = {} — zero model calls, nothing on stderr) for specific prompt content: an 81.5KB lens-walk review prompt over the fleet-presence diff failed on every transport (stdin and `-p`, with and without `--approval-mode plan`), while padded probes at 16/32/48/64/80KB and an `@@` hunk-header probe all pass, and bisection localized the trigger to a ~21KB slice of the scripts/fleet-presence.sh diff (prompt bytes 20000-41000; instructions-only and the 3000-20000 slice pass). Content-triggered, not size; root cause in the CLI unidentified (bisection stopped by operator call). Panel-backend implication: exit 0 + empty output is a real silent-failure mode — a panel invocation must verify a non-empty response (or `.stats.models` non-empty via `-o json`) before treating the leg as run, and fall back to chunked per-file prompts or the native lens fan-out when it fires.
