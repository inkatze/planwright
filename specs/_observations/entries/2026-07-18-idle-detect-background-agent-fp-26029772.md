- 2026-07-18 [planwright] Worker pane busy/idle detection is uncodified tower judgment, not a planwright script (fleet-liveness.sh checks process/window liveness only, not at-prompt-vs-working). The absence-based heuristic has a false-positive gap that bit the worker-permission-ergonomics run 2026-07-18: a worker whose MAIN loop is idle while a BACKGROUND subagent runs (footer shows down-arrow to manage and left-arrow N agent; body shows Waiting for N background agent to finish) has no esc-to-interrupt marker, so an absence-of-busy check reads it as idle/awaiting-input. Relaying the next gauntlet stage there would interrupt a productively-busy worker (e.g. a red-team/verify subagent). Robust detector = positive auto-mode-on anchor AND none of: esc to interrupt, to manage / Waiting for (background subagent), spinner words (Clauding/Slithering/Newspapering/Compacting/Explore) — debounced 2 consecutive frames. Consider codifying this as a planwright pane-state detector (e.g. fleet-attention.sh or a fleet-worker-state.sh) so towers do not each re-derive it, and so the attention surface (D-9/fleet) does not surface false attention. Related: the shipped memory tmux-worker-idle-detection captures the tower-side heuristic.
