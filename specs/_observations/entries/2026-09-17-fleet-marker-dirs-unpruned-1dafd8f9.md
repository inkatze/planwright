- 2026-09-17 [planwright] The per-tower attention-marker and delivery-record directories under the fleet home are written for every tower session and never pruned, so they accumulate one file per conversation for the life of the machine. Nothing needs them once a tower is gone, but anything that has to sweep them (the settling pass reads both to decide whether the operator is present anywhere) pays a mode check per dead entry, inside the fleet lock. Noticed 2026-09-17; a cleanup pass keyed on presence records would bound it.
