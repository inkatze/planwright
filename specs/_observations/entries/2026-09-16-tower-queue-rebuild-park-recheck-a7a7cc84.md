- 2026-09-16 [planwright] tower-comms Task 3 review: rebuild_if_absent re-runs the containment check on a path item's pointer but takes an attention item's root and park from the born line as they are (is_text on the park, nothing on the root), so a park that moved or was relinked since the add is restored unchecked; owner-only log, same trust domain, low reachability; deferred as a follow-up, not a defect.
