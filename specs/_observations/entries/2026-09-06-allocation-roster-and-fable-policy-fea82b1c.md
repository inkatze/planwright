- 2026-09-06 [planwright] Two allocation policy decisions taken by the operator 2026-09-06, both awaiting a chore PR that cannot land yet. FABLE IS ESCALATION-ONLY: fable is the ladder top (rank 3) and its only reachable coordinate is (fable, high) via the successor rule; it must never be a configurable STARTING tier, and a config naming it as one should be refused. Today this rule exists only as a comment in a gitignored machine-local overlay, so it does not survive that file. ROSTER IS APPEND-ONLY: the model roster is currently hardcoded in THREE places (scripts/allocation-ladder.sh ALLOC_MODELS, scripts/allocation-select.sh MODEL_VALUES, scripts/fleet-allocate.sh MODEL_VALUES) in two different orders, plus a hand-written rank map in alloc_model_rank/alloc_model_at, so a new model cannot be adopted by config at all. Collapse to ONE definition in allocation-ladder.sh (whose own header already says a second implementation is 'exactly the drift this file exists to prevent') and declare the roster append-only: new models may be added above fable, never inserted. Insertion is the hazard, not addition -- replay is memoryless and re-derives a unit's tier from recorded DIRECTIONS through the successor rule, so renumbering ranks changes what past runs replay to. SEQUENCING: the consolidation touches allocation-ladder.sh (open in PRs 407 and 411) and allocation-select.sh (open in 411 and 412), and workers may not rebase, so the PR is deliberately held until those merge. Related: the same session reverted fleet_model_bookkeeping and fleet_model_drain to the sonnet core ships, on a headroom argument rather than a spend one -- escalation raises effort to high then the model one alias, stopping at (fable, high), so an opus start leaves one to three rungs depending on effort.
