- 2026-07-16 [planwright] Parallel suite wall-clock (~12min on 12 cores vs 28:46 serial) is floored by single slow files: test-check-instructions.sh ~9-10min, test-orchestrate-select.sh ~8min, test-obs-consume.sh ~8min. The next test-speed win is splitting or slimming the stragglers, not more parallelism; CPU peaks ~365% because the workload is fork-bound.
