- 2026-09-09 [planwright] Doctrine manifests name whole files, so a skill that consults one section of a doc charges and loads the entire doc. That single fact produces both instruction-budget symptoms: the capped-charge law exists to stop an exempt docs overage from cascading into every dependent, and the cap is what makes the charged closure number thousands of words removed from what a worker really reads. SECTION-GRANULAR MANIFESTS would dissolve both at once for /orchestrate, which cites spec-format only for the brief check and the freshness gate -- that is the "Sign-off records and content anchors" section, 2,527 of the docs 9,749 words. Charged would be 18998-4000+2527 = 17525, margin 2475, above the 2000 restoration target; actual would be 24747-9749+2527 = 17525 as well, identical because no cap is needed, and the exemption cascade disappears. IT DOES NOT SAVE /spec-kickoff, and this is the part worth carrying forward: that skill genuinely consults most of the meta-spec (bundle format, status lifecycle, amendment ritual, kickoff brief, sign-off records, decided-rules -- roughly 5,900 words), so charging it by section would push it to about 20,900, past the hard error. It currently benefits from the 4,000 cap on a doc it really does need. So the two saturated closures need different remedies, and any section-granularity design has to say what happens to a consumer that legitimately needs most of a large doc. SCOPE if it becomes a spec: manifest grammar, resolve-rule-doc.sh returning a section rather than a file, per-section measurement and reverse use-site checking in check-instructions.sh, a heading-stability rule so renaming a heading does not silently break every skill citing it, and the fate of the capped-charge law and the standing exemptions once the cascade they prevent no longer happens.
