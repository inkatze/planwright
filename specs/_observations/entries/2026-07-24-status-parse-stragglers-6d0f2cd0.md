- 2026-07-24 [planwright] Two Status: header readers sit outside format-grammar REQ-B1.3 named consumer set and still keep private one-line parses after Task 2: scripts/check-memory-links.sh (a sed -n liveness filter) and scripts/migrate-status-lifecycle.sh (status_of, an awk $2 read). Neither is version- or status-keyed for an execution decision, so leaving them was the scoped call, but REQ-B1.3 claims the named eight are the full private-copy set and the sweep grep-verifies no other consumer remains. Either re-point both onto spec_parse_header_value or state the exemption where the sweep is defined; today the claim is slightly wider than the code.
