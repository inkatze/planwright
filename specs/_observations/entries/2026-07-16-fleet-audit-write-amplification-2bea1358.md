- 2026-07-16 [planwright] fleet-autonomy Task 1 self-review, performance/concurrency lenses: fleet-audit.sh record re-copies the whole UTC day file per append (copy-append-rename, deliberate per kickoff risk row 18 for lockless-reader atomicity), so a day's Nth action copies N-1 rows INSIDE the held cross-spec fleet lock — per-append cost and lock-hold time grow with daily action volume, and a longer hold widens fleet-state.sh's documented 15m stale-break window shared with fleet-attention.sh writers. Fine at the trail's intended low action frequency (risk row 31 keeps classification noise out); revisit the write mechanism (e.g. day-file sharding or O_APPEND-sized rows) only if Tasks 2-7 sweeps push real volume.
