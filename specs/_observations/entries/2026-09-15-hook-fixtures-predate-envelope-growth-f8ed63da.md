- 2026-09-15 [planwright] The hook payload fixtures under tests/fixtures/hook-payloads pin key sets read from CLI 2.1.226 to 2.1.241; the 2.1.271 to 2.1.273 binaries add agent_type to every event's shared envelope (and prompt_id, scratchpad_dir, agent_id, effort when set), so the older fixtures no longer match a real payload byte-for-byte. tower-comms Task 2 derived UserPromptSubmit.json from the newer binaries and pinned agent_type as its own key beside SessionStart's; re-derive the remaining fixtures in one pass and move agent_type into the pin test's shared-envelope exclusion when that lands.
