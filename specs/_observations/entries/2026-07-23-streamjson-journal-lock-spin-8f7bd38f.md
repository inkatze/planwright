- 2026-07-23 [planwright] fleet-streamjson.sh journal_lock breaks a stale lock with rmdir but never checks the result: it resets the retry counter and continues unconditionally. If the lock directory cannot be removed (recreated non-empty by a racing writer, or a permissions / read-only filesystem condition) the loop re-mkdirs, re-counts to 50, re-detects staleness, re-fails the rmdir and spins forever at roughly 5s per cycle, instead of returning the journal-lock-busy refusal (exit 2) the sibling arm uses. Fix: test the rmdir exit status and fall through to the existing refusal when it fails, or bound the stale-break attempts. Pre-existing on main (Task 4 code); surfaced by a panel-review gemini pass during Task 7, out of Task 7 scope.
