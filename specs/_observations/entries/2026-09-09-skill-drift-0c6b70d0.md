- 2026-09-09 [planwright] skill-drift(spec-kickoff): the sign-off step names discovery-rigor's canonical lens-coverage table, but discovery-rigor now delegates lens-set selection to artifact-lenses.md, which gives a spec its own nine-lens set with performance, concurrency, and error-handling marked n/a; the skill and kickoff-verification never mention artifact-lenses or the spec set, so a reader of the skill alone would emit the code table. The universal-binary kickoff used the spec set and captioned the table with the class.
