- 2026-09-21 [planwright] The gemini panel backend fabricated a source citation while otherwise engaging well. Reviewing PR #482, iteration 2 argued from "the 15-word slack margin noted in the observation" — the observation fragment was never in the prompt, which carried only the diff, the sibling sections and the tooling output. The rest of that pass was sound: reasons rather than superlatives, `none` rows naming what was checked, three substantive findings of which the reviewing worker applied one and disproved two on the text. So this is not the all-none blank-out already recorded in the panel-review-gemini-yield note; it is the opposite failure mode, a confident pass that invents a supporting detail. The tell is different too: a blank-out is visible in the shape of the output, whereas a fabricated citation reads exactly like a good one and is only caught by checking the claim against what the prompt actually contained. Worth having the panel step record the prompt's inventory (which files, which sections, which tool output) alongside the findings, so a reviewer can test any citation against it cheaply, and worth treating a backend citation to material the prompt did not include as an automatic drop rather than a finding to validate.
