- 2026-09-04 [planwright] The rename-aside stale break is narrower than the lock family comments imply. It stops two breakers racing the same link, but not a breaker descheduled between its staleness probe and its rename, which can still displace a live successor: modelled with an injected delay, the rename shape and the rm -rf shape both let two holders in. The legacy-directory break in fleet-state.sh has no atomic claim at all. Four hundred barrier-synced attempts at sixteen writers did not reproduce a double acquire on real binaries, so the window is real by construction but very narrow.
