- 2026-07-27 [planwright] doctrine/spec-format.md ## Validator-enforceable invariants (line 710) and the fourteen-item enumeration in the scripts/spec-validate.sh header block state the same list twice, in prose, in two files that drift independently. This is the cleanest instance in the repo of the guidance-4 rule as the petition scopes it: the enforcement is already deterministic, so the prose restating it can collapse to a pointer while enforcement stays exactly where it is. obs:362f4de7 records the same rot class biting for real (three transcribed figures in the exemptions record went stale when the doc grew) and asks whether records should cite the measuring tool rather than transcribe its output; this is that question applied to the validator. Change: make the validator the single source for WHAT it enforces - the doc keeps the invariant NAMES (they are cited by REQ) and drops the restated semantics, pointing at the script. Risk: inverting authority means a validator bug silently becomes the spec, so the doc must keep enough to distinguish "the validator does not check this yet" from "the format does not require this" - obs:f6601f8f is the live example, where REQ-to-task coverage is required by the format but unchecked by the validator, and a pure pointer would erase that gap instead of recording it.
