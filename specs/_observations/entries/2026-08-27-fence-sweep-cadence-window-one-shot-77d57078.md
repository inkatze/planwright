- 2026-08-27 [planwright] REQ-C1.3 says a fence already surfaced in the dedup'd sink SHALL NOT be re-checked every sweep, re-evaluated only once its cadence window elapses. fleet-fence.sh sweep suppresses on the sink entry's age, and that age is measured from FIRST observation and never renews, so the window is one-shot: after min_interval the ref is re-examined on every later pass. It cannot renew through the sanctioned surface either, since a surfaced strand is a queued operator decision and fleet-attention.sh fork exits 3 rather than overwrite one; the sibling fleet-presence.sh discover renews its cadence via a separate private stamp dir instead. The requirement's purpose clause (never an unbounded per-pass fan-out of origin/gh reads) IS met, and more strongly than it asks: both live reads are hoisted above the loop, one ls-remote and one state derivation per sweep whatever the fence count, so the skip only saves a local attribution subprocess. Found during the Task 4 review pass; comment and docs corrected to describe the real behaviour, the spec-versus-code question left for the operator. Options: amend REQ-C1.3 expression-only to state the bound the hoisting delivers; give the sweep a discover-style private cadence stamp; or accept it as a documented residue.
