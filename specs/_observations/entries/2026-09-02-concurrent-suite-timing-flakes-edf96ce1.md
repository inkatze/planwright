- 2026-09-02 [planwright] Two timing-sensitive fleet tests (test-fleet-throttle.sh: "the same excerpt after elapse is a fresh event and must re-engage"; test-fleet-streamjson.sh: "c12: queue should be clear after the answer") fail intermittently when a sibling worktree runs `mise run check` at the same time. run-tests.sh fans out to 12 parallel jobs, so two concurrent workers put ~24 jobs on the box and the elapsed/queue-drain assertions miss their windows. Both files pass in isolation immediately after. Observed during tower-front-door Task 1 while the task-9 worktree was mid-check; the diff under review was markdown prose only, so the failures could not have been caused by it. This is a live condition for fleet operation, where several workers reach their convergence gate at once: a worker can read a red suite it did not cause, and the execute-task contract classifies a logic CI failure as a non-retryable stop condition, so the false red costs a human handoff. Possible follow-ups for /spec-draft to weigh: make the two assertions tolerant of scheduling delay rather than wall-clock-tight; or have run-tests.sh scale its job count against observed load / a cross-worktree advisory lock.
