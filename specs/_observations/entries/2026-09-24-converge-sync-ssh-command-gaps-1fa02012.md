- 2026-09-24 [planwright] converge-sync-main.sh ssh-command handling, pre-existing and left alone by the core.sshCommand fix: the legacy GIT_SSH variable is still overridden by the exported GIT_SSH_COMMAND; the BatchMode splice assumes the first word is an OpenSSH binary (a leading VAR= assignment, a quoted path with spaces, a lowercase batchmode, or a non-OpenSSH command such as plink breaks or bypasses the guard, and a config that already says BatchMode=yes still prints the override note every pass); and the note line (`converge-sync-main: note: ...`) shares the `converge-sync-main: <reason>: <message>` failure shape and precedes any failure record on stderr, so a caller taking the first match would read `note` as the halt reason. Candidates for a merge-currency-guard amendment; the fix PR deliberately kept the BatchMode handling as it was.
