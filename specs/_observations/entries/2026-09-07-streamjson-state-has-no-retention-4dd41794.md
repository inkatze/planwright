- 2026-09-07 [planwright] The fleet state directory has no retention policy and nothing prunes it. MEASURED 2026-09-07: 219M total under CLAUDE_PLUGIN_DATA, of which `fleet/streamjson` is 204M across 99 worker directories (largest single worker 12M) and `fleet/streamjson-dead` is a further 15M. Everything else in the fleet home together is under 500K, so this is the whole of the growth. `streamjson-dead` is ORPHANED: nothing reads it and nothing prunes it, so it is pure accumulation. No knob, sweep, or age bound exists for either. Deliberately NOT fixed here: what a worker transcript is worth after its run ends is a lifecycle design call (they are the only record of what a dead worker actually did, which is exactly what the is_error investigation needed), and inventing an ad-hoc sweep would destroy that evidence before the policy that governs it exists. The honest home for this is the fleet-lifecycle-closure spec, alongside the close verb. Recording the measurement so whoever writes that policy starts from numbers rather than a guess.
