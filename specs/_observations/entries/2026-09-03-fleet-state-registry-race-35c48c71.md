- 2026-09-03 [planwright] The mkdir-lock mutual-exclusion defect recorded 2026-08-26 (lock-primitive-exclusion) now has a RED TEST, not just a measurement: tests/test-fleet-state.sh case 8 (20 concurrent registers) fails consistently on this machine — 17-19 of 20 records land, 3 of 3 runs after syncing origin/main at 52b3af5. That entry already names scripts/fleet-state.sh as still on the mkdir shape and deliberately left untouched as another spec contract, and identifies the symlink + owner-token fix; this adds only that the consequence is now observable as a suite failure rather than a harness measurement. Two details worth carrying into whoever takes the lock-family redesign: the loss is SILENT (spin_acquire returns 2 on give-up and register does `spin_acquire || exit 2`, so a timeout would surface non-zero — all 20 exit 0, so callers cannot detect their registration vanished), and it is NOT the bootstrap path (reproduced against a pre-warmed fleet home). It also contradicts the try_acquire KNOWN LIMITATION comment, which scopes the two-holder window to crash recovery and asserts the normal no-stale-lock contention path is fully serialized: this repro has no stale lock and no crashed holder. Surfaced from the concurrent-orchestrator-coordination Task 5 branch, whose diff does not touch fleet-state.sh.
