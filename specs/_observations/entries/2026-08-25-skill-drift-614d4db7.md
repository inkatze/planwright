- 2026-08-25 [planwright] skill-drift(self-review): the installed plugin (cache 0.31.0) is what scripts/resolve-rule-doc.sh resolves for a worker in this repo, but this repo ships v0.34.0+ doctrine, so a dispatched worker reads a stale copy of its own rules. Concretely, the repo copy of discovery-rigor.md gained an artifact-class lens-selection step pointing at doctrine/artifact-lenses.md, a file the installed copy does not have at all, and validation-rigor.md gained the matching non-code pass-1 reduction. Neither changed this pass (a code diff selects the default lens set either way), but a spec or inception review dispatched in this repo would walk the wrong lens set without noticing. Self-hosting skew, not skill-vs-doctrine drift: the fix is keeping the installed plugin current with the repo, or resolving doctrine repo-first when the host repo IS planwright.
