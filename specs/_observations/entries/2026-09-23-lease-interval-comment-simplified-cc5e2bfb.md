- 2026-09-23 [planwright] config/defaults.yml's tower_lease_interval comment says the lease only catches a tower that died holding an item. In scripts/tower-queue.sh an attended tower takes a held item over once the holder has been quiet for tower_quiet_interval (present() in the next candidate loop), so the lease interval actually governs when an unacknowledged hand-over is voided for redelivery and when ack/shelve start refusing. The fleet guide's row was corrected in tower-comms Task 10; the config comment predates that task and was left as is.
