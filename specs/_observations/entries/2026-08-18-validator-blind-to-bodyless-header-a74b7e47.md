- 2026-08-18 [planwright] The spec validator does not flag a spec file whose header block runs to end of file with no body content, but anchor-integrity Task 2 makes exactly that shape fail the content anchor closed. The meta-spec already calls such a block malformed (doctrine/spec-format.md, *Header-block extent*), so the validator is behind its own format definition; the consequence used to be invisible and is now a hard anchor refusal an author meets at dispatch time rather than at `mise run check`. Task 4 (the anchor-freshness guard) is one natural home, spec-validate.sh the other. Surfaced while updating four in-repo test fixtures that were header-only stubs.
