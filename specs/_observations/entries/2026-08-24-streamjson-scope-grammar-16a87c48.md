- 2026-08-24 [repo] fleet-streamjson.sh launch refuses a scope containing "/" (valid_field allows [A-Za-z0-9._=@:-] only), but /orchestrate and every sibling seam name a unit scope as the spec PATH (specs/<spec>). A tower following the skill passes "specs/guard-coverage" and gets a bare "fleet-streamjson: invalid scope" with no hint that the bare spec id is wanted. Either widen the grammar to accept the spec path, or have the usage/error name the expected shape.
