- 2026-08-18 [planwright] fleet dispatch 2026-08-18: when a subordinate tower runs with its cwd set to another worktree (the meta-tower dispatched it into the tower worktree), the worktree-creation path resolves <repo>/.claude/worktrees/<suffix> relative to that cwd, so the task worktree lands NESTED inside the tower worktree (specs-tower/.claude/worktrees/task-3) instead of at the primary checkout root (.claude/worktrees/task-3, where a sibling subordinate that resolved the primary checkout put task-2). Functional (correct branch, gitignored) but violates the D-37 placement letter and makes worktree discovery inconsistent across units. Candidate fix: the creation primitive should resolve the repo root from the git common dir (git rev-parse --git-common-dir) rather than cwd.
