- 2026-09-05 [planwright] The allocation ledger row is written before the worker is spawned, and nothing retracts it when the spawn fails. offload-dispatch resolves the tier, then tmux new-window can fail or return an unusable window id, and the dispatch reports failure and exits 1 with a launch row already committed for a launch that never happened; the print rung records one for a command a human may never run. There is no compensating no-op row and no reconcile pass, so last-tier, derive, stats and health all count it. Same shape applies between the engine row and the capability row when the process dies between them. Surfaced by the model-allocation Task 6 review; not fixed there because a compensation record is an adaptation-engine design decision, not launch-point wiring.
