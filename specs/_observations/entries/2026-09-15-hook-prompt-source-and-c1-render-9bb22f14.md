- 2026-09-15 [planwright] Two hardening ideas for the tower-comms reply hook and its readers, not applied for want of evidence: the hook does not read agent_type, so if a sub-agent prompt submission ever reuses the tower's session id it would confirm attention; and the log stores prompt text as raw bytes (C0 and DEL flattened, C1 kept, as tower-queue.sh does), so whatever renders a stored text back to a terminal (report, the queue's next) must sanitize on output per the echo discipline rather than trust the store.
