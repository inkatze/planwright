- 2026-09-07 [planwright] scripts/fleet-attention.sh releases its lock with `"$FS" unlock >/dev/null 2>&1 || true`, discarding both the exit status and stderr, and two reviewers flagged it independently. Today that discards nothing, because on origin/main (49a529e) fleet-state.sh unlock has no failure diagnostic to lose: the "it is not a lock symlink and is not an empty directory" message ships in PR #409, still OPEN and unmerged. So this is a finding that becomes live the moment #409 lands, not a defect on main, and it was deliberately NOT fixed here for two reasons: the premise is not yet true, and the fix belongs with someone who can weigh what a trap should print, since this is the EXIT/fatal-signal cleanup path and letting stderr through changes the output of every interrupted run, including the SIGINT path an operator sees. The narrow shape to consider when #409 merges is capturing the message and re-emitting it as one attributed line rather than deleting the redirect wholesale, keeping `|| true` so cleanup still cannot abort. Same discard-the-status family as the `case $(cat ...)` reads fixed in #405, #416, and this branch.
