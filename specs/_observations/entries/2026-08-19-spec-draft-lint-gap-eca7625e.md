- 2026-08-19 [planwright] The drafting commit for specs/fleet-lifecycle-closure landed a markdownlint error in specs/_pending/merge-currency-guard-amendment.md, so the bundle as committed would have failed the repo lint gate in CI. /spec-draft commits its bundle without running the repository lint over what it wrote; the error surfaced only at the kickoff pre-flip lint, one PR later. A lint pass over the written surfaces at draft-commit time would catch this class at authoring rather than at sign-off.
