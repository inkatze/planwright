- 2026-07-24 [planwright] The 2026-07-16 straggler triage (obs:78f60119) that D-9 rests on ranked the suite from LOCAL measurements, and that ranking does not survive the move to the reference runner. Reference-runner figures, pre-split (main ci run 30112471920): test-check-instructions.sh 52s, test-orchestrate-select.sh 10s, test-obs-consume.sh 1s — against local serial figures of 342.9s / 95.3s / 16.0s. Only check-instructions was ever a real straggler; obs-consume, which the triage reported at ~8min, costs ONE SECOND on the runner that matters. The true reference-runner top three (test-fleet-streamjson.sh 40.988s, test-fleet-liveness.sh 27.627s, test-fleet-attention.sh 16.944s, from serial best-of-3 run 30126631831; suite total-best-seconds 343.396 over 140 files) are files the triage never named. On the CAUSE, deliberately not overclaiming: an earlier version of this entry attributed the gap to macOS fork/exec being ~10x slower than Linux, but the local numbers were taken on a box shared with a concurrent sibling worker (worktrees/task-2 running its own full suite, load average 12-31) and one file clocked 902s locally against 11.9s in CI — 75x, which no platform factor explains. Platform cost and load starvation are both present and were not separable in this run. The durable lesson is the ranking discipline, not a platform constant: rank and budget test-performance work on the reference runner, never on a shared dev box, and treat any local ordering as a hypothesis. guard-coverage Task 7 must set check:test-time budgets from reference-runner numbers or every budget will be wrong by an unquantified local factor.
