- 2026-07-24 [planwright] The 2026-07-16 straggler triage (obs:78f60119) that D-9 rests on measured on a 12-core macOS dev box, and macOS fork/exec is roughly an order of magnitude slower than Linux for this fork-bound suite, so its ranking does not hold on the reference runner. Reference-runner per-file times from a main ci run, pre-split: test-check-instructions.sh 52s, test-orchestrate-select.sh 10s, test-obs-consume.sh 1s — against the local macOS serial figures 342.9s / 95.3s / 16.0s, inflated 6.6x / 9.5x / 16x and unevenly. Only check-instructions was ever a real straggler; the actual reference-runner top three are test-fleet-streamjson.sh (~40s), test-fleet-liveness.sh (~30s), test-fleet-attention.sh (~22s), none of them named by the triage. Lesson for any future test-performance work: rank on the reference runner, because the local ranking is a fork-cost artifact. guard-coverage Task 7 must set check:test-time budgets from reference-runner numbers or every budget will be wrong by the platform factor.
