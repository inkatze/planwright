- 2026-09-12 [planwright] classify-ci-failure.sh has no pattern for a signal-killed test runner: a mise run check whose test task ended with "xargs: " and "ERROR sh exited with non-zero status: no exit status" (the xargs pool and the sh wrapper both killed by a signal, about forty files never reporting a verdict, no assertion failure anywhere) classified as logic by the unknown-pattern default, which /execute-task must then treat as never-retryable. Seen 2026-09-12 on a host at load average 56 with several fleet workers running the suite concurrently. A signature for "no exit status" / an aborted dispatcher would let the skill route a runner kill to the bounded-retry arm instead of parking the unit.
