- 2026-09-12 [planwright] Worktrees created under the OLD flat suffix (.claude/worktrees/task-1, task-2, ...) before the rename to <spec>-task-<id> permanently wedge fleet-dispatch-worktree.sh for those tasks. The primitive computes only the new suffix, so the old worktree is invisible to its liveness check; git worktree add -b then fails because the branch is already checked out; and the collision reconcile reports "failed to recreate <branch> after rolling back a partial create" (exit 5). The message misdescribes the state: nothing was rolled back and the branch is intact, still checked out under the old name. The operator is left with a non-actionable exit-5 and a task that cannot be dispatched. Six such worktrees existed in the primary checkout on 2026-09-12, blocking tower-comms/task-1, tower-front-door/task-2 and task-3, operator-dialogue/task-8 and task-9, guard-coverage/task-7. The GC-adopt path needs to find a branch wherever it is checked out, not only under the suffix it would have chosen.
