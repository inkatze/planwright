- 2026-09-04 [planwright] fleet-streamjson.sh cmd_status decides ended vs completed with `{ [ "$st_kind" = exit ] && [ "$st_ec" != 0 ]; } || [ "$st_err" = true ]` — it never tests the result SUBTYPE. So a record `result error_max_turns <epoch>` renders `completed result=error_max_turns`, while fleet-stuck-detector.sh read_completion whitelists only result=success and reports completion-failed:result=error_max_turns. The two readers disagree about the same worker, and docs/fleet.md plus the detector own header both assert the opposite ("a non-success result subtype ... the supervisor own status renders it ended, and the detector agrees"). Not currently reachable from real frames, because the CLI sets is_error true whenever the subtype is not success, so the 2026-09-04 is_error fix incidentally covers it — but the agreement rests on that coupling rather than on either reader checking the subtype, and the documented contract claims a guarantee the code does not implement. Noticed 2026-09-04 reviewing planwright/chore/streamjson-is-error.
