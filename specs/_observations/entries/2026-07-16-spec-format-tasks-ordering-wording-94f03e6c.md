- 2026-07-16 [planwright] doctrine/spec-format.md defines the v2 ## Tasks section as holding blocks "in dependency order, never moving" while invariant-tasks D-10 and migrate-format-version.sh implement (and the migrated bundles ship) an id-sorted collapse; the validator enforces no ordering, so both texts are unenforced prose, but the two normative surfaces state different rules. Reconciling the spec-format.md wording is blocked by the same reachable-closure word budget that reverted the migration-path guidance entry (see 2026-07-15-orchestrate-closure-budget-exhausted); fold the reword into whichever diet/budget pass resolves that.
