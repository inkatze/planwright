- 2026-07-24 [planwright] A /drain sweep over the 28 in-repo bundles takes ~70s on a warm checkout, and essentially all of it is the derivation engine: drain-gates.sh consults scripts/orchestrate-state.sh once per v2 spec that has both task blocks and a gate marker (6 consults on the current corpus), and each does git log/rev-list work over the full history. Measured while checking format-grammar Task 2 for a perf regression: the same sweep against a non-git tree finishes in 4s, and the lib re-point adds only ~16s (23%) on top of the 70s engine floor. If /drain latency becomes a workflow complaint, the lever is the engine consult (cache per sweep, or narrow the git range), not the parse layer.
