- 2026-09-24 [planwright] doctrine/custom-steps.md, complete against every rule the spec assigns it, measures past the execute-task reachable-closure margin check:instructions --audit reports, so custom-steps design.md's cross-cutting Instruction budget note (the closure has unpinned headroom for the new rule doc) is false at that size; custom-steps Task 5 (the /execute-task wiring) needs a body diet beyond its own additions, a closure raise sized to the doc (tower-comms D-13's budget raise), or the doc trimmed to the script usage headers along a seam the spec does not name.
