- 2026-07-17 [planwright] fleet-liveness.sh classify appends to the per-worker observations TSV via copy-append-rename with NO fleet lock (single-observer-per-worker assumption, documented in the script header): two concurrent classify callers for one worker can silently lose an observation row and undercount the flailing streak. Zero callers exist today; when Task 3 (tower watchdog) and Task 4/8 (reconcile sweep) wire up classify consumers, they must either serialize classify per worker or the append needs the same acquire_lock discipline crash-record already uses.
