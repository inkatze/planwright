- 2026-09-03 [planwright] specs/fleet-lifecycle-closure/requirements.md carries its two original changelog entries as `- **YYYY-MM-DD** — ...` with the date bolded, while every guard escape keyed on dated entries (the validator's changelog-on-supersede and task-retirement escapes, the anchor-freshness pairing check) matches only the plain `- YYYY-MM-DD` form. The bundle's 2026-09-03 entry uses the plain form because the guard rejected the bold one, so the file now mixes a recognized form with two it silently ignores: a future supersede or retirement in that bundle recorded in the bold style would not satisfy REQ-A3.3 or REQ-D1.6. Either the meta-spec pins the plain form as the only dated form (it shows only that form today) and the two bold entries get an expression-only touch, or the guards learn the bold variant.
