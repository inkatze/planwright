- 2026-08-18 [planwright] fleet dispatch, verified 2026-08-18: hooks defined in a settings file passed via `claude --settings <file>` never register in -p (stream-json) sessions on CLI 2.1.233 — zero PreToolUse events while plugin SessionStart hooks fire; official docs claim --settings hooks should load, so this looks like a CLI bug worth a /feedback report. permissions.allow/deny from --settings DO apply. Working pattern, verified end-to-end with a throwaway worker: wire the PreToolUse guard through .claude/settings.local.json at the checkout root (loads with no trust step), pointing at an env-gated wrapper that no-ops unless PLANWRIGHT_WORKER_HANDLE is set and otherwise execs the newest plugin cache worker-command-guard.sh; the launcher exports PLANWRIGHT_WORKER_HANDLE/PLANWRIGHT_WORKER_SCOPE (the existing liveness env contract) so only dispatched workers get auto-approval and interactive/human sessions see a no-op. Candidate upstream shape: ship the env gate inside worker-command-guard.sh itself and document the settings.local.json wiring, replacing the mis-merge-prone raw fragment instruction.
