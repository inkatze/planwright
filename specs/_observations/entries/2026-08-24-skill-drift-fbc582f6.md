- 2026-08-24 [planwright] skill-drift(execute-task, orchestrate): doctrine/spec-format.md *Execution validity* gained a fourth remedy case in this PR — the one-time self-re-anchor for an anchor recorded before the header-block Status exclusion, or with the interim whole-file form (anchor-integrity REQ-A1.5). Both gate consumers enumerate the halt conditions and their remedies in their own prose: /execute-task pre-flight step 7 lists mismatch -> delta re-walkthrough and no/unparseable/non-sanctioned/wrong-writer -> repair the record, and /orchestrate carries the same list at its in-lock gate. Neither names the pre-change-anchor case, so a dispatch against an unswept adopter bundle gets routed to a delta re-walkthrough for content nobody edited. The doctrine is now ahead of the two skills that cite it. Routed to Task 4 (2026-08-25 panel review): both files sit at their instruction-hygiene ceiling — execute-task has 4 words of headroom against its restoration target and orchestrate 1 against its headroom floor, and tests/test-check-instructions.sh fails the build on any unexcepted below-target warning, so even a 7-word doctrine pointer breaches and would have to be funded by trimming prose the standing declared exceptions ask to leave alone. Task 4 already touches the freshness-gate surface and can budget the words deliberately; closing the drift there rather than out of a review pass.
