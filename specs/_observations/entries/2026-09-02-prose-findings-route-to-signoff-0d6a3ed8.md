- 2026-09-02 [planwright] A documentation-only convergence pass produced 8 Needs-sign-off items and zero Auto-applicable / Agent-resolvable ones, because both of the agent-owned buckets have code-shaped predicates: Auto-applicable requires a rule citation from a linter/type-checker/analyzer, and Agent-resolvable requires a failing-then-passing regression test. A doctrine-prose finding (an over-strengthened MAY, an absolute claim the doc itself forbids, a heading missing its deciding D-ID) can satisfy neither, so every validated prose finding with a single unambiguous fix falls to Needs sign-off and lands on the human checklist. artifact-lenses already generalizes the artifact class and Validation Rigor pass 1 off code; the bucket predicates in finding-categorization are not generalized the same way. Not structurally unreachable — a repo shipping prose pin-checks (planwright has several: check-doctrine-index, check-doc-links, the instruction-budget guard, the gloss check tower-front-door Task 12 authors) can ground a prose finding the same way a linter grounds a code one, and the index-enrollment step of this task did exactly that red-then-green. Observed on tower-front-door Task 1. Possible follow-ups for /spec-draft to weigh: name the prose-guard analogue explicitly in the Auto-applicable tool-grounding condition, or give Agent-resolvable a non-code evidence form the way artifact-lenses gives pass 1 one; either would keep the sign-off checklist for findings that genuinely change a contract rather than for every prose precision fix.
