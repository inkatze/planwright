- 2026-07-24 [planwright] skill-drift(execute-task): three structural gaps hit while running guard-coverage/6. (1) Ordering: the skill runs the review_sequence BEFORE PR creation, but a task whose Done-when requires evidence from a CI run on a PR cannot satisfy it in that order — the PR must exist first. guard-coverage Task 6 needs a reference-runner measurement outside the gated check job; I inverted the order and documented it, but the skill offers no sanctioned inversion. (2) Hard-disqualifier zones: the write-time security pass says work in such a zone (CI config among them) follows the gate-wiring pause protocol rather than landing autonomously, with no guidance for the case where the DELIVERABLE itself is in the zone — Task 6 Done-when requires a workflow file, so a strict pause blocks the deliverable outright. I landed it on the branch behind a draft PR with per-item pending-sign-off and named reverts, which seems the intended spirit, but the docs do not say so. (3) polish wider-suite stop: the trigger reads "fails after an iteration fixes and the failure cannot be resolved within the findings own scope," with no provision for a PRE-EXISTING or environment-specific failure unrelated to the branch. With CI green on all 140 files but the local suite red from a macOS-only unrelated test, it is ambiguous whether the loop should proceed, halt, or exclude the known failure — so convergence could not be run with confidence either way.
