- 2026-07-24 [planwright] drain-gates.sh does not recognize the Ready status: all 15 Ready spec bundles emit "unrecognized status; status atoms referencing it will not evaluate", while Done parses fine. Any GATE(when:) condition carrying a `spec <name> <status>` atom that references a Ready spec therefore never evaluates. Corrected 2026-07-26 (panel-review, direct probe): the failure is NOT silent — the Ready spec is dropped from the status map (scripts/drain-gates.sh whitelists draft|active|done|retired|superseded, not ready), so the atom returns bad-spec and the whole gate is reported MALFORMED as "unknown spec in atom". The real hazard is misdiagnosis, not silence: a well-formed gate reads as a grammar error, pointing a reader at the atom parser instead of at the status whitelist. Probe: a gate `spec <ready-spec> done` reports MALFORMED, the same gate against a Done spec reports SATISFIED. Confirms format-grammar Goal ("the gate grammar predates the six-status lifecycle") with tool output. Observed 2026-07-24 during an /orchestrate --fleet bookkeeping drain over specs/.
