- 2026-07-24 [planwright] drain-gates.sh does not recognize the Ready status: all 15 Ready spec bundles emit "unrecognized status; status atoms referencing it will not evaluate", while Done parses fine. Any GATE(when:) condition carrying a status atom that references a Ready spec therefore never evaluates; the failure is loud rather than silent, since it lands as a MALFORMED row (verified 2026-07-26: a lowercase "ready" atom reports "unrecognized atom" because ready is absent from the awk VALID map, and any other status atom naming a Ready bundle reports "unknown spec in atom" because the shell case list never adds that bundle to the stored-status map) on top of the per-spec note above. Confirms format-grammar Goal ("the gate grammar predates the six-status lifecycle") with tool output. Observed 2026-07-24 during an /orchestrate --fleet bookkeeping drain over specs/.
