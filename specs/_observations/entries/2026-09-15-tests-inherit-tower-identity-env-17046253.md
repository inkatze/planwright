- 2026-09-15 [planwright] Dispatched workers now carry PLANWRIGHT_TOWER_ID in their environment, and two tests assumed it unset: test-fleet-stuck-detector.sh (its env-identity case, neutralised in the tower-comms task 2 PR) and the new test-tower-loop-log.sh (which drops it in its runner). Any test that exercises the tower-identity resolution order (PLANWRIGHT_TOWER_ID, then PLANWRIGHT_TOWER_SESSION_ID or _PID) is exposed the same way; scripts/run-tests.sh could neutralise the identity env for the whole suite, the way check-hook-contracts.sh neutralises the worker identity, instead of each test remembering to.
