- 2026-08-24 [planwright] The guard-coverage Task 3 tree scan (check:purged-identifiers) hashes every 1..max-words n-gram of every tracked text line and takes ~15s over the current tree, making it the heaviest single member of the `check` aggregate. Candidate dedup was measured and rejected (21% faster for ~150MB of resident hash table). If Task 6/7 budget work finds the aggregate tight, the cheap lever is a committed first-word hash set so multi-word candidates only expand at positions whose first word can start a seed (~3x fewer hashes), at the cost of leaking one more guessable hash per seed.
