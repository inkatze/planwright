- 2026-09-07 [planwright] A spec bundle's stored Status header is authoritative on Format-version 1 but inert on version 2, where lifecycle state is derived from live task evidence and never written back: scripts/tasks-pr-sync.sh reconcile refuses a v2 bundle outright, printing that derived state is never stored (REQ-C1.1) and writing nothing, so a v2 bundle can sit at Ready indefinitely with every one of its tasks complete. That split misled a tower today into reporting eleven specs as unfinished backlog when the derivation showed them finished; ten bundles in the current tree store Ready while deriving Done, and the v1/v2 correspondence is exact, all ten v1 bundles are Done and all live bundles are v2. The header cannot be made self-describing: doctrine/spec-format.md constrains the value to the enum Draft, Ready, Active, Done, Retired, Superseded, and scripts/spec-anchor.sh aborts on a header block it cannot parse, so the drift has to be caught rather than annotated. Separately, a parked spec-format-v1-migration branch, sha 199dff4d9b85bd0f590f6820c08c54dee98170e7, deleted today, proposed migrating the ten remaining v1 bundles to v2: work with no beneficiary, since all ten are Done and frozen and check:memory-links already skips frozen bundles outright.
