- 2026-09-02 [planwright] doctrine/accumulator-taxonomy.md states the observation write side as 'every recording SKILL drops one fragment through the shared helper scripts/obs-record.sh'. scripts/allocation-feedback.sh is now the first SCRIPT writer of that accumulator: it complies with the helper rule, so nothing is falsified, but the canonical home of the accumulator's contract no longer enumerates its full writer set and says nothing about what a non-interactive writer owes (which scope to pass, which store, what a failure means).
