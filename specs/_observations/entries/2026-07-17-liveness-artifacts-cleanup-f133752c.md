- 2026-07-17 [planwright] fleet-liveness.sh accretes per-worker artifacts under <fleet-home>/liveness/ (observations/<worker>.tsv capped at 50 rows each but unbounded in file count, crash/<worker> records cleared only by crash-reset, pending/<worker> markers leaked when a session dies without SessionEnd). Task 4 (cleanup/housekeeping sweep) should include this subtree in its stale-resource sweep, keyed to worker teardown (the fleet-attention clear path).
