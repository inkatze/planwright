- 2026-07-19 [planwright] The tower command-guard (fleet-hardening Task 7) auto-approves individual tmux relay/observe commands (paste-buffer, capture-pane, plain load-buffer) but DEFERS orchestrate-relay.sh's attributed SEND shape `{ printf header; cat -- msg; } | tmux load-buffer -b buf -` because the inherited engine defers brace-group grouping. Defer is safe (falls to the classifier, never a false-allow), but the routine attributed send is not deterministically allowed. Follow-up: either teach the engine a bounded safe brace-group form, or have orchestrate-relay.sh emit a guard-friendly send shape (e.g. a planwright script by literal path that does the load-buffer internally).
