- 2026-08-27 [planwright] Two consecutive full 'mise run check' runs on the same tree each failed a DIFFERENT fleet-* timing assertion (test-fleet-attention.sh heartbeat race: 19 of 20 rows; test-fleet-throttle.sh 'the same excerpt after elapse is a fresh event and must re-engage'), and both passed in isolation immediately after. Same lost-append/timing class as the 2026-08-26 test-fleet-state.sh note, so the flake is broader than that one file: it tracks parallel-suite load, not the specific store. A load-shedding workaround would mask it; the fix belongs in the append/anchor paths these tests share.
