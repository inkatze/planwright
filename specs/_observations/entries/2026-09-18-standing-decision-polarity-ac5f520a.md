- 2026-09-18 [planwright] A standing decision records only what the operator LETS THROUGH: capture has no polarity, and the settle-by-rule route always answers with the permission row first option, which the writer fixes as the approve label. An operator saying "never let a worker run npm publish" can only express it by writing coverage for that command, which inverts the rule into an auto-approval. Today a prohibition has to be expressed by writing no rule at all, so the prompt reaches the operator. Worth deciding whether the queue should carry deny rules, or whether the rule doc should simply say prohibitions are not written down.
