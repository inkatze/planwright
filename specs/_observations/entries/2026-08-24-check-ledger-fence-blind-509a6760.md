- 2026-08-24 [planwright] scripts/check-ledger.sh is not fence-aware and now contradicts shipped doctrine. doctrine/spec-format.md (Fenced illustration, landed in format-grammar Task 5) states the rule universally: no line inside a column-0 fence parses as any element of the format, in ANY parser of spec bundles. check-ledger.sh is such a parser and has no fence guard. Reproduced: a format-version 2 tasks.md that documents the version 1 placement sections inside a ```markdown fence gets "duplicate task block: task 1 already defined" and exits 1, so the guard fails a bundle whose only sin is documenting the old format. Task 6 made spec-validate.sh and spec-anchor.sh fence-aware via the shared lexer scripts/spec-parse.sh ($spec_parse_awk_fence), but check-ledger.sh is named in neither Task 6 nor Task 8, so no task in the format-grammar bundle covers it. Fix is small: prepend $spec_parse_awk_fence to its awk passes and add a fenced-mock fixture. Left out of Task 6 deliberately: it is outside that task Deliverables and changing a CI corruption guard deserves its own test-spec entry.
