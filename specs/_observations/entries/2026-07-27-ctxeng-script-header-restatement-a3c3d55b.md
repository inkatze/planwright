- 2026-07-27 [planwright] Script header comment blocks are a large unbudgeted prose surface (108 scripts; fleet-liveness.sh 2218 header words, fleet-usage-gate.sh 1318, fleet-presence.sh 1166, fleet-attention.sh 1127, tasks-pr-sync.sh 1118, fleet-dispatch-headless.sh 1002, fleet-streamjson.sh 1001, fleet-dispatch-worktree.sh 881) and they enter model context whenever an agent opens the script. Guidance point 4 applies to only one class of that content, and the distinction matters because the rest is exactly the gotcha material the guidance says to KEEP: design rationale, the why-it-is-this-way, and non-obvious invariants (the fleet-liveness escalation-preserve guard, the commit-time last-write-wins rule) are all load-bearing and should stay. The trimmable class is header prose that RESTATES a rule whose normative home is elsewhere and whose enforcement is already deterministic. Clearest instance: scripts/check-instructions.sh lines 2-60 re-derive roughly 700 words of the budget, headroom-floor, capped-charge, suppression-form and raise-rationale law that doctrine/instruction-hygiene.md owns, complete with its own parallel citation list - two prose statements of one law, drifting independently, in the very pair of files that define the anti-bloat policy. Change: collapse that class to a one-line pointer at the owning doc plus the script-specific behaviour (flags, exit codes, the untrusted-input posture) that has no other home. Risk: a header is often the only documentation a script has, so the pointer must be to a doc that actually resolves for a reader outside the repo, and the security-posture paragraphs (untrusted PR-controllable input, read-never-execute) must survive since no other file states them for this script.
