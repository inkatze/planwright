- 2026-07-29 [planwright] The binding constraint on orchestrate reachable-closure additions has flipped from the error CEILING to the headroom FLOOR, and the two existing fragments (obs:160f6a6d "19998/20000", obs:63ce4e19 "19999/20000") are now stale in the opposite direction. After instruction-headroom relief the closure sits at 18986/20000 (margin-to-error 1014), but instruction_budget_closure_floor is 1000, so check-instructions emits a floor-breach WARN once margin drops below 1000 - i.e. usable headroom is 14 words, not 1014. format-grammar Task 5 hit this live: its doctrine/accumulator-taxonomy.md delta first came to +190 words, which produced "floor-breach: closure:orchestrate margin=951 below headroom floor 1000" and simultaneously made the standing declared-exception|closure:orchestrate entry report as stale cleanup (the below-target warning it names is replaced by the floor-breach warning, and per the exemptions header a declared-exception NEVER excuses a floor-breach - so there is no sanctioned way to record a breach, only relief or a compensating trim). The delta was cut to +127 to land. Two things worth deciding: (1) the floor-breach is a WARN and check-instructions still exits 0, so a breach can land silently in a repo that only reads the exit code - the protective floor nudges but does not block, which is deliberate but means CI green is not evidence the floor held; (2) doctrine/spec-format.md is capped-charged at 4000 into every aggregate, so meta-spec growth is aggregate-free while ANY other reachable doctrine is charged in full - which means the amendment doc most likely to grow is the one that costs nothing, and the small sibling docs carry the whole squeeze.
