- 2026-07-19 [planwright] instruction-headroom guard: the raise| parser (REQ-A1.4) validates <value> is non-empty but not an integer, so a non-integer/oddly-formatted value (raise|knob|abc|reason, 0100, or a trailing space) fails the downstream exact string-equality match and surfaces as a confusing 'silent raise' error rather than a 'malformed raise entry' error. Distinct from the sibling raise value-mismatch staleness edge but the same family: two raise-value parse edges, both deferred to a follow-up. Consider validating <value> as an integer at parse. Surfaced by the Task 2 nested Copilot-review adjacent pass.
