- 2026-09-22 [planwright] skill-drift(spec-draft): two gaps hit on the custom-steps draft (2026-09-22). (1) Pre-flight step 3 reads commit_on_draft as config/defaults.yml overridden by <repo>/.claude/planwright.local.yml, a two-layer model, while every other reader goes through config-get.sh and the four overlay layers; the skill should say config-get. (2) The working-location rule (D-44) has arms for the main checkout, the spec worktree, and an unrelated worktree, but no arm for being invoked inside a clean, user-created worktree made for the draft itself; creating the canonical <spec>-spec worktree from inside a worktree is recorded as nesting it wrongly (obs:de85a7f0), so the draft was branched in place on the existing worktree by operator choice. Worth an explicit arm: a clean worktree at origin/main with no spec branch may host the spec branch directly.
