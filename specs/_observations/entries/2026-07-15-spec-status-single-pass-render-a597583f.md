- 2026-07-15 [planwright] spec-status.sh consumes the engine stream with per-record subprocess lookups: completion_for and parked_class_of/parked_payload_of each fork printf+awk per task (O(n^2) rescans of an in-memory capture), and sanitize_printable forks tr per echoed field (~12 forks per completed task; ~470 tr forks across the test suite). Declined in Task 3 polish per D-12 (per-invocation derivation cost deliberately accepted, no revisit signal fired, n<=20 tasks in practice) — but if D-12 revisit signals fire (render latency complaints, gh secondary-rate-limit hits), the recorded fix shape is a single awk pass that pre-indexes completion/parked records and sanitizes in-stream. Surfaced by the /polish performance lens over invariant-tasks Task 3 (2026-07-15).
