- 2026-07-23 [planwright] tests/test-fleet-streamjson.sh c11 ("a prompt larger than the pipe buffer does not deadlock", REQ-A1.9) FAILED under a heavily parallel local `mise run check` on macOS (the Gatekeeper-slow headless suite running concurrently starved its bounded wait), but PASSES reliably in isolation. The test appears to have a wall-clock timeout on the large-prompt launch that is load-sensitive rather than a fixed deadlock detector. Not a Task 3 defect (fleet-streamjson.sh untouched by this branch). Candidate test-hardening for #312 / a follow-up: make c11 deadlock detection load-independent (e.g. detect the read-loop-blocked condition structurally, or scale the timeout to observed progress) so it does not flake under CI/local contention.
