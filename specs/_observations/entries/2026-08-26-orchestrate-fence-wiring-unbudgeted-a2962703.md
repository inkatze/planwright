- 2026-08-26 [planwright] The per-unit origin fence (concurrent-orchestrator-coordination Task 4) ships as scripts/fleet-fence.sh with fixtures, docs and a manual two-clone verification, but /orchestrate does not call it yet: the tower-side wiring (fence at dispatch inside the lock, the selection guard, the sweep in the watch loop) needs roughly 115 words more than skills/orchestrate/SKILL.md has left. That file sits 5 words under its headroom floor on main (3995 words against a 4000 limit), check-instructions turns a floor breach into a warning that its own closing-gate test then fails on, and neither escape hatch applies: declared-exception is explicitly 'never a floor-breach' and pending-diet is refused under the closeout direction. The skill already carries a declared exception parking its diet as deferred follow-up work. So the guarantee is implemented and tested but not live until either that diet happens or the wiring finds another home in /orchestrate's instruction closure (which itself has about 15 words of headroom). Operator decision 2026-08-26: land the mechanism, queue the wiring.
