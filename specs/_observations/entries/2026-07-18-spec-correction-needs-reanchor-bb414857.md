- 2026-07-18 [planwright] A spec-correction commit (a PS-* fix to an anchored spec file) merged to main WITHOUT a corresponding /spec-kickoff re-anchor strands the next task's execution freshness gate: the gate compares the stored kickoff-brief content anchor against the recomputed spec anchor, so a meaning-changing spec edit landed outside kickoff diverges them and blocks all further dispatch until a human re-anchors. Observed on fleet-autonomy: Task 3's PS-7 spec-correction merged without a re-anchor, stranding Task 8's dispatch until an operator ran /spec-kickoff (delta re-walkthrough #228). Durable-fix candidates: (a) a spec-correction touching anchored content triggers a re-anchor step, or (b) the freshness gate distinguishes expression-only edits from meaning changes. Feeds /spec-draft.
