- 2026-09-17 [planwright] A fleet tower that watches PR mergeability never notices a merge, so the watch loop does not advance when work actually lands. This run watched #468, #471 and #472 through a poll on mergeStateStatus and isDraft, which is what the flip decision needs, and it caught every BLOCKED to CLEAN transition. It did not watch state, so when the operator merged two PRs the tower learned about it only because they said so. The cost is not visibility, it is the loop: merging tower-comms task 4 met the dependency for tasks 5 and 6, and the fleet sat at zero in flight against a bound of two with two units newly ready and nothing dispatched. The skill already names what belongs on this edge and none of it ran until prompted: the reconcile sweep that re-derives completion from the merged PRs, fleet-fence.sh gc over the units the sweep resolves as merged, and the attention-store mirror going to merged and then clear for their rows. Fix shape for a watch surface: poll state (OPEN/MERGED/CLOSED) alongside mergeStateStatus and treat a transition to MERGED as a loop event, not just a status change, since it is the one transition that can change what is ready to dispatch. Worth noting the same edge matters for a spec branch: the delta re-walkthrough branch the operator cut earlier is now behind a main that moved twice, and nothing surfaced that either.
