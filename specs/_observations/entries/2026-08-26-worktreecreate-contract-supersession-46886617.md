- 2026-08-26 [planwright] SUPERSEDES the contract recorded in 2026-07-17-worktreecreate-decision-hook-a6f5511b (archived, consumed-by specs/fleet-lifecycle-closure). That entry recorded WorktreeCreate as a decision hook whose registered tracker "must echo the stdin worktree_path unchanged and always exit 0". Both halves are wrong, and shipping them broke worktree creation fleet-wide (obs f51f6b6e, root-caused in 2036d463 / 16facd5b). Corrected contract, re-verified for fleet-lifecycle-closure Task 2 by reading the CLI payload construction sites directly (LC_ALL=C grep -a -o on the binary) across every version on disk -- 2.1.226, 2.1.237, 2.1.239, 2.1.241, identical on all four: WorktreeCreate stdin is {hook_event_name, name} and carries NO worktree_path; registering a hook REPLACES native git worktree creation, so the hook is the CREATOR, expected to produce the worktree and print its absolute path, and a hook that echoes nothing has REFUSED the operation. Only WorktreeRemove carries worktree_path. The pass-through reading back-filled remove onto create. Also confirmed the published hook docs are not a usable source here: they omit WorktreeCreate input entirely and have drifted elsewhere (they name tool_output where PostToolUse sends tool_response, session_start_reason where SessionStart sends source, session_end_reason where SessionEnd sends reason) -- fixtures are pinned to the binary, not the docs. Resolution taken in Task 2: planwright registers NO WorktreeCreate hook and ships no hook-create handler, because it only ever wanted passive tracking and this event grants no passive mode; creation tracking rides record-create at the dispatch seam plus the scan reconcile. Enforced by scripts/check-hook-contracts.sh in mise run check (refuses any registration on an event whose silence refuses the operation, unless declared its implementer) and pinned by tests/fixtures/hook-payloads/. Doctrine home: doctrine/fleet-coordination-floor.md, "An open that can refuse is never registered for observation".
