- 2026-07-22 [planwright] The agents-json oracle probe (2-10s wall under load) makes classify a long call, but the per-worker observations TSV keeps its lockless copy-append-rename one-observer-per-worker assumption: any poll cadence shorter than the probe timeout can overlap classifies for one worker and silently lose an observation row (delaying the flailing streak). Probe-before-append moved the long pole outside the read-modify-write, but a cadence floor above the probe timeout, a per-worker probe lock, or taking the existing fleet advisory lock around the append+streak-read would close it properly; candidate task when the oracle joins the reconcile sweep.
