- 2026-09-02 [planwright] The fleet registry's death-handle column now says what to terminate, but the store authenticates no caller: any process running as the operator, including a dispatched worker, can append a record naming an arbitrary pid or tmux window. A bare 'process <pid>' also carries no start-time anchor, unlike the presence surface's composite identity, so a recycled pid is indistinguishable from the original. fleet-lifecycle-closure Task 3 documented the column as a hint rather than an instruction and left the mitigation where it belongs — the reaper's positive-death-evidence, self-target, and containment checks in Tasks 4/6 — rather than inventing authentication for a same-user file store. Worth an explicit assertion in Task 11's adversarial suite: a forged record must not be sufficient to kill anything.
