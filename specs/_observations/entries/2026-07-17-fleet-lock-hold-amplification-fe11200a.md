- 2026-07-17 [planwright] fleet-tower-watchdog holds the per-spec advisory lock across its relaunch bookkeeping, and the marker/audit writes inside that window each spin on the single cross-spec fleet lock (up to ~20s under contention, spawning config-get chains per busy retry). Bounded but degrades as fleet size grows; candidates: move marker/audit writes after the per-spec lock release (backoff write is the serialization point), or add jitter/caps to the fleet-lock retry loops shared by fleet-attention/fleet-audit/fleet-tower-marker.
