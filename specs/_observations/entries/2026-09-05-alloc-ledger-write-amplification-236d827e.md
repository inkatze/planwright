- 2026-09-05 [repo] alloc-ledger-write-amplification: a step-typed launch appends a second ledger row on every path, including the shipped all-inherit one where the row only says nothing happened. REQ-F1.1 requires inheritance outcomes be recorded, so the row is contract-correct, but once the launch points are wired this doubles per-unit ledger growth and takes the per-launch full-file scans from three to four over a file growing twice as fast. Worth revisiting whether an inheritance no-op earns a row, or whether the ledger needs compaction.
