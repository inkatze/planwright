- 2026-07-21 [repo] concurrent-orchestrator-coordination kickoff run-5 signed off (origin-fence-only, STRICT dead-owner strand handling). Deferred non-blocking hardening backlog (all availability/noise/test-coverage, none safety): (EH-F2) the non-strand git-op fail-closed surfaces (transient fetch, transient fence-push, transient fence-GC, --ff-only refusal) say "surface" but are not routed to a named durable channel like REQ-C1.7 dedup sink -> pin a durable channel so "surface" is never a transient log line. (EH-F3) fence-GC idempotent delete: "already-absent = success" rests on a per-ref-status discriminator like the REQ-C1.6 rejected-CAS-vs-transient split now specifies; make GC reuse that discriminator explicitly. (F5) GC-delete safety depends on the ledger never-un-terminal invariant (design.md ledger-governs-dispatchability); add an explicit note that a stale GC cannot delete a fresh re-fence because units never return terminal->Ready. (F7) strand dedup key = fence-ref+owner double-surfaces across an unknown-owner -> known-dead-owner classification transition; consider keying strand dedup on fence-ref alone. (F8) deployment precondition: origin must accept pushes to the custom refs/planwright-fence/* namespace and advertise the atomic capability; ruleset-restricted origins reject every fence push (fails closed, availability-only) -> document as a precondition in D-8/Scope. (Tests-F2) add an adversarial stale-tracking-ref fixture so the "live ls-remote, never stale tracking ref" property in REQ-C1.3 is caught not merely stated. (Tests-F3) convert the vacuous grep-for-absence assertions (no claims/ sub-surface, no LLM, no eval, no shared-registry) to positive assertions where feasible, mirroring the positive namespace-containment pattern. (clarity) REQ-C1.3 strand sweep should cross-reference REQ-A1.1 per-pass liveness cache so its death-probe fan-out bound is explicit.
