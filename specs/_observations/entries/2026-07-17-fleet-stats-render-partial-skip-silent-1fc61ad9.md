- 2026-07-17 [planwright] fleet-stats.sh render/line can show silently-incomplete stats on a PARTIAL audit corruption: fleet-audit query exits 0 but skips malformed rows (warning on ITS stderr), and derive_from_audit both suppresses that stderr (2>/dev/null, the intended quiet-degrade convention) AND only emits its own degrade message on a NON-zero query exit — so a partial skip warns from neither side. The quiet-render convention (all three derivers suppress sibling stderr; fleet-stats.sh audit is the diagnostic surface) is intentional and stays. A future ADDITIVE harden pass could have the deriver emit a partial-skip degrade signal WITHOUT dropping the stderr suppression (e.g. fleet-audit query exposes a skipped-row count the deriver folds into a visible marker). Surfaced by Copilot on PR 229 (Task 8), kept-as-is by tower decision. Out of scope for Task 8; feeds /spec-draft.
