- 2026-07-12 [planwright] prompt-hygiene Task 4: the check:no-ci-evals guard scans .github/workflows text for eval-task invocations (REQ-C1.6 'workflow files' scope), so it catches mise eval: forms and direct prompt-eval.sh calls, but NOT an eval task reached transitively through a mise.toml `depends` chain from a CI-run task (e.g. a future edit adding eval:skill to the check aggregate). REQ-C1.6 scopes the guard to workflow files, so this is out of literal scope; candidate follow-up is a mise-task-graph check that fails if any CI-invoked task transitively depends on an eval: task. Surfaced by the Task 4 /polish security lens (2026-07-12).
