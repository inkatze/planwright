- 2026-07-19 [planwright] release-publish.sh creation-path signing-mode read (mode=$(config-get.sh require_signed_tags 2>/dev/null) || mode="") has the same malformed-config swallow that Task 4 fixed on the resume path (S1): a malformed repo-tracked overlay makes config-get exit 4 (its explicit "refuse to silently degrade a shared team config" signal), which the caller collapses into the auto default, silently downgrading a require policy. Task 4 scoped its fix to the resume trust boundary only; the creation path (and any other 2>/dev/null||default config read) deserves the symmetric exit-4 fail-closed treatment. Surfaced during release-hardening Task 4 /self-review. RESOLVED (same PR, commit 43848da): the creation-path read now hard-fails on config-get exit 4 (release-publish.sh signing block), symmetric with the resume path. Remaining seed narrows to the version_file read (release-lib.sh rl_resolve_version_file, config-get exit 4 still collapses to the default path); require_ci is already safe (its default is the strict/fail-closed direction, so the swallow can only over-restrict, never downgrade).
