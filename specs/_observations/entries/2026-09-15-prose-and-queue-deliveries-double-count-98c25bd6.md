- 2026-09-15 [planwright] tower-loop-log.sh delivered logs every delivered turn with no item, which the scorecard counts as a prose delivery; once the queue's next writes delivered lines with an item for the same turn (tower-comms task 3), a queue-backed turn contributes both a prose delivery and an item, inflating delivered_items and the headline. Task 8's wiring should pass the item to the loop writer or make the queue the only delivered writer for those turns.
