- 2026-09-04 [planwright] The fleet home is created with a bare mkdir -p, no umask pin and no mode or ownership check, while fleet-presence.sh refuses its own subdirectory of that same home unless it is 0700, caller-owned and not a symlink. The symlink lock raises the stakes because the lock path is now consumed by -e, -L, readlink, mv and rm -rf rather than by a mkdir that simply fails on a pre-planted entry.
