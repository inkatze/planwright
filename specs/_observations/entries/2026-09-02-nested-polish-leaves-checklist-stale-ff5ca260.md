- 2026-09-02 [planwright] Running /polish --nested as a bare tower-level step leaves the PR pending-sign-off checklist stale, and under merge-as-signoff that silently understates what merging approves. Observed 2026-09-02: after nested polish passes converged, PR #381 carried 30 [pending-sign-off] commits but only 8 checklist entries, and PR #382 carried 9 with ZERO entries. The mechanism is sound and the contract is explicit -- skills/polish states the local-only rule as publishing being the owning skill job, and self-review says the invoking skill owns push and PR -- so nested polish deliberately does not touch the PR. The gap is that a tower can dispatch /polish --nested with NO invoking skill present, which satisfies every stated contract while leaving nobody to regenerate the checklist. gate-wiring says a loop exit regenerates the whole section in place; that only holds when a parent performs the exit. Consequence under the 2026-09-02 merge-as-signoff decision: the operator merging #381 would have approved 30 changes while the body showed 8, which is worse than the legibility problem recorded in pending-sign-off-legibility because the items are not merely unlabelled, they are absent. Candidates: have /polish --nested refuse to run without a declared owner, or have it emit the regenerated checklist in its handoff record so the dispatcher can apply it, or make checklist regeneration a lifecycle-hook attachment on the post-review-sequence state (see the hooks request in issue 383). The tower regenerated both bodies by hand from the marked commits before flipping, since the checklist is a pure function of base..head.
