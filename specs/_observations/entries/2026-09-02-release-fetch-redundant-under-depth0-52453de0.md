- 2026-09-02 [planwright] The explicit git fetch --force --tags origin +refs/heads/main:refs/remotes/origin/main in release-window.yml, and now in release-please.yml via release-hardening Task 9, may be redundant: actions/checkout uses its narrow refspec only when fetch-depth > 0, and with fetch-depth: 0 it uses the all-history refspec, which already covers refs/heads/* and refs/tags/*. If confirmed against the pinned checkout version, the cheaper correct shape is fetch-depth: 1 plus fetch-tags: true with no extra step, since the guard needs only origin/main tip tree and the tag refs. Not acted on in Task 9: the explicit fetch is named verbatim in the task deliverable and matches the sibling workflow, so changing it is a deliberate cross-workflow change rather than an execution-time call. The comment justifying it in both workflows would need correcting too.
