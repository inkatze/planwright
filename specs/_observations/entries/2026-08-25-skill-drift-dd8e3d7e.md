- 2026-08-25 [planwright] skill-drift(spec-kickoff): the content-anchor semantics the released skill and its doctrine copy describe were narrowed on main after the 0.31.0 release (header-block extent pinned, Status excluded from anchored content, every bundle re-anchored). The skill text still reads correctly, but a kickoff running from the plugin cache against a fresher main must compute its anchor with the repo-merged script or the freshness gate recomputes a different hash; this run merged main before anchoring for that reason.
