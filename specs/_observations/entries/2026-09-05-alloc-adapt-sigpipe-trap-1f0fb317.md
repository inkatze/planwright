- 2026-09-05 [repo] alloc-adapt-sigpipe-trap: allocation-adapt.sh traps EXIT/HUP/INT/TERM around the per-unit lock but not PIPE, while its sibling allocation-feedback.sh documents and works around exactly that hazard (a stderr write dying on SIGPIPE skips the EXIT trap and leaves the lock held until the stale break). The two scripts should agree; adapt writes diagnostics inside the hold.
