- 2026-07-22 [planwright] Local mise run check [test] fails on test-spec-graph.sh scenario 7 ("expected layout=graphviz with a working dot" — the capturing dot stub never receives a DOT source) on a machine with graphviz 15.1.0 installed at /usr/local/bin/dot; the identical failure reproduces on clean main with zero diff, so it is machine-local test-env breakage, not branch-caused. Linux GitHub CI passes the same test. Candidate fix: make the test dot-stub setup robust to a real graphviz 15 on PATH (or pin the stub PATH ordering). Surfaced during execute-task concurrent-orchestrator-coordination/1 (2026-07-22); operator directed proceed-and-trust-GitHub-CI.
