- 2026-09-15 [planwright] Two tower-queue.sh costs now sit on a per-prompt and per-loop-pass path: log resolves four config knobs per call (about 66 execs each, most of them PATH probes for the bash shebang of resolve-overlay-root.sh; reply lines resolve tower_tick_gap_max they never use), and a coalesced tick rewrites the whole event log twice (awk to a temp, then cat into a second temp) under the lock on every steady-state pass, so a month-old log is copied in full per pass. A per-fleet-home knob memo keyed on config mtimes and a direct rename of the coalesce temp would remove most of both.
