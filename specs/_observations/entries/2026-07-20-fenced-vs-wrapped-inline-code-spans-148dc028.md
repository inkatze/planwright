- 2026-07-20 [repo] Repo-wide presentation decision (operator-approved to capture, 2026-07-20): the skills wrap multi-line shell-command inline-code spans (single-backtick) across lines to satisfy MD013 line_length 100. These render correctly (CommonMark: newline->space) but Copilot RECURRINGLY flags them as "stray backticks / broken formatting when copied" false positives on nearly every skill diet PR (latest: 5 threads on PR #266 execute-task/SKILL.md; same pattern in dispatch-fetch.sh, spec-anchor.sh, resolve-catalog.sh, obs-record.sh, and every other skill). Decision to make: convert these wrapped inline-code command spans to fenced ``` code blocks repo-wide (cleaner render, permanently silences the recurring Copilot noise, but touches many pre-existing lines and needs an MD013 re-check per file), OR keep dismissing the FPs each cycle. Operator chose to dismiss-and-defer per-PR and capture this as the repo-wide choice for a future dedicated pass. See memory copilot-wrapped-code-span-false-positive.
