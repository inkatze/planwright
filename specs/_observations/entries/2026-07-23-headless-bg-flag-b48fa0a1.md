- 2026-07-23 [planwright] CLI 2.1.218 ships --bg/--background ("start the session as a background agent and return immediately; manage with claude agents"). The headless-oneshot dispatch (execution-backends Task 3) uses shell-level detachment (nohup runner + exit-status completion file) for a deterministic, version-independent completion signal; --bg is a candidate future refinement that would make detached one-shots first-class rows in the claude agents oracle, at the cost of an unverified result-capture/exit-code surface. Worth research when the flag stabilizes.
