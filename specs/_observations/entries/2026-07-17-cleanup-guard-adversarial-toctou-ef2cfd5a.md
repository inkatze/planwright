- 2026-07-17 [planwright] fleet-cleanup.sh self-targeting guard (the #29787 block) is robust vs ACCIDENTAL self-kill but not vs an adversarial/racing worker: the destructive kill-window / worktree remove re-resolve caller-supplied handles/paths at act-time separately from validation (TOCTOU via a symlink swap or tmux window-churn), and tmux self-identity is parsed without guarding a tab in the session/window name. Deemed OUT OF SCOPE for Task 4 (design threat model is accident-prevention). Follow-up if the threat model should include a hostile worker: operate on the validated realpath and re-check identity at act time. Surfaced by fleet-autonomy Task 4 self-review.
