- 2026-07-20 [repo] test-fleet-throttle.sh #33 ("the same excerpt after elapse is a fresh event and must re-engage") flakes under the heavy 12-way parallel load of a full `mise run check` (876s run): it observes a "Try again in 3 seconds" signal then busy-polls `check` until the 3s wall-clock anchor elapses. Under load the observe/check cycle can race the reset window and the post-elapse re-engagement misses. It passes deterministically in isolation (2/2 clean runs, 36 ok). Consider a fake-clock/injectable-time seam so the elapse test is deterministic under parallel load rather than wall-clock-bound.
