- 2026-07-24 [planwright] format-grammar Task 2 deliverable lists eight version-parse consumers including scripts/spec-graph.sh, but spec-graph.sh keeps no private Format-version parse: it delegates to orchestrate-select.sh --critical-path and spec-model.sh, and only MENTIONS the declaration in a comment. The re-point covered seven scripts; spec-graph.sh needed no change and the grep sweep confirms no private copy remains anywhere. The task list looks to have been drafted from a grep of comment mentions rather than of parses. Worth correcting in the spec text (expression-only) the next time format-grammar is amended, so Task 8 does not go looking for a parse that is not there.
