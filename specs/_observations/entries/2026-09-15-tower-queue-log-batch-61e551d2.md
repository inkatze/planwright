- 2026-09-15 [planwright] tower-comms Task 3 review: every event-log line a queue verb owes is a fresh tower-queue.sh log process (its own knob resolution and fleet-lock cycle, ~0.3s), and a rebuild owes one line per registered or dropped item, so a large rebuild takes the lock hundreds of times back to back and can starve the prompt-submit hook into dropping reply lines; a batch mode on the log verb (many lines, one lock hold, one knob round) or an in-process append after the store lock would halve every verb's wall clock and remove the starvation; deferred as a follow-up, not a defect.
