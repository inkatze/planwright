- 2026-09-03 [planwright] The tower dispatch brief for tower-front-door Task 2 restated the review_sequence by hand as "polish, then self-review" while the repo-tracked planwright.yml pins [self-review]; the worker followed the resolver. A dispatch brief that copies a config value invites exactly this drift: it should cite the knob and let the worker resolve it, or the dispatch path should inject the resolved value.
