- 2026-09-13 [planwright] tests/test-fleet-streamjson.sh leaks supervisor shells: a worker exercising it during universal-binary Task 1 accumulated 79 fleet-streamjson processes, 70 of them older than ten minutes against a suite that runs in one to two, every one still looping against a /tmp sandbox the test had already deleted. This is the same accumulation recorded in the zombie-supervisor-accumulation fragment, but reached from a different direction: not a crashed fleet run, but the ordinary test suite for the supervisor itself. It matters more here because the leak scales with how often the suite runs, and the specs most likely to run it repeatedly are exactly the ones rewriting lock and supervisor code. Load average reached 8 to 10 on an otherwise idle host. The suite should reap the supervisors it spawns in its own teardown, keyed on the sandbox root it created rather than on a recorded handle, so a test that fails partway still cleans up. Until it does, anything dispatching work that touches the streamjson rung should expect to sweep afterwards.
