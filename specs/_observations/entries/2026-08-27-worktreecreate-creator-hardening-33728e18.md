- 2026-08-27 [planwright] PR 372 review (chore/worktreecreate-hook-create): the minimal hook-create creator carries a hardening backlog beyond the D-11 chore scope, verified during review: a failed `git worktree add -b` leaves refs/heads/worktree-<name> behind (name permanently refused; reproduced), jq-empty payloads fall through to the unanchored sed capture so a nested "name" key is promoted even with jq present (reproduced), pre-checks are check-then-create TOCTOU with an unguarded `rm -rf` cleanup that can delete a concurrent winner worktree or follow a pre-planted/dangling symlink, the reattach arm echoes the lexical path with no containment or [ -d ] check (stale prunable entries echo a nonexistent dir), a dangling origin/HEAD symref defeats the HEAD fallback (symbolic-ref succeeds on a dangling target), valid_name admits git-refname-invalid flattened branches (a..b, trailing dot, .lock) and dot-led segments (.git), the "/"->"-" flattening is non-injective, the echoed path lost its valid_path gate and one warn interpolates an unsanitized resolved root, and no git call redirects stdin or bounds runtime. Candidate fixes are recorded in the PR 372 audit record (parked pending direction); seed for the fleet-lifecycle-closure hardening work.
