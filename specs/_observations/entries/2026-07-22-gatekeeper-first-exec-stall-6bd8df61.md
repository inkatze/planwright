- 2026-07-22 [planwright] macOS Gatekeeper synchronously assesses a freshly created script inode on its first execve (measured ~55s under load, ~0.3s cached). Git-fixture tests that copy executable hooks/scripts to new inodes stall once per copy per run; test-githooks.sh works around it by hard-linking the repo inodes and pre-warming one exec per hook. Relevant to the guard-coverage test/CI wall-clock budget work (Tasks 6/7): local wall-clock baselines on macOS can be polluted by one-time assessment stalls that CI (Linux) never sees.
