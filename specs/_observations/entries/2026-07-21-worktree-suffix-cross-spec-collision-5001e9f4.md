- 2026-07-21 [planwright] fleet-dispatch-worktree.sh suffix grammar is task-<id> only (header claims a function of (spec, task-id) but spec is absent), so two specs dispatching the same task number collide on .claude/worktrees/task-<id>: second dispatch fails with "failed to recreate <branch> after rolling back a partial create" and can leave the canonical branch behind with no worktree. Fired live 2026-07-21 dispatching inception task 1 while concurrent-orchestrator-coordination task 1 held the path; format-grammar task 1 would collide identically. Suffix needs the spec segment (e.g. <spec>-task-<id>) or a collision-aware fallback, and the rollback path needs to be atomic.
