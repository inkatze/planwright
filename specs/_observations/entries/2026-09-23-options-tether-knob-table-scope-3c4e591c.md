- 2026-09-23 [planwright] tests/test-check-options-reference.sh harvests the FIRST Knob-headed table in docs/fleet.md, while scripts/check-options-reference.sh tethers the first Knob-headed table that also carries a Default column. A Knob-headed table without a Default column placed earlier in the guide makes the two disagree: the test drifts knobs the checker never tethers, and the checker's real table silently leaves the test's scope. Surfaced in tower-comms Task 10, where a new operator-queue knobs table without defaults tripped it; resolved there by moving the rows into the tethered table. The parsers should share one table-selection rule, or the doc should carry exactly one Knob-headed table.
