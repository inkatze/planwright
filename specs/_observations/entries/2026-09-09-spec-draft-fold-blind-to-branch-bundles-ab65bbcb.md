- 2026-09-09 [planwright] spec-draft fold-detection scans specs/ in the current checkout, so a Draft bundle that exists only on its own unmerged spec branch is invisible to it. Hit 2026-09-08 drafting tower-comms: the action-item-ledger bundle (Draft since 2026-08-24, on planwright/action-item-ledger/spec, never merged) is the closest neighbour and was found only because a worktree named after it happened to exist and a branch listing was checked by hand. The same blindness applies to seed consumption: obs-consume cannot reach a fragment recorded on another unmerged branch (the plain-speech fragment 478f36fc lives on a chore branch), so the bundle cites it and defers the consume. Candidate: have fold-detection also list planwright/*/spec branches whose bundle is absent from the checkout and read their requirements.md Goal and Scope from the branch, and have the handoff name any citation whose fragment could not be consumed for that reason.
