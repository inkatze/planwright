- 2026-09-11 [planwright] Two gate entries in specs/model-allocation/tasks.md are reported MALFORMED by drain-gates.sh on every run and have been left that way deliberately, which is worth recording so the next person does not "just fix them" without seeing the bill. Both wrap a free-text condition in a GATE( ) form the closed grammar cannot express: the Configurable ladder knob entry and the Authored per-task complexity tier entry, each of whose condition is a prose statement about escalation observations showing a task class needing something the config prior cannot express. The remedy the drain output itself names is to drop the wrapper and write plain prose after **Gate:**, which is how every other free-text gate in the corpus is written, so the edit is two lines. THE COST IS THE REASON THEY ARE STILL THERE: model-allocation is Ready and format-version 2, and the content anchor hashes the bundle, so editing tasks.md moves the anchor and the execution freshness gate then blocks dispatch for that spec until a /spec-kickoff delta re-walkthrough re-anchors it. That is human work and a re-signed brief, spent on a cosmetic drain-report nit, for a spec that is otherwise dispatchable today. So the honest disposition is: fold this into the next kickoff that touches model-allocation for any other reason, where the re-anchor is already being paid for, rather than paying it for this alone. Recorded because it exists nowhere else -- it was noticed in a bookkeeping sweep and would otherwise live only in one session transcript, and a future operator meeting the same drain output has no way to know the omission was a decision rather than an oversight.
