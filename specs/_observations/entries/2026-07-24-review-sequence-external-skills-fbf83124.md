- 2026-07-24 [planwright] review_sequence cannot express the intended two-vendor convergence, and the docs claim it can. The operator asked for every unit to converge through /self-review --nested then /panel-review --nested (a Claude pass plus a non-Anthropic panel pass, so convergence variance is not exclusively the active Claude session). Setting review_sequence: [self-review, panel-review] is refused: scripts/resolve-review-sequence.sh admits a name only when <planwright-skills-root>/<name>/SKILL.md exists AND its argument-hint: line declares --nested, so the nestable set is exactly planwright-shipped skills, today {polish, self-review}. /panel-review is a dotfiles-local slash command, not a planwright skill, so it can never satisfy the predicate. Two consequences worth noting: (a) the refusal in the repo-tracked layer is a HARD FAIL (exit 4, empty stdout, "refusing to silently degrade a shared team review sequence"), so a well-intentioned config edit would break the convergence phase of every future dispatch rather than degrade - correct by policy, but it means this knob cannot be experimented with safely; (b) the operator-facing documentation asserts /panel-review --nested "fits review_sequence unconditionally (it needs no PR, only a diff against the base branch)", which contradicts the implementation. PLANWRIGHT_SKILLS_ROOT is an env override but resolves a SINGLE root on first-existing-wins, so repointing it to admit one external skill would hide planwright own skills - not a usable workaround. Options for a real fix: admit an adopter-supplied nestable-skill root as an additional search path; ship a panel-review adapter skill; or narrow the docs to say review_sequence admits planwright skills only. Workaround in use: review_sequence: [self-review] with the panel pass run as a separate tower-level step per unit. Observed 2026-07-24.
