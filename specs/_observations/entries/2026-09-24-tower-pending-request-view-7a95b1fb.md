- 2026-09-24 [planwright] A tower has no shipped read-only view of a stream-json worker's pending permission requests with the full tool input: fleet-streamjson.sh status gives only a count and age, so towers read req-*.json through ad hoc scripts. The auto-mode classifier refused one such read as "Create Unsafe Agents" (2026-09-24), stalling a worker until the operator added a path allow rule. Proposal: a read-only `pending [<worker>...]` verb on fleet-streamjson.sh (or full command text in fleet-attention.sh queue items) that an operator can allow by one stable plugin-path rule, kept separate from `answer` so every approval stays reviewed.
