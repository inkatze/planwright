- 2026-09-12 [planwright] Reading one config knob through resolve-config-knob.sh costs about a third of a second on the support bar (config-get forks the overlay-root resolver per layer), so a verb that reads four knobs pays over a second serially. tower-queue.sh log runs on every operator prompt from the prompt-submit hook and on every tower-loop pass, and works around it by resolving its four knobs concurrently; a multi-key read in config-get, or a cached merged view with an invalidation rule, would remove the workaround and the fork fan-out from every hot path that reads more than one knob.
