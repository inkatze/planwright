- 2026-07-15 [planwright] During the invariant-tasks Task 2 dispatch, the execution freshness gate read the primary checkout main view while the v1 reconcile writer had an uncommitted derived Ready->Active header mirror in that working tree: the whole-file anchor recomputation mismatched on the Status line alone, though committed main matched the recorded anchor exactly. The gate doctrine says "committed or not" but also frames the reference as "what /orchestrate and a merge see"; a mid-write sibling reconcile makes those two readings diverge for v1 bundles. Resolution used: committed main as the frame, since the delta was the sanctioned single-writer derived mirror, not a meaning edit. Format-version 2 removes the cause (no derived header writes), but the gate prose could state the committed-main frame explicitly for the v1 arm while it lives.
