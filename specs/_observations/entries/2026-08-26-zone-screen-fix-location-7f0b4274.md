- 2026-08-26 [planwright] gate-wiring routing step 1 fires the zone screen on "the finding, or any file its fix would touch". A test-only fix for a finding ABOUT hard-disqualifier-zone code (e.g. an untested arm of a secret screen) is ambiguous: the finding is in the zone, the fix touches only tests/. Resolved during inception task-2 convergence by reading fix LOCATION as governing, since the doctrine concern is autonomous modification of zone code. Worth stating explicitly in the doc.
