- 2026-07-27 [planwright] specs/prompt-hygiene/tasks.md line 255 defers a section-scoped resolver mode (load one named section of a rule doc instead of the whole file) behind the gate "point-of-use reads repeatedly load a whole reference doc for a single section". That gate names the wrong side. The live pressure is on RUN-START, created by the safety floor in doctrine/instruction-hygiene.md: because gating law must never be deferred, a doc containing ANY gating law loads whole at run start, bulk included. Result: /self-review and /polish each front-load discovery-rigor, validation-rigor, finding-categorization, gate-wiring (2483 words), refactor-instinct, security-posture and proportionality in full, for start-loads of 8995 and 8633 against the 8000 warn - both flagged today. Section-scoped resolution is the only ladder move that resolves the threshold-versus-safety tension the doc currently escalates to a human: the gating section loads run-start, the reference bulk becomes point-of-use, and no law is deferred. It is also exactly the Claude-5 progressive-disclosure recommendation (split long instructions into pieces loaded on demand). Change: widen the deferral gate to name the run-start case and re-rate its confidence, then treat section-scoped resolution as a headroom mechanism rather than a convenience. Risk: section addressing promotes doctrine headings to load-bearing identifiers, so a heading rename becomes a silently broken reference - it needs a resolution check in check-instructions.sh mirroring the existing manifest one, and a rule that a section reference resolves or fails loud, never falls back to the whole file.
