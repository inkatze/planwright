- 2026-09-03 [planwright] fleet-lifecycle-closure Task 4: the close decides liveness with a bare kill -0 in three new places (worker_alive, stop_live, and held_process through it), while the repo already owns a positive-evidence liveness seam, fleet-death-evidence.sh process, which exists precisely because kill -0 also fails with EPERM on a live process owned by another user and disambiguates with a targeted ps -p. fleet-streamjson.sh already calls that seam for cmd_status, so the branch adds a second, weaker implementation beside the one the same file consumes. Reproduced: kill -0 against a root-owned pid from an unprivileged uid returns 1, identical to a nonexistent pid, so a worker launched under another uid reads as dead - launch then starts a second supervisor over it and the close reports the process class released over a live tree. fleet-liveness.sh and fleet-dispatch-headless.sh carry the same bare kill -0, so this is a four-copy consolidation rather than a one-line fix, and the tree has already recorded the cost of a duplicated correctness-critical primitive twice (obs:30159d5c, obs:319e3472).
