- 2026-08-28 [planwright] The purged-identifier guard enforces its min-seeds floor at scan time but never re-derives max-words: a hand-lowered max-words narrows the candidate window past a multi-word seed and the guard still reports green (reproduced: a 3-word seed stops matching when max-words is edited to 1). Same class as lowering min-seeds, but with no floor to trip. Candidate: have the scanner also record a committed min-max-words floor, or drop max-words from the file and always scan the full 1..8 window at a measured cost. Surfaced during /polish over guard-coverage Task 3 (2026-08-28).
