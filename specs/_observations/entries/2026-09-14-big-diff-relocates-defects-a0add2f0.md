- 2026-09-14 [planwright] A 59-file unit did not converge under review; it relocated its defects. Five panel passes over the lock-primitive branch each found real defects, and passes three, four and five each found one INSIDE the previous pass fix - a clobber fixed in clear_legacy reappeared in take_link, a restore-then-delete fixed in take_link remained at two sites in _pw_lock_break, and the restore chokepoint introduced to fix that carried a path traversal of its own. The project suite was green every time (196 of 196) and every finding was real, so neither the tests nor the fixes were the problem. What the size did was make each pass see only the site it was pointed at while the sibling sites stayed, so a class closed in one place survived in two others. The worker own diagnosis after round four was that it had reasoned about a call site and then moved code, rather than about the property and where it has to be enforced - and the round that finally held was the one that made the unsafe shape unavailable (one restore function, one liveness question, one owner resolution, plus a test asserting no hand-rolled restores remain). That is the shape a fix should take on a shared primitive, and it is much harder to see the need for it when the diff is too large to hold at once. The lesson for task sizing: a unit that both introduces a primitive AND migrates every caller onto it should be two tasks, because the migration is what makes the review surface too big to catch the primitive own class errors.
