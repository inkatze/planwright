- 2026-09-09 [planwright] spec-validate.sh defaults its baseline to origin/main, so for a bundle that does not yet exist there every delta-sensitive rule (stable-ID checks, the dead-path pairing warning) short-circuits and a clean result says nothing about the delta; the kickoff lens pass only surfaced four unpaired requirement edits by re-running with --baseline HEAD. /spec-kickoff should run the baseline-HEAD form as well on a first activation, or the validator should fall back to the bundle-introducing commit when origin/main lacks the bundle.
