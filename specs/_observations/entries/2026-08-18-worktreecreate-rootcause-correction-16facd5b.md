- 2026-08-18 [planwright] SUPERSEDES the root-cause attribution in 2026-08-18-worktreecreate-regression-2-1-234-562be3ae: the EnterWorktree failure is NOT a 2.1.234 regression. Operator-verified against every CLI on disk (2.1.226, 2.1.231, 2.1.233, 2.1.234): the WorktreeCreate hook input schema is {hook_event_name, name} on all of them — no version sends worktree_path on create; the official docs carry no input schema for WorktreeCreate, and the worktree_path assumption was back-filled from WorktreeRemove's genuinely different shape. Actual bug is planwright's fleet-worktree-track.sh hook-create at HEAD: it reads .worktree_path, gets nothing, echoes nothing on deliberate exit 0 — and a registered WorktreeCreate hook REPLACES native git creation (the CLI's output contract says the hook provides the created worktree's absolute path, i.e. the hook is the creator, not a pass-through tracker), so worktree creation was overridden with a no-op. Upstream cause: obs a6f5511b recorded the contract as "echo the stdin worktree_path unchanged"; its supersession is REQ-H in the operator's draft spec. Interim operator fix in place: WorktreeCreate block deleted from the installed 0.31.0 hooks.json (WorktreeRemove retained), restoring native creation for new sessions. Proper fix (agreed chore PR): hook-create reads .name, grammar-checks, creates <repo>/.claude/worktrees/<name>, echoes that path. The planned Anthropic regression report was NOT filed (would have been misdirected).
