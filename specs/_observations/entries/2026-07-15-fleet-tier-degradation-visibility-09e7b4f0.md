- 2026-07-15 [planwright] orchestrate-meta-select.sh suppresses the per-spec selector stderr (2>/dev/null), so the new REQ-C1.9 rejected/near-miss reference-bullet warnings (a failed human park that therefore parks nothing) are invisible at the fleet tier - the tier most likely to run unattended; only the engine degraded/contradiction records and the rc-3 hold are re-stated. Related: a fleet fully held on transient evidence failure exits 1, colliding with nothing-ready at the machine surface (documented in the header; stderr is the only discriminator, so an unattended watch loop cannot retry-vs-stop on exit code alone). Hardening candidates: selectively forward selector stderr per spec, and/or a distinct all-held fleet exit.
