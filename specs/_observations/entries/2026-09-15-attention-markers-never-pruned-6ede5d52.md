- 2026-09-15 [planwright] The reply hook writes one attention marker per tower session under tower-comms/attention and nothing removes them; the queue's next verb enumerates that directory every turn and hard-fails the turn on any entry that is a symlink, widened or foreign-owned, so stale markers grow the per-turn cost without bound and any one that goes odd stops the queue. Prune markers older than the quiet interval on write, or hand the directory to the presence GC (tower-comms tasks 3 and 5).
