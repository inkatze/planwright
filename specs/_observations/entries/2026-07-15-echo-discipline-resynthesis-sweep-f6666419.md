- 2026-07-15 [planwright] sanitize_printable strips formed control bytes but an escape-interpreting echo (dash, macOS /bin/sh xpg_echo) re-synthesizes a live ESC from a LITERAL backslash sequence in the value - reproduced from a hostile v2 reference-bullet id. Task 5's branch converted orchestrate-select.sh and orchestrate-meta-select.sh to printf-only stderr (regression test V17); the same echo+sanitize pattern remains in fleet-state.sh, fleet-attention.sh, orchestrate-relay.sh, release-pending.sh, release-window-check.sh, spec-status.sh, and resolve-notification-channel.sh. Sweep candidates: convert to printf '%s\n', or grow echo-safety.sh a warn() helper so the posture is one function.
