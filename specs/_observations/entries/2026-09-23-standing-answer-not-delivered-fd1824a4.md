- 2026-09-23 [planwright] The standing-decision settle route in scripts/tower-queue.sh (settling pass, rule_covers then fleet-attention.sh claim --standing) stamps a claim on the worker's permission record and closes the queue item, but claim documents that it does not deliver, the stamped label is the record's first option ('approve in the worker session'), and nothing found in fleet-decision.sh, tower-loop-comms.sh, fleet-liveness.sh or the orchestrate skill consumes that claim to answer the worker's live harness prompt. The operator's item can close while the worker is still blocked. Surfaced by tower-comms Task 10's review; the fleet guide was worded to what the code does (the rule is recorded as the answer) rather than promising an unblock.
