- 2026-07-17 [planwright] REQ-B1.2/D-7 worktree disk-scan self-healing fallback is implemented as a manual fleet-worktree-track.sh scan CLI but is NOT wired to run periodically: fleet-sweep.sh reads the registry via list, never scan. So a missed/reverted WorktreeCreate hook fire leaves creations untracked until someone runs scan by hand. Follow-up: wire a periodic scan pass (e.g. into the housekeeping sweep) so the D-7 self-healing floor actually holds. Surfaced by fleet-autonomy Task 4 self-review.
