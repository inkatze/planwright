- 2026-09-13 [planwright] The fleet state lock now hands its owner token to the caller, and tower-queue.sh takes it. Seven consumers still use the token-less lock/unlock pair and so still release unconditionally: fleet-attention.sh, fleet-throttle.sh, fleet-audit.sh, fleet-usage-gate.sh, fleet-tower-marker.sh, fleet-liveness.sh, fleet-worktree-track.sh. Each can close its own acquire-window clobber the same way tower-queue did (capture the printed token, release with it), which is a small uniform change across seven files rather than anything this unit needed. Surfaced adopting the lock primitive (universal-binary Task 1).
