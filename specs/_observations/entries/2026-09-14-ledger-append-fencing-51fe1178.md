- 2026-09-14 [planwright] allocation-ledger.sh append re-verifies that it still owns the per-unit lock immediately before writing the row, which narrows but cannot close the window: the check and the append are two operations, and on the INHERITED-hold path nothing in the appending process holds the lock at all, so a force clear or sweep between them lets a row be written under a sequence derived against a lock somebody else now holds. Closing it needs a fenced write - the sequence carried into the row with the token it was derived under, and a reader that rejects a row whose fence does not match - which is a ledger-format change rather than a lock-primitive one. Current state is strictly better than the bare unit-name trust it replaced. Raised by a Copilot review thread on the universal-binary Task 1 PR.
