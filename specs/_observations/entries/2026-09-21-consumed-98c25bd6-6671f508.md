- 2026-09-21 [planwright] Consumed obs:98c25bd6 in tower-comms Task 8: tower-loop-log.sh delivered gained --item, so a queue-backed turn keys both its next-written and loop-written delivered lines to the same item and the scorecard stops counting one delivery twice (once as an item, once as prose). The fix is only half mechanical: nothing forces the loop to pass the flag, so a tower that forgets it re-inflates the headline silently. A scorecard cross-check (a delivered line with no item whose timestamp sits inside another delivered line with one) would catch that, and belongs with the measurement task rather than the wiring.
