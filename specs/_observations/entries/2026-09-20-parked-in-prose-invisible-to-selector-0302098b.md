- 2026-09-20 [planwright] tower-comms Task 8 derives ready deps-met from orchestrate-state.sh while the spec parks it: the Awaiting-input bullet is headed **Task 2** and says "Task 8 waits for that entry (D-15)" in its prose. Format-version 2 candidacy makes parked-ness a live reference bullet naming the task, and the selector reads the bullet head, so a task parked only in another bullet prose is invisible to it and would be dispatched. Two other decisions parked in the same bullet (risk rows 28a, 29a, 29b) are equally invisible. Worth either requiring one bullet per parked task, or teaching the derivation to scan bullet bodies for task references; the first is cheaper and makes the parking visible to a reader too.
