- 2026-09-09 [planwright] spec-validate.sh accepts a Dependencies line written as "Task 1, Task 2" (the form /spec-draft emitted for tower-comms and that several sibling bundles carry), while the derivation engine behind spec-status.sh and dispatch rejects it as malformed and treats every such dependency as unmet, so a validator-clean bundle renders every dependent task blocked. The validator should enforce the bare-id grammar the format prescribes, or the engine should accept the Task-prefixed form.
