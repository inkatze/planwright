- 2026-09-02 [planwright] fleet-usage-gate.sh reports the /usage signal unavailable and degrades to its reactive backstop, while the data it wants is already flowing through a stream the supervisor captures. Every stream-json worker emits rate_limit_event frames carrying unifiedWindows with per-window utilization and reset epochs: observed 2026-09-02 in ma-task-3 events.jsonl as five_hour utilization 0.62 resetsAt 1788406200, seven_day utilization 0.24 resetsAt 1788786000, plus status allowed and isUsingOverage false. The gate therefore runs blind (no proactive rung change, admit returns clean because it has no data to refuse on, not because it checked) beside a live per-worker feed. Candidate: have fleet-streamjson.sh persist the newest rate_limit_event per worker into the fleet home the way it persists session and pids, and have fleet-usage-gate.sh read that as a signal source before declaring unavailable. Caveats to design around: the frames are per-worker and reflect the account window at that worker last spoke, so freshness varies and the newest across workers is the right read; a fleet with no live worker has no fresh frame, so the unavailable path must stay; and utilization is a fraction of an opaque window, so rung thresholds need calibrating against it rather than assuming a token budget. Operator context: this session ran seven opus workers with the gate blind throughout, which is how the tower reached 62 percent of the five-hour window with no proactive warning available at any point.
