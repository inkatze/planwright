- 2026-09-17 [planwright] A clone wired before core.hooksPath became relative keeps the absolute value forever: wire-githooks.sh is idempotent only against the relative form, so check:githooks fails in every worktree of that clone until someone re-runs the wiring. Seen on the planwright clone 2026-09-17, whose config still read /home/inkatze/dev/planwright/githooks. Worth a one-line note beside the check: the remedy is not "re-clone" but "re-run wire-githooks.sh once per clone, even one that was wired before".
