- 2026-09-24 [planwright] fleet-dispatch-headless.sh stop resolves the unit from its handle plus --repo-root (or PLANWRIGHT_HEADLESS_STATE_DIR), but the registry record it writes at launch carries --state-dir and no repo root; a caller holding only the record (fleet-lifecycle-closure Task 6 fleet-cleanup.sh process, Task 8 sweep) must either reverse-parse the default layout or run from the right repo, and "not resolvable from here" shares exit 2 with "no such worker". Decide the delegation interface (a validated --state-dir, or a recorded repo root) when Task 6 lands.
