- 2026-08-25 [planwright] scripts/spec-validate.sh compares stable IDs against the origin/main TIP, not the merge base with HEAD, so any branch cut before main moved reports every D-ID and Task the newer main ADDED as "renumbered or removed since origin/main". Reproduced live 2026-08-25 on planwright/anchor-integrity/task-4: 38 spurious errors against specs/operator-dialogue after #351 landed, with no local edit to that bundle. CI is masked because actions/checkout builds the PR merge ref, so the failure is contributor-local only, which is where it costs the most (a red check:specs nobody can act on). anchor-integrity Task 4 hit the same class in its own pairing check and fixed it by resolving the baseline through `git merge-base <ref> HEAD` before reading blobs; the same one-line resolution fits spec-validate.sh (and any sibling that reads `<baseline>:<path>`). Surfaced during anchor-integrity Task 4 execution.
