- 2026-07-13 [planwright] kept-eval harness gap found at the Task 5 pilot: headless claude -p exposes no Skill tool, so a prose prompt never injects the SKILL.md and the run grades a bare model (which confabulated a completed dispatch marker at an invented path); plugin-registration init checks do not detect this. Fixed by literal slash-command prompts + a SKILL.md-H1 transcript sentinel (INVALID, not graded fail) + PROMPT_EVAL_KEEP_FAILED diagnosis seam. Generalizable: any behavioral eval of instruction files must verify the instructions actually entered context
