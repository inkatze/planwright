- 2026-08-27 [planwright] tests/test-fleet-throttle.sh case 33 ("an elapsed anchor re-engages on the next observation") is time-sensitive and flaked under a full `mise run check` run (160 test files in parallel), failing with `check exit 0` where it wants 1; it passes standalone every time. The case observes a reset 3 SECONDS out, busy-polls until the anchor elapses, re-observes the same excerpt, then asserts the re-observation re-engaged the hold. Under parallel load the 3-second window can elapse again between the re-observe and the check, so the assertion reads an already-elapsed anchor as a failure to re-engage. Unrelated to the branch that surfaced it (concurrent-orchestrator-coordination task 4 touches fleet-fence/fleet-presence, not the throttle). Options: widen the anchor beyond a few seconds for this case, freeze time through a clock seam instead of sleeping, or assert on the stored anchor rather than on a check exit code taken after an unbounded gap.
