- 2026-08-26 [planwright] panel-review's gemini backend fails with `fetch failed sending request` (exit 1, empty stdout) on a whole-branch diff around 150KB, while a small probe on the same key succeeds — a payload ceiling, not auth or network. Recovered by splitting the diff into four file-scoped chunks fed through the same prompt header; all four returned parseable lens tables. The skill's Backend-failure stop condition reads this as non-recovered unless the operator knows to chunk. Candidate: name a size threshold and the chunking fallback in the skill's per-backend invocation notes.
