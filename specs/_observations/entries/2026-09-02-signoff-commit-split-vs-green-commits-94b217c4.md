- 2026-09-02 [planwright] gate-wiring asks for one commit per Needs-sign-off finding so a reviewer rejects each with one revert. That is unachievable when the findings are interleaved in shared files AND each carries a regression test asserting the fixed behaviour: splitting them yields intermediate commits whose tests fail, trading a reviewer convenience for a broken bisect. Hit during fleet-lifecycle-closure Task 3's convergence with three sign-off items across three files; resolved by one commit plus targeted per-item revert recipes in the checklist, disclosed rather than hidden. Worth a stated rule: either the convention yields to green-commit ordering when they conflict, or it asks for the split only where a finding is file-isolable.
