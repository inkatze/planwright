- 2026-07-23 [planwright] Task 8 dashboard will poll fleet-status.sh; two costs surface under polling that are fine for the on-demand CLI: (1) read_streamjson spawns fleet-streamjson.sh status + awk per worker (~5-10 procs/worker) — a batched `status --all` in fleet-streamjson would collapse it to O(1); (2) the oracle probe (claude agents --json, 2-10s cold) is uncached and the merge stream carries no freshness/age marker, so a poller cannot pace itself or show staleness. Consider a cached-with-age oracle source line and a batched streamjson status for Task 8.
