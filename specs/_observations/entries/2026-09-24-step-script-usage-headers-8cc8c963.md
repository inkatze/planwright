- 2026-09-24 [planwright] custom-steps Tasks 2, 4, 9: doctrine/custom-steps.md delegates to the scripts' usage headers, or trimmed from its own text, the resolver's line format and usage-error exit, the preamble layout (one field per line, delimiters matched as whole lines) and prefix quoting, the excerpt bound and secret-screen action, the run-id order, and the status description and target, but no task's Deliverables names a usage header; the tasks delivering resolve-steps.sh and step-record.sh should list the header and what it pins.
