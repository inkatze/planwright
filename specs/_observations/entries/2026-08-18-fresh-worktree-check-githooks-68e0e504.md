- 2026-08-18 [planwright] mise run check fails check:githooks in every fresh worktree of an unwired clone: scripts/wire-githooks.sh is run only as an explicit CI step (guard-coverage D-3, wire-then-verify), never by mise, so a local `mise run check` is red on a content-independent config gap until someone runs the wire step by hand. core.hooksPath is clone-global, so one wiring covers every worktree — but nothing in the local flow prompts for it, and the failure reads like a repo problem rather than a one-time setup step. Candidate fixes: a check task that wires-then-verifies locally, a bootstrap task the getting-started doc names, or a SessionStart notice.
