- 2026-09-06 [planwright] Wiring /orchestrate's reconcile to report both terminal states closes the escalation feedback loop's completed arm end to end: fleet-fence.sh gc reports BEFORE it retires the ref, so the evaluation is reached even though nothing takes a fence yet. The disabled arm is reachable only conditionally. fleet-liveness.sh crash-record keys consecutive crashes by WORKER HANDLE, the disable that reports 'disabled' needs fleet_crash_disable_threshold of them on one handle, and the repo has no worker relaunch supervisor at all: the reconcile parks an orphan to Awaiting input and is doctrinally forbidden from re-dispatching, so the streak advances only if a human re-dispatches the unit under the same handle. crash-check, the gate that would authorize a relaunch, still has zero callers. Follow-up: decide whether the crash streak should key on the unit rather than the handle, or whether the orphan park should itself be the terminal report, so the disabled arm does not depend on handle reuse nobody guarantees.
