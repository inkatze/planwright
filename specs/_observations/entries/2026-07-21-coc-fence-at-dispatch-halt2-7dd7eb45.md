- 2026-07-21 [repo] concurrent-orchestrator-coordination /spec-kickoff run 2 (2026-07-21) HALTED AGAIN (inconsistency, REQ-B2.3), same work-division correctness axis as run 1. Root defect (3 lens agents converged, ground-truth confirmed): under D-3 separate-clone topology there is NO authoritative cross-clone fence at DISPATCH time. The iteration-3 rework delegated correctness to orchestration-concurrency branch-as-fence, but the task branch is created LOCALLY at dispatch and only reaches origin at PR-open post-convergence (orchestration-concurrency design.md:41 rejects push-at-dispatch, REQ-A1.3 no-remote first-class; execute-task SKILL.md:349 pushes at PR-open). So a claim lost in the dispatch->first-push window lets a peer clone dispatch a second full worker (double DISPATCH, not "wasted selection work"); only the 2nd PR is rejected. REQ-C1.1 claims this never happens and Task 4 Done-when contradicts it (validates via "rejected origin push"). OPERATOR RESOLUTION: Option A - dispatch pushes the task branch to origin IMMEDIATELY at dispatch, making branch-as-fence live cross-clone from dispatch AND death-surviving (fixes both the double-dispatch window and the orphan-worker reclaim guard). Preconditions to state: reachable origin required for separate-clone multi-tower (no-remote stays single-checkout solo); byte-identical cross-clone branch names. Compatible with orchestration-concurrency (it rejected pushing bookkeeping COMMITS to main, not a task-branch REF). Full ~39-finding backlog in kickoff-brief.md section 8 (run 2). Meta-lesson: both halts came from re-mechanizing the claim primitive without resolving the root architectural question (what is the authoritative cross-clone fence at dispatch); the rework should LEAD with the correctness model. Other high-value clusters: fail-closed-then-what action undefined across ~6 paths; corrupt-claim permanent strand (no quarantine path); bare-PID death handle defeated by PID reuse; tower-identity derivation unspecified (distinct-per-writer + self-exclusion rest on it); presence-GC unguarded rm; 0700 named as trust enforcement but no verify-or-refuse on pre-existing over-broad surface; security bars have no numbered D-ID home (cite D-6 which is scope-boundary only); orchestration-fleet REQ-D1.7 dangling ref; obs:8cbe0123/obs:3ecf4293 seed sources resolve to no files.
