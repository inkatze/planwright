- 2026-07-14 [planwright] Task 5 shipped with the behavioral pilot deferred: headless CLI slash-path skill injection is unavailable (probed with literal slash prompts, -p-argument delivery, and --bare dropped — all sentinel-INVALID), so the kept prompt-eval suite is guarded-inoperative. Follow-up needs a deliberate injection design as its own task (candidates: runner-side SKILL.md prepend with fidelity notes, an SDK-based driver, or a CLI feature request), then the pre/post /orchestrate pilot re-run on all four cells per tests/prompt-evals/results/comparison.md
