- 2026-07-26 [planwright] skill-drift(self-review): the gate-wiring pause protocol and format-version 2 parking semantics collide. The dispatched/unattended arm says a hard-disqualifier-zone finding is recorded to tasks.md `## Awaiting input`, but under v2 (spec-format, Reference bullets + Done derivation) a live Awaiting-input reference bullet outranks git evidence and blocks the bundle from deriving Done. So parking a zone finding whose question is answered by the PR sign-off decision itself leaves a stale bullet the moment the PR merges: the merge IS the answer, and nothing in either doc makes the bullet self-liquidating or assigns removal to the merging human. Hit on guard-coverage/6, where the finding (a workflow `run:` step piping a fallible command without pipefail) sits inside a commit already carrying a `[pending-sign-off]` marker and a PS checklist entry — i.e. the human review surface the pause protocol wants already exists and is finer-grained than the task-level bullet. Declined to park and surfaced on the PR instead, declared as a scoping. Candidate resolutions: (a) exempt zone findings from parking when the unit already has an open draft PR whose pending-sign-off checklist can carry them; (b) define an unpark-on-merge owner; (c) give v2 a distinct non-Done-blocking bullet class for review-surface halts. Adjacent to obs:c97cda97 item (2), which recorded the same zone/deliverable tension from the /execute-task side.
