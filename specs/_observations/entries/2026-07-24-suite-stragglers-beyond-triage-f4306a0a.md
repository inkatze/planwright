- 2026-07-24 [planwright] The 2026-07-16 straggler triage named three test files, but a serial per-file sweep during guard-coverage Task 6 found two larger ones it missed: tests/test-fleet-liveness.sh ~158s and tests/test-tasks-pr-sync.sh ~107s, against the named stragglers at 343s (check-instructions), 95s (orchestrate-select), 16s (obs-consume). After Task 6 splits the three named files, fleet-liveness becomes the suite wall-clock floor, so Task 7 must budget against it (or a follow-up must split it) rather than assume the post-split named files are the ceiling. Also: obs-consume was measured at 16s serial here versus ~8min in the triage, so the triage numbers were parallel-run wall-clock under saturation, not per-file cost.
