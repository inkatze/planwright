- 2026-08-25 [planwright] scripts/ is split roughly two to one on how it sources echo-safety.sh: about 37 scripts source it bare (`. "$script_dir/echo-safety.sh"`), about 19 guard it first, either with a readability check that exits 5 or with an inline sanitize_printable fallback. The split matters because `.` is a POSIX special built-in, so dash and a conforming /bin/sh abort on a missing helper, but bash outside POSIX mode only warns and continues with sanitize_printable undefined, and bash is what this repo's suite and CI run. The degradation is fail-safe rather than fail-open (an undefined function inside a command substitution yields the empty string, so a hostile token is dropped, not emitted raw), but a half-installed tree then reports the caller-error exit instead of the documented broken-install exit 5. model-allocation Task 1 added the guard to allocation-select.sh after a Copilot thread flagged it; the other bare sites all pre-date that PR and were left alone as out of diff. Possible seed: pick one pattern as the house rule and either sweep the bare sites or record why bare is sufficient, plus a lint that keeps new scripts on whichever side is chosen.
