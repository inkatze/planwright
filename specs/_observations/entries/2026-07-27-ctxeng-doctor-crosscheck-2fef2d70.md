- 2026-07-27 [planwright] Guidance point 7 ships tooling planwright does not yet acknowledge: claude doctor (CLI) and /doctor (in-session) now automate rightsizing of skills and CLAUDE.md for Claude-5 models. planwright ships its own deterministic analogue and RECOMMENDS it to adopters: the instruction-hygiene breadth entry in doctrine/guard-catalog.md (line ~140) advertises check-instructions.sh plus the kept prompt-eval convention as the way an adopter bounds an instruction layer. An adopter therefore now receives two overlapping recommendations with no stated relationship. Change: (a) add a sentence to the guard-catalog entry positioning the two - /doctor is a harness-side, model-generation-aware, judgement-based rightsizing pass; check-instructions is a deterministic CI-enforceable budget with a manifest-derived load model; they answer different questions and the catalog should say which; (b) run /doctor once against planwright own eleven skills and record whether its findings agree with the word budgets, since a disagreement is free evidence about whether the budgets are calibrated for this model generation. Risk: /doctor is a harness feature planwright cannot wire, version-pin, or depend on, so it stays advisory and must not be implied to replace the guard; the catalog entry should also avoid stating its behaviour in detail, which would be a fresh instance of the transcribe-instead-of-point rot obs:362f4de7 records.
