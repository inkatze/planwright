- 2026-07-18 [planwright] worker-permission-ergonomics execution (Task 1): the grounded permission fact underlying REQ-A1.3 and the brief (hook allow CANNOT override permissions.deny; deny→ask→allow precedence) is NOT explicitly confirmed by the current Claude Code hooks doc (code.claude.com/docs/en/hooks.md, checked 2026-07-18) — the doc states a hook can deny and that silence does not approve, but does not document allow-vs-deny precedence; a claude-code-guide reading even asserted the opposite (hook allow overrides deny). This is risk-register R3 (platform-contract version-sensitivity) materializing. It does NOT affect the shipped hook: the allowlist is read-only shapes with zero overlap with the worker deny block, and the adversarial suite pins that every deny-listed command defers, so deny stays effective regardless of platform precedence. Consider a REQ-A1.3 reword (assert the outcome — hook never emits allow for a deny-listed command — rather than relying on documented CC precedence) at the next spec-kickoff touch; re-confirm precedence via the REQ-C1.1 manual no-flood run against the running CC version.
