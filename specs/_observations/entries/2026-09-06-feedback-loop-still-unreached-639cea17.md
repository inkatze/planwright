- 2026-09-06 [planwright] Wiring the escalation feedback loop into fleet-fence.sh (gc, sweep) and fleet-liveness.sh (crash-record) moved its inertness out one layer rather than closing it: nothing in scripts/, skills/, hooks/ or commands/ invokes any of those three subcommands, and the identity flags they need are opt-in, so the loop still never runs end to end. docs/fleet.md discloses the flag half (nothing fires until whatever drives these commands supplies them) but not that the commands themselves are unreached, and docs/fleet.md already says of the fence that /orchestrate does not call this yet. No gate, task, or GATE(when:) deferral records the remaining distance to a working loop, which reproduces one level up the exact shipped-correct-but-never-called state that motivated the wiring.
