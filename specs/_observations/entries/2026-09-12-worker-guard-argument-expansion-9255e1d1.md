- 2026-09-12 [planwright] The worker-command-guard verifies a guarded verb's ARGUMENTS as literal text, so under the raw-command premise (Claude Code hands a hook tool_input.command unexpanded) an unexpanded $ in an operand slides past guard_find / guard_sed / guard_awk / guard_bashsh and the containment checks: `printf "%s" "-maxdepth 0 -exec id ;" >/dev/null; find . $_` and `for d in "-maxdepth 0 -exec id ;"; do find . $d; done` are approved and bash executes id; `for d in /etc/passwd; do cat $d; done` reads outside the repo. Reproduced on main and on the worker-stall branch, and the tower guard shares it. Pre-existing: the guards were sound only under the false expanded-command premise. A fix needs an unresolved $ in ANY word to defer, with for/select loop variables modelled (verify the body once per plain-literal head word, cap the count) so the observed `for d in a b c; do $P/scripts/x.sh $d; done` shape keeps approving; the tower guard needs the same treatment. Two more pre-existing shapes bypass the symlink-leaf refusal: a glob in the verb (`<root>/scripts/*.sh` is not a symlink, bash expands it to the real files) and arm-5 cache roots that are symlinks.
