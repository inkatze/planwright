- 2026-08-27 [planwright] test-fleet-streamjson.sh c12 ("queue should be clear after the answer", the re-open-on-resume case) is intermittently flaky: during the PR 372 review it failed inside a full `mise run check` (12 parallel jobs; a sibling shim process was OOM/timeout-Killed in the same run) and failed once more in an isolated run, then passed clean on the next isolated run with no file or environment change. Looks like async queue-drain timing under load. Worth a deterministic wait or retry in the harness so the CI gate does not report unrelated branches red.
