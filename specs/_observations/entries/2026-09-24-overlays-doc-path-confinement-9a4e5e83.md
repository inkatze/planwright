- 2026-09-24 [planwright] docs/overlays.md, the adopter-facing home of the overlay layer model, states the append/union, supersede-by-id and malformed-by-layer rules but not path confinement (D-8: each overlay file is canonicalized and containment-checked under its layer root before any read; an escaping file is never read). The rule lives only in doctrine/guard-catalog.md and the scripts/resolve-catalog.sh header, so a compression of the doctrine doc almost dropped its last doc copy. Candidate: state path confinement in docs/overlays.md §4 beside the by-layer table. Surfaced during guard-coverage Task 11 self-review (2026-09-24).
