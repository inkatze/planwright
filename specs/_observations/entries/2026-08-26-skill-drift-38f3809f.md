- 2026-08-26 [repo] skill-drift(self-review): the Discovery step says "one read-only sub-agent per canonical lens" and "one row per lens", but doctrine/discovery-rigor.md now defers the lens SET to doctrine/artifact-lenses.md by artifact class (code / spec / inception / human-facing), requires naming the class before the walk, and requires the coverage table caption to say which set was selected. The skill prose still reads as the code set unconditionally and never mentions class selection, so a reviewer following the skill alone walks code lenses over a spec or doctrine diff - the exact waste artifact-lenses.md exists to prevent. Same wording appears in /polish, which points at "the same rule docs as /self-review".
