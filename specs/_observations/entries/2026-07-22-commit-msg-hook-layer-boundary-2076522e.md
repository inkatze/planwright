- 2026-07-22 [planwright] The commit-msg hook backstop does not fire for `git cherry-pick` (no commit-msg hook in that flow) or `git am` (whose hook is applypatch-msg, not shipped), so a squash!/fixup!/amend! subject can land on a wired clone through those paths. Backstopped today by CI: check-commit-msgs.sh rejects such subjects as non-conventional over the PR range. Candidates if the residual ever matters: ship applypatch-msg (same content as commit-msg) and document the cherry-pick boundary in D-2; extending the four-hook contract is a spec-level decision, not an execution-time patch.
