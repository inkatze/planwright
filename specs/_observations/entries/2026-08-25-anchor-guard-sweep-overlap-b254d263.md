- 2026-08-25 [planwright] scripts/anchor-sweep.sh (format-grammar Task 6, #350) and scripts/check-anchor-freshness.sh (anchor-integrity Task 4) both walk every bundle, parse the brief anchor entry, and recompute — three brief-entry parsers now exist counting tests/test-anchor-landing-proof.sh, all of which must agree about the entry grammar and the sanctioned command forms. They are not redundant: the sweep reports for a parser-change landing (no skip semantics, no park carve-out, not wired into check), the guard is the standing merge gate with the REQ-D1.1/D1.2/D1.4 semantics, and the landing proof is Task 3 one-shot evidence. But the shared half — latest_anchor_entry plus the sanctioned-form validate-then-invoke — is worth extracting into the spec-parse lib so a fourth sanctioned form lands in one place. The two scripts were developed on branches that never saw each other; noting rather than acting per execution scope. Surfaced during anchor-integrity Task 4 execution.
