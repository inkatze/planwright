- 2026-07-23 [planwright] tests/test-fleet-throttle.sh case 33 ("the same excerpt after elapse is a fresh event and must re-engage") fails DETERMINISTICALLY on this macOS machine — reproduced both inside `mise run check` (12-job parallel) and re-run standalone in isolation, so it is not the known concurrent-run corruption. GitHub CI is green on the same code at both afe68ea and 38a6edc, so it is a macOS/local-environment sensitivity in the elapse/re-anchor path, not a main breakage. Surfaced during execution-backends Task 8, whose diff (scripts/fleet-dashboard.sh, tests/test-fleet-dashboard.sh, docs/fleet.md) touches nothing in the throttle path. Worth pinning down before the local check gate is trusted as a pre-push signal on macOS: today it forces every macOS run to hand-verify one FAIL against CI.
