- 2026-07-23 [planwright] fleet-streamjson.sh defensive-tail follow-up (panel iter-1 finding, recorded per sign-off, no code change): alarm-scan guards `a_id` with valid_reqid but consumes the sibling journal fields `a_kind`/`a_age` unguarded (attention_upsert kind + notify age). Degradation on a corrupted row is benign — a junk kind renders as the permission wording, age is awk-arithmetic so always numeric, neither is echoed raw — and the journal is self-written under the lock, so the guard asymmetry is gold-plating today. Seeded for the same hardening spec-draft as the streamjson-lock-hardening entries.
