- 2026-07-20 [planwright] fleet-hardening D-7 (amended 2026-07-20) takes a narrow, scoped exception to the never-shell-`git worktree` rule for the tmux dispatch primitive: it creates worker worktrees via `git worktree add -b planwright/<spec>/task-<id>` because native `claude --worktree <name>` always yields `worktree-<name>` and cannot produce the slashed D-36 branch. The SAME impossibility already applies to planwright's OWN spec-worktree creation (/spec-kickoff uses `git worktree add` to place <spec>-spec on planwright/<spec>/spec), so bootstrap-D-37 ('Worktrees created natively ... never shell out to git worktree', specs/bootstrap/design.md:595) AND docs/conventions.md:103-105 ('planwright never shells out to git worktree') are already aspirational. Core should formally reconcile the repo-wide invariant: relax the never-shell rule to sanction `git worktree add` for the slashed-branch placement contract, or add a native path that yields a slashed branch. Ties to the existing 2026-06-16 opportunity (specs/_observations/opportunities.md:68). Owned by bootstrap-D-37's bundle, not fleet-hardening.
