- 2026-07-19 [planwright] tests/test-release-mise-task.sh:52 — the [tasks.release] awk run-extractor still guards on the unanchored /run[[:space:]]*=/ pattern; the release-arm sibling block was anchored to /^[[:space:]]*run[[:space:]]*=/ in PR #242 (iter 1, after Copilot flagged it), leaving the two sibling blocks inconsistent. Lower-impact on the release block (it only prints and matches downstream via a substring case, so a stray rerun= line would not corrupt the assertion), so deferred rather than pulled into Task 7 scope; consider anchoring line 52 to match for consistency.
