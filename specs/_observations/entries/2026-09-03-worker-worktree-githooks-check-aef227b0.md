- 2026-09-03 [planwright] mise run check fails in a dispatched worker worktree at check:githooks because the shared .git/config (the primary checkout at /home/inkatze/dev/planwright) sets core.hooksPath to the absolute path of the primary checkout's githooks/ rather than the tracked relative githooks, and every worktree inherits it. The failure is environmental (identical on origin/main in the same worktree) and the classifier defaults it to logic, so the execute-task CI step reads a tower-owned config condition as a branch defect. Seen 2026-09-03 on format-grammar task 3. Options: wire-githooks.sh writing the relative form so worktrees resolve their own copy; the check accepting an absolute path that resolves to the common dir's tracked githooks; or the dispatch env re-wiring hooks per worktree. A worker should not edit the shared config, so the aggregate had to be re-run task by task with check:githooks excluded.
