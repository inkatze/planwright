- 2026-09-17 [planwright] orchestrate-meta-select.sh offers only one cross-spec ordering (fewest-in-flight, FIFO on ties), and an operator who wants specs drained sequentially has to override it by hand every step. This run supervised tower-comms, prose-disposition and operator-dialogue with the standing instruction to work them sequentially where the dependency chain allows and to pull from another spec only when the current one cannot fill the fleet. tower-comms had exactly one ready unit (task 4) and one slot free, so the operator policy said dispatch it; meta-select returned specs/prose-disposition 2, because tower-comms already had one unit in flight (PR #468) and prose-disposition had none. Both answers are defensible under their own rule, which is the point: the selector cannot express the operator one, so the tower has to declare a judgment override on every step of a multi-spec run rather than configuring the policy once. Fix shape: a fleet_spec_order knob (fewest-in-flight | sequential | declared-order) read by meta-select, so the ordering is configuration rather than a per-step overlay.
