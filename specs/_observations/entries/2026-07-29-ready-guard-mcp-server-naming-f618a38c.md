- 2026-07-29 [planwright] The ready-guard MCP matcher pins the literal tool name mcp__github__update_pull_request (merge-currency-guard REQ-C1.6/D-8), but the GitHub MCP tool name varies with how the server is registered: a claude.ai connector presents it as mcp__claude_ai_Github-Symmetry__update_pull_request, observed live in the Task 2 execution session alongside the plain mcp__github__ one. A session wired only to the connector-named server would bypass the MCP half of the guard entirely, since neither the hooks.json matcher nor the script would recognize the tool. Options: widen the matcher to mcp__.*__update_pull_request and have the script match the suffix, or document server-naming as a D-7-class residual. Not in scope for Task 2, whose Done-when names the literal tool.
