- 2026-07-22 [planwright] REQ-A1.5/D-12 pin non---bare at every -p-family launch site planwright emits, but scripts/prompt-eval.sh:479 deliberately passes --bare (eval harness subprocess, not a worker dispatch). Task 3 builds the launch-pin guard and must decide the scoping: exempt non-worker eval harness sites explicitly or reword the site inventory, so the guard neither false-fails on prompt-eval nor silently narrows the REQ.
