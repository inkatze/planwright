- 2026-09-05 [planwright] Resolving a launch tier costs roughly 55-75 process spawns per launch, and under the shipped inherit defaults that is what it costs to print the word inherit. The dominant share is allocation-adapt.sh: a knob read forks resolve-config-knob.sh each time, allocation-ledger.sh re-forks fleet-state.sh once per path computation with no memoization (three per append), and the per-unit ledger is scanned end to end several times per launch, so cost grows with a unit history nothing rotates. The adapt path already short-circuits the inherit case, but only after every knob read, both gate probes and the lock acquire. Cheap wins if this ever matters: memoize store_dir, hoist the pure config reads above the lock, and short-circuit inherit earlier. Surfaced by the model-allocation Task 6 review; not acted on because it is pre-existing chain cost and the task is a mechanism build, not a performance change.
