- 2026-09-12 [planwright] fleet-streamjson.sh launches its worker directly rather than through fleet-dispatch-env.sh, so a stream-json worker inherits neither CLAUDE_PLUGIN_ROOT nor PLANWRIGHT_ROOT (verified by reading /proc/<worker.pid>/environ: neither var present). The worker-settings auto-approve hook therefore cannot resolve its script path and the worker blocks on commands the guard would approve - confirmed by feeding the pending requests exact command to worker-command-guard.sh, which returned permissionDecision allow. The ghost-text pin D-10/REQ-D1.1 claims every fleet-launched session goes through that wrapper; the stream-json rung does not, so it misses the pin too. Observed 2026-09-12 dispatching /polish to a stream-json worker.
