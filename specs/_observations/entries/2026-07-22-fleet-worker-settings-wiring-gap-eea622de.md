- 2026-07-22 [planwright] Fleet bring-up has no step that delivers config/worker-settings.json to dispatched workers: fleet-dispatch-worktree attach plans launch bare `claude --worktree <suffix>` with no --settings, so the routine-toolset allowlist and worker-command-guard hook never reach worker sessions and every pre-flight command prompts (observed 2026-07-21: 4 workers, ~8 hours overnight (dispatched 2026-07-21 evening, first prompts sat unanswered until 2026-07-22), zero commits, tower hand-approving after the fact). Operator signed off 2026-07-22 on relaunching with `claude --settings <profile>`. Candidate fix: a worker_settings config knob the attach plan appends after a recorded one-time human sign-off, plus a docs/fleet.md bring-up step making the sign-off explicit.
