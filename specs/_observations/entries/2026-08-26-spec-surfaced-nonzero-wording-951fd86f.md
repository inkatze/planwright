- 2026-08-26 [planwright] model-allocation test-spec REQ-F1.1 asks a failed ledger append to be "surfaced non-zero", while requirements REQ-F1.1 and design D-6 require the unit to launch anyway ("never a blocked launch"). Task 2 read "surfaced non-zero" as "the helper non-zero exit is propagated to a surface rather than swallowed" and implemented: stderr diagnostic plus the attention push plus a mirrored governance row, with the resolver still exiting 0 and emitting the allocation, since a non-zero resolver exit is what a dispatch caller would treat as a blocked launch. The two readings are not distinguishable from the bundle text alone. Worth an expression-only pass over that test-spec sentence so a later task does not implement the other reading and change dispatch behavior.
