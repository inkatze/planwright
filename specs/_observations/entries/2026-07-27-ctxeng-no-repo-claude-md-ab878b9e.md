- 2026-07-27 [planwright] planwright ships eleven skills and 34 doctrine docs and has NO CLAUDE.md anywhere in the repo (verified by find). An agent working ON planwright rather than through the installed plugin therefore starts with zero orientation and must rediscover by search the things that most reliably break its work: that instruction files are budget-guarded by check:instructions and that the closing gate fails the whole suite even when the check itself exits 0 (obs:5acc5668), that doctrine loads are declared through a machine-parsed Doctrine: manifest, that specs/_observations holds ~190 fragments worth grepping before diagnosing anything, and the pipeline invariants. The Claude-5 guidance is specific about the shape this should take: keep CLAUDE.md lightweight, state repository purpose briefly, emphasise gotchas over obvious detail, and structure it as a tree loaded at the right moment rather than a comprehensive repository. Change: add a short root CLAUDE.md that is pointers plus gotchas only, deferring every body of law to the doc that owns it. Risk and precondition: a root CLAUDE.md is an ALWAYS-ON surface in every session in this repo, loaded even when no skill is invoked, and it would land in exactly the unmeasured class the unmeasured-read-surface fragment describes - so it must be enrolled in check-instructions with its own budget class BEFORE it is written, or planwright acquires an unbounded instruction surface while shipping the doctrine that forbids exactly that.
