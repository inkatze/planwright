- 2026-07-12 [planwright] test-spec-graph.sh's graphviz-layout assertion (expects layout=graphviz with a working dot) intermittently FAILs under the parallel `mise run check` depends fan-out but passes deterministically in isolation with dot present and working — a parallel-execution flake in the CI aggregate, not a logic failure. It mimics a real logic failure (classify-ci-failure defaults it to logic) and costs a full re-run to disprove. Candidate: serialize or retry the graphviz probe so the local check gate is deterministic. Surfaced during autopilot-reflex Task 9 /polish convergence.
