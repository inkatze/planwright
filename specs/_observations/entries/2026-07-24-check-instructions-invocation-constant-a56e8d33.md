- 2026-07-24 [planwright] Quantifies obs:e1c0f637 (check-instructions.sh config-get fork storm): a guard invocation costs ~3.5s essentially independent of corpus size — the real repo (--closeout and --audit) and a three-file fixture tree both take ~3.5s. tests/test-check-instructions.sh made 105 invocations for 342.9s serial, so cost = invocations x 3.5s with no corpus term. That makes the batched multi-key config-get read the whole win for this suite: it would cut every fixture invocation, not just the corpus ones, and it is the only lever left now that guard-coverage Task 6 has split the file seven ways (splitting redistributes invocations, it does not remove any).
