- 2026-09-24 [planwright] custom-steps test-spec: no REQ-E1.1 scenario has a convergence step exit normally with a queued meaning-class spec fork and asserts that no PR opens (under on-failure: continue included), and the REQ-E1.2 scenario uses only a pushing post-pr step, so the unconditional re-emission of post-pr's own table for a non-pushing step is untested; both are spec-side gap-fills for the next amendment.
