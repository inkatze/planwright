- 2026-07-19 [planwright] The freshly-written /bin/sh executable named `git` used as a PATH shim (tests/test-dispatch-fetch.sh c3/c10, and the pre-existing tests/test-release-arm.sh) intermittently stalls under the parallel `mise run test` harness on macOS: first exec of a just-created executable under /var/folders trips Gatekeeper/syspolicyd notarization. Under that contention test-dispatch-fetch (its real-fetch c4 in one run, the shim cases in others) and the untouched test-fleet-tower-watchdog show FAIL/hang while both pass 5/5 standalone; Linux CI is unaffected (Gatekeeper is macOS-only). Seed for a test-harness pass: pre-create/warm the shim once, add a per-op timeout in scripts/run-tests.sh, or document macOS parallel runs as CI-Linux-authoritative.
