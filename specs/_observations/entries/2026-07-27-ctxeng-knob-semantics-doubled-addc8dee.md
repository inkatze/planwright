- 2026-07-27 [planwright] The 72 config knobs are described twice, independently, in prose: as inline comments in config/defaults.yml (499 lines) and as table rows in docs/options-reference.md (6457 words, several rows running 200+ words each - dispatch_backend, kickoff_ready_ci_wait, context_budget_threshold). scripts/check-options-reference.sh enforces that a row EXISTS for every option; nothing checks that the two descriptions AGREE. That is precisely the overlapping-and-conflicting-messages failure the Claude-5 guidance names, and it is live for agents, not just readers: a skill resolving a knob reads whichever surface it lands on, and the two can diverge without any guard firing. Change: single-source the semantics - author the authoritative sentence once (defaults.yml comment or the reference row) and generate or check the other, e.g. extend check-options-reference.sh to compare a normalised first sentence per knob rather than mere presence. Risk: options-reference rows legitimately carry human-facing material that does not belong inline (migration notes such as the dispatch_backend subagent-to-full-session default flip, degradation policy, cross-doc links), so a naive generator would either bloat defaults.yml or drop that content; the split wants an explicit "normative one-liner" versus "extended notes" field rather than whole-row generation.
