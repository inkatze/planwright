- 2026-08-27 [planwright] tests/test-fleet-throttle.sh carries a flaky assertion that fails only under the parallel load of a full `mise run check`: "the same excerpt after elapse is a fresh event and must re-engage (check exit 0)". Passed on an immediate isolated re-run, and the same tree had failed a different load-dependent assertion (obs:c57faff7, streamjson c12) on the prior gate run while throttle passed - the two alternate, which is the signature of a shared time/CPU-contention sensitivity rather than either test being individually wrong. Not caused by fleet-lifecycle-closure Task 2, which touches neither fleet-throttle.sh nor its test. Fix: audit the file's elapse-based assertions for bare post-state checks that assume the clock advanced, and wrap them in the polling helper the sibling assertions already use, the same remedy obs:c57faff7 names.
