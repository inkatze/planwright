- 2026-09-06 [planwright] check-workflow-posture.sh assertion 4 now also requires a privileged workflow_run workflow to carry head_repository.full_name == github.repository, and .github/workflows/release-please.yml carries it, but guard-coverage's bundle still enumerates the old two-part assertion: requirements.md REQ-C1.2, design.md D-6, test-spec.md's REQ-C1.2 fixture lists, and tasks.md Task 4's Scope/Done-when all stop at 'keeps its base-branch filter and consumes no PR artifact'. The kickoff-brief's REQ-C1.1 audit still reads as unapplied ('Recommended hardening, surfaced not applied'), and its stated reason for not folding it in — 'a fifth would fail the repo's current main rather than guard it' — no longer holds on two counts: main now carries the clause, and it landed as a third conjunct of assertion 4 rather than a fifth assertion, so REQ-C1.2's count of four is intact. obs:131af768 is now fully consumed. Needs a /spec-kickoff delta re-walkthrough (meaning-class), not an expression-only edit.
