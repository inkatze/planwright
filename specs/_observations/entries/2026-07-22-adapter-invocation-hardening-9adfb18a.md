- 2026-07-22 [planwright] Both adapter_caps copies (orchestrate-backends.sh, orchestrate-degrade.sh) slurp unbounded adapter advertise output with no timeout before the 512-byte line bound applies: a hanging or firehose adapter blocks/inflates the caller (including the new fleet-liveness push-capable path). Portable bounded-capture (head -c without masking the adapter exit status) and a timeout need real design under the bash-3.2/BSD floor; candidate hardening task.
