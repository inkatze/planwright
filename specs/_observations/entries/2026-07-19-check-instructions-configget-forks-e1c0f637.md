- 2026-07-19 [planwright] check-instructions.sh forks config-get once per budget knob (~14 forks/run after Task 2 added 4 floor knobs; each config-get itself forks resolve-overlay-root up to 3x). Cost is constant per run (the guard-performance invariant holds: no corpus-scaling growth), but it grows linearly with the number of overlay-tunable budget/floor knobs and already makes tests/test-check-instructions.sh take ~2min wall clock (dominated by ~75 guard invocations x ~14 forks). A batched multi-key config-get read (one fork returning several keys) would cut this materially as more knobs land. Seed for a future tooling spec.
