- 2026-07-14 [planwright] prompt-eval skill-injection sentinel greps the raw JSONL transcript with grep -F for the SKILL.md H1 line: an H1 containing JSON-escaped characters (double quote, backslash) would appear escaped in the transcript, never match, and false-INVALID a genuinely injected run. Harmless today (shipped H1s are plain text) but a latent trap for the sentinel mechanism — fold into the behavioral-pilot-injection-design follow-up
