- 2026-08-28 [planwright] tests/test-fleet-streamjson.sh case c5 ("the queue should clear once the only pending request settles") failed once inside `mise run check` and passed cleanly on two isolated re-runs (19 ok, 0 fail each). The aggregate runs N test files at a time, so the case is likely timing-sensitive under parallel load rather than genuinely broken; it did not fail in an earlier full run of the same tree. Candidate: give c5 a settle-poll with a deadline instead of whatever fixed wait it uses now, so the parallel runner cannot starve it. Surfaced during /polish + /copilot-review over guard-coverage Task 3 (2026-08-28); unrelated to that branch, which touches only the purged-identifier guard.
