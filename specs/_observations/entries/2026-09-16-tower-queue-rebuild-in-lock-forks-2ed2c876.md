- 2026-09-16 [planwright] tower-comms Task 3 review (performance lens, measured): rebuild_if_absent runs a shell loop under the exclusive fleet lock with two execs per candidate (cksum, cut for the id) plus a check_pointer subshell per path item, about 8-10 ms of lock hold per rebuilt item (25 items 0.6 s, 100 items 1.4 s under strace), so a fleet with a few hundred awaiting-input rows holds the lock past the 2 s tower_hook_lock_wait default on the rebuild alone, before the debt drain starts; minting ids inside the awk pass or in one batch removes the per-item forks; deferred as a follow-up, not a defect.
