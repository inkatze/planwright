- 2026-09-24 [planwright] fleet-usage-gate.sh ships the /usage parser and the restriction ladder, but nothing in planwright feeds it: docs/fleet.md leaves the throwaway-pane /usage scrape as environment-specific operator wiring, so on this host the usage cache was never created and every capture reads unavailable (found 2026-09-24 when the operator asked the tower to watch the session limit). A hand-built scrape then failed on the folder-trust dialog and prompt detection. Ship the scrape as a planwright script and run capture+evaluate from the tower loop step on fleet_usage_read_cadence_seconds, so every tower has the signal without operator wiring.
