- 2026-09-05 [planwright] test-fleet-registration.sh case e8 (a SIGTERMed registration must not leave the shared .fleet.lock standing) failed once during a full 12-job suite run and could not be reproduced afterwards: 3 isolated runs and 6 runs under 12 competing CPU burners all passed. It is NOT caused by the model-allocation Task 6 branch: scripts/fleet-register.sh and scripts/fleet-state.sh are byte-identical to origin/main there. The assertion is precisely a signal-versus-lock window, so the honest reading is a real pre-existing race between the TERM landing and the lock trap being armed or run, in the same shared-lock area whose sibling registry race is fixed in unmerged PR #409. Not dismissed as flaky and not fixed here: the lock is fleet-state.sh territory, which that PR already has open. Worth reproducing deliberately under IO contention (many concurrent lock create/remove cycles) rather than CPU contention, which did not surface it.
