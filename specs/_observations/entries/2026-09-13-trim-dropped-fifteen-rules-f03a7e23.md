- 2026-09-13 [planwright] A compensating trim that reported itself as pure dedupe dropped fifteen rules, each the last place that rule lived for at least one skill loading the doc. Only the convergence pass caught it, via a lens dedicated to normative-statement preservation checked against the doctrine manifests. Examples: guard-catalog stopped saying release tagging never auto-fires (no skill loads release-tagging.md, and builder is the only skill surfacing that entry); gate-wiring stopped enumerating the hard-disqualifier zones and pause triggers, which orchestrate is the skill that writes and it loads gate-wiring without finding-categorization or security-posture; discovery-rigor lost the sub-agent brief ban on severity pruning and the stake/reversibility scaling rule that spec-kickoff reaches only through that doc. The lesson is not that the trimmer was careless - it believed each cut was a restatement. It is that a rule stated once in a doc that several skills load has no redundancy to harvest, and whether a sentence is the last copy for SOME loading skill is only answerable against the manifests, not by reading the doc. Any future compensating trim should check each deletion against the per-skill manifest closure before it is taken, not after. The measured residue after restoring was 159 words on one surface, and the honest conclusion is that the dedupe diet on the run-start doc set is exhausted: what remains is law or the rationale attached to it.
