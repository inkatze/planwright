- 2026-09-06 [planwright] The ledger unit key <spec>:task-<id> is asserted in allocation-feedback.sh but produced by nothing that writes a ledger: the launch-boundary caller (allocation-adapt.sh resolve <unit>) takes <unit> from its caller with no grammar and no stated shape, so scripts/fleet-fence.sh now assembles that key at a terminal transition on the strength of a comment. If a launch ever records under a different key — notably a cohesion bundle, where the fence writes one ref per member id while the ledger may be keyed on the lead — the terminal evaluation reads an empty ledger, reports below-thresholds and exits 0, which is indistinguishable from correct shipped-posture silence. No guard, test, or doc ties the two ends.
