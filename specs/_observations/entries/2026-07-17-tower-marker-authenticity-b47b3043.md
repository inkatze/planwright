- 2026-07-17 [planwright] The tower marker store is worker-writable (shared fleet home), so a hostile or buggy worker could plant a well-formed interactive marker and have the SessionStart signpost nudge a human with a constrained claude --resume command (no escapes or free text can be smuggled; field grammars bound the blast radius). Accepted trust boundary today; candidate hardening: an ownership/authenticity check on marker rows (e.g. tower-written markers signed or written to a path outside worker allowlists).
