- 2026-07-29 [planwright] ready-guard.sh is wired as a global PreToolUse Bash matcher, so every Bash tool call in every planwright session now spawns three jq processes (tool_name, tool_input.command, cwd type discipline) plus the tokenizer, on top of worker-command-guard.sh doing the same. REQ-C1.3 requires zero NETWORK cost on a non-matching command and that holds, but nothing requires zero parse cost. A raw-evidence pre-screen (the existing raw_evidences_ready_flip on the unparsed stdin) placed before any jq call would short-circuit the overwhelming majority of Bash calls to a single builtin regex, and is behavior-preserving: a payload with no gh-pr-ready or update_pull_request text already reaches defer through the tokenizer. Deliberately not applied during Task 2: it restructures security-critical dispatch and would need its own mutation sweep, which is outside that task Done-when.
