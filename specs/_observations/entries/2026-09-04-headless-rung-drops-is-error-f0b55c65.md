- 2026-09-04 [planwright] The is_error signal is captured on the stream-json rung and dropped on the headless one. fleet-dispatch-headless.sh writes result.json carrying is_error (its header says so) but its completion record is a separate `exit` file holding `<rc> <epoch>`, and fleet-stuck-detector.sh read_completion exit branch reads only the rc. So an API 529 that kills a headless worker which then exits 0 is a clean completion on that rung, exactly the defect the 2026-09-04 stream-json fix closed on the other. Same failure, same reader, different rung, still open — and the evidence is already on disk in result.json, unread. Noticed 2026-09-04 reviewing planwright/chore/streamjson-is-error.
