- 2026-09-18 [planwright] /spec-kickoff pre-flight step 5 assumes that if planwright/<spec>/spec exists as a branch with no worktree, the worktree was pruned and the branch should be reused. After the spec PR squash-merges, GitHub deletes the remote branch and the local one survives as a merged leftover whose commits are not ancestors of main; reusing it would resurrect the original kickoff commit onto a new PR. The tower-comms delta re-walkthrough hit this and branched planwright/tower-comms/spec-delta off main instead. Step 5 needs a landedness test (merge-base --is-ancestor against the merged PR head OID, since git cherry and commits-ahead both lie under squash-merge) before it treats an existing spec branch as live.
