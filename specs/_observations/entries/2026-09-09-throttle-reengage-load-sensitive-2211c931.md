- 2026-09-09 [planwright] test-fleet-throttle.sh "re-engage never observed within its anchor after 5 attempts" fails intermittently under the full parallel gate and passes otherwise, independent of test-fleet-streamjson.sh c12 (one clean run went red on this while streamjson passed). Recorded separately because the two were being counted as one number: the gate false-red rate measured earlier this session (13 of 43 runs, 10 unexplained) is an aggregate over at least two distinct load-sensitive suites, so "the gate is flaky" hides the fact that each has its own mechanism and needs its own diagnosis. The remedy that worked on c12 is the transferable part rather than the fix itself: the assertion reported only a boolean, and making it print what it actually saw turned an unreproducible flake into a diagnosed race in two runs. This throttle assertion has the same shape -- it names an expectation and an attempt count and nothing about what WAS observed, so anyone who picks it up starts the same archaeology from zero.
