- 2026-09-22 [planwright] The spec branch planwright/human-gates/spec was checked out in a worktree not named <spec>-spec (the one /spec-draft ran in). /spec-kickoff pre-flight step 5 reads that as "unrelated worktree" and would try to recreate <spec>-spec while the branch is already checked out; the run proceeded in place instead. The location rule should accept any worktree whose checked-out branch is the spec branch.
