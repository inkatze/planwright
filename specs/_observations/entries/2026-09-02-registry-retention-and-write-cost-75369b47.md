- 2026-09-02 [planwright] The fleet registry (fleet-state.sh) gained its first live writer in fleet-lifecycle-closure Task 3 and has no retention story: no prune, no TTL, no terminal record, and no deregister verb, so every dispatch appends forever and fleet-status renders every historical worker. Each append is also a full copy-append-rename of the whole file under the shared fleet lock, so cost is O(N) per write and O(N^2) over a fleet home's life, with the copy inside the critical section other fleet writers block on. Deliberately out of Task 3's scope (it is a writer task; lifecycle and the periodic reconcile are Task 8's), but the growth begins the moment Task 3 lands.
