- 2026-09-03 [planwright] fleet-lifecycle-closure Task 4: the task unified recover.lock and launch.lock onto a shared lock_take that records the holder pid and breaks by rename, with a comment stating plainly that rmdir-then-mkdir is not a compare-and-swap and that two callers racing to break one stale lock would both win it. journal.lock was left on exactly that pattern - it records no holder, so it is permanently on the age arm, and its stale path still does rmdir then mkdir; journal_unlock likewise rmdirs with no ownership check, so a slow holder that outlived a break of its own lock removes its successor's. The consequence is the lost update the new primitive was built to prevent: two answer invocations that both time out on one stale journal lock can both believe they hold it and one journal rewrite overwrites the other's receipt row. It was out of Task 4's scope, which named recover.lock and launch.lock specifically, and journal_lock has blocking-retry semantics the non-blocking election does not, so the merge is not mechanical. Worth doing deliberately rather than leaving one of three lock classes on the discipline the other two just abandoned - and note doctrine/fleet-coordination-floor.md now describes the lock class with a single break discipline, which reads as if all three had it.
