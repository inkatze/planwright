- 2026-09-02 [planwright] scripts/check-workflow-posture.sh assertion 4 checks a privileged workflow_run workflow for a base-branch filter and no PR-produced artifact, but says nothing about what such a job CHECKS OUT, because until release-hardening Task 9 no privileged workflow_run workflow in the repo had a checkout. Task 9 adds one to a contents:write + pull-requests:write job, so the "must not resolve a fork-controlled ref" invariant is now enforced only by a workflow-specific grep in tests/test-release-please.sh. A fifth assertion in the posture guard (a privileged workflow_run job checkout ref:/repository: must not resolve any workflow_run- or head_-prefixed field) would make it repo-wide and cover future workflows and adopter templates.
