- 2026-09-09 [planwright] The validator accepted 'Dependencies: Task 2, Task 9' lines with 0 warnings while orchestrate-state.sh's tokenizer treats the word Task as a non-conforming token and emits malformed-deps notes for them (the status render showed the notes for the four parked tasks). The grammar in spec-format.md is bare ids. Candidate: have spec-validate.sh warn on a Dependencies token that is not a task id or none, so the drafted form is caught before kickoff instead of by the render.
