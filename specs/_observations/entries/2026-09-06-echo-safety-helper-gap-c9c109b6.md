- 2026-09-06 [repo] scripts/fleet-fence.sh err() echoes sanitized text at seven call sites, one of them git-push stderr from origin. It is the last live instance of the printf-not-echo class and check:echo-safety cannot see it: the value reaches echo through a helper function positional parameter, which a lexical scan cannot follow. Fix the helper (one line) when that file is free, and consider teaching the guard the intra-file helper shape at the same time so the allowlist need not grow.
