- 2026-09-18 [planwright] The notification-channel enum gained `push` (tower-comms D-19, REQ-F1.6) in the resolver, the seam, config/defaults.yml, docs/options-reference.md and docs/fleet.md, but doctrine/attention-notification-capability.md still lists five channels. That doc declares itself the home of the capability-vs-style split, so it now describes a smaller style set than the resolver enforces. The tower-comms task block that owns the docs refresh is a later one, so this is a known gap rather than a miss, but it is the doc a reader would trust first.
