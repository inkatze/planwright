- 2026-09-07 [planwright] docs/overlays.md and scripts/resolve-config-knob.sh disagree about where a malformed adopter or machine-local value degrades TO. overlays.md says it "degrades to the next lower layer with a loud stderr warning"; resolve-config-knob.sh header says adopter/machine-local malformed warns and degrades to the CORE DEFAULT. These differ whenever a layer below the malformed one also sets the key: overlays.md promises the repo-tracked value, the script promises the core default. Surfaced by a cold read of docs/allocation.md during model-allocation Task 7, which had restated the script wording and now points at overlays.md instead of asserting either. Whichever is correct, the other is a live doc defect; the customization-overlay by-layer policy is cited by several skills.
