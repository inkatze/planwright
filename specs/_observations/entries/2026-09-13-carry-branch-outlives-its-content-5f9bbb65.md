- 2026-09-13 [planwright] The observation-carry chore branch survived past the point where it had anything to carry. Its fragments had all reached main through an earlier carry PR, yet the branch still held entries/ copies of three fragments main had since consumed into archive/ - so check:obs refused it for duplicate uids across entries/ and archive/, and once those three were dropped the branch diffed to literally zero files against main. The PR then sat open and BLOCKED, not because its content was contentious but because two of its commits were unsigned, which mattered only for content that no longer needed to move. The carry dedups against origin/main when it builds the branch, but nothing re-evaluates an EXISTING chore branch: it is never re-deduped against a main that moved, never closed when its delta empties, and its reused branch accumulates entries the archive has already claimed. Worth deciding whether the carry should refuse to reuse a branch whose delta is empty, and whether it should dedup against archive/ as well as entries/.
