- 2026-08-26 [planwright] a panel-review iteration-2 pass caught a fail-open the iteration-1 fix introduced: translating git's NUL-delimited `--name-only -z` output to newlines re-splits a path containing a newline, and naming the pieces after other staged files makes every piece resolve, so the crafted path is screened zero times and the walk still reports clean. Argues for the nested loop's re-review pass being load-bearing on security fixes specifically, not just a convergence formality.
