- 2026-07-19 [planwright] The echo+sanitize_printable stderr pattern flagged as a re-synthesis straggler in obs 2026-07-15 (spec-status.sh et al.) is still being COPIED into brand-new scripts: fleet-hardening Task 8 dispatch-fetch.sh shipped with `echo "...$(sanitize_printable ...)"` (caught and converted to printf `%s` in self-review). A one-time straggler sweep will not stop the recurrence; the durable fix is mechanical — a lint guard that rejects `echo` of a sanitize_printable result (or an echo-safety.sh warn()/emit() helper that is the ONLY sanctioned stderr path), so a backslash-interpreting sh (dash) cannot re-synthesize an escape a new caller forgot about.
