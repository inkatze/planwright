- 2026-09-06 [repo] test-fleet-throttle.sh case 33 raced its own three-second anchor under the parallel load the suite creates: observe took 2s and the following check 1s, so the anchor had honestly elapsed before the check ran. Measured, not assumed. Same shape as the known test-fleet-streamjson.sh c12 and test-fleet-state.sh case 8 flakes: an assertion whose budget is smaller than process-startup latency under 32-way parallelism. Worth sweeping the suite for other wall-clock windows under ~5s rather than fixing them one flake at a time.
