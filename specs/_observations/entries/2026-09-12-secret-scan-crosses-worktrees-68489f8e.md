- 2026-09-12 [planwright] scan:secrets is not isolated per worktree: gitleaks scans every ref reachable in the shared .git, so ONE worker committing a credential-shaped test fixture fails the secret scan for every other concurrent worker, on branches that do not contain it. Observed 2026-09-12: the tower-comms task-1 worker pushed f7c99d5 carrying rule aws-access-token at tests/test-tower-queue-log.sh:236, and the review worker on the unrelated tower-comms task-7 branch then had scan:secrets fail, having passed the same scan minutes earlier. A branch-scoped run (--log-opts origin/main..HEAD) was clean. The failure is maximally confusing: the finding names a file and commit the failing branch never touched, so the obvious reading is that your own diff leaked something. It also means the guard gets LESS trustworthy the more the fleet parallelises, and that a fixture one worker legitimately allowlists on its own branch still breaks siblings until that allowlist reaches main. Worth scoping the scan to origin/main..HEAD for a branch check and keeping the all-refs scan for the repository-wide sweep, which are genuinely two different questions.
