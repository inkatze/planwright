- 2026-07-20 [repo] Machine-local config (.claude/planwright.local.yml, gitignored) does NOT propagate to task worktrees: from a worktree, git rev-parse --show-toplevel returns the WORKTREE path, so config-get resolves the machine-local layer at <worktree>/.claude/planwright.local.yml, which does not exist (the gitignored file lives only in the primary checkout). Result: fleet workers dispatched into worktrees resolved review_sequence=[polish] instead of the operator-set [polish, panel-review], silently skipping the panel-review leg on several tasks (fh-3, ih-5/6/8 among merged). CLAUDE.md claims a repo-root local env file "covers all worktrees via ancestor-directory config loading" but resolve-overlay-root.sh keys off the worktree toplevel, not an ancestor walk to the primary checkout. Fixes to consider: (a) resolve the machine-local layer from the primary checkout / main worktree, not the current worktree toplevel; (b) an ancestor-directory walk; (c) dispatch-time copy of the local overlay into each new worktree.
