- 2026-07-17 [planwright] fleet-tower-marker.sh read emits the raw TSV row unsanitized (the tab bytes are the machine contract the watchdog field-splits on), while the human-facing signpost render sanitizes. read is documented for direct human invocation too; a TTY-only sanitize (sanitize when stdout is a terminal, raw when piped) would close the residual escape-injection surface for an operator inspecting a hostile-planted marker without breaking the machine consumer.
