- 2026-09-24 [planwright] custom-steps REQ-D1.6, REQ-G1.3: an isolated command step runs as a runner subprocess that no PreToolUse hook or permission rule sees, so the reserved controls (merge, ready, force-push, amend, squash, rebase, and push at pre-implementation, pre-ci, convergence, or pre-pr) bind it by declaration alone; a resolver-side refusal of command lines naming those controls (custom-steps Task 2, the resolver) would narrow the gap, though a declared script that runs git itself stays unseen.
