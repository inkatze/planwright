- 2026-09-24 [planwright] custom-steps REQ-D1.6, REQ-G1.3: an isolated command step runs as a runner subprocess that no PreToolUse hook or permission rule sees, so the reserved controls (merge, ready, force-push, rewrite, push at the no-push points) bind it by declaration alone; a resolver-side refusal of command lines naming those controls (Task 2) would narrow the gap, though a declared script that runs git itself stays unseen.
