- 2026-07-29 [planwright] Whether Claude Code's Bash permission matcher reaches into command substitution is UNDOCUMENTED and unverified, and worker-settings.json used to assert flatly that a denied push "cannot be smuggled via &&/;/|/subshell". The permissions doc (code.claude.com/docs/en/permissions, 2026-07-29) enumerates separators &&, ||, ;, |, |&, & and newlines — $() and backticks are not in that list — while separately saying commands the analysis cannot parse prompt instead. So `git status "$(git push origin main)"` is either denied, prompted, or ALLOWED depending on undocumented behavior; guard-coverage Task 1 modeled it as allow and recorded it as model-doc boundary MB-7, the one boundary that errs toward allow on a command that really executes. githooks/pre-push is what actually stops the main-push case. Resolve by observing a real session against a scratch settings fragment, then pin the answer in docs/permission-matcher-model.md. Same class as cc-hook-allow-vs-deny-undocumented (platform-contract version-sensitivity).
