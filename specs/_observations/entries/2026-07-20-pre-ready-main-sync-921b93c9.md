- 2026-07-20 [repo] Recurring fleet failure: in-flight worker PRs go behind/DIRTY vs main because main advances during the long gap between a worker converging on its branch head (Copilot-clean/CI-green) and flipping ready (hours for a heavy task). Observed on fleet-hardening #271 (DIRTY after amendment #272 + task merges landed) and #268 (7 behind). The safety net (tower relays merge-main to in-flight workers after each merge, per dotfiles direct-windows-update-after-merge) is a manual tower step that fails under load. Durable fix proposal: (1) /execute-task gains a MANDATORY pre-ready step — fetch + merge origin/main, resolve, re-run check, re-verify CI+Copilot on the NEW head, THEN gh pr ready — so a ready flag only fires when the PR is genuinely current+mergeable (removes the tower-timing dependency; the party signaling merge-me owns being current). (2) /orchestrate reconcile deterministically fans out a merge-main relay to ALL in-flight units on every merge event, not as a judgment call. GitHub already blocks merging a DIRTY PR, so the human is protected, but the ready signal is currently untrustworthy without (1).
