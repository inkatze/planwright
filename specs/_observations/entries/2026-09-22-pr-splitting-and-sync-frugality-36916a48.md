- 2026-09-22 [planwright] Operator wish, custom-steps drafting session 2026-09-22: planwright should be smarter about splitting work into PRs so branch updates and conflict resolution are rare, and the convergence main-sync should be skippable when the PRs merged since the branch head are unrelated to it and little time has passed, rather than syncing on every iteration unconditionally. Two concrete behaviors to weigh: unit sizing and file-scope partitioning at orchestration selection so concurrent units rarely touch the same files; and a relatedness-plus-recency predicate on the top-of-iteration sync (merge-currency-guard D-4 owns that rule today and syncs unconditionally). Surfaced while deciding what a post-pr step that moves the head owes the earlier verification; that question was settled as regenerate-the-record, not re-run, and this wish was routed here because neither unit sizing nor the sync rule is the custom-steps bundle to decide.
