- 2026-08-26 [planwright] docs/fleet.md knobs table now has a machine-read contract the doc itself does not state: within a row, knob names in the first cell pair POSITIONALLY with the leading backticked defaults in the Default column, so the multi-knob row (fleet_model_execution / fleet_model_bookkeeping / fleet_model_drain against opus / sonnet / sonnet) is correct only while both lists stay in the same order. check-options-reference.sh fails closed when the two counts disagree, but a same-length reorder of one side alone would pass. Either a one-line note in the doc near the table, or splitting multi-knob rows into one row per knob, would remove the trap.
