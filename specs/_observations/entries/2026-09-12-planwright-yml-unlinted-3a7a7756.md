- 2026-09-12 [planwright] lint:yaml runs yamllint over config/, .github/, and lefthook.yml only; .claude/planwright.yml, the repo-tracked config overlay every guard reads through config-get, is covered by no linter. A malformed edit there surfaces only indirectly, as config-get hard-failing inside whichever guard reads a knob next. Surfaced while reviewing the tower-comms closure raise, which is the first knob this repository sets in that file that a fail-closed guard measures against.
