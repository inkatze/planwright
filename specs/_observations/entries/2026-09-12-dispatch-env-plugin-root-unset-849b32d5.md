- 2026-09-12 [planwright] config/worker-settings.json wires the auto-approve hook as "${CLAUDE_PLUGIN_ROOT}"/scripts/worker-command-guard.sh, but nothing in the dispatch path exports CLAUDE_PLUGIN_ROOT: fleet-dispatch-env.sh sets neither it nor PLANWRIGHT_ROOT. The hook command therefore resolves to ""/scripts/worker-command-guard.sh, never fires, and a dispatched worker prompts on every routine Bash command - including read-only sed/grep compounds the guard exists to auto-approve. Observed 2026-09-12: a tmux worker stalled on its second command with jq present, so this is the unset-root path, not the jq-absent degradation. Unattended fleet operation is impossible until the dispatch env exports a resolved planwright root.
