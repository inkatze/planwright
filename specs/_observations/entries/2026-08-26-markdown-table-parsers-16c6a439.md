- 2026-08-26 [planwright] guard-coverage Task 9 added three more hand-rolled markdown-table parsers (doctrine index rows, the backend capability table, the fleet knobs table), joining the ones already in check-options-reference.sh and check-doc-links.sh. Each re-implements the same steps: find a header row, map columns by name, skip the separator, split body cells on the pipe, strip backticks and emphasis. They agree today only because they were written together. A shared table-reading helper (header-driven column mapping, cell stripping, a fail-closed no-header/zero-row contract) would make the next tether a few lines rather than another parser, and would give the fail-closed posture one implementation instead of five.
