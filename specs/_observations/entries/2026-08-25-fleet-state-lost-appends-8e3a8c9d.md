- 2026-08-25 [planwright] The fleet concurrency tests lose appends on this machine even with PLANWRIGHT_TEST_JOBS=1, so it is not test-runner parallelism: test-fleet-state.sh (registry race), test-fleet-attention.sh (heartbeat race), test-fleet-audit.sh (concurrent writers) each report fewer rows than the writers they spawned, and which of them fails varies run to run. Reproduced on a pristine unmodified HEAD tree (git archive of 1bd20ee) as well as on a feature branch, so it is not caused by any in-flight change. Either the state writers lose updates under contention on this filesystem or the tests assume an atomicity the writers do not guarantee; worth deciding which before the next fleet task treats a red run as signal.
