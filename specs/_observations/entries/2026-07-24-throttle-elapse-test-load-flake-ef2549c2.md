- 2026-07-24 [planwright] tests/test-fleet-throttle.sh case 33 ("re-anchor after elapse") flaked under parallel load during the Task 8 convergence run: FAIL "the same excerpt after elapse is a fresh event and must re-engage (check exit 0)". The case anchors a throttle on a "Try again in 3 seconds" excerpt, spins until it elapses, re-observes the same excerpt, and asserts check now reports throttled. Its budget is 3 real seconds between the re-observe and the check, so on a box running run-tests with 10 parallel jobs (plus, in this instance, other work) the anchor can expire before the check lands and the assertion reads not-throttled. Evidence it is load and not a regression: the file is untouched by the branch that surfaced it, an earlier full mise run check on the same branch passed it in 113s, and an isolated /bin/bash tests/test-fleet-throttle.sh on the idle box passes the same assertion. The neighbouring case 32 uses a 3-hour reset and is immune; only the deliberately-short anchor is exposed. Worth making the elapse case robust rather than fast: raise the anchor above the parallel-load jitter, or assert against an injected clock rather than wall time, the way the other timing cases avoid real sleeps. Seed material. Confirmed again 2026-07-29 under the 0.32.1 tower, now with a deliberate same-session control rather than an incidental one: with three wave-1 workers running suites concurrently the elapse case failed three times on the same assertion, and after waiting for a genuinely quiet box (polling for zero mise run test|check processes AND 1-minute load average below 4, not merely process-absence) two consecutive runs passed with 0 FAIL. That makes the A/B load-fails / quiet-passes on one machine within minutes, upgrading the load explanation from inference to a stated reproduction condition; still unfixed five days on, with no injected clock and no widened anchor. Two process notes. The worker's first pair of "isolation" runs were not isolated at all (a sibling worker's full suite ran throughout) and one of them had actually FAILED, which surfaced only by reading the run logs directly instead of accepting the worker's summary, so treat any "I ran it in isolation" claim as unverified until the quiet condition is demonstrated. And because this fragment already carried the diagnosis and the candidate fixes, the correct action was to extend it rather than file a ninth sighting; the backlog holds several near-duplicate throttle entries that each re-observe the symptom without adding a condition.
