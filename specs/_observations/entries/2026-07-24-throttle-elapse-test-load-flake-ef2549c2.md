- 2026-07-24 [planwright] tests/test-fleet-throttle.sh case 33 ("re-anchor after elapse") flaked under parallel load during the Task 8 convergence run: FAIL "the same excerpt after elapse is a fresh event and must re-engage (check exit 0)". The case anchors a throttle on a "Try again in 3 seconds" excerpt, spins until it elapses, re-observes the same excerpt, and asserts check now reports throttled. Its budget is 3 real seconds between the re-observe and the check, so on a box running run-tests with 10 parallel jobs (plus, in this instance, other work) the anchor can expire before the check lands and the assertion reads not-throttled. Evidence it is load and not a regression: the file is untouched by the branch that surfaced it, an earlier full mise run check on the same branch passed it in 113s, and an isolated /bin/bash tests/test-fleet-throttle.sh on the idle box passes the same assertion. The neighbouring case 32 uses a 3-hour reset and is immune; only the deliberately-short anchor is exposed. Worth making the elapse case robust rather than fast: raise the anchor above the parallel-load jitter, or assert against an injected clock rather than wall time, the way the other timing cases avoid real sleeps. Seed material.
- 2026-07-29 [planwright] Still reproduces five days on, and now with a deliberate same-session control rather than an incidental one. Under the 0.32.1 tower, three wave-1 workers ran suites concurrently; the elapse case failed three times on the same assertion. The worker was then made to wait for a genuinely quiet box (polling for zero `mise run test|check` processes AND 1-minute load average below 4, not just process-absence) and run twice: both passed, 0 FAIL. So the A/B is now load-fails / quiet-passes on one machine within minutes, which upgrades the diagnosis above from inference to a reproduction condition. Two process notes worth keeping. First, the worker's initial "isolation" runs were not isolated at all (a sibling worker's full suite ran throughout) and it reported them as isolation evidence; the false claim was only caught by reading the run logs directly rather than accepting the summary, and one of those two runs had in fact FAILED. Treat any "I ran it in isolation" claim as unverified until the quiet condition is shown. Second, this fragment already carried the diagnosis and the fix options, so the correct action was to append the control here rather than file another sighting; the backlog holds several near-duplicate throttle entries that each re-observe the symptom. Still unfixed: no injected clock, no widened anchor.
