- 2026-09-03 [planwright] tests/test-fleet-registration.sh assertion e8 ("a signalled register left the shared fleet lock held" / "a later registration could not acquire the lock") failed once under the parallel runner and passed on a re-run. Isolated runs pass three-for-three on both the feature branch and an unmodified origin/main worktree, and four of five full-suite runs were green, so it is load-sensitive rather than a real regression: e8 signals a register mid-flight and then asserts the shared lock was released, which is exactly the shape that goes wrong when the signalled process is descheduled under contention. Worth pinning down before check:test-time (guard-coverage Task 7) freezes a budget over a suite with an intermittent member. Surfaced during Task 10 execution (2026-09-03).
