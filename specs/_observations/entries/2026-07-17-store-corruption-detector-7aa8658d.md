- 2026-07-17 [planwright] The attention store's one-row-per-worker invariant (single writer under lock, upsert_row) is assumed by every reader, but nothing DETECTS a violation. Task 2 hardened the REQ-A1.3 --unless-awaiting guard (fleet-attention.sh upsert_row) to fail safe on a multi-row worker (any matching awaiting-input row wins), because a clobbered human decision is irreversible; sibling readers store_row_field/classify (fleet-liveness.sh) and render still assume a single row and were deliberately left unhardened (a misclassification self-heals on the next reconcile). Root-cause coverage should be one standalone corruption detector (e.g. a check that flags any worker handle appearing on >1 row, wired into the reconcile sweep and/or a check: task) rather than spreading defensive any-matching-row checks across every recoverable reader. Surfaced by Copilot on PR #214 iter 2.
