- 2026-07-20 [planwright] test-fleet-throttle.sh has a wall-clock elapse-based assertion ("the same excerpt after elapse is a fresh event and must re-engage") that flakes under concurrent CPU load (observed when a second worktree ran the fleet suite in parallel during instruction-headroom Task 11 CI); the test is state-isolated (mktemp -d + PLANWRIGHT_FLEET_STATE_DIR) so it is timing, not shared-state. It passes cleanly standalone. Candidate hardening: inject/override the clock or widen the elapse tolerance so a scheduling stall under load cannot miss the re-engage window.
