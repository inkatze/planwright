- 2026-09-06 [repo] The vacuous-absence shape fixed in test-fleet-worktree-track.sh survives in two other suites, and both instances guard a security property. case $(cat ...) discards the read status, so a failed read prints nothing and nothing matches no pattern, passing the assertion. test-allocation-feedback.sh case 5b asserts a planted canary never reaches the committed fragment; test-fleet-credit-continuation.sh asserts no raw escape byte reached stderr. Either read failing turns a leak or sanitizer test green without reading anything. Verified that set -e does not cover this: a failing assignment aborts, but a command substitution in a case word does not, which is exactly why the assignment-form reads in those files are already safe and the case-form ones are not. Fix shape is the one already applied: bind the output, fail on a failed read.
