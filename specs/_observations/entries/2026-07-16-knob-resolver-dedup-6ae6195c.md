- 2026-07-16 [planwright] fleet-autonomy Task 1 shipped scripts/resolve-config-knob.sh, a generic four-layer knob resolver with the REQ-E1.4 by-layer policy. The four pre-existing per-knob resolvers (resolve-dispatch-isolation.sh, resolve-notification-channel.sh, resolve-context-budget-threshold.sh, and the enum arm of resolve-review-sequence.sh) each hand-roll the same ~180-line policy shape and could delegate to the shared helper, deduplicating ~500 lines and pinning one policy implementation. Outside Task 1 scope (no behavior change was authorized for existing resolvers).
