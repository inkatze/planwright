- 2026-09-07 [planwright] docs/overlays.md states "Each resolver has an --explain provenance mode that names the supplying layer", and its three examples (config-get.sh, resolve-rule-doc.sh, resolve-catalog.sh) all have one. scripts/resolve-config-knob.sh does not: its arg loop accepts only --key/--type/--values/--fallback and falls through to usage/exit 2. Verified by invoking it with --explain. The generalization is therefore false for the knob resolver every allocation and fleet knob resolves through, and it propagated: model-allocation Task 7 imported the sentence into docs/allocation.md, where an operator following it in a section titled "Which knob won" would have hit exit 2. Corrected there to point at config-get.sh --explain; the overlays.md claim itself is untouched and still over-broad.
