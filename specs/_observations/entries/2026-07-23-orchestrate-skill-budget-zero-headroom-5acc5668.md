- 2026-07-23 [planwright] skills/orchestrate/SKILL.md now sits at margin 250 against the headroom floor of 250 - literally zero headroom (4,000 words against the 4,250 error threshold). The next word ANY task adds to that skill breaks CI (check:instructions closing gate, which fails the full suite even though check:instructions itself exits 0). Cause: two independently-at-ceiling branches merged. origin/main after execution-backends Task 3 (#314) was at 3,999 words; the Task 5 branch was at 4,000; merging them plus an 11-word clarifying parenthetical in the --backend flag prose tripped 'WARN: floor-breach: margin=239 below headroom floor 250'. The parenthetical was reverted (the merge keeps the terse generic form), which bought back exactly zero margin, not headroom. This is the DEFERRED fleet-hardening #271 diet surfacing with a concrete trigger: the declared exception says the skill is below the 2x restoration target (margin 337 < target 500) after #271 grew the body with the observation-carry step, and defers the diet. It is no longer only a latent quality note - it now BLOCKS in-flight work: Task 3 already had to route its headless-oneshot drive mechanics to the capability contract and docs/fleet.md instead of the skill (see 2026-07-23-orchestrate-skill-budget-headless), and Task 8 may need to document the backend-agnostic dashboard in the same skill. Seed for a hardening spec: land the deferred /orchestrate diet (relocate mechanics to point-of-use doctrine per the floor-breach doctrine, never trim doctrine or raise the ceiling) before the next task needs to write there. Not fixed here: trimming the skill is a scoped decision belonging to that spec, not to a Copilot-review pass.
