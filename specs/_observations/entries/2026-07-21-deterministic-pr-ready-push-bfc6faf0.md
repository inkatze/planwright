- 2026-07-21 [repo] DESIGN GAP (operator-flagged 2026-07-21): "PR verified-ready-for-the-humans-merge" has NO deterministic push path — it reaches the human ONLY via the tower (LLM) polling GitHub and manually calling PushNotification, which is brittle by design and DID fail this run (a run of ready PRs #282/#283/#284/#285 went un-pushed; #277/#281 were missed proactively). Operator expected hooks/deterministic mechanisms to PUSH state with tower-poll as FALLBACK, not the tower as the sole mechanism. Current infra is half-there: fleet-liveness.sh + the Notification hook DO deterministically push worker awaiting-input (forks) to fleet-attention.sh, and fleet-attention.sh already MODELS a `pr-ready` worker state — BUT (1) nothing fires `pr-ready` deterministically (no PostToolUse hook maps a worker `gh pr ready` / MCP draft->ready flip to a fleet-attention heartbeat pr-ready record), and (2) fleet-attention explicitly classifies pr-ready as NON-ACTIONABLE (only awaiting-input drives the decision-queue/push). Fix: (a) a deterministic ready-flip -> attention record (a PostToolUse guard on gh pr ready / the MCP ready path, sibling to the merge-currency ready-guard idea — both intercept the same two ready surfaces; OR the execute-task ready step calls fleet-attention heartbeat pr-ready + a notify); (b) make pr-ready an ACTIONABLE, push-worthy signal (it IS a reserved-human moment: the merge); (c) keep tower-poll strictly as the fallback. Ties to fleet-hardening fork-park-attention (Notification hook), fleet-autonomy Task 2, notify-when-ready-to-review, and merge-currency-guard (same ready-flip interception surface).
