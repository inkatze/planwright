- 2026-09-02 [planwright] Repo-wide: the idiom `echo "...$(sanitize_printable "$v")..."` does not make untrusted text terminal-safe under dash, which is /bin/sh on the Linux support bar. sanitize_printable strips control BYTES; the four printable characters backslash-0-3-3 survive it, and dash's echo then expands them into a real ESC. Verified: /bin/sh -c 'echo "$1"' _ 'x\033[31m' emits a live escape, printf '%s\n' does not. fleet-lifecycle-closure Task 3 converted fleet-state.sh and the new fleet-register.sh to printf; roughly 170 sites elsewhere still use echo. A mechanical sweep plus a guard would close the class.
