- 2026-08-27 [planwright] A second `mise run check` run flaked in a DIFFERENT suite from the first, so this is a suite-level pattern rather than one bad case: run 1 failed tests/test-fleet-throttle.sh case 33, run 2 failed tests/test-fleet-streamjson.sh c12 ("queue should be clear after the answer") alongside `awk: close failed on file "/dev/stdout" (Broken pipe)`. Both files pass standalone every time; the fence suites under review passed in both runs. run-tests.sh fans out N files at a time (N = core count), so the shared shape is short real-time windows and pipe lifetimes that hold when a file runs alone and do not when the box is saturated. This makes a red `check` ambiguous — a reviewer cannot tell a real regression from load noise without re-running the named file alone. Options: retry a failed file once serially before declaring failure, mark the time-sensitive cases as needing serial execution, or give the affected assertions a clock/pipe seam instead of real elapsed time.
