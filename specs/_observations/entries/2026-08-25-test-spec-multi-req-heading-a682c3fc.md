- 2026-08-25 [planwright] scripts/spec-model.sh parse_test_spec emits one TEST record per test-spec entry heading (spec_parse_req_token, first id only), while scripts/spec-validate.sh counts every REQ a heading names toward coverage (spec_parse_req_tokens). A heading naming two REQs therefore reads as covered by the validator and unverified in the model: on specs/bootstrap, REQ-C1.2 and REQ-E1.5 get no TEST record, and five such shared headings exist there. Pre-existing, not introduced by format-grammar Task 8 — the pre-lib parse was also first-match-only — but newly expressible now that both the singular and plural extractions sit in one grammar home. Left as-is: whether the model record is entry-shaped (one per verification path) or REQ-shaped (one per REQ covered) is a record-vocabulary decision, not a re-point. Surfaced by /panel-review --nested (gemini backend) over format-grammar Task 8.
