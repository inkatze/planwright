- 2026-07-19 [planwright] release-hardening REQ-B1.2 names cross-machine/fresh-clone resume as in-scope, but git verify-tag needs the signer key in the resuming machine local trust store (for SSH, gpg.ssh.allowedSignersFile), so under require (and auto when the origin tag is signed) a genuinely valid signed tag refuses fail-closed on a foreign machine that lacks that trust material. Reproduced (verify-tag rc=1, "allowedSignersFile needs to be configured"). Code faithfully implements the spec; the spec design oversells cross-machine resume. Options for spec-draft: keep fail-closed and narrow the cross-machine claim, or have auto downgrade to accept-with-warning when no local trust material exists. Surfaced during release-hardening Task 4 /self-review.
