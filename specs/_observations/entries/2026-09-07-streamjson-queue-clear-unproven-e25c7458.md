- 2026-09-07 [planwright] A confident mechanism for the recurring `tests/test-fleet-streamjson.sh` "queue should be clear after the answer" failure was proposed, acted on, and then FAILED TO REPRODUCE; recording it so the next person does not re-derive the same wrong answer. THE SIGHTING IS REAL: the case failed once on 2026-09-07 inside a full `mise run check` (12 parallel jobs) on the fleet-state-symlink-lock branch, which touches no stream-json code at all; the file passed in isolation immediately after and the whole gate passed on rerun. THE PROPOSED MECHANISM: `handle_line` released the journal lock BEFORE writing the attention queue item, so an `answer` taking the lock in that gap would find a pending receipt, deliver, and clear an attention queue the supervisor had not written yet -- the queue row then landing after the only thing that would have cleared it, leaving an operator-visible awaiting-input row for an answered request that nothing can ever clear. THE MECHANISM DOES NOT REPRODUCE. Reverting only the ordering on the fresh-request branch and widening the gap with an explicit `sleep` produced ZERO failures: 5 runs at 0.3s and 4 runs at 3s. Three seconds is orders of magnitude wider than any scheduling delay the real failure could involve, so the negative is meaningful rather than a timing miss. The ordering change was therefore DISCARDED rather than shipped -- it reads as more obviously correct, which is exactly why it should not stay without evidence. Cause remains UNKNOWN: the three prior observations calling this a load flake are neither confirmed nor disproven by any of this. A HARNESS TRAP worth knowing, because it nearly became the evidence: a first attempt copied only `scripts/fleet-streamjson.sh` into a scratch directory, where it could not resolve its sibling scripts, so BOTH the before and after arms died before doing any work and each reported 0 orphaned rows. Two zeros read exactly like "no difference" and would have supported whichever conclusion was wanted. Any before/after harness for these scripts needs the whole `scripts/` tree, and needs a positive control proving the before arm can fail. FOR WHOEVER TAKES IT NEXT: stop building synthetic reproductions and instrument the real one -- the failure only appears under genuine full-gate parallelism, so capture the worker's journal, the attention store, and the queue render at the moment of failure inside a loaded run, rather than trying to provoke it in isolation.
