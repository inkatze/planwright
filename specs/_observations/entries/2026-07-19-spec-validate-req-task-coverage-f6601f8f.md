- 2026-07-19 [planwright] spec-validate checks task structure and (apparently) REQ->test-spec coverage, but NOT REQ->task coverage: fleet-hardening kickoff found REQ-B1.4 + design D-7 had a requirement, a design decision, and a test-spec entry yet NO implementing task, and the validator passed 0 errors/0 warnings. An unbuilt SHALL requirement is invisible to the validator; the kickoff walkthrough caught it by hand. Candidate: add a validator check that every REQ-ID (and every D-ID) is cited by at least one Task block, warning at Draft / erroring at Ready+Active.
