- 2026-09-02 [planwright] tests/test-fleet-throttle.sh case 33 ("an elapsed anchor re-engages on the next observation") fails on origin/main at 5440eb5 / v0.36.0, independently of any branch. Verified by running it in a clean detached worktree at that commit on 2026-09-02: FAIL "the same excerpt after elapse is a fresh event and must re-engage (check exit 0)". The same file passed a full mise run check earlier the same day at the same base, so the case is wall-clock dependent rather than load dependent: it observes a "5-hour limit reached. Try again in 3 seconds." excerpt, spins on run check until the 3s anchor elapses, then re-observes the identical excerpt and requires check to report engaged again. Worth pinning the clock (an injectable now) rather than sleeping against real time, so the suite does not depend on the hour it runs in.
