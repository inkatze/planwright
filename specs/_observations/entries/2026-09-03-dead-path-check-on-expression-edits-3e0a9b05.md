- 2026-09-03 [planwright] The coverage-based dead-path check (format-grammar REQ-D1.8, landed with task 3) warns on any live REQ whose bullet text changed since the baseline while its test-spec entry did not, with no exemption for an expression-only edit that a dated Changelog entry already records. Its own rollout tripped it: qualifying a bare D-36 as bootstrap D-36 inside orchestration-concurrency REQ-F1.1 produced the warning on the same PR that recorded the edit as expression-only. The warning clears once merged (the baseline moves) and never blocks, so it is noise for exactly one review, but a reviewer reading it may look for a missing test-spec edit that the ritual says is not owed. A refinement worth weighing at the next format-grammar amendment: silence it when a dated Changelog entry since the baseline names the REQ id, the same escape the supersede and retirement checks already accept.
