- 2026-09-05 [planwright] Corrects the earlier fleet-register-signal-lock-race note, which guessed at the wrong script. test-fleet-registration.sh case e8 backgrounds scripts/fleet-state.sh register, sleeps a hard-coded 0.05s, then SIGTERMs it and asserts .fleet.lock is not left standing. The only code under that assertion is fleet-state.sh, which the model-allocation Task 6 branch does not modify at all, so the failure cannot come from this branch; what varies is whether 50ms is long enough for the child to reach and arm its lock trap, which under a loaded 12-job suite it sometimes is not. Observed in 2 of 4 full-suite runs, never in 3 isolated runs, 6 runs under 12 CPU burners, or 4 runs against concurrent dispatch load, which is why CPU contention is the wrong reproduction lever: the window is process-startup latency, not throughput. This is the registry race PR #409 already fixes; fleet-state.sh is off limits here.
