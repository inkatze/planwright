- 2026-07-22 [planwright] test-fleet-throttle.sh "same excerpt after elapse must re-engage" assertion failed once under heavy parallel load (mise run check with concurrent heavy jobs, [test] wall time 1003s) and passes standalone and on a quiet box; the elapse-window assertion appears sensitive to wall-clock delay under load. Candidate for a load-robust rewrite (injected clock or widened window) per the prefer-end-to-end-but-deterministic testing doctrine.
