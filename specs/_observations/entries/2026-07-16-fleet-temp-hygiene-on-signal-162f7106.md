- 2026-07-16 [planwright] fleet-autonomy Task 1 self-review, error-handling/concurrency lenses: an INT/TERM mid-run leaks scratch temp artifacts because cleanup is in-band only — resolve-config-knob.sh's degrade-path mktemp -d scratch dir (no trap at all) and fleet-audit.sh's .audit.XXXXXX write temp (EXIT trap releases only the lock). Both replicate the pre-existing sibling patterns exactly (resolve-notification-channel.sh's scratch, fleet-attention.sh's upsert temp), so the branch-scoped fix was declined; the right altitude is one repo-wide temp-hygiene-on-signal chore over the resolver and fleet script families (trap-owned cleanup of in-flight temps).
