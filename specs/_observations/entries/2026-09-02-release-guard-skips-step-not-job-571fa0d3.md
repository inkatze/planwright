- 2026-09-02 [planwright] Three release-hardening spec files (requirements.md REQ-H1.1, design.md D-13, tasks.md Task 9) say the guard SKIPS THE JOB on exit 1. The implementation gates only the proposal STEP via if: steps.window-check.outputs.open == false; the job runs and concludes green. The workflow own comment states the divergence outright, and it is precisely why the GITHUB_STEP_SUMMARY suppression note was needed, since a job-level skip would have been visible in the Actions list for free. The substitution is recorded nowhere in the bundle. Worth resolving in the direction the spec meant or amending the spec wording, since the note is now load-bearing because of it.
