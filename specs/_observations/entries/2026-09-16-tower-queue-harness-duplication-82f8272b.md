- 2026-09-16 [planwright] tower-comms Task 3 review (structure lens): the four tests/test-tower-queue-*.sh files carry the same forty-line harness preamble (run, marker, field, rec, lease_of, record_count) copied four times, with add_req already at two signatures and record_count at two spellings, while tests/lib/ is the repo's home for shared harnesses; and tower-queue.sh re-implements is_uuid (fleet-presence.sh) and carries two copies of the attention-row field map (AWK_Q load_attention and the rebuild's awk), inlines item_pass in cmd_add, and spells the ranked-kind enum five times; a consolidation follow-up, not a defect.
