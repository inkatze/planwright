- 2026-09-03 [planwright] scripts/fleet-state.sh `register` silently loses records under concurrent contention while every caller exits 0. Reproduced standalone: 20 concurrent registers against a WARMED fleet home (bootstrap already done) land 17-19 of 20 records, with zero exit status reported for all 20; tests/test-fleet-state.sh case 8 fails 2 of 3 isolated runs on this. Pre-existing and unrelated to the concurrent-orchestrator-coordination/task-5 diff (fleet-state.sh and its test are untouched since origin/main 5440eb5). The loss is SILENT: spin_acquire returns 2 on give-up and register does `spin_acquire || exit 2`, so a lock timeout would surface non-zero — it does not, so callers cannot detect that their registration vanished. This contradicts the KNOWN LIMITATION comment on try_acquire, which scopes the two-holder window to the crash-recovery path and asserts the normal no-stale-lock contention path is fully serialized and is what the register/bound-incr guarantees rely on; the repro has no stale lock and no crashed holder (lock held for ms against a stale threshold of minutes), so either the defect is wider than that comment scopes it or the serialization claim does not hold. Worth resolving with the owner-token lock discipline that comment already defers to a follow-up (the observation logged for orchestration-fleet Task 9): both land on the same lock family.
