- 2026-07-15 [planwright] Framework scripts using mktemp + trap cleanup share two accepted-but-sweepable gaps: the trap is installed only on EXIT (an uncaught SIGINT/SIGTERM under plain sh skips the EXIT trap and leaks the temp file) and is installed after mktemp (a signal in between leaks it). Present identically in orchestrate-state.sh (work file), tasks-pr-sync.sh (documented at its wsh_tmp comment), and the new spec-status.sh, which followed the house idiom deliberately. Declined per-script in Task 3 polish (consistency with siblings; leak impact is one small temp file); candidate: one repo-wide sweep adding "trap ... EXIT INT TERM HUP" with a placeholder trap before mktemp, in a single chore PR. Surfaced by the /polish error-handling and concurrency lenses over invariant-tasks Task 3 (2026-07-15).
