- 2026-09-12 [planwright] fleet-dispatch-worktree.sh reconcile could not tell a branch checked out under an older worktree path (the flat task-<id> layout) from a partial create or an adoptable orphan: git refused the second checkout and the branch delete, both errors were swallowed, and the operator read 'failed to recreate ... after rolling back a partial create' (exit 5) over a branch that was neither rolled back nor lost. Reproduced 2026-09-12 with and without commits on the branch. The class: a reconcile whose arms only knew the states it expected, so an unexpected state was reported in the vocabulary of an expected one. Fixed by checking git worktree list for the holding path first, treating a session under that path's name as live, and otherwise refusing with exit 6 and the git worktree move that resolves it.
