- 2026-07-27 [planwright] The Claude-5 removal claim rests entirely on eval evidence (80%+ of the system prompt removed with NO MEASURABLE LOSS on their coding evals). planwright cannot currently make the analogous measurement, so every trim taken on this guidance would be guessed - which contradicts the test-and-measure rule in doctrine/instruction-hygiene.md that the same trims would cite. Current state: tests/prompt-evals/fixtures holds two fixtures, both /orchestrate (print-ready, refuse-draft), covering one of eleven skills; tests/behavioral-evals/fixtures holds two (greeter, kickoff); obs:df835e81 records the prompt-eval suite as guarded-inoperative because headless skill injection is unavailable; obs:260e0756 records /execute-task at zero behavioral coverage; obs:dc2883b2 records that any eval of instruction files must first prove the instructions entered context at all. Consequence for sequencing: the injection design and per-skill fixture pairs are a PREREQUISITE for a context-engineering trim spec, not a follow-up to one, and any such spec should order them first. Change: state that ordering explicitly wherever the trim work is scoped (specs/instruction-headroom or a new spec), and pick the fixture set from what a trim could break - one invariant-refusal cell per skill that has a hard invariant, plus one skill-selection cell for the frontmatter descriptions. Risk of skipping it: a trim that regresses an invariant refusal is exactly the failure the safety floor exists to prevent, and word counts and review reading cannot detect it; pass^3 gating already exists in the convention, so the cost is fixture authoring, not new machinery.
