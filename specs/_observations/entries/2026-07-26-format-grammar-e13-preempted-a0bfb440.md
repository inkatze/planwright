- 2026-07-26 [planwright] format-grammar REQ-E1.3 has been overtaken by events and its verification path no longer matches convention. The REQ says the invariant-tasks GATE(when:) prose-atom entry SHALL be rewritten to free-text "as an expression-only amendment with a dated changelog entry", and format-grammar/test-spec.md REQ-E1.3 pins verification to "the bundle carries the dated changelog entry and marked re-anchor entry". As of 2026-07-26 the rewrite itself has landed on planwright/chore/free-text-gates (PR #321) with neither entry. Two things to reconcile when format-grammar dispatches. (1) The sweep half of the verification already passes, so the task is smaller than written. (2) The changelog/re-anchor half conflicts with established convention: the identical class of edit was done as a tasks.md-only chore with no changelog and no re-anchor in 69ec355 (PR #101, 4 gates across customization-overlay and spec-comprehension), and the mechanics back that up - the canonical tasks.md extraction excludes Deferred bullets, so a gate reword is anchor-neutral (verified: invariant-tasks anchor aca92687 identical before and after, matching the live kickoff-brief entry). Writing the changelog line into requirements.md would itself churn the anchor and force a re-anchor that only the sanctioned writer may record - i.e. the ritual REQ-E1.3 asks for would break a currently-valid anchor rather than protect one. Candidate: amend REQ-E1.3 and its test-spec entry to drop the changelog/re-anchor clause for Deferred-bullet gate edits, or state the general rule that Deferred/Out-of-scope bullet edits are outside the amendment ritual because they are outside the anchor.
