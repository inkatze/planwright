- 2026-07-22 [planwright] migrate-format-version.sh restructure_tasks retains private copies of two lib-implemented grammar elements after the format-grammar Task 1 re-point: the sortkey() id-ordering function and the five-field definition-bullet recognizer (plus its own duplicate-id detection). Not extract_tasks itself, but REQ-B1.1 says no consumer keeps a private copy of a grammar the lib implements, and no format-grammar task owns this surface (Task 2 covers header/parked-map parses; REQ-B1.5/Task 8 lists spec-validate, orchestrate-select, spec-model only). Divergence risk: a future sixth definition field updates the lib recognizer while restructure_tasks silently classifies the new bullet as non-definition content and refuses. Candidate for a Task 8 scope extension or a dotted follow-up task via /spec-kickoff amendment.
