- 2026-09-02 [planwright] The release-please window guard reads origin/main at step time, but the release-please action makes its own API calls minutes later, and nothing re-checks. If the standing release PR merges in between, the guard has already written open=false, and release-please, now unable to see a latest release, falls back to bootstrap-sha and regenerates exactly the bogus 124-commit proposal (PR #339) that release-hardening Task 9 exists to prevent. obs:c2afd203 records only the mirror direction, where the guard sees an open window and work is dropped; this is the false-positive direction, and it reproduces the original incident rather than a new one. Candidate mitigation, already named in c2afd203 for the other direction: a re-check after the proposal step that fails the job when the window opened mid-run.
