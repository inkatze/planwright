- 2026-07-17 [planwright] fleet-attention.sh `queue --count` overloads "exit-0 + empty stdout" to mean "deferred to a backend attention surface" (provides_attention_surface). Every consumer must INFER deferral from that absence, which is fragile: any other empty-output cause is misread as deferral. Durable root fix is producer-side — signal deferral EXPLICITLY (a distinct exit code, or a `deferred` sentinel on stdout) so consumers branch on an intentional signal, not an inference. Surfaced while wiring fleet-stats.sh queue_count (Task 8), which now case-splits exit-0+empty -> `deferred` vs failure -> `?` as a consumer-side stopgap. Contract change is fleet-attention.sh territory (Task 4/7), OUT OF SCOPE for Task 8. Feeds /spec-draft.
