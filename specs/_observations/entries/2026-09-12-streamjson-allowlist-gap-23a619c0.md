- 2026-09-12 [planwright] A stream-json-persistent worker running /execute-task stalls on its FIRST doctrine-resolution step: the compound Bash that assigns the plugin root to a shell variable and loops resolve-rule-doc.sh over the manifest is not matched by config/worker-settings.json (which allowlists Read/Edit/Write/Grep/Glob plus literal git/gh/mise prefixes only). The request pends forever as a decision-queue item, and the tower may not auto-answer it, so an unattended fleet run makes zero progress per worker. Observed on 0.40.0, which already carries the #441 widening. The gap is structural: /execute-task opens by resolving its own doctrine through plugin scripts, so every worker hits it before doing any task work. Candidate fixes: allowlist the plugin script root as a Bash prefix, have the skill resolve doctrine through a tool the fragment already allows, or ship the resolution as a pre-computed manifest the worker reads.
