- 2026-07-24 [planwright] Three GATE(when:) gates were reported MALFORMED because their clauses are prose, not because of how those clauses are joined. drain-gates.sh eval_atom accepts exactly three atom forms (task <id> completed, spec <name> <status>, after <YYYY-MM-DD>) and does support several valid atoms joined by the literal " and " (verified 2026-07-26: two- and three-atom and-joined conditions evaluate to SATISFIED / SURFACED / DORMANT with zero malformed). What the grammar has no form for is OR, negation, and quantification, so a human-judgement condition has no structured form at all. Three instances as of 2026-07-24: specs/invariant-tasks/tasks.md:228, and-joined, so each prose clause is listed as its own unrecognized atom; specs/worker-permission-ergonomics/tasks.md:92, a single prose clause; and specs/worker-permission-ergonomics/tasks.md:99, OR-joined, where the whole condition stays ONE unrecognized atom because the split is on " and " only. All three are deferred future-work gates, so nothing is blocked today, but such a gate degrades to free-text in practice. Observed during an /orchestrate --fleet bookkeeping drain; the original framing of this entry ("the parser rejects multi-atom conditions joined by and") was wrong and was corrected by the panel pass on PR #321.
