- 2026-07-24 [planwright] Three GATE(when:) gates were reported MALFORMED, but not for the reason this entry originally gave. drain-gates.sh eval_atom accepts exactly three atom forms (task <id> completed, spec <name> <status>, after <YYYY-MM-DD>) and DOES support several valid atoms joined by the literal " and " (verified 2026-07-26: two- and three-atom and-joined conditions evaluate to SATISFIED / SURFACED / DORMANT with zero malformed), so a conjunction as such is expressible. Two independent causes are in play across the three gates. (1) Prose clauses, which no atom form matches, and where the join is irrelevant: specs/invariant-tasks/tasks.md:228 is and-joined, so each prose clause is listed as its own unrecognized atom, and specs/worker-permission-ergonomics/tasks.md:92 is a single prose clause. (2) An unsupported joiner: specs/worker-permission-ergonomics/tasks.md:99 is OR-joined, and OR is fatal on its own even for valid atoms (verified 2026-07-26: "spec <name> done OR after <date>", both atoms individually valid, still reports MALFORMED) because the split is on " and " only, so the whole condition stays ONE unrecognized atom rather than being listed per clause. The grammar has no form for OR, negation, or quantification, which is why a human-judgement condition has no structured form at all. All three are deferred future-work gates, so nothing is blocked today, but such a gate degrades to free-text in practice. Observed during an /orchestrate --fleet bookkeeping drain; the original framing ("the parser rejects multi-atom conditions joined by and / OR") was wrong on the and half and was corrected by the panel pass on PR #321.
