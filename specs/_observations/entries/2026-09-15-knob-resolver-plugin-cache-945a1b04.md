- 2026-09-15 [planwright] scripts/resolve-config-knob.sh (via config-get) reads core defaults through PLANWRIGHT_ROOT / CLAUDE_PLUGIN_ROOT before the script's own tree, so inside a Claude Code session with the plugin installed a worktree's tests resolve knobs from the installed plugin cache, not the checkout under test: a knob added on the branch warns 'unset in every layer' on every call locally and is clean in CI (seen on tower-comms Task 3 with tower_quiet_interval). Local runs need the plugin roots unset to match CI; the test harness could pin PLANWRIGHT_CONFIG_DEFAULTS to the checkout's config/defaults.yml so a worktree always tests its own config.
