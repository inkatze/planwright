- 2026-09-03 [planwright] fleet-status.sh merge/render still carries the five-state liveness column and knows nothing of the four-state stuck-detector that now ships beside it (fleet-stuck-detector.sh, fleet-lifecycle-closure Task 7): the operator-facing table renders working(1139s) while the detector would say finished-but-unreaped/live-peer. The status view is the natural consumer of the detector state and owner columns, the same way it consumes streamjson status through its subcommand rather than re-deriving it. Not wired in Task 7 because the task deliverables named the attention store, not the status renderer, and the sweep (Task 8) is the first consumer that needs the column mechanically. Observed 2026-09-03.
