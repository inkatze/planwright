- 2026-07-24 [planwright] tests/test-fleet-throttle.sh is load-flaky at the "same excerpt after elapse is a fresh event and must re-engage" assertion. Reproduced FAILING on pristine v0.31.0 (git archive HEAD, no local changes) under a loaded machine, and PASSING twice on an idle one; the surrounding code polls a 3s wall-clock anchor inside a 30s deadline loop (tests/test-fleet-throttle.sh, the run check / now() loop), so a scheduling stall between the re-observation and the check makes the re-engage window miss. classify-ci-failure.sh calls it logic (assertion mismatch), which is the right default but wrong here. Fix candidates: pin the clock the way other suites pin --today, or widen the anchor so a stalled worker cannot outrun it. Surfaced during format-grammar Task 2, whose diff touches nothing this test reads.
