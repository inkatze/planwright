- 2026-07-23 [planwright] orchestrate-backends.sh cmd_detect advertises stream-json-persistent as a present rung whenever the claude CLI resolves, and the /offload skill selects by advertised capability rather than backend name, but offload-dispatch.sh still hard-refuses that backend with not-yet-drivable, dispatch support lands with execution-backends Tasks 3-4. Task 4 has since merged (PR #312) and fleet-streamjson.sh is live, so the refusal is now stale: /offload can legitimately select the rung and then hard-fail at dispatch. Fix: wire the stream-json-persistent dispatch arm to fleet-streamjson.sh launch (likely Task 3 scope, which owns the remaining dispatch wiring), or stop advertising the rung until that arm exists. Pre-existing on main; surfaced by a panel-review gemini pass during Task 7.
