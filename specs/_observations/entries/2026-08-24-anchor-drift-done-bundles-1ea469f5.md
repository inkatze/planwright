- 2026-08-24 [planwright] The format-grammar Task 6 re-anchor sweep (scripts/anchor-sweep.sh) found ten bundles whose kickoff-brief anchor no longer recomputes: autopilot-reflex, bootstrap, customization-overlay, kickoff-lifecycle, observation-recording, orchestration-concurrency, orchestration-fleet, output-hygiene, prompt-hygiene, spec-comprehension. All ten are Status Done, so no freshness gate trips on them (nothing dispatches against a Done bundle) and REQ-C1.4 is not violated. The drift is pre-existing and unrelated to the fence-awareness landing: the sweep produces byte-identical output under the pre-change and post-change parsers. Cause is post-sign-off content edits to Done bundles that never carried a re-anchor entry. Remedy per bundle is a human classification (expression-only re-anchor, or a /spec-kickoff delta re-walkthrough if any delta is meaning-class); an execution skill cannot classify content deltas it did not make. Worth deciding whether Done bundles should be exempt from the pairing obligation by rule, or swept and re-anchored on reopen.
