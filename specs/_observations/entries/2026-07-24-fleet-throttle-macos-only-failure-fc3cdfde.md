- 2026-07-24 [planwright] Two timing-sensitive test files, tests/test-fleet-throttle.sh (assertion "the same excerpt after elapse is a fresh event and must re-engage") and tests/test-fleet-tower-watchdog.sh, failed locally on macOS during guard-coverage Task 6 while PASSING on the reference runner (throttle 10.6s; run-tests reports all 140 files green in CI). Cause is most likely LOCAL CONTENTION, not a platform bug: a concurrent sibling worker (worktrees/task-2) was running its own full suite on the same machine throughout, load average 12-31, and the same measurement pass clocked test-fleet-usage-gate.sh at 902s against 11.9s in CI — 75x. Both failing files assert on elapsed wall-clock within bounded windows, exactly what starvation breaks. An earlier version of this entry claimed a reproducible macOS-only failure and guessed at a BSD date parse; that was overconfident — the run cited as "standalone" was itself contended, and no uncontended local run was obtainable because the machine is shared with the sibling unit. Two things still worth doing: get one uncontended macOS run to settle whether a real platform bug hides under the noise, and consider whether these two tests should assert on monotonic elapsed time with generous bounds (or skip under detected high load) so they degrade honestly instead of failing on a busy dev box. Relevant to the bash 3.2 + BSD-tooling floor only if an uncontended run still fails.
