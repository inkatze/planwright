- 2026-07-24 [planwright] tests/test-fleet-throttle.sh fails reproducibly on macOS and passes on the reference runner. Standalone local run: FAIL "the same excerpt after elapse is a fresh event and must re-engage (check exit 0)" after 35 verdicts; the same file passes in CI (10s) and run-tests reports all 140 files green there. Found incidentally during guard-coverage Task 6 and deliberately not fixed there (outside that task files). Worth a look because the repo commits to a bash 3.2 + BSD-tooling floor, so a macOS-only failure in a re-engage-after-elapse assertion is exactly the portability class that floor exists to protect — likely a date or elapsed-time parse that behaves differently under BSD date, or local throttle state leaking between runs. A contributor on macOS currently cannot get a green mise run check.
