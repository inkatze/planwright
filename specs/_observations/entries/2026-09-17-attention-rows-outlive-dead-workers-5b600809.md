- 2026-09-17 [planwright] Decision-queue rows from dead workers are never cleared, so the attention surface accumulates permanent high-priority noise. Four rows have been sitting in the queue for 35583 seconds (about ten hours) at priority high: anchor-integrity:task-6 (ai-6b), concurrent-orchestrator-coordination:task-5 (coc-5), guard-coverage (guard-coverage-task-3) and tower-front-door:task-1 (tfd-task-1). Each is an OVERDUE permission request or worker question. fleet-streamjson.sh status reports ai-6b, coc-5 and guard-coverage-task-3 as "dead supervisor+worker" and tfd-task-1 as "completed result=success", so none of them can ever be answered: the process that asked is gone and the receipt cannot be delivered anywhere. The orchestration-modes doctrine says the mirror is level-triggered and should clear any row whose unit is no longer in flight or awaiting input, which is exactly this case, but nothing runs that arm against rows whose worker died rather than finishing cleanly. The practical effect is that the queue length stops tracking anything an operator can act on, and a real overdue decision is now the fifth item behind four that are undeliverable. Fix shape: on each watch iteration, cross-check every queue row against its worker handle liveness and clear rows whose worker is dead or completed, recording the drop rather than silently discarding an unanswered question.
