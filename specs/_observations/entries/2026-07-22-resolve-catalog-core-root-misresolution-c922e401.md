- 2026-07-22 [planwright] resolve-catalog.sh returned an EMPTY merged catalog with exit 0 during the execution-backends kickoff gap check: resolve-overlay-root.sh core resolved to ~/.claude/planwright (which held only fleet-state/, no config/) instead of the plugin cache root, so the shipped decision-domains seed was never read. Empty-merge-with-exit-0 also hides the failure. Suggest: core-root resolution should verify the resolved root actually contains config/, or fail loudly; kickoff worked around it by reading the plugin seed directly.
