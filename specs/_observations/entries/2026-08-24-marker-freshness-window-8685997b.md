- 2026-08-24 [planwright] orchestrate/format-grammar task 6, 2026-08-24: the runtime dispatch marker went STALE before the worker it records was even launched, re-opening a double-dispatch window. The marker is written inside the locked window as step 4 of the dispatch record, and its freshness clock (stale_marker_threshold, 15m) starts there. But the evidence that supersedes it is the worker branch first commit, which cannot land until some time after launch. On this step the dispatch path between marker write and launch took ~17m (resolving the stream-json-persistent plumbing: worker-settings regeneration, dispatch-guard lint, launch-line emission), so by the time the worker was up the task re-derived ready/deps-met and a second live tower on the same spec (tower-format-grammar) could have selected it again. Refreshing the marker after a verified-live launch restored in-progress. Candidate fixes to weigh: have the dispatch step re-stamp the marker after a verified-live launch (make the launch, not the record write, start the clock); or let the derivation treat a live backend handle for the unit as in-progress evidence alongside the marker, so marker staleness is not the sole bridge to the first commit.
