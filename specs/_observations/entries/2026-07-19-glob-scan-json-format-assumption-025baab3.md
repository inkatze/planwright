- 2026-07-19 [planwright] The check-glob-allow-rules footgun scan matches one rule token per line, relying on config/*.json being pretty-printed one-entry-per-line, but no formatter/check enforces that layout — a future JSON reformat/minify would silently fail-open the guard (compact multi-token arrays evade the scan). Consider a JSON-formatting guard in the check suite, or hardening the scan (e.g. jq-extract permissions arrays with the awk line-scan as jq-absent fallback). Root of a Task 6 self-review F1.
