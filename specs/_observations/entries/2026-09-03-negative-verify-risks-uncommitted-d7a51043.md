- 2026-09-03 [planwright] Two workers on 2026-09-03 independently reached for the same unsafe shape while doing negative verification (proving a new guard actually fails against the pre-fix state). coc-5b: `cp scripts/fleet-presence.sh /tmp/fp-backup.sh && ... git show HEAD:scripts/fleet-presence.sh > scripts/fleet-presence.sh && <run test>; cp /tmp/fp-backup.sh scripts/fleet-presence.sh`, with 12 uncommitted insertions in that file and zero commits on the branch. ai-6c: `cp skills/spec-kickoff/SKILL.md /tmp/final-skill.md && git checkout HEAD -- skills/spec-kickoff/SKILL.md && ...`, with that file uncommitted and 8 commits on the branch. Both put the only copy of uncommitted work in /tmp and relied on a later step of the same command line to restore it, so an interrupt between the overwrite and the restore loses the work outright. This is the same shape that destroyed 595 insertions earlier in the run via fleet-dispatch-worktree.sh --attach-dry-run (attach-dry-run-durable), arrived at independently, which is what makes it a pattern rather than one worker error. The pull is structural: negative verification genuinely requires the pre-fix content in place, and reverting the working file is the most obvious way to get it. Two safe forms exist and neither is harder: read the old content without touching the tree (`git show HEAD~1:<path>`), or copy the whole worktree to a scratch dir and run there, which coc-5b adopted without complaint once told. The durable fix is to say so where workers will read it, since the tower only catches these by inspecting each command as it escalates, and that inspection disappears the moment permissions are auto-approved. Both were held for a commit first and complied. Related: attach-dry-run-durable, out-of-band-worker-stop.
