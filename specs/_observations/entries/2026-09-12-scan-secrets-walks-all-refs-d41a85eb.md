- 2026-09-12 [planwright] scan:secrets (gitleaks detect --source . with no log-opts) walks every ref in the object store, and worktrees share one store, so a sibling worktree branch carrying a token-shaped test fixture fails every co-located worker's mise run check; seen 2026-09-12 when a tower-comms branch tripped the format-grammar worker's gate while a HEAD-scoped scan of the same tree was clean. Scoping the scan to the branch under test, or to refs reachable from HEAD, would keep one worker's branch from grading another's.
