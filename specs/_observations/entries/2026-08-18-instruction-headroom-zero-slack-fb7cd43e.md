- 2026-08-18 [planwright] Some instruction-budget aggregate surfaces sit close enough to their 2x restoration target that ANY conforming doctrine addition trips a below-target warning and forces a declared exception. Measured on inception Task 3: start-load:self-review had a baseline margin of 1005 against a target of 1000, so adding the artifact-class lens-selection reference to discovery-rigor.md and validation-rigor.md (the minimum REQ-I1.3-conforming edit, ~144 words, with the lens SETS deliberately externalized into a separate doc so only the reference is charged) put it below target with no trim available that would not gut another spec doctrine. builder and spec-kickoff went the same way from the eight-entry decision-domains growth. The exception mechanism worked as designed (floors held; only targets missed), but a surface with single-digit slack means the guard can no longer distinguish sloppy growth from contract growth: every future addition looks identical to it. Candidate: surface baseline slack in the --audit report (a margin-to-target column already exists, but nothing flags a surface whose slack is smaller than a typical single edit), or treat sub-100-word slack as its own named signal so the diet is scheduled before the next contributor is forced into an exception. Surfaced during inception Task 3 /self-review (2026-08-18).
