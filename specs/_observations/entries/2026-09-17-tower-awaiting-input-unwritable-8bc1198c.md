- 2026-09-17 [planwright] On a format-version 2 bundle the reconcile sweep is told to write an orphan or halt bullet to the primary checkout main view (REQ-B1.4), and a tower that may not commit to main has nowhere to put it. This repo advances main only through merged PRs, so a tower commit on main can never be pushed and rides into every later task branch diff instead; the tower therefore runs from its own worktree on a disposable branch. That leaves the sanctioned durable target for an Awaiting-input bullet unreachable: writing it on the tower branch strands it (nothing merges that branch), and writing it on main violates the rule that keeps worker bases pristine. This step hit it for real: PR #468 is merge-blocked on a spec-file conflict and the escalation could only go to the attention queue (fleet-attention.sh decide) plus the step report, with no durable tasks.md record. The attention queue is explicitly the projection and tasks.md the durable record, so the durable half is simply missing. Fix shape: route tower-authored Awaiting-input bullets through the same chore-branch-plus-PR mechanism observation-carry.sh already uses for stranded fragments, so a bullet the tower writes reaches main the only way anything reaches main here.
