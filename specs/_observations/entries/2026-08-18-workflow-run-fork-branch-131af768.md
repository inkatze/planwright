- 2026-08-18 [planwright] guard-coverage Task 4 audit surfaced a hardening the task deliberately did not apply: .github/workflows/release-please.yml gates its privileged workflow_run job on `head_branch == 'main'` plus the `branches: [main]` filter, but a workflow_run branch filter matches the TRIGGERING run's head branch (verified against this repo's actions/runs API), so a fork PR whose head branch is literally named `main` satisfies both. Impact is bounded (release-please checks out no PR code and downloads no artifact, so nothing PR-authored reaches the write token; worst case is an early idempotent release-PR refresh), which is why the Task 4 audit affirmed D-6 rather than reopening it. The closing edit is one clause: `github.event.workflow_run.head_repository.full_name == github.repository` in that job's `if:`. Not folded into check:workflow-posture as a fifth assertion because REQ-C1.2 enumerates four and a fifth would fail current main rather than guard it.
