- 2026-09-03 [planwright] Guard-coverage Task 5 shipped the mise.toml task-graph parser the 2026-08-18 adhoc-config-parsers observation predicted, and two of that entry's premises are now out of date. Its inventory still calls check-no-ci-evals.sh "grep over workflow text"; it is now a quote-and-comment-aware TOML reader (headers, nested and multi-line arrays, multi-line strings, glob dependencies, aliases) with its own PARSE BOUNDARY and FAIL-CLOSED sections. And its escape hatch ("no shared parser exists to reuse") no longer holds: one exists, but embedded as a heredoc-into-a-variable inside the guard with no reusable seam. Task 11 commits to a registration check parsing "mise.toml only - the same file-based-task parse boundary Task 5 documents", so it must now either re-implement the reader or shell out to a script whose only interface is a pass/fail exit code. The repo already has a sourced-helper precedent (scripts/release-lib.sh, sourced by four release scripts). Deciding the seam before Task 11 starts is cheaper than after. Surfaced by the Task 5 convergence review (2026-09-03).
