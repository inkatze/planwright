- 2026-07-23 [planwright] fleet-streamjson.sh security follow-up (low-severity, declined in task-4 polish): (1) the per-worker capture dir relies solely on a best-effort chmod 700 (|| :); the fleet-home parent and the capture files themselves are created with the default umask, so a chmod failure leaves worker transcripts/stderr/session_id world-readable silently; consider fail-closed dir creation and 600 capture files. (2) handle_line classifies events by unanchored whole-line substring match on "type":"control_request"/"result" rather than the parsed top-level type; safe today only because the CLI JSON-escapes conversation content, a latent forge/liveness-spoof surface if that invariant ever changes. (3) req-<id>.json / result / events.jsonl writes use plain > redirects that follow symlinks; same-UID so no privilege boundary is crossed, but O_NOFOLLOW/O_EXCL would harden. Seeded for a hardening spec-draft.
