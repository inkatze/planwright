- 2026-07-15 [planwright] doctrine/spec-format.md's format-version 2 section does not state how fenced code blocks interact with reference bullets, section headings, or header lines (the fences-are-illustration posture is implemented in drain-gates/orchestrate-select and asserted in their tests, but normatively homeless), and no rule covers a tasks.md carrying duplicate contradictory Format-version: lines (every parser silently takes the first match; closer to unparseable than parsed). Both are candidates for an expression-only meta-spec clarification plus a validator rule.
