- 2026-07-15 [planwright] The 2026-06-29 anchor-blocker(kickoff-lifecycle) prediction fired live during invariant-tasks Task 3 dispatch (2026-07-15): the reconcile-derived Ready->Active bundle Status header flip (uncommitted, on the primary checkout) changed the v1 content anchor and the /execute-task freshness gate recomputed a mismatch, even though the committed spec content on both local main and origin/main still anchored exactly to the brief-recorded value. Ruled a false halt with evidence (committed-tree anchor recomputed equal to the recorded anchor; the sole working-tree delta isolated to the three Status header mirrors, which tasks-pr-sync.sh:76-85 documents as "NOT yet anchor-excluded"), and execution proceeded. The "inert until kickoff-lifecycle Task 3 ships the Draft->Ready producer" assumption in that caveat has expired: the producer shipped, Ready bundles now flip to Active mid-execution, and every later-task dispatch of a multi-task v1 spec will hit this. The deferred follow-up (exclude the bundle Status header from spec-anchor.sh + a coordinated re-anchor migration) needs prioritizing; note invariant-tasks format-version 2 removes the stored flip entirely for migrated bundles (REQ-C1.6), so the v1 exposure window closes when Task 6 migrates live bundles, but adopter v1 bundles keep hitting it. Surfaced during /execute-task pre-flight over invariant-tasks Task 3 (2026-07-15).
