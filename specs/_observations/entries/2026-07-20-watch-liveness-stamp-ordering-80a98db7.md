- 2026-07-20 [planwright] robustness-gap(fleet-attention-watch): do_pass stamps the liveness marker before the snapshot cp and awk read, both of which return 2 and terminate the watch loop (do_pass || exit 2). read_store_guard precedes the stamp, but a transient failure after it (store unlinked between the -f test and the cp, disk-full, momentary EACCES) leaves watch.alive freshly stamped while the watcher exits, so liveness reports fresh for up to --max-age (default 900s) after the watch died — undercutting 'a dead watch is itself detectable' for exactly the path that kills the watcher. Declined on-branch: narrow TOCTOU trigger, impact bounded to a max-age detection delay, and the three-layer defense-in-depth (reconcile + heartbeat-age hung classifier + tower watchdog) still surfaces the parked worker; the fix is not hermetically unit-testable so the edit risk to the load-bearing script was judged unjustified. Candidate: stamp liveness only after a pass completes (move it to each success return in do_pass, including the no-store early return) so watch.alive proves a completed pass. Surfaced during /polish over fleet-hardening Task 2 (PR #259).
