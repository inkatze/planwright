- 2026-09-05 [repo] fleet-state-registry-race: tests/test-fleet-state.sh loses concurrent registry writes. Isolation runs against a pristine origin/main extract failed 5 of 5 with "registry race: 18-19 records, expected 20 (lost or torn writes)", and the same suite also reported "bound race: 6 grants, expected exactly 5" under the parallel gate. Reproduced in isolation, not only under parallel load, so the mechanism is the registry append/lock path in scripts/fleet-state.sh rather than test scheduling. Pre-existing and independent of the per-step tier work, which touches neither that script nor that suite. Distinct from the known test-fleet-streamjson.sh c12 ordering race on PR #408.
