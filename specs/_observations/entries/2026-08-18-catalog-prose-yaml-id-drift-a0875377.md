- 2026-08-18 [planwright] doctrine/decision-domains.md (normative prose) and config/decision-domains.yaml (machine view) carry the same catalog with no mechanical link between them: the prose headings are numbered titles ("### 14. Domain & knowledge engineering") and the yaml keys are stable kebab ids ("knowledge-engineering"), and the id is not derivable from the heading. Nothing in CI cross-checked them before inception Task 3, which doubled the catalog from 11 to 19 entries and so doubled the drift surface; that task added a count-parity assertion (tests/test-inception-doctrine.sh), which catches an entry added to one side and forgotten on the other but not a renamed or mismatched id. doctrine/guard-catalog.md and config/guard-catalog.yaml have the same shape. Candidate: annotate each prose entry with its stable id (an inline code span in the heading, or an "- **Id.**" field alongside trigger/considerations/disposition), then assert set equality rather than count equality. That also makes an overlay author supersede-by-id without reading the yaml. Surfaced during inception Task 3 /self-review (2026-08-18).
