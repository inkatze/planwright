- 2026-08-26 [repo] Markdown line-length (markdownlint MD013) is only enforced by mise run check / CI, not at commit time: githooks/ and lefthook.yml carry the anchor-freshness mirror but no markdown lint, so a prose edit to skills/ or doctrine/ can commit clean and red CI. Observed on anchor-integrity Task 5, where a one-line skill-prose fix reached the full-CI run before MD013 flagged it. A staged-path-scoped markdownlint job in the same pre-commit mirror would close it at the same cost profile the anchor guard already accepted.
