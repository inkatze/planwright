- 2026-09-22 [planwright] The clone-level core.hooksPath regressed to an absolute path pointing at the main checkout's githooks/ (fixed to the relative 'githooks' earlier in the project), so every worktree ran the main checkout's hooks instead of its own branch's, and check:githooks failed operator-dialogue Task 12's full gate on 2026-09-22 before any of the branch's own checks ran. The tower re-ran scripts/wire-githooks.sh to restore it. Nothing records what rewrote the value; whatever wires hooks on a fresh clone or worktree (lefthook install is a candidate) is worth checking for writing an absolute path, since the regression is silent until a gate trips on it.
