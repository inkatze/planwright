- 2026-09-12 [planwright] orchestrate-relay.sh relay-command stages a message into a tmux workers input line via load-buffer/paste-buffer, but paste alone does not submit it: the pane shows "[Pasted text #1 +N lines]" and the worker never sees the message until a human presses Enter. Since doctrine forbids send-keys impersonation, a tower cannot complete a relay unattended - the last keystroke is always a human's. Observed 2026-09-12 relaying expanded scope to the pw-settings-pin worker. Either this is the intended human-confirms-every-relay posture and should be stated as such in inter-orchestrator-coordination, or unattended relay needs a sanctioned submit path; today the skill reads as though relay completes on its own.
