- 2026-09-24 [planwright] converge-sync-main.sh forces ssh BatchMode=yes for its fetch; on a host whose ssh key lives in a desktop password-manager agent that needs an approval, the forced batch fetch fails with "signing failed ... communication with agent failed" while a plain git fetch from the same shell succeeds, so the convergence sync halts on an otherwise reachable remote. Worked around in tower-front-door task 5 by merging the last-fetched origin/main by hand.
