- 2026-07-26 [planwright] tests/test-fleet-streamjson.sh case c12 ("a terminal request re-surfacing on resume is re-opened and re-queued", REQ-E1.5) failed once under full-suite load on 2026-07-26 with "c12: queue should be clear after the answer", then passed in isolation (all 18 cases green) and on a full-suite re-run. The branch it failed on (anchor-integrity task-1) changed only markdown prose in doctrine/ and skills/, and the test file references neither, so causation is ruled out. The file carries ~26 timing-sensitivity markers (background launches, wait, sleep, mtime probes, pipe-buffer cases), so the queue-clear assertion after an answer looks racy under parallel load rather than wrong. classify-ci-failure.sh calls it "logic" (assertion-mismatch pattern), which is correct as a text heuristic but routes a load-dependent flake to immediate escalation. Worth either hardening c12 with a bounded wait for the queue to drain, or teaching the classifier about known-flaky cases so a worker does not park a healthy unit. Second instance 2026-07-29 (merge-currency-guard task-2): full `mise run check` exited 1 with exactly two failures, this c12 case (identical text, "c12: queue should be clear after the answer") and the sibling throttle elapse flake (obs:ef2549c2), under another worker's concurrent suite batches at one-minute load 26. Causation was ruled out STRUCTURALLY this time, not by analogy to the prose-only branch above: test-fleet-streamjson.sh writes its own /bin/sh CLI shim and points PLANWRIGHT_STREAMJSON_CLI at it, so no real Claude Code session exists in the test and no PreToolUse hook can fire inside it -- which is what cleared the branch's new global PreToolUse Bash matcher of the suspicion that its per-call jq cost (obs:ec12d9b8) had starved the bounded wait. Both files then passed twice alone at ambient load 4.0-7.6 with zero concurrent test suites (four runs, four clean: streamjson 19 ok x2, throttle 36 ok x2), a stronger pass than an idle-box run because the cases survived real CPU pressure from an unrelated qemu VM at ~42% and only suite CONCURRENCY breaks them. Cost: several poll-and-rerun cycles across two workers to establish that. Two independent workers hitting both flake families in one session makes the classifier fix above (a known-flaky-case list consulted before escalating) look worth more than hardening either case alone. A practical gating note for whoever does it: predicates matching only absolute paths (`[^ ]*/tests/test-`) miss the relative invocation run-tests.sh and workers actually use (`/bin/bash tests/test-foo.sh`), and for the throttle file a static log is a WAIT, not a finish -- gate on the terminator line, not byte growth.
