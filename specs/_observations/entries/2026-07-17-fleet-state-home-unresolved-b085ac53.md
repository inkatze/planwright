- 2026-07-17 [planwright] orchestrate(instruction-headroom --watch, tmux): fleet-throttle.sh check and fleet-tower-marker.sh record exit non-zero because no cross-spec fleet home resolves under a marketplace plugin install — $CLAUDE_PLUGIN_DATA unset, no writer-mode manifest at ~/.claude/planwright/plugin.json, PLANWRIGHT_FLEET_STATE_DIR unset. For a single-spec attended --watch the fleet-wide throttle/tower-marker are not the applicable gate so dispatch proceeded, but a real --fleet/--meta run would have these governance surfaces silently unavailable. Consider resolving fleet home from the plugin cache path or documenting the required env for marketplace installs.
