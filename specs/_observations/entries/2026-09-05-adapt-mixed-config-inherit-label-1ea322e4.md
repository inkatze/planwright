- 2026-09-05 [planwright] allocation-adapt.sh labels a MIXED non-fleet config (one dimension a concrete tier, the other the inherit sentinel) as inherit=full in the ledger inputs, and writes the concrete value into the resolved cells beside an inherit. The label is wrong on its face (only one dimension inherited), and the half-real resolved pair used to be offered by last-tier as a launch tier. model-allocation Task 6 narrowed last-tier to require both cells real, which closes the consequence, but the mislabelled row is still written by the engine. Left alone deliberately: it is Task 2 code, the consequence is now contained, and Task 6 records its own capability-driven inheritance accurately in its own row. Worth a small fix in an adaptation-owning task: label partial vs full from the actual per-dimension outcome.
