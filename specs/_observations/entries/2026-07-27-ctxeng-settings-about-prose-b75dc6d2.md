- 2026-07-27 [planwright] config/worker-settings.json carries a 739-word single-string _about field and config/tower-settings.json a 1378-word one, both inside JSON config. A large share of worker-settings _about enumerates in prose what each deny glob catches - "git push * --force*" and "git push * -f*" catch flags after the remote, "git push *+*" catches the plus-refspec form, "git push *:main" and "git push * main" catch the short and bare destinations - while those globs sit literally below in the same file, and scripts/check-glob-allow-rules.sh already guards the :* versus /* discipline. Guidance point 4 applies as the petition scopes it: enforcement stays, the mechanical restatement of enforcement goes. Change: keep only what the file cannot derive - the worker-scoping blast radius (this stanza must never be merged into a tower or interactive settings.json, enforced by placement and by nothing at runtime), the deny-before-ask-before-allow precedence, the :* versus /* glob trap, the deliberate non-registration in hooks/hooks.json, and the gh-pr-ready / --undo asymmetry - and move the per-glob walkthrough to docs/overlays.md. Risk: this is a permissions allowlist that explicitly demands human sign-off before use, and the security rationale is what makes that sign-off meaningful, so only the derivable per-glob enumeration is in scope; the posture paragraphs and the best-effort caveat about exotic refspec spellings must survive verbatim. Second risk: _about is not measured by check-instructions (see the unmeasured-read-surface fragment), so a trim here is invisible to every existing guard and needs its own before/after count.
