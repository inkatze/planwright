- 2026-09-15 [planwright] The tower-comms reply hook waits for the fleet lock inside the operator prompt submit (UserPromptSubmit blocks the turn): measured 0.2 s uncontended and 2.2 s against a held lock at the default tower_hook_lock_wait of 2s, with about 1200 execs from the 20 ms retry forks. The bound is the kickoff decision (REQ-G1.6), and the marker that confirms attention is already written before the wait, so the wait buys the turn nothing; the spec owner may want a smaller hook-side default, a detached log write, or a tower-comms lock separate from the fleet-wide one.
