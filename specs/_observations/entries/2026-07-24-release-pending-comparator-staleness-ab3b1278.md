- 2026-07-24 [planwright] release-bookkeeping.sh reported "v0.31.0 is pending. Run scripts/release-publish.sh" when v0.31.0 was ALREADY published: the annotated, GPG-signed tag existed on origin (refs/tags/v0.31.0 -> 1434c70). The shared release-pending.sh comparator had compared against local refs that had not yet fetched the existing tag, so it saw a phantom untagged window. Acting on it would have attempted to re-tag an already-published version. Because the same comparator backs the untagged-window merge lock (autopilot-reflex REQ-D1.8), a stale-refs read can also assert a release window that is not real. Suggests the comparator should fetch (or require a fresh-fetch stamp) before declaring pending. Observed 2026-07-24 during an /orchestrate --fleet bookkeeping drain.
