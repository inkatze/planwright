- 2026-09-23 [planwright] skills/orchestrate/SKILL.md tells the loop to resolve its tower identity with publish's flag and shows '--pid <pid>' as the example, while scripts/tower-reply-hook.sh only stamps the attention marker for a tower whose presence was published by --session-id. A loop publishing by pid never gets attention confirmed, so the operator queue knocks and never hands anything over. The skill notes the hook needs a --session-id record, but the example flag points the other way. Surfaced by tower-comms Task 10's review.
