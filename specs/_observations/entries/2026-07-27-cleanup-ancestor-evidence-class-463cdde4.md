- 2026-07-27 [planwright] fleet-cleanup.sh worktree refuses a SECOND provably-safe shape: an aborted dispatch whose branch carries zero commits, so HEAD is byte-identical to (or an ancestor of) origin/main. No upstream is ever configured for a freshly-cut task branch, so the upstream-parity path cannot hold, and the merged-PR path added for the deleted-upstream case (see cleanup-no-upstream-refusal) does not apply because no PR exists. Yet `git merge-base --is-ancestor HEAD origin/main` proves nothing can be lost -- the strongest evidence of all, and cheaper than either existing path since it needs no remote and no gh. Hit 2026-07-27 tearing down an operator-aborted guard-coverage task-1 dispatch; removal had to bypass the actuator via a plain `git worktree remove`, which is exactly the LLM-judgement-driven cleanup D-6 exists to prevent. Follow-up: add ancestor-of-base as a third positive-evidence path (it subsumes the common aborted-dispatch case and needs no network), ideally folded into the same change that adds --merged-pr rather than shipped separately.
