- 2026-09-24 [planwright] The --watch loop names context-budget-monitor.sh per iteration in skill prose, but a tower driving its loop turn by turn (event-driven by a Monitor, as fleet towers do) never runs it: on 2026-09-24 a tower reached 10% context with no near-limit signal and no handover prepared until the operator noticed. Fold the context check (and the usage rung) into tower-loop-comms.sh step, so the one call every iteration already makes also emits near-limit and usage-rung lines the tower must act on.
