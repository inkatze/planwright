- 2026-09-02 [planwright] scripts/fleet-state.sh valid_field (the shared worker/scope registry grammar) accepts a leading-dash value: its charset includes '-' but it has no leading-dash arm, unlike the sibling grammars in offload-dispatch.sh and fleet-presence.sh. A stored handle like '-dash' is an option-injection token for any consumer that later passes it as an argv element. fleet-lifecycle-closure Task 3 guarded its own ingress (fleet-register.sh refuses a leading-dash handle or scope) rather than tightening the shared grammar, since attention/liveness/streamjson all read it; tightening it repo-wide is a small separate change.
