- 2026-07-16 [planwright] migrate-format-version.sh is the first authoritative-state writer (spec files, kickoff briefs) to serialize through orchestrate-lock.sh, whose no-fencing-token design is justified by "a lock holder writes no authoritative state" (D-1, lock header). The premise no longer holds: two acquirers racing the stale-break can both hold the lock, and a migration racing tasks-pr-sync computes different tasks.md content. Accepted for the one-shot (risk register row 9: quiet-window operation), but the lock primitive's premise text and the fencing decision deserve a revisit if any other authoritative writer adopts the lock.
