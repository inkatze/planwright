- 2026-09-17 [planwright] Every owed event-log line in tower-queue.sh is a full re-exec of the script that re-resolves four knobs and takes the fleet lock, so a verb that closes N items pays N lock round-trips: the settling pass turned a fixed one-line cost into one proportional to what it settles. A reconcile sweep landing evidence for fifty stale PRs makes one pass into fifty lock acquisitions every other tower queues behind. A batch form of the log verb (one lock, N appends) would collapse it; noticed 2026-09-17 while the bulk merge fixture spent most of its wall clock there.
