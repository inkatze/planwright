- 2026-08-27 [planwright] A narrowing that lands in doctrine and the skills never propagates back to the REQ and design decision that authorized it, and nothing catches the gap. On anchor-integrity Task 5, b752e92 and 0b06518 narrowed the stale-anchor pre-flight trigger from any signed-bundle edit to anchored content only, across doctrine/spec-format.md and all three skills - but REQ-C1.1 still SHALLs a pre-flight "before any edit", D-5 still decides "before any bundle edit", and the test-spec Gherkin still says "about to edit that bundle". A verifier running REQ-C1.1 manually against the shipped skills finds them non-conforming, and the guard suite is silent because no check compares a REQ text to the prose implementing it. The 2026-08-27 straggler note covers doctrine-to-skill drift; this is the reverse direction (implementation drifting away from the contract that authorized it), and it is the more dangerous one because the fix is meaning-class and therefore cannot be applied by the loop that finds it.
