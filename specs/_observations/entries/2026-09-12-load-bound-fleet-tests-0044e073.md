- 2026-09-12 [planwright] Under a load average of 35-50 (several fleet workers running mise run check concurrently), three consecutive full-gate runs on an unchanged prose-only branch each failed one or two fleet tests the branch does not touch: test-fleet-state.sh with the run-tests.sh scratch directory (mktemp -d under /tmp, trap-removed on exit) gone mid-run so its .log/.time/.fail files could not be written, then test-fleet-streamjson.sh (c20: the worker never reached its hold loop) and test-fleet-throttle.sh (re-engage never observed within its anchor after 5 attempts). Each passes in isolation on the same machine. The bounded waits in those tests and lint:shell (20 minutes under load versus seconds idle) are calibrated for an idle runner, so the local gate is not a reliable arbiter while a fleet is running, and D-25 exhausts its two transient retries before the load clears.
