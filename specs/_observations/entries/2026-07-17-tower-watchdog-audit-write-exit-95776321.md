- 2026-07-17 [planwright] fleet-tower-watchdog.sh: several post-commit audit writes (fleet-audit.sh record) exit 2 on failure AFTER the launch/marker/backoff/disable already committed, so a scheduler alerting on non-zero exits fires on a completed relaunch/disable/reset when only the trailing audit write failed. Documented as fail-closed intent (header lists exit 2 for a failed escalation write), but the "exit 0 = tick completed" contract and the audit-write case are in tension; worth deciding whether audit-write failure should degrade to a warning + outcome print rather than a hard exit 2.
