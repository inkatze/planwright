- 2026-07-18 [planwright] spec-draft produced a security/parser-mechanism bundle (worker-permission-ergonomics) that passed spec-validate and normal drafting but whose kickoff lens pass found the spec did NOT meet its own stated zero-false-allow bar: ~15 exploitable shell false-allow vectors (unknown-flag writers like sort -o, git pre-subcommand -c/alias injection, inline VAR= env-prefixes, fish bare-paren substitution, >&file redirects, path-prefixed verbs, missing path containment, and an unpinned fail-closed emission contract). Consider having /spec-draft run an adversarial shell-vector lens during drafting for specs whose deliverable is a command/permission parser or other security-boundary mechanism, so these are caught at draft time rather than only at kickoff (or later in code review / the field). The cost of finding them grows the later they surface.
