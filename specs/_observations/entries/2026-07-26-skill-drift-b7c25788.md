- 2026-07-26 [planwright] skill-drift(execute-task): anchor-integrity Task 1 added a fourth freshness-gate halt class to doctrine/spec-format.md Execution validity - a sanctioned command form that resolves to nothing in the current environment is treated as absent-anchor-class and fails closed. /execute-task pre-flight step 7 and /orchestrate list only the three prior classes (no entry / unparseable / non-sanctioned form / wrong writer). Behavior is already safe in both skills, since an unresolvable recorded command exits non-zero and their "any other nonzero -> park to Awaiting input" arm catches it, so this is naming drift rather than a live gap. Task 1 repaired the actively harmful half of the same drift (both skills transcribed a closed two-item sanctioned-form list that the third form invalidated) but deliberately did not expand their halt enumerations, which is skill-prose territory. Fold into whichever task next amends those skills rather than as its own change.
