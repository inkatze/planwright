- 2026-07-21 [repo] concurrent-orchestrator-coordination kickoff run-4 HALTED (fail closed, no anchor): the worker-liveness-claim rework (Architecture A) is NOT signable. ~31 validated lens findings, 6 HIGH. DIAGNOSIS of the four-halt pattern: runs 1-3 were one bug (authoritative signal not natively cross-clone-and-death-surviving) fixed on the LOCALITY axis three times; run-4 finally got locality right, and the kickoff lens then found NEW-CLASS holes on OTHER axes the model never checked. Correctness of this primitive = locality x lifecycle x granularity x case-completeness x versioning x recovery-action; each run validated only the axis that bit last time. The run-4 HIGHs, per axis: (lifecycle) the claim is created BEFORE the worker runs yet must CONTAIN the worker death handle -- a process <pid> is unknowable pre-fork, and a pre-created tmux window read alive-but-worker-never-ran strands silently (never even unclassifiable, so never surfaced); (granularity/keying) a single unit-keyed atomic create cannot fence a cohesion-bundle set -- a peer selecting a non-lead member creates its own key and double-dispatches (contradicts REQ-C1.1); (case-completeness) reclaim-blocking artifact = "open PR OR commits on ref" but GC-resolution = "PR merged/present OR ledger-done", so a dead worker that pushed commits but no PR is neither reclaimed nor GC-d nor surfaced (silent strand), and a PERMANENTLY-erroring configured origin is treated transient every pass so a positively-dead worker is never reclaimed -- both contradict REQ-C1.3 "always reclaimed"; (versioning) quarantine-on-first-parse assumes parse-fail=corruption, but a live claim written by a different-planwright-version peer is well-formed-but-unparseable -> quarantined -> unit freed -> DOUBLE DISPATCH (this reverses the earlier "schema-skew is availability-only" call: it is a safety bug); (classification) a reused pid reads confident-ALIVE not unknown, so the bare-pid case is SILENTLY honored, contradicting the stated unclassifiable-always-surfaced guarantee (root: worker handle grammar is process <pid> with no start-time, unlike the reuse-resistant tower-identity composite). Plus: "surfaced" for unclassifiable liveness has no durable dedup-d sink (unlike the dead-letter), and unclassifiable presence-records/claims have no TTL/aged sweep (REQ-C1.3 tightened but REQ-C1.5 growth guarantee not). S-CLASS recurrence: the spec models "two shipped dispatch backends, one mangles + git branch -m renames" but only ONE ships (fleet-dispatch-worktree.sh) and it DIRECT-CREATES the canonical branch; the mangle is native claude --worktree behavior that primitive was built to eliminate; git branch -m is nowhere. So REQ-C1.6(2)/D-8/test-spec REQ-C1.6(d) harden + fixture a PHANTOM backend -- but this SIMPLIFIES (canonical name holds by construction; the whole S1-S5 rename/verify burden dissolves to a conditional future-path note). STRUCTURAL RECOMMENDATION for the rework (do these BEFORE re-touching the model): (1) enumerate the failure-axis matrix ONCE {locality, worker-lifecycle, keying/granularity, full death-state-machine, version/schema skew, defined recovery-action per fail-closed path, durable sink per residue} and make the spec answer every cell -- converts "find the next interleaving" (unbounded) into "fill every cell" (finite/checkable); (2) DOWNGRADE the top-level guarantee from absolute ("authoritative floor / always reclaimed / no unit dispatched twice") to "best-effort exclusion + every residue is bounded-and-swept OR durably-surfaced-to-operator, never silent" -- then findings #1/#3/#4/#5 stop being inconsistencies (spec contradicts mechanism) and become verifiable coverage items; you cannot be SURE an absolute holds over an open-ended partial-failure substrate, you CAN be sure a bounded-or-surfaced invariant holds per residue; (3) RE-OPEN Architecture A vs B: worker-claim does NOT actually beat origin-fence on "always reclaimed" (both strand on the unclassifiable case, findings #1/#3/#5), yet paid heavy complexity (worker-handle provenance, per-unit reclaim locks, under-lock re-reads, four-residue GC, quarantine) for a guarantee it does not reach -- the simpler origin-fence-only model with operator-surfaced (not auto-recovered) strands may now win. Lower-severity backlog (surface uid-scoping/owner-check/EEXIST-verify, echo-sanitize on unparseable bytes, repo-id path-grammar, sentinel location/quadrants, rm permission-failure, no-origin detection fails-open, manual-anchor gaps, ordering-fixture determinism, obs-path entries->archive, D-4 tag, no-strand absolutes, stale D-5 parenthetical, D-1 one-vs-two, CAS all-zeros literal) all detailed in kickoff-brief.md run-4 halt record. Route to /spec-draft with the axis-matrix + guarantee-downgrade framing FIRST; let the model fall out of the filled table.
