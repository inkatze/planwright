- 2026-08-25 [planwright] D-10's 'classify a failure before acting' discipline is specified per-path in prose, but each script hand-rolls the classification by grepping git's stderr (main-currency.sh now does this for non-fast-forward vs transient vs dirty-tree; the fence-push path in concurrent-orchestrator-coordination Task 4 will need the same split by per-ref rejection status). scripts/classify-ci-failure.sh already exists as the precedent for putting exactly this kind of pattern-matching in one tested place. A sibling classify-git-failure.sh would stop the fragile stderr greps from being re-derived per script, and would give the rejected-CAS-vs-transient-push split Task 4 needs a tested home rather than a second hand-rolled copy.
