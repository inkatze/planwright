- 2026-07-17 [planwright] The known fleet-state.sh stale-lock double-break race (non-atomic check/rm/mkdir; framed there as counter over-count) has a stronger consequence for fleet-throttle: two towers breaking the same stale lock can interleave the read-modify-write and lose the larger of two reset epochs, resuming dispatch early — the risk-9-forbidden outcome. Same crash-recovery-only window as the tracked shared-lock-stale-break-race deferral; the owner-token lock redesign fixes both. A related signal gap: a SIGINT between the fleet-state lock child mkdir and HOLD_LOCK=1 in fleet-audit/fleet-throttle leaks the lock until the stale threshold. Documented in fleet-throttle.sh header; recorded here for the redesign task.
