- 2026-07-22 [planwright] fleet-state.sh materializes the fleet home with a default-umask mkdir -p (no explicit mode), so on a permissive umask the cross-spec home (attention store, tower markers, presence sentinels) is group/other-readable; fleet-presence.sh now creates it 0700 when IT bootstraps first and 0700-verifies its own presence/* dirs, but a home created by fleet-state keeps the umask mode and the presence sentinels under it inherit that exposure. Consider a mode-explicit create + verify-or-refuse posture for the fleet home itself (REQ-A1.4 analog at the home level).
