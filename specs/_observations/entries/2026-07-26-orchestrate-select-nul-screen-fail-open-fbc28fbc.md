- 2026-07-26 [planwright] scripts/orchestrate-select.sh's inline NUL screen is the un-hardened form of the guard scripts/spec-parse.sh ships: it compares two unchecked command substitutions, `[ "$(wc -c <$f)" -ne "$(tr -d '\000' <$f | wc -c)" ]`, so if either spawn fails (fork exhaustion, ulimit) both operands are empty, `[ "" -ne "" ]` errors, `if` reads the error as false, and the screen is SKIPPED — the snapshot read below then silently drops the NULs and splices the flanking bytes, which is exactly the un-parking corruption the screen exists to refuse. spec_parse__nul_screen guards the same failure with checked assignments plus an empty-operand refusal and fails CLOSED. Pre-existing (origin/main line 122) and unchanged by format-grammar Task 2, but Task 2's `-` contract clause (g) now designates this caller-owned screen as the substitute for the lib's own on the stdin path, so it is more load-bearing than before. Remedy: mirror the lib's checked-assignment form inline, or expose the lib's screen as a public entry point the snapshot callers can call.
