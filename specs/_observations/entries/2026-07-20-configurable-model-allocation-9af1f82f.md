- 2026-07-20 [repo] planwright model/effort selection (fleet-resource-select.sh select <task-type>) resolves a units model/effort/command, but the policy is not operator-configurable for SMART, budget-aware allocation. Operator wants: (1) smart per-task model choice by complexity/type (cheap model for mechanical/doc tasks, opus for hard logic, fable reserved for the single foundational/hardest unit per wave — currently the towers manual heuristic, per operator memory worker-model-by-complexity); (2) ALLOCATIONS: per-model budget caps / shares of the shared account limit, "reserve N hardest units for fable", degrade-to-cheaper-model when the proactive usage gate ([[proactive-shared-usage-governance]]) says budget is tight; (3) all of it CONFIGURABLE via planwright config layers (defaults < adopter < repo-tracked < machine-local). The degradation ladder must degrade capability (cheaper model), never safety. Ties into the proactive shared-usage governance observation: budget-aware model allocation needs the real /usage read to decide when to downshift.
