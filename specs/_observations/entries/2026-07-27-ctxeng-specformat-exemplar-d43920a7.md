- 2026-07-27 [planwright] doctrine/spec-format.md is 6945 words, the largest instruction file by far, and carries the only PERMANENT per-file exemption in config/instruction-budget-exemptions.txt because its "a compliant four-file bundle is authorable from this doc alone" contract cannot be honoured by a prose trim (the exemption rationale records only ~99 removable words). The Claude-5 references-over-prose guidance offers the move the prose ladder cannot: a spec may also be an artifact, so the authorable-from-alone contract can be satisfied by an EXEMPLAR plus a validator instead of by exhaustive narration. Change: ship a canonical reference bundle under templates/ (templates/ today holds only release-please and release-window, no spec exemplar) or nominate a real bundle as canonical, then collapse the four per-file grammar sections (## requirements.md line 89, ## design.md 158, ## tasks.md 184, ## test-spec.md 277) to their decided rules plus a pointer to the exemplar and to scripts/spec-validate.sh. The doc already licenses this internally with its "Decided rules over enumerated claims" section (line 70). Risk: the contract silently weakens unless the exemplar is guard-pinned - the change needs a test asserting the exemplar validates clean AND exercises every grammar construct the doc stops narrating, otherwise a construct that appears in neither the exemplar nor the validator becomes unspecified. Second risk: this is the meta-spec, so a wrong trim propagates into every future bundle; it wants the kept-eval pilot, not a review-only sign-off.
