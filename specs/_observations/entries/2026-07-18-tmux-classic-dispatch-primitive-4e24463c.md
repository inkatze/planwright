- 2026-07-18 [planwright] claude --worktree <suffix> --tmux=classic folds three tmux-backend dispatch steps into one native command: it creates the git worktree (satisfying D-37 never-shell-git-worktree), creates a classic tmux session, and launches the worker — replacing manual tmux new-window + a send-keys claude --worktree launch. Evaluate adopting it as the /orchestrate tmux-backend dispatch primitive. Caveats observed 2026-07-18: (a) plain --tmux uses iTerm2 native panes that are NOT load-buffer/capture-pane relay-targetable, must pass =classic; (b) it still creates the mangled branch name worktree-<suffix>, so the D-36 rename to planwright/<spec>/task-<id> is still needed post-launch; (c) it switched the attached tmux client to the new worker session, which could disrupt a tower operator watching another session; (d) branch/session are named from the --worktree suffix, so pass a bare suffix (task-1) not a slashed D-36 name.
