- 2026-07-19 [planwright] release-pending.sh version_file containment (REQ-D1.1, D-5) closes re-following the ORIGINAL symlink (reads the canonicalized $vf_real, not $vf_path), but the read `<"$vf_real"` still re-opens by path, so a concurrent attacker with repo-working-tree write access who wins the check→read race could swap the canonicalized file for an escaping symlink (path-based TOCTOU). Not exploitable beyond what committing an escaping version_file already grants (canonicalization catches that), and a portable fd-based fix (O_NOFOLLOW/fstat) is beyond the bash 3.2/BSD floor D-5 pins — accepted-as-limitation for this bundle. Seed for a future release-input-hardening spec if the threat model ever includes a live repo-tree race. Surfaced at release-hardening Task 5 /polish convergence (security lens).
