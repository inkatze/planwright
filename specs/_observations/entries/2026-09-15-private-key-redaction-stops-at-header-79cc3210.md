- 2026-09-15 [planwright] tower-queue.sh redact() replaces only the -----BEGIN ... PRIVATE KEY----- header, so a pasted key body reaches events.log, which the reply hook now writes on every operator prompt in a tower session and retains for tower_log_rotate_age. The rule should span to the END line or drop the whole value on a BEGIN match; JWTs and bare bearer tokens are outside the six shapes too. Not changed in the tower-comms task 2 PR because the file is task 3's in flight.
