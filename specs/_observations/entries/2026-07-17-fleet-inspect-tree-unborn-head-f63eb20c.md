- 2026-07-17 [planwright] fleet-cleanup.sh inspect_tree returns uninspectable for an unborn HEAD (git rev-list HEAD exits 128 on a fresh/orphan worktree) which could falsely escalate. Low realism: the sweep scope (registered worktrees + tower checkout) always has commits; only git worktree add --orphan reaches it. Safe fix if wanted: treat an unborn HEAD as 0 unpushed and let status --porcelain decide clean/dirty. Surfaced by fleet-autonomy Task 4 panel-review (corroborated by S1 concurrency lens); deferred out-of-scope.
