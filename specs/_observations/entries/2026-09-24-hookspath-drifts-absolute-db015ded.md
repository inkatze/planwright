- 2026-09-24 [planwright] The shared clone config core.hooksPath keeps reverting to an absolute path (/home/.../planwright/githooks) after being rewired relative: on 2026-09-24 a worker ran scripts/wire-githooks.sh to set it relative, and within hours it read absolute again. check:githooks run directly inside a worktree then fails while passing inside mise run check. Something in the fleet (a hook installer, lefthook install, or a wiring step run from the primary checkout) re-writes it absolute; find the writer and make every writer set the relative form.
