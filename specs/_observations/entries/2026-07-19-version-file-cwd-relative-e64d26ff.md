- 2026-07-19 [planwright] release-pending.sh resolves a repo-relative version_file against the process CWD (cd -- "$dir"; pwd -P) while the containment root is git rev-parse --show-toplevel, so running the comparator from a subdirectory canonicalizes the knob path relative to CWD, not the repo root. No escape (a legitimate in-tree file under CWD still passes containment; an absent one is a clean refusal), but the "contained" anchor (CWD) and the "root" (toplevel) can denote different subtrees. Pre-existing: the pre-canonicalization read of the version_file was already CWD-relative, and Task 5 containment inherits rather than introduces it. Seed for a future release-input-hardening spec (alongside the recorded check-then-read TOCTOU) if version_file resolution is ever pinned to the repo root. Surfaced at release-hardening Task 5 /self-review (security/correctness lens).
