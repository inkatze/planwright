- 2026-09-02 [planwright] tests/test-fleet-throttle.sh case 33 ('the same excerpt after elapse is a fresh event and must re-engage') flakes under the parallel run-tests load: it anchors a 3-second reset, waits for it to elapse, re-observes, then asserts check reports throttled. Under 12-way load the freshly anchored 3 seconds can elapse between the re-observe and the check, so check reports allowed and the case fails. Passes reliably in isolation. Same shape as the recorded fleet-state lost-append flake: a real-time window too short for a loaded box. A longer anchor for this case, or a clock the test controls, would fix it. Observed 2026-09-02 during fleet-lifecycle-closure Task 3, whose diff touches no throttle path.
