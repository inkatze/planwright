- 2026-08-18 [planwright] A park note written by an earlier /orchestrate step promised something the contract forbids: guard-coverage Task 3 sits in ## Awaiting input with "a later step re-dispatches Task 3 once native creation is confirmed working in a fresh session". The halt contract and the orphan predicate both say a parked unit is never auto-re-dispatched (re-dispatch is a human call after they see the note), and the v2 selector correctly refuses Task 3 while the live bullet names it. This step confirmed native worktree creation now works (the WorktreeCreate hook-block mitigation persisted in the installed hooks.json), so the notes stated precondition is met and it still cannot self-clear. Candidate: park notes should state the human action that unparks, never a future automatic step.
