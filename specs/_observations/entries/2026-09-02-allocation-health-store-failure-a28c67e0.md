- 2026-09-02 [planwright] scripts/allocation-ledger.sh's health verb cannot report a store-resolution failure: it calls check_health "$(ledger_path $1)", and ledger_path's exit 2/5 (unresolvable fleet home, missing fleet-state.sh) terminates only the command-substitution subshell. The parent then health-checks the empty string, whose [ -e ] is false, and returns 0 for HEALTHY. Every caller that gates on health therefore reads 'the records can be trusted' when the store cannot be found at all.
