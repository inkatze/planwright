- 2026-07-27 [planwright] doctrine/instruction-hygiene.md already states the Claude-5 no-repeat-yourself rule ("a definition stated once in doctrine governs every skill that cites it; a skill restates at most the one-line gist it needs inline") but nothing MEASURES it. check-instructions.sh runs a resolution check (does every manifest doc resolve) and a reverse use-site check (is every point-of-use doc named in body prose) - both verify that a reference EXISTS, neither verifies that the skill has not RESTATED the doc it references, which is the duplication the guidance says to delete. Known live instance: the freshness-gate procedure is described near-identically in skills/orchestrate/SKILL.md step 2 and skills/execute-task/SKILL.md pre-flight step 7, already flagged as a factoring candidate by obs:6ee8bb6f, and obs:b7c25788 records the two skills having transcribed a doctrine list that later went stale in both copies - the exact drift cost of restatement. Change: add an informational near-duplicate detector to check-instructions --audit reporting shared normative n-grams (say 8+ words) between a SKILL.md body and each of its run-start docs, and between sibling skills. Risk: naive n-gram overlap false-positives on quoted grammar tokens, REQ/D citation strings, and fixed vocabulary lines such as the v2 Execution pointer, so the detector must exclude fenced blocks, manifest lines, and citation runs, and must be advisory-only since some restatement is the sanctioned one-line gist.
