- 2026-07-27 [planwright] The reachable-closure budget only counts files named in a doctrine manifest, so any bulk a skill routes into docs/, config/, or a script header is INVISIBLE to check-instructions while still entering model context at run time. This has become a budget-evasion channel rather than progressive disclosure: skills/orchestrate/SKILL.md names docs/fleet.md (9392 words) three times as the authoritative contract for resource governance, backend failure postures, and exit codes (lines 222, 302, 306), and obs:ff91ed4e records headless-oneshot drive mechanics being relocated to docs/fleet.md and the capability contract specifically because the skill had no budget left. Live arithmetic: /orchestrate charges closure 18859/20000 but its honest actual is already 21804 (the capped-charge rule discounts doctrine/spec-format.md from 6945 to 4000), and an invocation that follows the fleet.md pointer loads roughly 31000 words of instruction. The Claude-5 progressive-disclosure guidance is about deferring to a surface that is loaded on demand AND accounted for; deferring to an unaccounted one just hides the load. Change: give the manifest a third class (e.g. "Doctrine: external <path>" or a separate "Reads:" line) covering non-doctrine runtime reads so they charge closure, or promote the fleet contract into a real point-of-use rule doc. Risk: enrolling these surfaces turns several budgets red on the first run, so the change must land together with a decided disposition per surface (raise, exemption, or relocation), not as a bare measurement change that blocks all in-flight work.
