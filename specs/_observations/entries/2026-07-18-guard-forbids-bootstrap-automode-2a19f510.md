- 2026-07-18 [planwright] fleet-dispatch-guard.sh check-launch hard-refuses both --permission-mode auto and --permission-mode bypassPermissions (REQ-E1.4/D-19: the worker-settings allowlist is the sole approval mechanism). But before the worker-command-guard auto-approve hook exists (the very deliverable of worker-permission-ergonomics Task 1), the static worker-settings allowlist cannot match dynamic $VAR-expansion plugin-script commands, so a guard-clean launch (acceptEdits/default/--settings) still floods the worker with permission prompts, and a tmux worker stalls because the tower may not type answers into a worker input line. The only flood-free bootstrap is auto mode, which the guard forbids. Net: the spec dispatch guard blocks bootstrapping the spec own first task; it required an explicit human auto-mode override on 2026-07-18 (worker launched claude --worktree task-1 --tmux=classic --model opus --permission-mode auto; the auto-mode classifier then auto-approved the plugin-script commands). Consider a sanctioned bootstrap exception or a documented human-override bootstrap path.
