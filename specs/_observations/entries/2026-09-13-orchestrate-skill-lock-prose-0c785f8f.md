- 2026-09-13 [planwright] skills/orchestrate/SKILL.md step 1 of the dispatch record now describes the per-spec lock inaccurately, and skill prose is frozen on the universal-binary branch so it was not edited. Two claims: "Exit 1 (another live holder) is a clean no-op" — exit 1 now means only that some holder has it, and a detached hold outlives its holder; and the documented invocation `scripts/orchestrate-lock.sh acquire specs/<spec>` takes the detached default, which nothing auto-breaks, so a tower that dies mid-window leaves a lock only an explicit release clears (scripts/tasks-pr-sync.sh passes --owner-pid "$$" for exactly this reason). Step 5s release is unaffected. Surfaced adopting the lock primitive.
