- 2026-09-03 [planwright] skill-drift(execute-task): pre-flight step 5 and its siblings in orchestrate and spec-kickoff say "a Ready or Active bundle's findings are errors: surface them and halt", which the validator's D-25 status model backed until format-grammar task 3 landed two warn-at-every-status heuristics (the citation-range and dead-path checks, REQ-D1.3 and REQ-D1.8, D-13 and D-14). Those findings never change the exit status the skills key on, so the halt behavior is unchanged, but the sentence now reads as a blanket that the meta-spec (Format-version 2, Validation) no longer states. The doctrine gained the carve-out wording with the same task; the three skills did not, so a reader of the skill alone expects a halt a warning will not produce.
