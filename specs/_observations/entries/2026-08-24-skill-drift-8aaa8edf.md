- 2026-08-24 [planwright] skill-drift(spec-draft): pre-flight worktree resolution (D-44) is unachievable when the session was launched directly inside an unrelated worktree — EnterWorktree refuses creation from a worktree session and ExitWorktree is a no-op for a launch-time worktree, so the run degraded to the current worktree on the correctly-named spec branch; the skill lacks a documented degradation arm for this starting state
