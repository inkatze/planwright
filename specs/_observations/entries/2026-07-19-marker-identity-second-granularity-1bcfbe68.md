- 2026-07-19 [planwright] fleet-liveness marker identity (marker_live) uses a SECOND-granular heartbeat token: a same-second escalation on a reused handle can carry a colliding token and let a marker read as live against a row it does not own. fleet-hardening Task 2 closed this for the fork-park path with a field-9 (reason) ownership discriminator, but the SHIPPED permission path (marker_live_permission) retains the pre-existing same-second window (a permission row has no field 9 to discriminate on). Future fleet hardening: give marker identity a monotonic/unique token (e.g. a per-decision nonce) instead of the second-granular heartbeat, so the permission path is equally robust. Surfaced by the Task-2 convergence review (independent concurrency lens).
