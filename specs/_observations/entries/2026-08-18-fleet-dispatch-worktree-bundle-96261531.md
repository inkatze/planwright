- 2026-08-18 [repo] orchestrate(anchor-integrity): selection picked Task 2, whose Done-when declares a required cohesion bundle 2+3 (not independently mergeable), but fleet-dispatch-worktree.sh (the sole sanctioned git-worktree-add primitive, D-7) only accepts a single task id per branch (task-<id>) — no task-<id>-<id> bundle creation path exists for any backend (tmux, headless-oneshot, or stream-json-persistent). The orchestrate skill doctrine names bundle branches as a real shape (cohesion-first bundling, D-9) but the tooling has not caught up.
