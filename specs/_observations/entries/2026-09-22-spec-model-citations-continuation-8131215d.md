- 2026-09-22 [planwright] spec-model.sh parse_tasks (custom-steps kickoff lens pass, 2026-09-22): the Citations bullet is emitted from its first physical line only and continuation lines are dropped, so a wrapped Citations line loses every id past the first line for spec-onepager (load-bearing score) and spec-scope (blast radius); spec-anchor and spec-validate keep continuation lines and are unaffected. Repo-wide: merge-currency-guard wraps 4 of 4 task citation lines, customization-overlay 5 of 7. custom-steps put each Citations bullet on one physical line as a workaround (MD013 is off under specs/). The fix belongs to the format-grammar bundle: accumulate the bullet like spec_parse_extract_tasks does.
