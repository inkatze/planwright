- 2026-07-23 [planwright] fleet-streamjson.sh writes the per-worker result file in place (printf > result, lines ~527/583), unlike session/journal which use temp+rename. fleet-status.sh (Task 7) is the first concurrent unlocked reader of it: a read landing between the writer O_TRUNC and the data yields an empty file, which cmd_status classifies as `completed unknown` for what is really an ended/crashed worker (and a completion between the -f result check and the pid probes yields a transient `dead`). Give result the same temp+mv treatment as session. Out of Task 7 scope (Task 4 code); low-likelihood sub-ms window.
