- 2026-09-06 [planwright] The dash-echo escape hazard (echo re-expands a literal backslash into the control byte sanitize_printable just stripped) is not confined to the lines a change touches: scripts/fleet-liveness.sh refuses its worker and scope arguments through echo "...$(sanitize_printable ...)", and scripts/allocation-select.sh:298/305/318 does the same for an unknown selection key — verified live, an ESC byte reaches stderr. Each new caller that routes argv into one of those diagnostics extends the reach without touching the defective line. A guard that flags echo with a sanitize_printable interpolation would catch the class; today it is caught only when a reviewer remembers.
