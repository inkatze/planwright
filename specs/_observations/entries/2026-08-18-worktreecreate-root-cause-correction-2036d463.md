- 2026-08-18 [repo] orchestrate(guard-coverage): correction to 2026-08-18-worktreecreate-regression-2-1-234-562be3ae.md -- the 2.1.234-regression attribution is superseded. Operator-verified across the CLI versions on disk (2.1.226/2.1.231/2.1.233/2.1.234) that WorktreeCreate input on stdin has always been {hook_event_name, name} -- it never carried worktree_path on create (that shape is WorktreeRemove-only, per the CLIs own --help/output contract: WorktreeCreate "provides the absolute path to the created worktree" as its RESULT, i.e. the hook is the creator, not a pass-through tracker). fleet-worktree-track.sh hook-create at HEAD reads .worktree_path from the input, finds nothing, and echoes nothing on its deliberate exit 0 -- so a registered WorktreeCreate hook has been silently replacing native creation with a no-op fleet-wide, on every version, not just after todays upgrade. Upstream cause: observation a6f5511b (2026-07-17) recorded the contract as "echo the stdin worktree_path unchanged", back-filling the WorktreeRemove input shape onto WorktreeCreate. Immediate mitigation already applied by the operator: the WorktreeCreate block removed from the installed 0.31.0 plugins hooks.json (WorktreeRemove left in place), restoring native creation for sessions started after the edit. Real fix (not yet landed, tracked toward a chore PR / a REQ-H in the operators draft spec): hook-create should read .name, grammar-check it, create <repo>/.claude/worktrees/<name>, and echo that computed path. Do not file an external regression report against this version bump -- the defect is planwrights own hook contract, not the harness.
