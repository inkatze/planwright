- 2026-09-03 [planwright] doctrine/spec-format.md *Validator-enforceable invariants* still lists the original nine rules while scripts/spec-validate.sh enforces far more (the v2 ledger rules, fence balance, the retirement escape, and the format-grammar task 3 hardening rules: cited-but-empty REQs, decision shapes, canonical task headings, Awaiting-input purity, citation range, dead-path). The script header is the accurate list. The doctrine list cannot grow from an execution task: spec-format.md edits are budget-gated (format-grammar REQ-G1.1) and a list restating the script is the fragile-filler shape the writing rules warn about. Either the doctrine list should point at the script header as the enforced set, or the next spec-format amendment should carry the delta deliberately.
