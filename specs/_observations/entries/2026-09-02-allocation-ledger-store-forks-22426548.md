- 2026-09-02 [planwright] scripts/allocation-ledger.sh store_dir() forks scripts/fleet-state.sh root on every call with no memoization, and fleet-state.sh re-resolves the plugin manifest and runs an unconditional mkdir -p each time. One verb costs 1-3 of them, so a single allocation-feedback evaluation issues about nine, and the launch-boundary path in allocation-adapt.sh pays the same shape on every dispatch. Caching the resolved store in a variable at first use would remove all but one, at no behavior change.
