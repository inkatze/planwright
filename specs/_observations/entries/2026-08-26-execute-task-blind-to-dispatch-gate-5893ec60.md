- 2026-08-26 [planwright] skill-drift(execute-task): the skill has no step that reads a BUNDLE-LEVEL dispatch gate. fleet-lifecycle-closure tasks.md carries one in its ## Tasks intro prose ("No task here can be dispatched until the out-of-band WorktreeCreate chore fix D-11 names has landed"), recorded there by /spec-kickoff Finding 5.2 precisely because it blocks every task rather than any one of them. /execute-task pre-flight checks status, validator, brief, freshness, and per-task Dependencies -- none of which sees intro prose -- and its Stop conditions list has no entry for it either. Task 2 was dispatched with the gate still unmet and only caught it because the operator context named the WorktreeCreate work explicitly; a less-informed run would have proceeded straight past a condition the spec owner wrote to be blocking. Related: obs e78dd05a already flagged that a design decision naming an out-of-band fix has no ship mechanism; this is the downstream half -- even when the gate IS recorded, nothing reads it. Candidates: (a) give /execute-task a pre-flight step that surfaces any bundle-level gate text for the human, (b) make dispatch gates a structured tasks.md field the validator and selector can both read rather than prose, or (c) have /orchestrate check it at selection time so it never dispatches into a gated bundle. Surfaced during fleet-lifecycle-closure Task 2.
