- 2026-07-24 [planwright] Splitting a test file silently staled backticked cross-spec references to it. specs/observation-recording/test-spec.md, specs/instruction-headroom/{tasks,test-spec,kickoff-brief}.md and specs/prompt-hygiene/{tasks,test-spec,diet-plans,kickoff-brief}.md name tests/test-obs-consume.sh and tests/test-check-instructions.sh as the verification path for properties that guard-coverage Task 6 moved into sibling files. No guard catches it: check-doc-links.sh validates only markdown [text](target) links, and the spec validator does not resolve backticked paths. Mitigated for readers by each split file naming its siblings in its header, but a mechanical tether (resolve backticked tests/*.sh and scripts/*.sh mentions in spec prose, fail on a missing target) would catch the class. Fits the guard-coverage REQ-F drift-tether family.
