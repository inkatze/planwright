- 2026-08-25 [planwright] scripts/fleet-audit.sh loses appends under concurrent writers, and it is NOT load-flakiness: tests/test-fleet-audit.sh "concurrent writers" failed 3/3 in isolation in a task-1 worktree AND once on the unmodified primary checkout at main 2e6ada8 (expected 8 rows, got 6/7). The failure was previously characterised as a lost-append race that only shows under a wide parallel test run; the isolated reproduction on pristine main says the race is in the audit-trail append path itself, not in test scheduling. Relevant because every autonomous termination fleet-lifecycle-closure adds writes a fleet-audit record (REQ-F1.5, D-5): a reaper whose audit trail silently drops rows under contention cannot be audited, which is the property the trail exists to provide. Not in Task 1 scope (doctrine only, no scripts touched).
