- 2026-08-25 [planwright] scripts/fleet-audit.sh loses appends under concurrent writers, and the failure is not confined to a loaded test run: tests/test-fleet-audit.sh "concurrent writers" failed 3/3 when run alone in a task-1 worktree and once on the unmodified primary checkout at main 2e6ada8 (expected 8 rows, got 6 and 7), then passed on a later 4-job suite run — nondeterministic in both conditions, not scheduling pressure alone. Observed with a doctrine-only diff, so it is pre-existing rather than branch-induced. Relevant because every autonomous termination fleet-lifecycle-closure adds writes a fleet-audit record (REQ-F1.5, D-5): a reaper whose audit trail silently drops rows under contention cannot be audited, which is the property the trail exists to provide. Not in Task 1 scope (doctrine only, no scripts touched).
