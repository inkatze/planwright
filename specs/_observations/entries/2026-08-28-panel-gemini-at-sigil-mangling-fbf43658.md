- 2026-08-28 [planwright] The gemini backend for /panel-review silently mangles @-sigil code: gemini CLI treats a bare @token in a prompt as its file-inclusion syntax, so Perl array variables in a reviewed diff (@_, @w, @paths) get expanded into repo file paths before the model sees them. On the guard-coverage Task 3 pass this produced a confident, top-ranked, entirely fabricated finding that the scripts were corrupted and non-functional, naming real repo paths (including an observation fragment written minutes earlier) as the substituted variable names. Mitigation that worked: prepend an explicit instruction that @-prefixed tokens are language variables, not file references. Candidate: have the panel-review backend seam escape or fence @-sigils for the gemini backend, since every shell/Perl/Ruby diff this repo reviews carries them. Surfaced during /panel-review over guard-coverage Task 3 (2026-08-28).
