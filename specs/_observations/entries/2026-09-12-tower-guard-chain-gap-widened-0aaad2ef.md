- 2026-09-12 [planwright] The 2026-07-28 command-guard-root-chain divergence widened: the worker guard now trusts five root arms (PLANWRIGHT_ROOT, CLAUDE_PLUGIN_ROOT, <claude-dir>/planwright, its own location, and every root resolve-installed-roots.sh names) and resolves a same-command trusted-root assignment, while the tower guard still trusts two (the repo checkout and a literal CLAUDE_PLUGIN_ROOT) with no assignment tracking. A tower whose skill text resolves ${CLAUDE_PLUGIN_ROOT} to the marketplace cache while the hook has no CLAUDE_PLUGIN_ROOT in its environment defers every plugin-script call, the same stall class the worker fix closed. The worker-stall branch rewrote the tower header to state the raw-command premise as "inherited from the worker guard" without giving it the arms that act on that premise.
