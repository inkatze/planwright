- 2026-09-03 [planwright] During guard-coverage Task 5 convergence (2026-09-03), PR #397 was created with `gh pr create --draft` and flipped to ready-for-review without any `gh pr ready` call. The issue timeline records `ready_for_review` by the authenticated user at 19:39:25, between the second (19:36:02) and third (19:43:24) `review_requested` events, all three of which came from `/copilot-review --nested` step (f) running `gh pr edit <n> --add-reviewer @copilot` (the native gh route the skill prefers on gh >= 2.88.0; this host has 2.96.0). No other lifecycle command ran in that window. The likely mechanism is that requesting a reviewer on a draft PR un-drafts it so the request is valid, but that was not confirmed against gh or GitHub docs. This matters because both /copilot-review --nested and /execute-task treat the draft->ready flip as a reserved human action, and the skill even gates its own optional ready-flip behind an explicit per-run confirmation - a side effect of the re-request route bypasses that gate entirely and silently. Worth confirming the mechanism and, if it holds, either re-drafting after the request or preferring the REST transport (which may not have the same side effect) in the skill step. Surfaced by the Task 5 copilot-review pass.
