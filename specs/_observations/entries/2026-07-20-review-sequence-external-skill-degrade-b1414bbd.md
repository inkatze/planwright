- 2026-07-20 [repo] review_sequence resolver degrades the WHOLE list to [polish] when it contains an entry that is not a planwright plugin skill: panel-review is a dotfiles-local command, not a plugin skill, so execute-task rejects [polish, panel-review] (warn+degrade, exit 0) even when the machine-local config sets it. Net: setting review_sequence=[polish, panel-review] does NOT make execute-task run panel natively; only an explicit /panel-review --nested step in the worker brief runs the panel leg. Consider: (a) per-entry degrade (keep valid entries, drop only unrecognized), or (b) an allow-list for external nestable review skills so operators can compose dotfiles review commands into review_sequence. Separate from the worktree machine-local propagation gap already filed.
