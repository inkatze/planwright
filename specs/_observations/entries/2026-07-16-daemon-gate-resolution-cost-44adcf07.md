- 2026-07-16 [planwright] fleet-autonomy Task 1 polish, performance lens: one fleet-daemon-gate.sh check costs ~6 shell spawns plus (in production, PLANWRIGHT_REPO_ROOT unset) ~2 git rev-parse walks via the resolver->config-get->overlay-root chain, and fleet-audit.sh query with no time bounds scans every daily file the store ever accumulated. Fine per rare action; a Tasks 2-7 sweep gating N resources should resolve the switch once per sweep (re-check at step boundaries, not per resource), and a stats-surface consumer (Task 8) should pass --since bounds. If sweeps land per-resource gate calls in practice, consider a batch/caching seam in the resolver family.
