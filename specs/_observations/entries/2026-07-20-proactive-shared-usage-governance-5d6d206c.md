- 2026-07-20 [repo] planwright fleet usage governance is REACTIVE ONLY: fleet-throttle.sh (D-12) waits until a session HITS the rate limit and renders the retry text, then pauses fleet-wide until reset — it never gates on usage ahead of the wall. Its stated reason ("no supported machine-readable way to query account-level usage", Claude Code issues #38380/#40793/#33820) is now STALE: the /usage command output is capturable and parseable (the tower did it this run via a throwaway pane, per operator memory). Proposal: planwright reads real account /usage PROACTIVELY and gates dispatch on a CONFIGURABLE budget threshold (e.g. pause heavy/opus dispatch at N% weekly, warn earlier), keeping the reactive throttle as the hard backstop. Because /usage is ACCOUNT-GLOBAL, this is inherently shared-aware — it reflects every concurrent tower, all workers, AND unrelated non-planwright work the tower cannot see, which is exactly the "orchestrators do not keep tabs on usage when other orchs/work run" gap the operator flagged. No per-orchestrator reservation bookkeeping needed; the shared /usage number is ground truth. Revisit D-12s no-machine-readable-usage premise. Pairs with [[configurable-model-allocation]].
