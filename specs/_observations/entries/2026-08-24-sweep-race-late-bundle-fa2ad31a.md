- 2026-08-24 [planwright] A corpus-wide re-anchor sweep races bundles that get signed off while the sweep PR is open. anchor-integrity REQ-A1.4 anticipates the exposure ("classifications and the landing proof SHALL be re-verified at the sweep PR merge SHA") but only as a verification step, not a repair procedure: on this run fleet-lifecycle-closure (#347) was signed off on main after the branch swept the 25 bundles that existed, so CI - which tests the merge ref, not the branch - went red on a 26th bundle the branch could not see. Re-verification correctly DETECTED it; nothing says what to do next, and the answer turned out to require merging main into the task branch so the new bundle exists in the tree to be swept at all. Any future parser or hash-scope change lands with the same paired sweep and hits the same race, and the window widens with review latency. Candidate fixes: name the top-up pass in the sweep procedure (merge base, classify only the bundles the merge adds, re-run the proof) so it is a known step rather than a surprise; or have the Task 4 guard report a brief-less-or-unswept NEW bundle with a distinct message that names the top-up, so the failure is self-explaining to whoever sees it red.
