- 2026-08-24 [repo] Machine-local gen-worker-settings.sh resolves the NEWEST plugin cache (printed "plugin root: .../0.32.0") while the running /orchestrate skill and the scripts it invokes per plugin-script-invocation are 0.31.0. The dispatched worker therefore runs 0.31.0 skill prose against a 0.32.0-derived worker-settings/guard. Nothing broke this run, but the two roots should be pinned to the same version at dispatch, or the skew should be surfaced at launch.
