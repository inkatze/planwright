- 2026-07-17 [planwright] orchestrate(instruction-headroom --watch, tmux backend): the dispatched /execute-task worker prompts on nearly every command despite being launched with --settings config/worker-settings.json. Two causes: (1) worker-settings runs defaultMode:default (prompt mode, mandated by the no-auto dispatch guard), and its allowlist patterns do not match the actual execute-task command shapes (worker prefixes each Bash call with `export PLANWRIGHT_ROOT=...; $PLANWRIGHT_ROOT/scripts/<x>.sh`, and the persistent-allow grant keys on a different pattern); (2) any command Claude Code flags "Contains simple_expansion" or containing a for-loop offers NO persistent-allow option (only Yes/No), so every expansion/loop command prompts individually with no way to pre-approve. Net: unattended tmux driving requires near-continuous manual approval via send-keys, which both violates the spirit of the never-type-into-worker-input invariant and floods the tower context. The subagent backend (allowlist merged into worker settings, questions funnel to the tower prompt queue) is the lower-friction designed path. Consider: shipping worker-settings allowlist entries that match the export-prefixed script-invocation shape, and/or documenting that tmux backend implies hands-on prompt answering by the operator attached to the window.
