- 2026-09-20 [planwright] scripts/observation-carry.sh printed `carry updated` with `stranded 1` on a run that carried the fragment successfully. The immediate re-run printed `carry noop`, `stranded 0`, exit 0, and the fragment was already present on the reused chore PR (#476), so nothing was ever stranded; the first run's count contradicted its own outcome. The script documents stranded as the fail-loud signal a caller must surface (REQ-K1.6, REQ-K1.7), and a non-zero count is supposed to pair with a non-zero exit naming the stranded observations, so a tower following the contract would report a data-loss risk that did not exist, or worse, learn to ignore the field. Suspected an off-by-one between the fragments selected for the carry and those counted as remaining after the commit-tree push, since the count matched exactly the one fragment being carried. Worth asserting in the suite that stranded is zero whenever the carry reports updated and the push succeeded, and that a non-zero stranded always accompanies a non-zero exit.
