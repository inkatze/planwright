- 2026-09-09 [planwright] doctrine/spec-format.md carries a 1,712-word "Versioning of this meta-spec" section that no skill consults -- grep across skills/ and scripts/ finds no reference to it -- yet every skill whose manifest names spec-format loads it. It is maintenance content about how the meta-spec itself is revised, charged to workers doing spec work. Same shape, smaller: "Sign-off records and content anchors" is 2,527 words, the docs largest section, and it governs the kickoff brief rather than the four-file bundle, so it is not part of what makes the doc authorable-from-alone (the contract the permanent exemption rests on, and which is asserted in the exemption entry rather than anywhere in the doc itself -- worth confirming before anyone relies on it). Relocating the versioning section to a maintenance doc no skill loads would cut 1,712 words of real context from every consumer while leaving the charged metric untouched, since spec-format stays capped at 4,000 either way. That is a real win with no scoreboard win, which is exactly why it keeps not getting done -- and it is the only lever that helps /spec-kickoff, the surface no accounting change can rescue.
