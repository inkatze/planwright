- 2026-09-17 [planwright] tests/test-fleet-streamjson.sh leaks orphaned supervisor processes that never exit, and the leak compounds into the load-sensitive failures the suite is already known for. Found 46 live processes matching "_supervise sjw30" on this host, the oldest at 2 days 22 hours elapsed, 9 of them holding the tower-comms-task-2 worktree as their cwd and the rest spread over other worktrees; one had 00:00 elapsed, i.e. the in-flight gate run was spawning more while the count was taken. Each is /bin/sh <worktree>/scripts/fleet-streamjson.sh _supervise sjw30 <sandbox>/h30/streamjson/sjw30, reparented to init (PPID 1). Every sandbox dir sampled (/tmp/tmp.YK8uJlxii1, tmp.s8OsH1FNTw, tmp.C5FFLOkqzG, tmp.oxQ7TihA4v, tmp.u6zYnOZAqR) is GONE: the test cleanup trap removed the mktemp tree but never stopped the supervisor it launched, so each survivor is a polling loop watching files that no longer exist. Consequences, with one earlier claim retracted: 46 concurrent pollers are standing background load on the host and are a plausible contributor to c20 being load-sensitive. This fragment originally also blamed the leak for check:test-time measuring test-fleet-streamjson.sh at 143.639s against its 120s per-file budget (host load 18.65 during that run). That is FALSE and is retracted: after the sweep, on an idle host at load 0.24, the same file measured 155.710s, slower than the contaminated run. The duration is the test's own, not the leak's. The same clean gate also put test-allocation-adapt.sh at 123.556s and test-spec-validate.sh at 120.137s over the same 120s budget, so the real finding there is suite-wide budget drift that needs measuring on the CI reference runner, tracked separately from this leak. It is self-reinforcing, since every suite run leaves more survivors and makes the next run slower and flakier, which is the shape of the three dismissed-as-flake failures recorded in the load-sensitive-test-failures note. Also note fleet-streamjson.sh status reported the matching worker as "completed result=success" while nine of its processes were still alive in that worktree, so the recorded status is not evidence of process death. Fix shape: the test trap must stop the supervisor (fleet-streamjson.sh stop, or kill the recorded supervisor pid) before removing the sandbox, and the suite should assert no _supervise process outlives a case; a sweep for existing survivors is needed separately, since three days of them are already resident.
