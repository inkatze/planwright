- 2026-09-24 [planwright] custom-steps REQ-D1.7: on the arm where the runner stops waiting on a timed-out session it cannot stop, a step with on-failure continue lets the abandoned session keep editing the same worktree while later steps and the unit run there; the custom-steps kickoff brief's risk row 15 covers only a late push, not concurrent working-tree edits; the spec's posture is kept and the gap recorded for the next amendment.
