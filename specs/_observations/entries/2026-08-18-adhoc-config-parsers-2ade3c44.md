- 2026-08-18 [planwright] Complexity growth worth watching, noticed during guard-coverage Task 4. The repo is accumulating hand-rolled parsers for external config formats, each with its own documented parse boundary and its own fail-closed rules: check-options-reference.sh (flat `key: value` YAML), check-no-ci-evals.sh (grep over workflow text), the new check-workflow-posture.sh (a block-style YAML subset in awk: indentation, block scalars, flow leaf values, anchors rejected), and guard-coverage Tasks 5 and 11 will each add a mise.toml task-graph parser. They already share failure modes (zero-entity fail-closed, a declared parse boundary, tabs/anchors/multi-doc rejection) and re-solve them independently. Not actionable inside a task — no shared parser exists to reuse and inventing one mid-task would be scope creep — but if a fifth lands, a shared "strict block-mapping reader" helper with one documented boundary is probably cheaper than the fifth boundary doc. Seed material for /spec-draft.
