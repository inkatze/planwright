- 2026-07-22 [planwright] worker-settings.json hard-denies Bash(git merge:*) to protect the never-merge invariant, but that same deny blocks the sanctioned pre-ready main-sync step (git merge origin/main INTO the task branch) that execute-task doctrine mandates before a ready-flip. Deny precedence means no allow rule can carve out the legitimate direction. Fired live 2026-07-22 on inception task 1; workaround was the operator running the merge via a ! command in the worker session. Upstream fix candidates: narrow the deny to protect only merges while on main (needs a hook, globs cannot see the current branch), or move main-sync into a guard-approved helper script the profile allowlists.
