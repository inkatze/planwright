- 2026-09-03 [planwright] tests/test-fleet-registration.sh case e8 (a signalled register must release the shared fleet lock so a later registration can acquire it) failed once under the parallel runner on 2026-09-03 during format-grammar task 3 CI, on a branch that touches no fleet code; the same file passed in the previous full run of the same tree and passed three consecutive isolated runs afterwards. The shape is a timing race between the signal delivery and the lock release under machine load from sibling test files, so the runner can report a red suite for a green branch roughly once in a few runs. Worth a look at the case's signal timing or a bounded wait before asserting the lock is free.
