- 2026-09-12 [planwright] The stall class where a dispatched worker pends before any task work has one design-level cause: worker-permission-ergonomics D-2 chose a PreToolUse hook because 'it sees the fully-expanded command', and that was never true — Claude Code hands a hook the raw tool_input.command (measured on CLI 2.1.270 through a --settings hook), so every $ROOT/scripts/x.sh and P=... && $P/... shape the guard existed to approve deferred by construction, while Claude Code's own classifier refuses the same shapes as 'Contains simple_expansion'. A second, independent gap stacked on it: the guard trusted only the LAUNCHER's root (the dispatch-env wrapper exports its own checkout as PLANWRIGHT_ROOT/CLAUDE_PLUGIN_ROOT), while the skill text a worker runs resolves ${CLAUDE_PLUGIN_ROOT} to the marketplace cache, so even the compliant literal-path call deferred whenever the tower drove a checkout. Reproduced both in isolation (a literal cache-path loop pended through the real launcher; the same loop under the checkout root completed). #441 did not prevent it because it fixed the hook's OWN resolution (the hook now runs) without touching what the hook trusts or what it sees. The silence had its own cause: alarm-scan had no caller anywhere and status rendered a parked worker as running. Fixes: the guard reads the raw command and resolves a same-command trusted-root assignment under strict bounds, trusts every installed root via a shared resolver, launch proves the hook approves the opening resolve-rule-doc.sh call per root before spawning (exit 9 otherwise), the supervisor runs the pending-age scan itself on a tick, and status says awaiting-input.
