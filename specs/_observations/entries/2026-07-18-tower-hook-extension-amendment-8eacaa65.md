- 2026-07-18 [planwright] Decision (Diego, 2026-07-18): extend worker-permission-ergonomics to cover the TOWER, not only workers, via a spec amendment. Context: during the first orchestration run of this spec, the tower own orchestration toolset (tmux relay/observe, claude --worktree worker launches, planwright scripts) was stochastically blocked by the Claude Code auto-mode classifier, and the agent could not self-grant a fix (editing its own settings allowlist is correctly classifier-blocked as self-escalation). Stopgap applied this session: hand-edited .claude/settings.local.json allow rules (machine-local, plugin-version-pinned to 0.21.0, brittle, cannot match dynamic command shapes). Durable direction chosen: ship a tower-settings profile that wires the deterministic worker-command-guard.sh PreToolUse hook for the tower, revisiting the REQ-C1.2 / brief section 7 R7 worker-only scoping. Design nuance for the /spec-kickoff amendment to resolve: the tower safe command set DIFFERS from the worker set (tower needs tmux relay + claude --worktree launches; workers need git/mise/gh), so the same hook known-safe classifier likely needs a tower-oriented extension or a distinct safe-set rather than verbatim reuse, and the worker-only scoping had a security rationale the amendment must consciously re-open. Next step: human runs /spec-kickoff specs/worker-permission-ergonomics in amendment mode; orchestrate never auto-chains kickoff (REQ-J1.3). Test/determinism requirement (Diego, 2026-07-18): whatever ships for the tower MUST be deterministic and tested, mirroring Task 1 — an adversarial test suite over the tower known-safe set (tmux relay/observe, claude --worktree launches, planwright scripts by resolved literal path) with ZERO false-allows, plus an assertion that dangerous orchestration ops (merge/force-push/amend, and any never-merge/never-ready guardrail) are denied and that the tower safe set is allowed deterministically (allow evaluated before the non-deterministic auto-mode classifier). Rationale: the classifier is a Claude Code LLM feature that cannot itself be made deterministic or tested; determinism only comes from fronting it with a tested allow layer (a hook or a shipped profile). The static settings.local.json rules are deterministic-but-brittle (enumerate commands, cannot match shapes) and are the stopgap the tested hook/profile replaces.
