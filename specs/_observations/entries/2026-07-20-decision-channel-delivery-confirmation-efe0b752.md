- 2026-07-20 [planwright] fleet-decision.sh answer closes the fork (first-answer-wins) BEFORE emitting the downward buffer-paste delivery, matching orchestrate-relay's emit-model. A post-claim failure (fs error, or the tower drops the emitted command) leaves the fork closed but the answer undelivered. Task 4 mitigates this: the chosen answer is PERSISTED at <fleet-home>/attention/answers/<worker> and answer returns a distinct exit 4 (consumed-but-undelivered), so the tower can re-emit the delivery from the artifact without re-claiming. A fuller fix (a delivery-ACK / re-deliverable answer state the reconcile drives, or a two-phase claim-then-confirm) is a future enhancement beyond Task 4's Done-when. Seed for a possible fleet-hardening follow-up.
