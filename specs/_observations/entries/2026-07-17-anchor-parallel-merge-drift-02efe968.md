- 2026-07-17 [planwright] Parallel task-PR execution creates content-anchor drift that blocks the next dispatch even when nothing meaningful changed. When two task PRs each carry an expression-only self-re-anchor entry (written by /execute-task on their own branch), whichever merges second leaves the kickoff brief amendment log's file-last anchor entry stale against combined main: the recomputed anchor no longer matches the file-last recorded anchor, so /orchestrate and /execute-task halt Task N at the freshness gate. Observed on fleet-autonomy 2026-07-17: Task 7 (PR #213) and Task 3 (PR #217) merged out of order; Task 7's file-last entry (a9b4689f) predated Task 3's merged tasks.md/requirements.md/test-spec.md changes, forcing a manual /spec-kickoff delta re-walkthrough that appended a current-state reconciling anchor (79ffced3) purely for bookkeeping, with no content change. Friction only, no fix implied; candidate remedies for /spec-draft to weigh: freshness gate matches against any recorded execution-valid anchor entry rather than strictly file-last; or the reconcile writer auto-appends a current-main anchor entry on merge; or /execute-task recomputes its expression-only re-anchor against latest main at merge time instead of branch-cut time.
