- 2026-09-22 [planwright] The advisory sidedness check (scripts/check-sidedness.sh) reports doctrine/interaction-style.md's own Progressive-disclosure line ('Present one layer of detail at a time...') as an emit mandate naming no destination side, so the doctrine that defines the sidedness rule does not yet pass its own check; 19 findings across the live corpus on 2026-09-22, most in skills the operator-dialogue repair tasks own (resume, spec-draft, spec-kickoff, execute-task, self-review, polish, builder). The check is advisory and REQ-I1.4 binds only touched docs, so nothing is violated; the list is the repair tasks' drain queue.
