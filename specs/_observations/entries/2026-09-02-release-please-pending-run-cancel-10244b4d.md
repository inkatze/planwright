- 2026-09-02 [planwright] release-please.yml sets concurrency cancel-in-progress: false with the comment Queue (no cancel) so an in-flight PR update finishes. GitHub cancels any previously PENDING run in the group when a new one queues, so the queue depth is one, not unbounded. Three merges landing close together lose the middle one entirely: its commit gets no proposal and no step summary, and it is covered only if a later run guard sees a closed window. Combined with the absence of any workflow_dispatch trigger there is then no operator lever to re-arm short of landing another commit on main. The comment invites the reader to conclude no work is lost here, which is the same shape as the nothing-is-dropped header claim corrected in Task 9.
