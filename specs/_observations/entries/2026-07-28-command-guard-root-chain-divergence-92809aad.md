- 2026-07-28 [planwright] The two command guards now resolve a trusted planwright installation root DIFFERENTLY: worker-command-guard.sh walks the full codebase chain (PLANWRIGHT_ROOT, CLAUDE_PLUGIN_ROOT, <claude-dir>/planwright, then its own sibling root) while tower-command-guard.sh still reads CLAUDE_PLUGIN_ROOT alone, so a tower whose session does not export that var cannot approve a plugin-script call the worker approves. Deliberate scoping in the defer-fix (the tower had no measured defect and fleet-hardening REQ-C1.2 keeps the tower set tighter), but it is a second axis of drift for the shared-library extraction obs command-guard-engine-dup already scopes: the extraction should own ONE root resolver with per-guard trust-dir policy, not two chains.
