- 2026-09-24 [planwright] doctrine/custom-steps.md leaves these to the resolver, record-helper, execute-task-wiring, kickoff-wiring, worker-guard, and status-verb tasks (custom-steps Tasks 2, 4, 5, 6, 7, 9) or the next amendment rather than growing past its per-file budget: which record field carries a degradation, an unapplied or clamped timeout, an inherited tier, the stop arm, or the names a failed record carries; what a failed record write does to the step and the point; where a resolver exit other than 0 or 1 sends the unit at an in-run point, and whether a resolver exit 1 (park or ask) at a flip point refuses the flip; the exit code of an unwired non-empty list outside check mode; which layer wins the matrix after an adopter list degrades to core; whether malformation is judged over the whole merged catalog or only the entries a point names; whether a human-directed re-run of a halted or asked point is the same firing and what its records replace; whether a flight's convergence takes the main sync and writes PR-body tables; whether a flip point's step table folds into the flipped PR's body; how the preamble reaches an in-session skill step; where a flight halt parks given no tasks.md; which status a PR whose head is outside planwright/ gets; two flippers running one flip point on the same head from different worktrees (last post wins); a failing step at pre-spec-ready-flip under the ready-flip opt-out (what kickoff records, whether the CI gate still runs); whether post-pr's re-emit and draft check run after a halted post-pr step; a printable-only rule for prompt targets and skip reasons; the command-target charset (custom-steps Task 7, scripts/worker-command-guard.sh, owns it); fixtures for both attendance flags together, for a timeout on a continue step attached to an in-session predecessor, for a context value carrying a newline or control byte, for an Awaiting-input entry carrying no target text or excerpt, for a timeout unapplied on a step degraded to in-session, and for the pipeline-entry read (the list ignoring a copy that resolve-rule-doc.sh or an environment arm would reach, a missing or duplicated line counting as a broken install); and the custom-steps test-spec.md REQ-D1.2 scenario needing a headless launch to reach the unattended arm.
