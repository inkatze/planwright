- 2026-09-24 [planwright] doctrine/custom-steps.md leaves these to the resolver, record-helper, worker-guard, and wiring tasks (custom-steps Tasks 2, 4, 5, 7, 9) or the next amendment rather than growing past its per-file budget: which record field carries a degradation, an unapplied or clamped timeout, an inherited tier, the stop arm, or the names a failed record carries; what a failed record write does to the step and the point; where a resolver exit other than 0 or 1 sends the unit at an in-run point; the exit code of an unwired non-empty list outside check mode; which layer wins the matrix after an adopter list degrades to core; whether malformation is judged over the whole merged catalog or only the entries a point names; whether a human-directed re-run of a halted or asked point is the same firing and what its records replace; how the preamble reaches an in-session skill step; where a flight halt parks given no tasks.md; which status a PR whose head is outside planwright/ gets; two flippers running one flip point on the same head from different worktrees (last post wins); a printable-only rule for prompt targets and skip reasons; the command-target charset (custom-steps Task 7, scripts/worker-command-guard.sh, owns it); fixtures for the pipeline-entry read arms (an overlay or environment-root copy ignored, a missing or duplicated line as a broken install); and the custom-steps test-spec.md REQ-D1.2 scenario needing a headless launch to reach the unattended arm.
