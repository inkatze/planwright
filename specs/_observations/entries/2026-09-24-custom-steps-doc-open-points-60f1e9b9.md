- 2026-09-24 [planwright] doctrine/custom-steps.md leaves these to the resolver, record-helper, and wiring tasks (custom-steps Tasks 2, 4, 5, 9) or the next amendment rather than growing past its per-file budget: which record field carries a degradation, an unapplied timeout, an inherited tier, the stop arm, or the names a failed record carries; what a failed record write does to the step and the point; where a resolver exit other than 0 or 1 sends the unit at an in-run point; which layer wins the matrix after an adopter list degrades to core; whether malformation is judged over the whole merged catalog or only the entries a point names; how the preamble reaches an in-session skill step; where a flight halt parks given no tasks.md; which status a PR whose head is outside planwright/ gets; two flippers running one flip point on the same head from different worktrees (last post wins); a printable-only rule for prompt targets and skip reasons; the command-target charset (the worker command guard owns it); and the REQ-D1.2 Gherkin needing a headless launch to reach the unattended arm.
