- 2026-09-12 [planwright] A crashed fleet run left 52 fleet-streamjson.sh supervisor shells alive, spread across every worktree, each still looping against a /tmp/tmp.*/bin/claude sandbox that no longer existed. Nothing reaps them: the supervisor does not check that its own claude binary is still on disk, and no tower sweep looks for supervisors whose sandbox root is gone. They survive the tower that spawned them, so each crashed run adds another layer. Reaping needs no judgment - a supervisor whose sandbox directory is deleted can never do work again - so this is a mechanical sweep the reconcile pass could own, keyed on the sandbox root existing rather than on the recorded handle being observable.
