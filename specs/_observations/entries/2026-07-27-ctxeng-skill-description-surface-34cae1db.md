- 2026-07-27 [planwright] The eleven SKILL.md frontmatter "description" fields are planwright closest analogue to the tool descriptions the Claude-5 guidance targets, and they are the single most always-on instruction surface in the repo: the harness lists every installed skill in every session regardless of which one runs, so all eleven are resident even when no skill is invoked - strictly more always-on than any mandatory-at-start load. Measured: 59 to 169 words each, roughly 940 words total (resume 169, drain 140, execute-task 84, orchestrate 78). check-instructions.sh has no budget class for this aggregate; the words are charged only inside each per-file body count, where they are indistinguishable from procedure. Content-wise they violate guidance point 4 (do not repeat across boundaries): skills/resume/SKILL.md description spends most of its length restating the read-only invariant list that the body then restates again, and skills/self-review/SKILL.md description enumerates the four audit-record artifacts the body specifies. Change: add a warn-only instruction_budget_catalog aggregate over the sum of frontmatter descriptions, plus an authoring rule in doctrine/instruction-hygiene.md that a description carries trigger and boundary (when to pick this skill over its siblings) and never procedure. Risk: a description is a SELECTOR, and over-trimming degrades skill choice rather than skill execution - a failure mode no word budget can see and no existing fixture grades, so this surface specifically needs a kept-eval selection fixture before anything is cut.
