- 2026-09-18 [planwright] The permission record pins the attention store field 10 (the instance id) to a literal dash, so the answer channel staleness guard is a tautology on that route: a worker can answer its own prompt and raise a NEW one between the settling pass reading the row and the claim landing, and the claim answers the new prompt while the settle closes the item that represented the old one. The same gap makes the command line next shows the operator readable only after the lock is released. Both close if a permission record carries a per-prompt identity, but that identity is also what the queue item id derives from, so it changes what counts as one question.
