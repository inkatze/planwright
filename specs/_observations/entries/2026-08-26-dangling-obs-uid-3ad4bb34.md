- 2026-08-26 [planwright] `obs:b63a8778` is cited about twenty times across specs/fleet-lifecycle-closure (requirements.md, tasks.md) and now from doctrine/fleet-coordination-floor.md, but no fragment file with that UID exists under specs/_observations/entries/ or specs/_observations/archive/. The content survives only as a parenthetical inside 50eac4ac's body ("the fleet oracle has no notion of alive-but-finished, which let two completed workers sit unread for two hours in the same session that leaked ~95 worker-hours"), which is where the ~95-hour figure the requirements Goal and the lifecycle-closure floor both quote actually comes from. Neither `check:obs` nor `check:links` catches it: check:obs validates the grammar and uniqueness of files that exist and check:links resolves doctrine cross-references, so a citation to a UID with no fragment passes both. Relevant because the observations store is the provenance mechanism the whole spec format leans on — REQ-A1.2's uniqueness guarantee is worth little if a dangling `obs:<uid>` is indistinguishable from a live one, and the fleet-lifecycle-closure bundle's most-cited incident is the one that cannot be read. Pre-existing and bundle-wide rather than introduced by this branch; surfaced while reviewing Task 1, which propagated the citation into doctrine. Out of Task 1 scope (doctrine only, no store repair). A guard asserting every `obs:<uid>` cited from specs/ and doctrine/ resolves to a fragment would turn this into a mechanical check.
