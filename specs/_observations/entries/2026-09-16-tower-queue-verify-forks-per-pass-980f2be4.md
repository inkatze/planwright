- 2026-09-16 [planwright] tower-comms Task 3 review (performance lens, measured): every queue verb forks one ls per verified path and repeats the pre-lock surface verification under the lock (an idle next: 15 of 67 execs are ls, 8 of them inside the lock; ack: 23 of 154), build_towers_file re-reads the caller's own delivery record because its pipeline loop discards the variables, and tower_shelve_return is resolved by every locked verb though only shelve reads it; one ls over all operands, one verification under the lock, and a knob resolved where it is read would trim every tower-loop pass; deferred as a follow-up, not a defect.
