# Consumed observations

- 2026-06-11 [planwright] The decision-domains catalog's adopter-extension channel (where project-level entries live and how skills merge them with the core seed list) is named abstractly in doctrine but has no config-model home yet; needs pinning when T16's builder consumes the catalog (or in T1's options reference); surfaced while authoring the catalog doctrine (T15). — consumed-by: specs/customization-overlay (2026-06-15)
- 2026-06-11 [planwright] REQ-D1.2's non-convergence rule ("drop or downgrade") has no decision tree (2-of-3 vs 1-of-3 pass agreement); skills will improvise differently; candidate spec amendment surfaced while migrating Validation Rigor (T3). — consumed-by: specs/bootstrap (2026-06-16)
- 2026-06-15 [planwright] Need a sanctioned doctrine/process customization (overlay) mechanism (Diego, 2026-06-15): adopters and the author must be able to layer personal/team preferences (e.g. a per-task review-gauntlet ordering, dispatch-isolation defaults) onto planwright WITHOUT editing core doctrine/skills, so core stays general for adopters (REQ-I class). Today this lives ad hoc in personal memory / dotfiles CLAUDE.md. Plan a dedicated spec: a precedence model (core defaults < adopter overlay < repo overlay), where overlays live, how skills resolve them, and the boundary between "general capability (lands in core, opt-in via config)" vs "personal style (stays in overlay)". Seeds a new post-bootstrap spec via /spec-draft. — consumed-by: specs/customization-overlay (2026-06-15)
- 2026-06-10 [planwright] The hand-drawn ASCII dependency graph in `tasks.md` drifted from the `Dependencies:` lines on first authoring; consider generating the graph from `Dependencies:` lines (or treating the lines as sole source of truth and dropping the drawing). — consumed-by: specs/output-hygiene (2026-07-02)
- 2026-06-12 [planwright] Task 8 defined the observations-log archive-on-consume mechanics inside /spec-draft (consumed entries move to `specs/_observations/archive.md` with a `consumed-by: specs/<spec> (<date>)` annotation, trim rides the spec branch): Task 10's accumulator-taxonomy doctrine doc (in flight in parallel) is the canonical home for that drain ritual and should adopt or supersede the skill-local definition when it lands. — consumed-by: specs/output-hygiene (2026-07-02)
- 2026-06-12 [planwright] Skills cannot relative-link doctrine docs: the writer copies `skills/<name>/` to `<claude-dir>/skills/` but doctrine to `<claude-dir>/planwright/doctrine/`, so a relative link that works in-repo breaks in writer delivery (and check-doc-links cannot guard it). Resolution-path-only referencing is the rule T11's skills follow; T19's onboarding docs should state it for adopter-authored skills. — consumed-by: specs/output-hygiene (2026-07-02)
- 2026-06-16 [planwright] Doctrine cross-reference links into non-doctrine trees pass the in-repo link check but break in writer-mode delivery: `scripts/check-doc-links.sh` resolves `[text](target)` relative to the linking file's in-repo location, where `doctrine/` and `skills/` are siblings under the repo root, so a doctrine→skill link like `guard-catalog.md`'s `[builder skill](../skills/builder/SKILL.md)` resolves fine. But `install.sh` (writer mode) copies `doctrine/` to `<claude-dir>/planwright/doctrine/` and skills to `<claude-dir>/skills/<name>/` (NOT under `planwright/`), so `../skills/...` from an installed doctrine doc resolves to the nonexistent `<claude-dir>/planwright/skills/...`. The link is green in CI yet dead in the delivered artifact. (`../config/...` survives because config installs under `planwright/`; `../specs/...` would be worse — specs are not delivered at all.) This is why task-1's `customization-boundary.md` refers to skills by name in backticks rather than linking them. Candidate: either (a) have check-doc-links validate doctrine links against the delivered layout (or restrict doctrine cross-refs to sibling doctrine docs + `../config/`), or (b) document the convention "doctrine docs link only to what ships beside them post-install." Today only `guard-catalog.md:16` trips it. Surfaced during customization-overlay Task 1 (boundary doctrine doc) self-review convergence. — consumed-by: specs/output-hygiene (2026-07-02)
- 2026-06-17 [planwright] Signed-off kickoff-brief sections accumulate derived-stat / field-list drift when later amendments change the underlying spec files without a mechanism updating the brief. A recent work-fork spec hit three such instances, each caught only by a later review pass and never by a check: a glossary gloss, a requirement field list, and the test-spec coverage-mix counts. Candidate: a validator or /spec-draft check that recomputes brief-embedded derived stats (e.g. the coverage-mix tally) against their source files, or a convention that brief sections cite rather than copy such figures. Clusters with the anchor-drift-guard candidate logged 2026-06-16. Surfaced in a work fork; relocated here for mining. — consumed-by: specs/output-hygiene (2026-07-02)
- 2026-06-28 [planwright] Concurrent-tower annotation drift on one spec: while Task 7 ran, a second tower's UNCOMMITTED edit to the primary checkout's `specs/orchestration-concurrency/tasks.md` annotated completed tasks as `- **Status:** done` + `- **Merged:** PR #N · <date>`, a format the canonical reconcile writer (`tasks-pr-sync.sh`, REQ-B1.1 sole writer) never produces — it emits `- **Status:** Completed · PR #N merged <date>`. Two manual-reconcile annotation styles now coexist in flight on the same spec; they will conflict when both land. `check-ledger` classify() was widened to accept a bare `done` as completion so the guard tolerates the free-form Status reality spec-format declares, but the deeper issue is that manual reconciles (done before T4 ships the level-triggered projector) drift in format. Candidate: T4's reconcile, once merged, should normalize the annotation format; until then manual reconciles should match the `tasks-pr-sync` output string. Surfaced during orchestration-concurrency /execute-task Task 7 convergence (2026-06-28). — consumed-by: specs/output-hygiene (2026-07-02)
- 2026-06-29 [planwright] annotation-ownership gap surfaced during orchestration-concurrency Task 4: the reworked `tasks-pr-sync` reconcile writes section *placement* only (REQ-B1.1) and preserves each block's Status/Last activity annotations byte-for-byte; the old edge-triggered hook used to rewrite the Status line to `Completed · PR #<n> merged <date>` on `gh pr merge`. In the level-triggered model the derivation has no PR number (it reads git, not annotations — D-1), so a merged task moved to `## Completed` keeps whatever Status annotation /execute-task last wrote (typically `PR #<n> draft`). The placement (section membership) is authoritative and correct; the annotation is now cosmetically stale until refreshed. /execute-task owns the draft annotation but never merges, so nobody refreshes the completion annotation. Candidate: have the merge-triggered hook path (which carries the PR number in its event) refresh just the triggering task's Status annotation while the reconcile owns placement, or have `--bookkeeping`'s merged-PR pass stamp `Completed · PR #<n> merged <date>` for tasks it moves to Completed (it can look up the number via `gh pr list --json number,headRefName`). Out of scope for Task 4 (placement-only per REQ-B1.1; no Done-when/test-spec line requires annotation rewriting; coupling the reconcile to a gh-per-task lookup would break no-remote-first-class). Surfaced during orchestration-concurrency /execute-task Task 4 (2026-06-29). — consumed-by: specs/output-hygiene (2026-07-02)
- 2026-06-29 [planwright] convention-gap(gate-wiring): `[pending-sign-off]` commit-subject marker placement is inconsistent across the repo's own history (19 marked commits). `doctrine/gate-wiring.md` says "the commit subject ends with the `[pending-sign-off]` marker", and some commits conform (e.g. `a6908d2` `docs(...): ... [pending-sign-off]`), but others — including this branch's `fd91ada` and the three Task-4 self-review commits that followed its precedent — place it right after the `type(scope):` prefix (`fix(scope): [pending-sign-off] desc`). The checklist regeneration greps for the marker anywhere in the subject, so both styles work functionally; the gap is purely the doctrine wording vs. practice. Candidate: either relax gate-wiring.md to "the subject carries the `[pending-sign-off]` marker" (placement-agnostic) or add a lint that enforces one canonical position so the convention stops drifting. Surfaced during /self-review over the orchestration-concurrency Task 4 branch (2026-06-29). — consumed-by: specs/output-hygiene (2026-07-02)
- 2026-06-29 [planwright] convention-gap(committed-artifacts): the `[[name]]` memory-link syntax (a private cross-session memory convention) appears in committed planwright spec artifacts — `orchestration-fleet/requirements.md` Sources and `design.md` cross-cutting cite `[[workflows-not-plugin-invocable]]`, and `/spec-draft`-emitted prose carries it. These links resolve only against the author's machine-local memory store, so a spec reader (or adopter) cannot follow them; the documentation lens flagged them as unresolvable references in a committed artifact. spec-format's citation syntax has no `[[ ]]` kind. Candidate: either define a sanctioned way to reference recorded observations/memories from a committed spec (e.g. a Sources entry pointing at the observations log line), or have `/spec-draft` neutralize `[[ ]]` into plain prose + a Sources pointer before commit. Surfaced during the orchestration-fleet `/spec-kickoff` documentation lens pass (2026-06-29). — consumed-by: specs/output-hygiene (2026-07-02)
