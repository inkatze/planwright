- 2026-07-21 [repo] EXECUTION BACKENDS (operator-scoped 2026-07-21, from the fable fleet-backend research + operator additions): planwright hard-codes tmux as both execution and observation substrate; the research (two verified reports, CLI v2.1.217, probes at /tmp/sdk-probe*) proved a stream-json worker is the SAME binary/harness/full-session (SessionStart hooks fire in -p; persistent session_id; --resume recovers; structured tool_use/result/rate_limit_event; can_use_tool control_requests carry full permission payloads AND AskUserQuestion forks 1:1 with the decision-queue schema; gotcha: --permission-prompt-tool stdio pends INDEFINITELY if unanswered — harness must couple can_use_tool receipt with a decision-queue item + pending-age alarm), and `claude agents --json` is an authoritative busy/blocked oracle for the EXISTING tmux fleet (retires pane-scrape false idles TODAY). Spec scope (working title execution-backends; supersedes the empty background-agent-backend stub; fold-check vs orchestration-fleet which owns the backend seam/capability-advertisement/degrade ladder, and vs fleet-autonomy which owns the MODEL axis): (A) backend capability contract + registry — subagent / headless-oneshot / stream-json-persistent / tmux-interactive, each advertising can_observe, can_steer_inflight, session-grade, human-attachable, overhead (subagent IS steerable via resume-with-context but NOT session-grade: dies with the parent). (B) task-execution backend = OPERATOR DEFAULT config knob, default full-session — tasks are beefy enough to always warrant one; NO per-task auto-pick, NO per-task prompts; overridable globally/per-spec. (C) on-demand offload command (/offload) with adaptive selection ONLY here (ad-hoc petitions): tower picks the backend from the ask/workflow or ASKS when unsure; two axioms — (1) tower-frugality: the tower does work inline ONLY for pure reasoning over existing context + the operational heartbeat (bounded small-result state checks + reserved actions where the tower is the decision-maker; subagent fixed overhead makes sub-1KB lookups negative-ROI to offload, and decision-critical verification evidence should be first-hand), everything with large/unpredictable context ingestion offloads (it is the raw material that kills tower lifespan, not the answer — a tower holding only distillates is cheap to hand off, strengthening disposable-tower); (2) smallest-sufficient-rung: subagent unless the work must survive the tower / be human-attachable / run beyond the session — the selection-side mirror of degrade-capability-never-safety. (D) backend-agnostic worker status view for non-tmux humans (and the operator once streaming makes peeking hard), sourced from claude agents --json + the event stream + the attention store; form-factor fork: CLI table vs rendered dashboard vs both phased. Risks/watch: --bare may become the -p default (pin non-bare); Agent Teams experimental (one-team-per-session, no resumption) — watch, do not adopt; SDK-as-library subscription-auth terms unsettled (drive the installed CLI instead).
Consumed-by: specs/execution-backends (2026-07-21)
