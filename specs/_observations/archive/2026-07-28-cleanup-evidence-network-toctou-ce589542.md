- 2026-07-28 [planwright] fleet-cleanup.sh worktree now performs a NETWORK round-trip (gh pr view) between reading the target worktree HEAD and running git worktree remove, materially widening the validate-then-act window the existing cleanup-guard-adversarial-toctou observation describes: that window was previously local-only and sub-millisecond, and is now bounded only by gh latency (and unbounded if gh hangs, since the call carries no timeout -- matching release-arm.sh, which also calls gh untimed). Not a hole today: a hang is fail-safe (nothing is reclaimed) and the dirty check already ran, so the realistic loser is a worker that goes clean -> commits -> clean inside the window. Surfaced reviewing PR #328 (--merged-pr evidence class). Follow-up if the threat model ever widens past accident-prevention: re-read HEAD under the act-time path and compare before removal, and consider a bounded gh timeout so a stalled forge cannot stall a sweep.
Consumed-by: specs/fleet-lifecycle-closure (2026-08-18)
