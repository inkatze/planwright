- 2026-07-15 [planwright] invariant-tasks Task 5 (REQ-B1.5, REQ-C1.3) changed drain-gates.sh semantics that doctrine/accumulator-taxonomy.md still describes pre-change: the lane definitions have no UNRESOLVED concept (PENDING/DORMANT rows can now carry an 'unresolved (completion evidence unavailable)' clause when v2 completion evidence is unavailable), and the 'single pass (one awk read, so no two parse phases can see different file versions)' claim is stale - evaluation now spans the FV read, the v2 pre-parse, the engine's own read, and the main parse, guarded by the widened digest bracket (detection, not prevention). Doctrine needs a delta to record the accepted behavior; also fold the meta-select unexpected-selector-exit fail-open (only rc 2 fails closed; a crashed selector's rc falls through to not-a-candidate) as a hardening candidate.
Consumed-by: specs/format-grammar (2026-07-16)
