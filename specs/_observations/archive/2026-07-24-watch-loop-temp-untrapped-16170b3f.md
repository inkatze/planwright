- 2026-07-24 [planwright] Task 8 hit and fixed a signal-cleanup gap in its own atomic-write path (a TERM landing mid-render abandoned the .planwright-dash temp beside the operator target; PENDING_TMP now publishes it to the INT/TERM/EXIT traps). The same shape exists in at least one sibling and was left alone as out of this branch scope: fleet-attention-watch.sh:98 mktemps awf_tmp beside its target and removes it only on the two normal return paths, with no signal trap covering it, and the dp_snap temps at :178 are likewise trap-free. Those scripts are long-running watch loops stopped by signal, so the exposure is the same one Task 8 just closed: one abandoned dotfile per stop, accumulating in a fleet directory nobody inspects. Worth a sweep of every mktemp-beside-target site in scripts/ (fleet-audit.sh:277 and fleet-attention.sh:382/807/851 are the other instances) to decide whether the trap-registration pattern should be a shared helper rather than repeated per site. Seed material, not a Task 8 deliverable.
Consumed-by: specs/fleet-lifecycle-closure (2026-08-18)
