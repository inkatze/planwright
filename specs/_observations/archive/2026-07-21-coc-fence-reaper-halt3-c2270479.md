- 2026-07-21 [repo] concurrent-orchestrator-coordination kickoff run-3 HALTED (fail closed, no anchor): a third genuine inconsistency on the work-division correctness axis, plus a validated ~18-finding backlog. Headline: REQ-C1.3 promises "a crashed tower never strands a unit" but D-8/D-7 strand it — the origin fence ref is zero-commit until PR-open (execute-task pushes at PR-open, SKILL.md:349), the D-7 reclaim guard blocks on mere ref existence with no stale-break, and NO GC sweep touches origin refs (all three sweeps are machine-local). OC recovers this via a checkout-local dispatch-marker stale-break (design.md:96-100) that does not fire cross-clone under D-3 separate clones — the mirror image of the run-2 halt. Crux: safe recovery needs a cross-clone WORKER-liveness signal (not tower). New AUTHORITATIVE-guarantee breaks beyond the strand: A1 lost-CAS-ACK strands a live tower (fence ref encodes no owner, all push identical base-sha); A2 cohesion-bundle non-lead units are unfenced (one branch/PR per bundle) -> genuine double dispatch; A3 no-remote multi-tower fails OPEN (fence needs a remote; precondition declared not enforced). Spec-vs-shipped-code: S1/S2 the mandated "rename worktree branch before fence push" step does not exist on fleet-dispatch-worktree.sh (creates canonical branch directly) and is only needed on claude --worktree/tmux; the fence push is never sequenced before worker launch, so "abort before launching a worker" is unproven. Plus Q1 quarantine "repeated across passes" has no state store in the stateless-tower model (never quarantines -> invisible permanent strand), Q2 permanently-unknown-liveness + dead-letter no-GC residues, and robustness U1-U3/S3-S5. Full detail in kickoff-brief.md section 8 (run 3). Route to /spec-draft: lead with the correctness model AND reconcile it with the shipped dispatch primitives this time.
Consumed-by: specs/concurrent-orchestrator-coordination (2026-07-21)
