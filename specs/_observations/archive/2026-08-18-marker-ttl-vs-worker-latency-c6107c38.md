- 2026-08-18 [planwright] fleet session 2026-08-18: orchestrate-marker staleness aged out a dispatch marker (~6.5h old) while its stream-json worker was still alive and pre-first-commit, so orchestrate-state re-derived the task as ready and orchestrate-meta-select re-offered an in-flight unit — a double-dispatch hazard the meta-tower caught only from session knowledge plus fleet-streamjson.sh status liveness. Gap: the derivation treats a stale marker as absence-of-dispatch without consulting the backend liveness evidence that exists in the streamjson store (supervisor/worker pids, journal). Candidate fixes: derivation consults backend liveness before demoting a stale marker (positive-evidence-of-death discipline, mirroring the reconcile sweep orphan predicate), or the supervisor refreshes the marker as a heartbeat while the worker lives, or first-commit latency bounds the marker TTL. Long opus /execute-task runs routinely exceed hours before their first branch commit.
Consumed-by: specs/tower-front-door (2026-08-27)
