- 2026-07-24 [planwright] The fleet attention surface reports liveness but not work-progress, and that is the first thing an operator asks for. fleet-status.sh render gives WORKER/SCOPE/STATE/SJ/PEND with an elapsed counter (working(1139s)) and nothing about which phase of /execute-task a worker is in (writing the failing test, implementing, running CI, in /polish). During a live 3-worker fleet run the operator asked "do we have any kind of dashboard to see where those sessions are at in terms of work?" and answering required hand-parsing each worker events.jsonl for recent tool names and the last assistant text. Useful signals available cheaply from the event stream: recent tool-use histogram, last assistant text line, task_started/task_updated subtypes, and commit count on the unit branch. Feeds execution-backends Task 8 (rendered status dashboard) requirements. Secondary note: fleet-status.sh render mixes in machine-wide oracle sessions from unrelated repos under "sessions (oracle, unjoined)", which is noise when scoping to one fleet. Observed 2026-07-24.
Consumed-by: specs/fleet-lifecycle-closure (2026-08-18)
