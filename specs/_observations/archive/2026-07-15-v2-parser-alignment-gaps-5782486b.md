- 2026-07-15 [planwright] Task 5's self-review hardened orchestrate-select.sh and drain-gates.sh v2 parsing (fence-as-illustration for parked maps and Format-version, CRLF section trim, prose-bullet tolerance per the validator's inner-whitespace rule), but the sibling parsers still diverge: spec-status.sh's parked-map awk has no fence guard, no CRLF trim on section headings (a CRLF checkout classifies no bullet, so a live Awaiting-input bullet cannot block derived Done - REQ-B1.6's inverse), and warns on plain prose bullets; spec-validate.sh's reference-bullet scan is fence-blind (a fenced example bullet can raise a spurious duplicate error). Align all four parsers on one posture; consider extracting the parked-map parse into a shared sourced helper (echo-safety.sh pattern) instead of a fourth copy.
Consumed-by: specs/format-grammar (2026-07-16)
