- 2026-09-06 [planwright] scripts/fleet-state.sh bound-incr can grant PAST its bound under contention, and it is reproducible rather than theoretical: driving the primitive directly (20 concurrent bound-incr against max 5, unmodified origin/main d094274, no test harness involved) produced 6 grants in 1 of 15 trials on a 12-core box at load average ~10. No invocation exited 2, so this is not spin_acquire budget exhaustion; it matches the stale-lock break the script's own header already documents as a deferred limitation awaiting the lock-family owner-token redesign. What is new is the consequence: it surfaces as an intermittent CI failure in tests/test-fleet-state.sh case 9 ('bound race: 6 grants, expected exactly 5'), a case NOT on the known-load-sensitive list, so it reads to a worker as a real defect in whatever branch happens to be under test. Case 8 in the same file (registry race, 19 of 20 records) is the already-known flake and fails far more readily, 3 of 3 standalone runs at the same load. Not fixed here: fleet-state.sh was outside this branch's remit.
Consumed-by: specs/universal-binary (2026-09-08)
