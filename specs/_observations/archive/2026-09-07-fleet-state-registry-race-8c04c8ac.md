- 2026-09-07 [planwright] tests/test-fleet-state.sh case 8 (registry race) fails deterministically on this host, not only under parallel-suite load: three consecutive isolated runs of the file alone gave 19, 18 and 18 records against an expected 20. A varying shortfall across runs is a lost-write race in the registry append path of scripts/fleet-state.sh under concurrent writers, rather than harness scheduling noise, so the standing characterization of this suite as a load-sensitive flake understates it. Observed from the model-allocation Task 7 branch, which changes no file under scripts/ (scripts/fleet-state.sh and tests/test-fleet-state.sh are byte-identical to origin/main), so the branch cannot be the cause. Not investigated further: out of scope for a documentation task.
Consumed-by: specs/universal-binary (2026-09-08)
