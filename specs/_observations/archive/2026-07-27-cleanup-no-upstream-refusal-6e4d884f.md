- 2026-07-27 [planwright] fleet-cleanup.sh worktree refuses the MOST COMMON stale-worktree shape: a worktree whose PR merged and whose remote branch GitHub then auto-deleted. Its pushed-proof is "has an upstream and HEAD equals it", so a deleted upstream reads as "cannot prove it is pushed, refusing" even though a STRONGER proof exists -- the branch tip oid equals the merged PR headRefOid (gh pr view --json headRefOid), i.e. the content is provably on main. Hit on all five leftover worktrees at the 0.32.0 replacement-tower pre-flight; the tower could not reclaim any, which blocked two dispatch slots via the task-<id> suffix collision (see worktree-suffix-cross-spec-collision). Follow-up: admit merged-PR-head-oid equality as a second positive-evidence path in the pushed check, or add a --merged-pr <n> evidence flag the caller must substantiate. Keep the refusal default; the gap is the missing evidence CLASS, not the strictness.
Consumed-by: specs/fleet-lifecycle-closure (2026-08-18)
