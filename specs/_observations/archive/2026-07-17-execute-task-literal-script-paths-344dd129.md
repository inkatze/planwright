- 2026-07-17 [planwright] CONFIRMED empirically (instruction-headroom Task 1, tmux, fat allowlist): broadening the worker allowlist to cover read-only shell + literal-path scripts silences plain commands (the workers first compound `git branch; echo; git status; ls specs/` ran with zero prompts), but every command that invokes a script or path through a shell variable — e.g. `ls "$PLUGIN_ROOT/scripts/"`, `$PLANWRIGHT_ROOT/scripts/x.sh` — is flagged "Contains simple_expansion" by Claude Code and prompts with NO persistent-allow option, regardless of any allow rule (the rule is matched against the literal `$VAR`, never the expansion). Durable fix: execute-task (and sibling skills spec-kickoff/orchestrate that shell out to plugin scripts) should resolve the plugin/planwright root ONCE and invoke scripts by the resolved literal absolute path, so a literal-path allow entry like Bash(/Users/.../plugins/*/scripts/*.sh:*) actually matches and non-auto workers stop flooding. This is the root cause of the tmux-worker-prompt-flood and the higher-value half of that fix.
Consumed-by: specs/worker-permission-ergonomics (2026-07-18)
