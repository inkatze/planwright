- 2026-07-15 [planwright] The Format-version extractor shared across spec-status.sh, tasks-pr-sync.sh, and check-ledger.sh (and spec-validate first_header) matches the first `**Format-version:**` line anywhere in the file, not only the header block; a body line at column 0 starting with that literal would mask a missing header declaration instead of failing closed. Family-wide trait predating invariant-tasks Task 4 (flagged at its review, declined as out-of-unit: fixing one copy would desynchronize siblings); a shared header-block-scoped parse is the fix shape.
Consumed-by: specs/format-grammar (2026-07-16)
