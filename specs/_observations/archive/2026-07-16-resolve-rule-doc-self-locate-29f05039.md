- 2026-07-16 [repo] resolve-rule-doc.sh computes script_dir (line ~107, used to locate sibling resolve-overlay-root.sh) but the core-doctrine fallback loop (for root in PLANWRIGHT_ROOT/CLAUDE_PLUGIN_ROOT/writer_root) never checks "$script_dir/..", so when Claude Code does not export CLAUDE_PLUGIN_ROOT into skill Bash subshells (observed unset in tmux workers and tower), the first doctrine lookup fails until the model hand-exports a root. Four sibling scripts (builder-guards, resolve-review-sequence, resolve-overlay-root, config-get) already self-locate via script_dir/... Fix: append "$script_dir/.." as the final core fallback (additive, lowest precedence, subsumes plugin+writer roots; passes existing test #11 which uses a fake doc name). Being fixed on branch fix/resolve-rule-doc-self-locate.
Consumed-by: specs/skill-rigor (2026-07-21)
