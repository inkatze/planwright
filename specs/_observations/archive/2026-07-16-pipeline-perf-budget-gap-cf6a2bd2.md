- 2026-07-16 [planwright] Review doctrines missed the ~12-min serial test suite because every lens is diff-scoped (Discovery Rigor Performance lens + Refactor Instinct review-mode explicitly exclude pre-existing whole-system properties) and tool-grounded discovery only flags pass/fail. There is no test/CI wall-clock budget gate (unlike check:instructions for prompt size), so accumulated suite slowness stays invisible until the gauntlet's ~10x per-PR reruns amplify it (boiling-frog). Direction: add a test-time/perf budget to the tool-grounded gate (a check:test-time analog to check:instructions) and name test/CI ergonomics as an explicit Performance-lens target so wall-clock regressions become tool-grounded findings. The parallelize-suite chore treats the symptom; the budget gate closes the loop.
Consumed-by: specs/guard-coverage (2026-07-17)
