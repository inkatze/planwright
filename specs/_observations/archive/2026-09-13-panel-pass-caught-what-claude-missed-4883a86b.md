- 2026-09-13 [planwright] The non-Anthropic panel pass earned its place on the lock-primitive branch, and the Claude-side passes did not substitute for it. universal-binary Task 1 went through polish and a nine-lens self-review fan-out that found and fixed real defects with working exploits, and the orchestrator then flipped the PR ready. A gemini panel pass afterwards found four more, two of them serious and both reproduced locally: pw_lock_acquire_for returns success while assigning the hold to nobody and leaving the caller unable to release, wedging the lock permanently; and pw_lock_clear_legacy restores with mv -f where its own adjacent comment promises the restore fails if the path was retaken, so a peer that acquires in the window has its lock overwritten - the clobber-a-successor class the owner token exists to prevent. Two smaller ones followed the same shape: a guard rejecting newlines but not the break-claim delimiter the header warns about, and an unquoted handle split with no set -f. The pattern worth keeping is that three of the four are places where the code contradicts a claim made a few lines above it in its own comment or header. A reviewer sharing the authors training distribution reads the comment and the code as agreeing; a different lineage reads them as two statements to compare. The orchestrator also flipped the PR ready before the panel and Copilot passes ran, which asserted a completeness that did not exist - the ready-flip should follow the configured gauntlet, not the configured review_sequence alone, since the two non-nestable passes are exactly the ones review_sequence cannot run.
Consumed-by: specs/custom-steps (2026-09-22)
