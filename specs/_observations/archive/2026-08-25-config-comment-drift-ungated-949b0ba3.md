- 2026-08-25 [planwright] config/defaults.yml carried a factually wrong comment ("Effort and command are fixed table cells (code, not config)") from fleet-autonomy Task 7 through Task 10, which made both overlay-tunable and added a contradicting block directly beneath it. Nothing caught it: check-options-reference validates that every KEY has a reference row, but no guard reads the prose around the keys, so a comment can contradict the file it annotates indefinitely. The same class applies to script headers describing tables that later move. Possible seed: extend the options guard, or a narrower lint over defaults.yml comment blocks that name scripts or claim configurability.
Consumed-by: specs/prose-disposition (2026-09-03)
