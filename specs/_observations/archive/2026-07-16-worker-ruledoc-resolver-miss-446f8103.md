- 2026-07-16 [planwright] In worker worktrees the rule-doc resolver (scripts/resolve-rule-doc.sh) fails for every doctrine doc because CLAUDE_PLUGIN_ROOT and PLANWRIGHT_ROOT are unset in the worker's Bash subshells; it falls back to the empty writer root ~/.claude/planwright and reports not-found. Observed on both #196 (self-review) and #199 (self-review/panel) workers, each self-healing by reading the plugin cache dir directly. Review quality held only because the full rigor doctrine is duplicated inline in the user global CLAUDE.md. Gap: skill-dispatched workers do not inherit the plugin-root env the resolver needs, so any rule-doc resolution in a worktree is degraded-and-self-healed rather than reliable. Direction: export/propagate the resolved plugin root into worker subshells (or have the resolver locate the plugin cache without the env var) so resolution is deterministic, not memory-dependent.
Consumed-by: specs/skill-rigor (2026-07-16)
