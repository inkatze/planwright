- 2026-08-18 [planwright] A deferred permission prompt strands a dispatched worker indefinitely with NO signal distinguishing it from a working one. Observed 2026-08-13: worker-command-guard deferred a command containing $(...) (command substitution, the undocumented-matcher class obs aed9ca33 scopes) and a dispatched research worker sat blocked at the resulting prompt for 55 MINUTES before anyone noticed. Nothing in the fleet oracle separates blocked-at-a-prompt from mid-long-tool-call: both present as an unchanging pane and a live pid, so elapsed-time alone cannot discriminate and every threshold either false-alarms on slow work or misses a real block. This is the permissions gap (obs eea622de, a4a4fa59) meeting the detection gap: the profile that would have pre-approved the command never reached the worker, AND nothing fired when the prompt appeared. Note the bound: cross-session messaging cannot close this half -- a message cannot answer a permission prompt and cannot reach a session blocked rather than working, so the detector must be a local signal (a PermissionRequest/Notification hook push, or a positively-matched prompt signature), never a steer channel.
Consumed-by: specs/fleet-lifecycle-closure (2026-08-18)
