- 2026-08-19 [planwright] In a /spec-kickoff run the operator disengaged from the section-2 opening ("that wall of requirements makes me immediately check out"). The surface had emitted a long goal restatement followed by two multi-option selectors whose stems and previews were dense with REQ-IDs and D-IDs. interaction-style already mandates progressive disclosure, small bites, and self-contained selectors, and kickoff-dialogue requires teaching at the operator frontier, so this was an instantiation failure rather than a missing rule. The recovery that worked: one plain-language question per turn, identifiers parked until traceability needed them, and the agent carrying the derivation (deriving candidate answers from the code) instead of asking the operator to supply a list. Worth considering whether the kickoff surface needs an explicit density bound rather than relying on the general discipline.
Consumed-by: specs/operator-dialogue (2026-08-24)
