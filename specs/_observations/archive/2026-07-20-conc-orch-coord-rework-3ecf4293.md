- 2026-07-20 [planwright] spec-kickoff HALT (2026-07-20): the concurrent-orchestrator-coordination sign-off lens pass found a genuine inconsistency and ~20 coordination-mechanics design gaps; kickoff halted, bundle stays Draft, mechanical hygiene fixed. Rework needed via /spec-draft. Root axes: (1) machine-local vs checkout-local surface — the per-spec advisory lock is <spec-dir>/.orchestrate.lock (checkout-local), so under D-3 separate per-tower clones the claim TOCTOU serialization (REQ-C1.1/D-5) does NOT hold; claim serialization, repo scoping, and user-private access all need a machine-local surface. (2) death-evidence-handle location + claim/presence lifecycle — the death handle lives in the presence file but presence GC deletes it (surviving claim un-reclaimable); reclaim is not serialized (concurrent reclaimers double-dispatch); claims never released on completion/dispatch-failure and dead claims never GCd; reclaim proves the tower dead not its worker. Plus: no repository id in the presence schema (one host-wide path -> cross-repo false peers); fresh-host bootstrap collides with REQ-A1.5 fail-closed; no self-exclusion; no presence-vs-claim file discriminator; meta-tower marker absent from schema; non-atomic writes/torn reads; death-predicate error outcome undefined + heartbeat-liveness shortcut; hung-tower strands units; death-predicate inside lock -> livelock; git-sync (--ff-only optional, merges into current branch, fetch-failure unhandled, merge-vs-rebase test unmeasurable under ff); security bars not imported (echo-safety, per-field validation beyond identity token, path containment, user-private surface, checkout-path leaking into committed PR bodies). Full detail: specs/concurrent-orchestrator-coordination/kickoff-brief.md section 8.
Consumed-by: specs/concurrent-orchestrator-coordination (2026-07-20)
