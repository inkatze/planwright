- 2026-07-15 [planwright] The GATE(when:) status-atom grammar (doctrine/accumulator-taxonomy.md) and drain-gates.sh's stored-status whitelist both predate the six-status lifecycle: 'ready' is absent from each, so a stored-Ready spec's status can never be referenced by a status atom and every Ready spec in a sweep root emits an 'unrecognized status; status atoms referencing it will not evaluate' note. Under format-version 2 every executing bundle is stored-Ready, so the note becomes the common case. Reconcile the grammar (and whitelist) with the kickoff-lifecycle status set.
Consumed-by: specs/format-grammar (2026-07-16)
