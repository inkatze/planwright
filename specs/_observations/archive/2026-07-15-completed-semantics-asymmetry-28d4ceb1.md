- 2026-07-15 [planwright] The two consumers of "task <id> completed" answer it differently by design: drain-gates.sh applies reference-bullet authority to gate atoms (a parked-after-merge task stays unmet, v1 parity with ## Completed membership), while orchestrate-select.sh dep-satisfaction uses the raw engine completed set (REQ-C1.2 letter; v1 parity, since v1 deps already used engine evidence) - so a v2 dependent of a re-parked merged task is still dispatchable while its gate stays PENDING. Both surfaces preserve their own v1 semantics and the asymmetry predates v2, but the spec never states it; a REQ-C1.2/B1.4 clarification sentence would keep a future reader from "fixing" either side into the other.
Consumed-by: specs/format-grammar (2026-07-16)
