- 2026-07-13 [planwright] Diet reflow of a SKILL.md can break structural prose guards (tests/test-*-status-*.sh that grep the skill body). Two modes hit in Task 6: (1) a line-wrap inside a grepped phrase inserts the continuation-line indent, so a single-space regex misses even after newline-flattening (keep pinned lists/phrases unbroken on one line); (2) condensing a contract sentence can silently invert meaning (wrote reconcile "(and the Status text) is the sole job", implying it authors Status, vs the correct REQ-B1.1 "owns placement only, does not author Status"). Sibling diets (Tasks 5/7/7.5) MUST run the content-pinned tests; local mise `test` hangs on this machine so rely on GitHub CI or run the specific tests/test-*.sh standalone.
Consumed-by: specs/instruction-headroom (2026-07-16)
