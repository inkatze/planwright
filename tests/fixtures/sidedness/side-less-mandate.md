# Planted sidedness fixture

This file lives under `tests/`, outside the corpus the sidedness check scans by
default, so the live run never reports it. The unit test passes it explicitly.

## Loop end

Present the four tables and the declined log.

Present the counts and the pending sign-offs to the operator.

The skill must emit the full CI output.

Emit the lens-coverage table artifact-side, recorded in the draft PR body.

- **Handoff.** Show the whole audit record set.

```sh
# Inside a fence the scanner stands down:
Present the four tables.
```

Never paste a wall of everything at once.
