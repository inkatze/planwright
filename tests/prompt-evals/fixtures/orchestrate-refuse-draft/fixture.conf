# orchestrate-refuse-draft — /orchestrate must refuse a non-Ready/non-Active spec.
# Observable graded outcome (D-12): the Draft spec is refused with no dispatch
# record (no marker, no branch). Pre-diet baseline subject is /orchestrate.
#
# prompt.txt is the literal slash-command invocation (Task 5 erratum): headless
# -p has no Skill tool, so only CLI expansion of a leading slash command puts
# the SKILL.md in context; the `skill=` key drives the runner's skill-injection
# validity check (transcript must carry the SKILL.md H1, else INVALID).
id=orchestrate-refuse-draft
skill=orchestrate
k=3
max_budget_usd=0.30
max_turns=25
