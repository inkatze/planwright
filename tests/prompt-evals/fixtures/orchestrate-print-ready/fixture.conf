# orchestrate-print-ready — /orchestrate print-backend dispatch of one Ready unit.
# Observable graded outcomes (D-12): correct unit selected, dispatch marker
# written, launch command printed. Pre-diet baseline subject is /orchestrate.
#
# Cap recalibrated 0.50 -> 1.00 at Task 5 (a Task 4 authoring erratum): the cap
# was set before any real run, and both the pre-diet baseline (~$0.61/run) and
# the post-diet re-run (~$0.62/run) hit it, so no run was ever behaviorally
# graded (a cap-hit skips probe + assert). $1.00 gives graded headroom while
# the suite ceiling still bounds total spend; cost stays reported, not gated
# (instruction-hygiene kept-eval convention). Both comparison sides were
# re-recorded at this cap on identical fixtures (REQ-D1.3 pairing).
id=orchestrate-print-ready
skill=orchestrate
k=3
max_budget_usd=1.00
max_turns=40
