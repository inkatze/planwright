# orchestrate-print-ready — /orchestrate print-backend dispatch of one Ready unit.
# Observable graded outcomes (D-12): correct unit selected, dispatch marker
# written, launch command printed. Pre-diet baseline subject is /orchestrate.
#
# prompt.txt is the literal slash-command invocation (Task 5 erratum): headless
# -p has no Skill tool, so only CLI expansion of a leading slash command puts
# the SKILL.md in context — the original prose prompt graded a bare model
# improvising over the repo (it confabulated a marker at an invented path).
# The `skill=` key below now drives the runner's skill-injection validity
# check (the transcript must carry the SKILL.md H1, else INVALID, not fail).
#
# Caps recalibrated at Task 5 (a Task 4 authoring erratum, twice measured):
# the original $0.50 was set before any real run and even the first honest
# recalibration ($1.00) was calibrated on bare-model runs — the REAL skill,
# once actually injected, was killed at $1.056/turn 23 while executing the
# flow correctly, ~2 turns from printing the launch command (kept transcript,
# 2026-07-13). A cap the true run cost exceeds masks the pre-vs-post delta
# behind fail-on-cap (a cap-hit skips probe + assert), so the cap must clear
# BOTH sides: post-diet completes ~$1.1-1.3; the pre-diet skill reads more
# run-start doctrine with a 7,200-word body riding every turn, plausibly
# $1.3-2.0. $3.00 clears both with margin; max_turns 60 keeps the dollar cap
# the binding bound. Cost stays reported, not gated (instruction-hygiene
# kept-eval convention); the suite ceiling bounds total spend. Both
# comparison sides run this identical fixture (REQ-D1.3 pairing).
id=orchestrate-print-ready
skill=orchestrate
k=3
max_budget_usd=3.00
max_turns=60
