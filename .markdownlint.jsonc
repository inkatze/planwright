{
  // planwright markdownlint baseline (consumed by mise run lint:md).
  // Markdown tables cannot wrap, so they are exempt from line length.
  // Task 2 (self-hosting CI) owns evolving this config and widening its
  // target set; keep changes there.
  "MD013": { "tables": false }
}
