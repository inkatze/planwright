# planwright options reference

The single canonical reference for every planwright config option (REQ-K1.8,
D-43). The tracked defaults live in [`config/defaults.yml`](../config/defaults.yml);
per-repo overrides live in `<repo>/.claude/planwright.local.yml` (gitignored,
agent-maintained, per-repo entries written only on human confirmation per
REQ-K1.1).

Every option present in the default config must have a row here.
`scripts/check-options-reference.sh` enforces that; planwright's CI runs it
(wired in by the self-hosting CI task) and fails on an undocumented option.

Config-model fallback: a skill that finds a config file absent, unreadable,
or malformed falls back to the tracked defaults below and surfaces a
warning when it reads the config, before any default-driven action fires
(REQ-K1.7).

Format constraints the checker relies on: each row's first table cell must
contain only the backticked option name (annotations go in the Effect
column), and `config/defaults.yml` must stay flat `key: value` lines (nested
YAML keys would be invisible to the parser, which fails closed when it
parses zero keys).

| Option | Default | Effect | Consumed by |
| --- | --- | --- | --- |
| `commit_on_draft` | `true` | Auto-commit the completed Draft bundle after elicitation finishes (D-41). Commit only, never push. | `/spec-draft` |
| `commit_on_kickoff` | `true` | Auto-commit the kickoff brief plus the Draft→Ready status flip after sign-off (D-41). Commit only; the push, draft PR, and terminal ready-flip are separate kickoff steps. | `/spec-kickoff` |
| `commit_on_state_move` | `true` | **v1-only.** Auto-commit `tasks.md` orchestration-state edits — section moves (e.g. `/drain` relocating a block between human-owned sections) and the per-block `Status` / `Last activity` annotations (e.g. `/execute-task` stamping `Last activity`) — with a fixed conventional message (D-41). Vacuous under format-version 2: a v2 bundle commits no derived execution-state edit (the version-keyed machinery no-ops those writes, D-7), so there is nothing to auto-commit and the knob is not consulted. Status is read through the derived render, not a committed snapshot (see [orchestration state](orchestration-state.md)). | `/drain`, `/execute-task` |
| `mark_spec_pr_ready_on_kickoff` | `true` | On a clean kickoff completion, `/spec-kickoff` marks the **spec** PR ready (un-draft) as its terminal step, after the configurable `review_sequence` verification has converged (D-6, D-7) — the narrow exception to bootstrap D-26's all-drafts rule: only the spec PR is readied, task PRs stay drafts, merge stays the human's (never auto-merge). `false` leaves the spec PR draft for the human to un-draft. The flip is also suppressed when sign-off parked on a fork or verification did not converge; a flip failure degrades to `tasks.md` Awaiting input (REQ-D1.2, REQ-D1.3). | `/spec-kickoff` |
| `kickoff_ready_ci_wait` | `10m` | The bounded wait `/spec-kickoff`'s terminal ready-flip holds for the spec PR's head-SHA check rollup to complete before it marks the PR ready (skill-rigor D-3, REQ-B1.1). CI rarely reports within seconds of the push, so the gate polls the **head commit's** `statusCheckRollup` (checks on the SHA, never PR review states — an errored or stale review must not masquerade as a completed check) at a bounded cadence (tens of queries per wait, not hundreds) up to this bound, and flips only on a **positive green** (at least one completed check and overall success); head identity is re-confirmed immediately before `gh pr ready`, so a head that moved during the wait refuses the flip. On red, empty, unresolved, a query failure, an expired wait, or a moved head it leaves the PR draft and records the pending ready-flip in `tasks.md` `## Awaiting input` as the re-entry point (naming the pending flip and the neutral failure class only; full remedy detail stays operator-facing, D-3). Same `<n>[m]` minutes convention as the `stale_*_threshold` knobs; an absent or malformed value falls back to the default with a one-line warning. Read at pre-flight step 4 alongside `mark_spec_pr_ready_on_kickoff`; the gate is skipped cleanly when the upstream no-remote/no-PR degradation arm already fired. | `/spec-kickoff` |
| `dispatch_backend` | `full-session` | Which dispatch backend `/orchestrate` uses for execution units (D-38, REQ-F1.8; execution-backends D-8, REQ-B1.1–B1.5). Values: the **semantic** `full-session` (default), or an explicit literal — `subagent`, `tmux`, `print`, `in-session`, the contract-defined `stream-json-persistent` and `headless-oneshot` rows, or a pluggable adapter name. `full-session` resolves at dispatch time, via the contract's pinned degradation ladder, to the richest **present** session-grade backend: unattended, the richest **non-interactive** one (never a silently-chosen interactive backend, REQ-B1.4); attended, the same, except the once-per-tower-session tmux-context ask (D-8: when the tower itself runs inside tmux, the operator is asked whether tmux joins the candidate set — non-blocking, unanswered resolves unattended, the answer persisted spec-locally); a host advertising no session-grade rung degrades to the pre-flip behavior (`subagent`, then `in-session`). An **explicit literal is honored-or-halted** (REQ-B1.5): present, it is used verbatim (interactive included — explicit config is the operator's standing answer); not advertised on the host, the dispatch fails closed to Awaiting input naming the missing backend, never a silent substitute (dispatch-time ladders apply to semantic values only; mid-run runtime failover keeps its logged one-rung descent). **Migration note:** the default flipped `subagent`→`full-session` in execution-backends Task 5 — a declared departure from the default-preserving rule, operator-approved in that spec's primary seed and softened by the ladder; restore the old default with `dispatch_backend: subagent` in an overlay. `stream-json-persistent` autodetects present when the `claude` CLI is on PATH; `headless-oneshot` reports absent until its dispatch support lands. Each value maps to an advertised capability set per the [backend capability contract](../doctrine/backend-capability-contract.md) (D-2). Resolved through `scripts/resolve-dispatch-backend.sh` (per-spec override, by-layer malformed policy, ask-state) over `scripts/orchestrate-backends.sh select-unattended` (ladder + honored-or-halted); attended runs with **no** configured semantic/explicit preference still get the present-and-ask flow. | `/orchestrate` |
| `dispatch_backend_per_spec` | `{}` | Per-spec `dispatch_backend` overrides (execution-backends D-9, REQ-B1.3): a flat inline map of `{<spec>: <backend>}` entries riding the config overlay layers as one key (the highest-precedence layer that sets it supplies the whole map, D-5 last-layer-wins). A per-spec entry **wins over the global `dispatch_backend` value in every layer combination** — specificity beats layer precedence across the two keys; a spec with no entry falls through to the global value. Entry values take the same forms as `dispatch_backend` (`full-session` included) with the same fail-closed rule for unadvertised literals (REQ-B1.5). Kept in config, not in the spec bundle: a backend preference is execution policy, not signed spec content (D-9). A malformed map follows the REQ-E1.4 by-layer policy (degrade+warn for adopter/machine-local, hard-fail for repo-tracked). Resolved through `scripts/resolve-dispatch-backend.sh`. | `/orchestrate` |
| `dispatch_isolation` | `per-step` | How `/execute-task` sequences a unit's steps (D-5, REQ-C1.3): `per-step` (default) runs implementation and each configured `review_sequence` skill in its own fresh `/resume`-seeded session, so context stays bounded and each review's perspective is uncontaminated by prior steps (backends that cannot spawn a fresh session approximate it via context clears between steps); `per-unit` keeps today's single-session behavior for the whole unit. The `per-step` default is the assigned human decision; `per-unit` stays supported for constrained hosts. Resolved through `scripts/resolve-dispatch-isolation.sh`; a value other than `per-step`/`per-unit` is malformed under the REQ-E1.4 by-layer policy (degrade+warn for adopter/machine-local, hard-fail for repo-tracked). The default-flip from the historical `per-unit` behavior is tracked cross-spec as the required bootstrap D-38 amendment. | `/execute-task` |
| `context_budget_threshold` | `50` | When a long-running tower auto-heals (D-4, REQ-C1.1): a completed-step budget — the number of orchestration steps a tower runs before it hands off to a fresh tower (the [continue-as-new](../doctrine/context-budget-autoheal.md) pattern), so it never silently degrades from context exhaustion. The signal is a step-count proxy because Claude Code exposes no supported live context-usage introspection (the transcript is internal/unstable; no env var, hook field, or CLI query for remaining budget — Task 5 research). `off` disables auto-heal (the historical single-tower behavior). The default is conservative because the handover is cheap and lossless (the fresh tower rebuilds from durable state), so handing off early is the safe direction. Resolved through `scripts/resolve-context-budget-threshold.sh`; a value other than a positive integer of at most 15 digits or `off` is malformed under the REQ-E1.4 by-layer policy (degrade+warn for adopter/machine-local, hard-fail for repo-tracked) — the 15-digit cap is an overflow guard that only rejects typos far larger than any real step budget, so use `off`, not an extremely large integer, to disable auto-heal. `scripts/context-budget-monitor.sh` evaluates a step count against it. | `/orchestrate` |
| `max_parallel_units` | `3` | Concurrency cap on simultaneously in-flight execution units, per spec (REQ-F1.8). Resolved via `config-get` through the overlay layers; an absent key keeps the default, `0` is honored as a paused spec (no new dispatches), and the meta-tower's selector (`scripts/orchestrate-meta-select.sh`, which honors this per-spec cap alongside the fleet bound) warns and falls back to the default on a malformed value — anything other than `0` or a non-zero integer with no leading zeros (the leading-zero exclusion keeps shell arithmetic from reading the value as octal). | `/orchestrate`, `scripts/orchestrate-meta-select.sh` |
| `fleet_max_parallel_units` | `3` | Fleet-level concurrency bound for the meta-tower ("tower of towers", `/orchestrate --meta`, D-6, REQ-D1.5): the maximum number of in-flight execution units summed across **all** specs the meta-tower supervises — distinct from `max_parallel_units`, which caps a single spec. At or over the bound the meta-tower holds this step (a ready unit in some spec waits) rather than exceeding fleet-wide concurrency; the cap is enforced against the live cross-spec derivation, so it rebuilds from disk after any crash. The default is conservative — `3` holds total fleet concurrency to a single spec's worth, so enabling the opt-in meta-tower never multiplies load until an operator raises it. Resolved through `scripts/orchestrate-meta-select.sh` via `config-get`; an absent key keeps the default, a malformed value warns and falls back. | `/orchestrate` (`--meta`), `scripts/orchestrate-meta-select.sh` |
| `stale_lock_threshold` | `15m` | Age past which a per-spec advisory lock is treated as stale and may be broken by the next runner (D-10). | `/orchestrate`, `tasks-pr-sync` hook |
| `stale_marker_threshold` | `15m` | Age past which a runtime dispatch marker whose branch carries no commits is treated as stale, reverting the task to Ready so a dispatch that crashed before its first commit cannot wedge it In progress (orchestration-concurrency D-3). | `scripts/orchestrate-state.sh` (task-state derivation) |
| `dispatch_fetch_ttl` | `2m` | TTL bounding the fetch-before-gate dispatch primitive: before the freshness gate and merge detection run, `/orchestrate` and `/execute-task` fetch `origin` so both evaluate against a current `origin/main` without advancing local `main` (fleet-hardening D-9, REQ-D1.1). A successful fetch is stamped; a gate within this TTL reuses the stamp, so an `/orchestrate --watch` idle loop and the reconcile sweep coalesce onto one fetch instead of hitting the network every cycle. Same `<n>[m]` minutes convention as the staleness knobs; a malformed value warns and falls back to the default, and the SECONDS env override `PLANWRIGHT_DISPATCH_FETCH_TTL` wins for tests. | `scripts/dispatch-fetch.sh`, `/orchestrate`, `/execute-task` |
| `review_sequence` | `[polish]` | Ordered review sequence `/execute-task`'s convergence phase runs: an inline list of nestable review-skill names (a *nestable* review skill is one invocable with `--nested`, e.g. `/polish`, `/self-review`). The default reproduces today's single `/polish --nested` convergence; an overlay can reorder or extend it. Resolved through `scripts/resolve-review-sequence.sh`; an entry naming an unknown or non-nestable skill is malformed under the REQ-E1.4 by-layer policy (degrade+warn for adopter/machine-local, hard-fail for repo-tracked) (D-6, REQ-D1.3). | `/execute-task` |
| `notification_channel` | `none` | Which channel the fleet attention/notification seam pushes through (D-13, REQ-E1.4) — the capability-vs-style split (REQ-A1.5): the notification *seam* is core, the channel *value* is overlay-owned. `none` (default) is pull-only (nothing is pushed; the operator reads the decision queue via `scripts/fleet-attention.sh queue`), dependency-free and behavior-preserving; `tmux-popup` pushes a multiplexer popup (multiplexer-user persona); `os-notify` an OS notification (non-terminal-user persona); `editor-toast` an editor toast the editor tails (editor-feedback persona); `statusline` (fleet-autonomy D-14, REQ-F1.2) renders the derived fleet stats (last-cleanup time, watchdog-trip count, throttle-engaged state) natively in the operator's own Claude Code terminal via the `statusLine` feature — the operator wires `scripts/fleet-statusline.sh` into their `settings.json` `statusLine` (see [fleet.md](fleet.md#the-statusline-channel)), and that command drives the line only when this channel is selected. It is pull-shaped (Claude Code invokes the command on its own schedule), so the `notify` push seam is a no-op for it — the stats appear via the status line, not a pushed message. Resolved through `scripts/resolve-notification-channel.sh`; a value outside the enum is malformed under the REQ-E1.4 by-layer policy (degrade+warn for adopter/machine-local, hard-fail for repo-tracked). The channel adapters live in `scripts/fleet-attention.sh` (`notify`); the `statusline` render is `scripts/fleet-stats.sh` / `scripts/fleet-statusline.sh`. | `scripts/fleet-attention.sh` (`notify`) |
| `fleet_dirty_tree_threshold` | `15m` | The grace period the dirty-tree housekeeping sweep (fleet-autonomy D-8, REQ-B1.3; `scripts/fleet-sweep.sh`) waits before escalating a working tree with uncommitted or unpushed changes — or one that could not be inspected — to the decision queue. A tree must be **continuously** in that state past this threshold before it is escalated, so an actively-worked tree is not flagged mid-edit and a transient git-lock contention self-resolves by the next sweep (kickoff risk 10). Minutes, with an optional `m` suffix (the `stale_*_threshold` convention); an absent or malformed value falls back to `15m`. Resolved through `config-get` across the overlay layers. The sweep also re-runs the `tasks.md` reconcile for every spec bundle in the tower's checkout as the REQ-A1.8 missed-push backstop; that pass and the sweep's tree scope (every tracked worker worktree plus the tower's own checkout, D-8) are not tunable here — only the grace is. | `scripts/fleet-sweep.sh` |
| `fleet_daemon_pause` | `false` | The operator kill-switch for the autonomous daemon layer (fleet-autonomy D-15, REQ-F1.3): `true` pauses every autonomous daemon action the fleet introduces (cleanup, restart, throttle) without disabling fleet operation entirely — one lever for an operator debugging something unusual; task work (towers dispatching, workers executing) is unaffected. Every daemon mechanism the bundle introduces (Tasks 2–7) is contracted to check it via `scripts/fleet-daemon-gate.sh` **before** acting, and the gate fails closed: an unresolvable switch blocks the daemon action (degrade capability, never safety). Resolved through the shared knob resolver (`scripts/resolve-config-knob.sh` → `config-get`, fleet-autonomy D-22/REQ-G1.5); a value other than `true`/`false` is malformed under the REQ-E1.4 by-layer policy (degrade+warn for adopter/machine-local, hard-fail for repo-tracked). Write authorization: the resolving layers are human-owned surfaces (the repo-tracked overlay is committed, PR-reviewed config; the adopter and machine-local overlays live outside a dispatched worker's allowlisted write set), the same human-reviewed, human-installed posture `config/worker-settings.json` has — a worker's permission allowlist must not be widened to cover them. | `scripts/fleet-daemon-gate.sh` (called by every fleet daemon mechanism) |
| `fleet_flailing_threshold` | `3` | How many consecutive no-forward-progress observations (heartbeats with an unchanged progress token) classify a still-heartbeating worker as `flailing` (fleet-autonomy D-2, REQ-A1.2), queueing the "this task may be stuck" human fork — never an automatic nudge or restart (REQ-A1.3). No mature precedent exists for this exact signal (kickoff risk row 3): the default is deliberately small because a wrong value is safe (it only shifts when the escalation fires, never loses work), and it is overlay-tunable to the fleet's real observation cadence. Resolved through the shared knob resolver (`scripts/resolve-config-knob.sh` → `config-get`, D-22/REQ-G1.5) as a positive integer; a malformed value follows the REQ-E1.4 by-layer policy. | `scripts/fleet-liveness.sh` (`classify`) |
| `fleet_hung_heartbeat_seconds` | `900` | Heartbeat age in seconds past which a worker that should be working is treated as no longer heartbeating — the `hung` boundary (fleet-autonomy D-2, REQ-A1.2). Where a death-evidence handle is available, the positive-evidence predicate (`scripts/fleet-death-evidence.sh`) corroborates before `hung` is classified, and an `unknown` verdict refuses the classification (lost observability is never death, REQ-A1.7); with no handle, elapsed time alone is the classifier's documented boundary (kickoff risk row 29). Resolved through the shared knob resolver as a positive integer; malformed values follow the REQ-E1.4 by-layer policy. | `scripts/fleet-liveness.sh` (`classify`) |
| `fleet_crash_backoff_base_seconds` | `30` | The first relaunch delay after a worker crash; each further consecutive crash doubles it, capped at 3600s (fleet-autonomy D-3, REQ-A1.4 — the PM2/supervisord exponential-backoff shape). Resolved through the shared knob resolver as a positive integer; malformed values follow the REQ-E1.4 by-layer policy. | `scripts/fleet-liveness.sh` (`crash-record`) |
| `fleet_crash_disable_threshold` | `3` | Consecutive crashes after which a worker is disabled — no further relaunch authorized — with the disable escalated as a decision-queue entry rather than restart-looping indefinitely (fleet-autonomy D-3, REQ-A1.4; the 3-strike precedent D-3 cites). A disabled worker relaunches only after a human runs `crash-reset` (the disable flag is sticky and persisted in the crash record; `crash-check` reads that flag, not this knob). Resolved through the shared knob resolver as a positive integer; malformed values follow the REQ-E1.4 by-layer policy. | `scripts/fleet-liveness.sh` (`crash-record`) |
| `tower_relaunch_backoff_base` | `60` | Base of the tower crash-loop backoff schedule, in seconds (fleet-autonomy D-4, REQ-A1.9): after `n` consecutive failed relaunches, the cron-scheduled tower-liveness watchdog (`scripts/fleet-tower-watchdog.sh`) waits `base * 2^(n-1)` seconds before the next attempt — structurally the same escalating shape as the worker crash-loop schedule (REQ-A1.4), but the tower's OWN knob, sharing no state or config with the worker one (many workers, one tower per spec; a shared counter would cross-contaminate — kickoff risk row 32). Resolved through the shared knob resolver (`scripts/resolve-config-knob.sh` → `config-get`, D-22/REQ-G1.5); anything other than a positive integer (no leading zero, ≤15 digits) is malformed under the REQ-E1.4 by-layer policy (degrade+warn for adopter/machine-local, hard-fail for repo-tracked). | `scripts/fleet-tower-watchdog.sh` |
| `tower_relaunch_disable_threshold` | `3` | Consecutive-failure threshold at which the tower-liveness watchdog disables itself (fleet-autonomy D-4, REQ-A1.9): once this many consecutive relaunch attempts have failed, the cron check stops relaunching, records a decision-queue entry (`scripts/fleet-attention.sh`), and audits the disable — escalating to a human instead of relaunch-looping on every cron tick. A tower later observed demonstrably alive (the human intervened) resets the counter and re-arms the watchdog. Same resolution and malformed-value policy as `tower_relaunch_backoff_base`. | `scripts/fleet-tower-watchdog.sh` |
| `fleet_model_execution` | `opus` | Which model the `execution` task-type tier of the fleet dispatch rule table runs (fleet-autonomy D-11, REQ-E1.1) — the `/execute-task` workhorse: judgment-heavy, routed to the strong tier. One column of the deterministic task-type-keyed table in `scripts/fleet-resource-select.sh` (`select execution` prints `<model>TAB<effort>TAB<command>`; effort and command are overlay-tunable too — see `fleet_effort_execution` / `fleet_command_execution`, fleet-autonomy Task 10 REQ-E1.8). Legal values are the stable Claude Code model aliases `fable`/`opus`/`sonnet`/`haiku` — aliases, not dated model ids, so the enum survives model releases. Resolved through the shared knob resolver (`scripts/resolve-config-knob.sh` → `config-get`, D-22/REQ-G1.5); a value outside the enum is malformed under the customization-overlay REQ-E1.4 by-layer policy (degrade+warn for adopter/machine-local, hard-fail for repo-tracked). No LLM call is ever in the resolution path (D-18); the selectable command set stays disjoint from `review_sequence`'s nestable-review-skill set (REQ-E1.2). | `scripts/fleet-resource-select.sh`, `/orchestrate` (dispatch) |
| `fleet_model_bookkeeping` | `sonnet` | Which model the `bookkeeping` task-type tier runs (fleet-autonomy D-11, REQ-E1.1) — the tower's reconcile/drain sweep pass (`/orchestrate --bookkeeping`): mechanical, mid tier. Same table, resolver, alias enum, and by-layer malformed policy as `fleet_model_execution`. | `scripts/fleet-resource-select.sh`, `/orchestrate` (dispatch) |
| `fleet_model_drain` | `sonnet` | Which model the `drain` task-type tier runs (fleet-autonomy D-11, REQ-E1.1) — the read-only gate-evaluation pass (`/drain`): the lightest tier (default `low` effort). Same table, resolver, alias enum, and by-layer malformed policy as `fleet_model_execution`. | `scripts/fleet-resource-select.sh`, `/orchestrate` (dispatch) |
| `fleet_effort_execution` | `high` | The reasoning-effort tier the `execution` task-type dispatches at (fleet-autonomy Task 10, D-24, REQ-E1.8). Task 7 fixed effort in code; Task 10 makes it overlay-tunable so the whole selection policy is operator-configurable, the shipped default preserving today's mapping. One of `low`/`medium`/`high`. Same shared resolver and by-layer malformed policy as `fleet_model_execution`; deterministic, no LLM call (D-18). | `scripts/fleet-resource-select.sh`, `/orchestrate` (dispatch) |
| `fleet_effort_bookkeeping` | `medium` | The reasoning-effort tier the `bookkeeping` task-type dispatches at (Task 10, REQ-E1.8). Same enum (`low`/`medium`/`high`), resolver, and by-layer malformed policy as `fleet_effort_execution`. | `scripts/fleet-resource-select.sh`, `/orchestrate` (dispatch) |
| `fleet_effort_drain` | `low` | The reasoning-effort tier the `drain` task-type dispatches at (Task 10, REQ-E1.8). Same enum (`low`/`medium`/`high`), resolver, and by-layer malformed policy as `fleet_effort_execution`. | `scripts/fleet-resource-select.sh`, `/orchestrate` (dispatch) |
| `fleet_command_execution` | `execute-task` | The dispatch-entry slash command the `execution` task-type runs (fleet-autonomy Task 10, D-24, REQ-E1.8). Overlay-tunable like model and effort, default-preserving. The enum is exactly the dispatch-entry set `execute-task`/`orchestrate`/`drain` — none of which is a nestable review skill — so review-sequence disjointness (REQ-E1.2) holds by **construction** at every overlay layer: a configured command outside the enum is malformed under the by-layer policy and refused. Same resolver, deterministic, no LLM call (D-18). | `scripts/fleet-resource-select.sh`, `/orchestrate` (dispatch) |
| `fleet_command_bookkeeping` | `orchestrate` | The dispatch-entry slash command the `bookkeeping` task-type runs (Task 10, REQ-E1.8). Same enum (`execute-task`/`orchestrate`/`drain`), resolver, disjointness-by-construction, and by-layer malformed policy as `fleet_command_execution`. | `scripts/fleet-resource-select.sh`, `/orchestrate` (dispatch) |
| `fleet_command_drain` | `drain` | The dispatch-entry slash command the `drain` task-type runs (Task 10, REQ-E1.8). Same enum (`execute-task`/`orchestrate`/`drain`), resolver, disjointness-by-construction, and by-layer malformed policy as `fleet_command_execution`. | `scripts/fleet-resource-select.sh`, `/orchestrate` (dispatch) |
| `fleet_throttle_default_hold` | `300` | The bounded fallback hold, in seconds, when `scripts/fleet-throttle.sh observe` detects Claude Code's native rate-limit signal but cannot parse its reset time (fleet-autonomy D-12, REQ-E1.3; kickoff risk row 24). The rate-limit prompt is version-sensitive UI text, so an unparseable rendering degrades to this hold with a warning — never an indefinite pause (an 8-day sanity ceiling bounds every engagement: the engage CLI refuses a beyond-ceiling epoch as a caller bug, and the shared path clamps), never an immediate resume, never an opaque halt. Bounded and short is the safe direction: if the limit still holds after the hold expires, the native signal re-fires and re-engages. Resolved through the shared knob resolver (`scripts/resolve-config-knob.sh` → `config-get`, D-22/REQ-G1.5); a non-positive-integer value is malformed under the customization-overlay REQ-E1.4 by-layer policy. | `scripts/fleet-throttle.sh` (`observe`) |
| `fleet_usage_read_cadence_seconds` | `300` | How often (seconds) the proactive `/usage` budget gate refreshes its cached account-usage signal, off the dispatch hot path (fleet-autonomy Task 9, D-23; REQ-E1.5). Capturing `/usage` is expensive, so the signal is scraped on this cadence and cached — never synchronously per dispatch. Must be shorter than `fleet_usage_signal_ttl_seconds` (validated: a cadence at or above the TTL would leave every read stale). Resolved through the shared knob resolver (`scripts/resolve-config-knob.sh` → `config-get`, D-22/REQ-G1.5); a non-positive-integer value is malformed under the customization-overlay REQ-E1.4 by-layer policy. | `scripts/fleet-usage-gate.sh` (`capture`/`evaluate`) |
| `fleet_usage_signal_ttl_seconds` | `900` | How long (seconds) a cached `/usage` signal stays fresh before the gate treats it as unavailable (fleet-autonomy Task 9, D-23; REQ-E1.5). A signal staler than the TTL is reported unavailable — never acted on, never a guessed number — and governance falls back to the reactive throttle backstop (REQ-E1.7). Must exceed `fleet_usage_read_cadence_seconds` (validated). Resolved through the shared knob resolver (`scripts/resolve-config-knob.sh` → `config-get`, D-22/REQ-G1.5); a non-positive-integer value is malformed under the REQ-E1.4 by-layer policy. | `scripts/fleet-usage-gate.sh` (`evaluate`) |
| `fleet_usage_sustained_loss_count` | `3` | How many consecutive read cadences the `/usage` signal may be unavailable before the **required** operator surface fires (fleet-autonomy Task 9, D-23; REQ-E1.5) — a warning escalating to an Awaiting-input hold (`scripts/fleet-attention.sh park`), so a permanently broken `/usage` parse (the D-23 version-drift fragility) is never silent. Below the count each unavailable read only warns; the hold is cleared when the kill-switch is engaged (the operator has assumed control). Resolved through the shared knob resolver (`scripts/resolve-config-knob.sh` → `config-get`, D-22/REQ-G1.5); a non-positive-integer value is malformed under the REQ-E1.4 by-layer policy. | `scripts/fleet-usage-gate.sh` (`evaluate`) |
| `fleet_usage_session_downshift` | `50` | The session-window (~5-hour) usage percentage at or above which the restriction ladder selects the `downshift` rung (fleet-autonomy Task 9, D-23/D-28; REQ-E1.6). The session window's thresholds (`fleet_usage_session_downshift` < `fleet_usage_session_reduce_concurrency` < `fleet_usage_session_defer_heavy`) must be strictly ascending and within 1–100 (validated; a non-monotonic or out-of-range set is refused). The session window is **capped at `defer-heavy`** — a session spike never proactively halts the fleet (the reactive backstop catches a genuine session wall). Deterministic `>=` comparison, never an LLM call (REQ-G1.2). Resolved through the shared knob resolver (`scripts/resolve-config-knob.sh` → `config-get`, D-22/REQ-G1.5); malformed under the REQ-E1.4 by-layer policy. | `scripts/fleet-usage-gate.sh` (`evaluate`) |
| `fleet_usage_session_reduce_concurrency` | `70` | The session-window usage percentage at or above which the ladder selects the `reduce-concurrency` rung (fleet-autonomy Task 9; REQ-E1.6). Same session threshold set, ascending-and-in-range validation, `defer-heavy` cap, deterministic comparison, resolver, and by-layer malformed policy as `fleet_usage_session_downshift`. | `scripts/fleet-usage-gate.sh` (`evaluate`) |
| `fleet_usage_session_defer_heavy` | `85` | The session-window usage percentage at or above which the ladder selects the `defer-heavy` rung — heavy/opus units wait while cheaper units still dispatch (fleet-autonomy Task 9; REQ-E1.6). This is the session window's ceiling rung (the cap). Same session threshold set, validation, deterministic comparison, resolver, and by-layer malformed policy as `fleet_usage_session_downshift`. | `scripts/fleet-usage-gate.sh` (`evaluate`) |
| `fleet_usage_weekly_downshift` | `60` | The weekly-window usage percentage at or above which the ladder selects the `downshift` rung (fleet-autonomy Task 9, D-23/D-28; REQ-E1.6). The weekly window's thresholds (`fleet_usage_weekly_downshift` < `fleet_usage_weekly_reduce_concurrency` < `fleet_usage_weekly_defer_heavy` < `fleet_usage_weekly_defer_all`) must be strictly ascending and within 1–100 (validated). Unlike the session window, the weekly window **can reach `defer-all`** — hitting the weekly wall can halt work for days, so it escalates harder and earlier. The **more restrictive** of the two windows governs. Deterministic `>=` comparison (REQ-G1.2); resolved through the shared knob resolver; malformed under the REQ-E1.4 by-layer policy. | `scripts/fleet-usage-gate.sh` (`evaluate`) |
| `fleet_usage_weekly_reduce_concurrency` | `75` | The weekly-window usage percentage at or above which the ladder selects the `reduce-concurrency` rung (fleet-autonomy Task 9; REQ-E1.6). Same weekly threshold set, ascending-and-in-range validation, deterministic comparison, resolver, and by-layer malformed policy as `fleet_usage_weekly_downshift`. | `scripts/fleet-usage-gate.sh` (`evaluate`) |
| `fleet_usage_weekly_defer_heavy` | `85` | The weekly-window usage percentage at or above which the ladder selects the `defer-heavy` rung — heavy/opus units wait while cheaper units still dispatch (fleet-autonomy Task 9; REQ-E1.6). Same weekly threshold set, validation, deterministic comparison, resolver, and by-layer malformed policy as `fleet_usage_weekly_downshift`. | `scripts/fleet-usage-gate.sh` (`evaluate`) |
| `fleet_usage_weekly_defer_all` | `95` | The weekly-window usage percentage at or above which the ladder selects the fleet-critical `defer-all` rung — no new dispatch is admitted, heavy or cheap (fleet-autonomy Task 9; REQ-E1.6). Only the weekly window can reach this rung; the session window is capped a rung below at `defer-heavy`. Same weekly threshold set, validation, deterministic comparison, resolver, and by-layer malformed policy as `fleet_usage_weekly_downshift`. | `scripts/fleet-usage-gate.sh` (`evaluate`) |
| `fleet_usage_rung_min_dwell_seconds` | `300` | The minimum dwell (seconds) before a **descend** (relaxing) rung transition on the restriction ladder (fleet-autonomy Task 10, D-25/D-28; REQ-E1.10). A rung is held at least this long after its last transition before it may step down, so a `/usage` read oscillating across a threshold cannot flap the ladder down-and-up. A **climb** (more restrictive) is always immediate — restriction degrades capability only and never a safety floor (fast-attack/slow-release). The dwell basis is the audit-derived last-transition timestamp (D-28), so a memoryless relaunch computes the same window. Positive integer; resolved through the shared knob resolver (`scripts/resolve-config-knob.sh` → `config-get`); malformed under the REQ-E1.4 by-layer policy. | `scripts/fleet-usage-gate.sh` (`evaluate`) |
| `fleet_usage_unavailable_grace_seconds` | `1800` | The grace window (seconds) during which the ladder **holds** its last-known rung while the `/usage` signal is unavailable, before **decaying** to `normal` (fleet-autonomy Task 10, D-25/D-28; REQ-E1.10). A transient scrape failure must not relax restriction; a persistent one must not pin the fleet at a restrictive rung forever against a signal it can no longer read. Measured from the audit-derived last-transition timestamp; the decay is edge-triggered and lock-serialized like any transition. The reactive backstop (REQ-E1.7) remains the floor throughout. Positive integer; same resolver and by-layer malformed policy as `fleet_usage_rung_min_dwell_seconds`. | `scripts/fleet-usage-gate.sh` (`evaluate`) |
| `fleet_downshift_model` | `sonnet` | The most capable model a **routine** (non-reserved) unit may run once the restriction ladder is at `downshift` or a heavier rung (fleet-autonomy Task 10, D-24/D-25; REQ-E1.10) — a **clamp**: `scripts/fleet-allocate.sh resolve` picks the cheaper of the unit's base model and this cap, so a unit already at or below it is unchanged. Degrades **capability only** — never below a full session-grade worker (the value is a real Claude Code alias `fable`/`opus`/`sonnet`/`haiku`), never relaxing the determinism floor (REQ-G1.2), never engaging `--permission-mode auto` (REQ-E1.4). A **reserved** unit is exempt (keeps its base tier through `downshift`/`defer-heavy`, yields at `defer-all`). Resolved through the shared knob resolver; malformed under the REQ-E1.4 by-layer policy. | `scripts/fleet-allocate.sh` (`resolve`) |
| `fleet_downshift_effort` | `medium` | The highest reasoning effort a routine unit may run at once the ladder is at `downshift` or heavier (Task 10, REQ-E1.10) — a clamp like `fleet_downshift_model`, one of `low`/`medium`/`high`. Same resolver and by-layer malformed policy. | `scripts/fleet-allocate.sh` (`resolve`) |
| `fleet_concurrency_normal` | `3` | The maximum simultaneous workers at the `normal`/`downshift` rungs (fleet-autonomy Task 10, REQ-E1.10). The full concurrency limit; the ladder drops to `fleet_concurrency_reduced` at `reduce-concurrency` and heavier. Positive integer; same resolver and by-layer malformed policy. | `scripts/fleet-allocate.sh` (`resolve`) |
| `fleet_concurrency_reduced` | `1` | The maximum simultaneous workers at `reduce-concurrency` and every heavier rung (Task 10, REQ-E1.10) — the reduced concurrency the `reduce-concurrency` rung selects. Positive integer; same resolver and by-layer malformed policy as `fleet_concurrency_normal`. | `scripts/fleet-allocate.sh` (`resolve`) |
| `fleet_cap_fable` | `55` | The account-global usage percentage (1–100) at or above which the `fable` tier is withdrawn from **routine** units (fleet-autonomy Task 10, D-24; REQ-E1.9) — a per-tier **global-usage threshold**, never per-model accounting or a reservation ledger (E1.5 forbids both). As the most expensive tier, `fable` ships the **lowest** threshold, so it drops out for routine work soonest; a withdrawn routine unit falls to the next cheaper tier. A **reserved** unit is exempt (REQ-E1.10). The cap is a stateless `>=` read against the more-restrictive of the two `/usage` windows and is **inactive when the signal is unavailable**. Validated 1–100; same resolver and by-layer malformed policy. | `scripts/fleet-allocate.sh` (`resolve`) |
| `fleet_cap_opus` | `70` | The global-usage threshold at or above which the `opus` tier is withdrawn from routine units (Task 10, REQ-E1.9). Below `fleet_cap_sonnet` so opus (more expensive) drops out before sonnet does. Same stateless-threshold semantics, reserved-unit exemption, unavailable-signal inactivity, validation, resolver, and by-layer malformed policy as `fleet_cap_fable`. | `scripts/fleet-allocate.sh` (`resolve`) |
| `fleet_cap_sonnet` | `90` | The global-usage threshold at or above which the `sonnet` tier is withdrawn from routine units (Task 10, REQ-E1.9). Higher than `fleet_cap_opus` (cheaper tiers withdraw later). Same semantics as `fleet_cap_fable`. | `scripts/fleet-allocate.sh` (`resolve`) |
| `fleet_cap_haiku` | `100` | The global-usage threshold at or above which the `haiku` tier is withdrawn from routine units (Task 10, REQ-E1.9). The default `100` means the cheapest, session-grade-floor tier is effectively **never** withdrawn — there is nothing cheaper to fall to. Same semantics as `fleet_cap_fable`. | `scripts/fleet-allocate.sh` (`resolve`) |
| `fleet_credit_continuation_spend` | `false` | Whether the fleet may spend credits to continue past the rate-limit wall (fleet-autonomy D-27, REQ-E1.11). When Claude Code's wall offers a credit-continuation prompt ("spend credits / extra usage to continue past the limit"), `scripts/fleet-credit-continuation.sh decide` — invoked on the same captured wall text the reactive throttle reads — decides how the fleet responds. The shipped default `false` is **decline and wait** for the window to reset (the REQ-E1.7 reactive backstop): auto-spend is **never** the default, since an unattended, unbounded money spend is an irreversible action planwright reserves for the operator, exactly like merge (a runaway loop could spend without limit). Set `true` only to explicitly permit spending credits to keep the fleet moving. A spend-**avoidance** knob, not a spend-accounting ceiling (Out of scope): it neither meters nor caps spend, it only refuses to incur it without opt-in. The decision is a deterministic reaction to the detected prompt (no LLM call — REQ-G1.2), audit-logged (`scripts/fleet-audit.sh`, mechanism `credit-continuation`) and kill-switch-gated (`fleet_daemon_pause`); an unrecognized wall variant is a clean no-op that falls through to the plain reactive backstop, never an accidental spend. Resolved through the shared knob resolver (`scripts/resolve-config-knob.sh` → `config-get`, D-22/REQ-G1.5); a value other than `true`/`false` is malformed under the REQ-E1.4 by-layer policy (degrade+warn for adopter/machine-local, hard-fail for repo-tracked). Write authorization: the resolving layers are the same human-owned surfaces `fleet_daemon_pause` uses — a worker's permission allowlist must not be widened to cover them. | `scripts/fleet-credit-continuation.sh` (`decide`) |
| `require_signed_tags` | `auto` | Signing policy for the release tag (autopilot-reflex D-4, REQ-D1.4). `auto` (default) signs when the repo has signing configured (`git user.signingkey`), otherwise creates an annotated **unsigned** tag with a warning; `require` refuses to tag unless signing is configured and succeeds; `never` always creates an unsigned annotated tag. Signing is delegated entirely to the repo's git config (`gpg.format`, `user.signingkey`, `gpg.ssh.program`) — no signer is named in the script and no signing material enters CI (a signed tag is verified with `git tag -v` before push). planwright's own repo sets `require` via its repo-tracked overlay ([`.claude/planwright.yml`](../.claude/planwright.yml)). **Cross-machine / fresh-clone resume caveat:** on a partial-publish resume the origin tag's signature is re-verified against the *resuming* machine's local trust store, so under `require` (and under `auto` when the origin tag is signed) resuming on a machine that does not trust the tag's signer will refuse fail-closed — `git verify-tag` cannot validate a signature without the signer's key locally (for SSH, present in the file named by `gpg.ssh.allowedSignersFile`). This is inherent to signature verification, not a policy knob: to resume elsewhere, add the signer to the resuming machine's local trust store, or set `never`. See [`doctrine/release-tagging.md`](../doctrine/release-tagging.md) and [`docs/getting-started.md`](getting-started.md) ("Releasing planwright"). | `scripts/release-publish.sh` |
| `require_ci` | `true` | Whether `scripts/release-publish.sh` requires positive GitHub CI confirmation on the release commit before publishing (release-hardening D-7, REQ-G1.3). `true` (default) preserves the strict gate: only a `green` verdict publishes, and a `none` verdict ("no positive CI confirmation" — no checks at all, only the excluded release-window lock, or every non-excluded check `NEUTRAL`/`SKIPPED`) refuses like every other non-green verdict. `false` relaxes **only** that `none` verdict to allow publish (all three `none` sub-cases), emitting a stderr diagnostic naming `require_ci=false` so the deliberate relaxation leaves an audit signal; a **failing**, still-**pending**, unread-**overflow** (>100 checks), or **CI-query-failure** (infra outage) verdict stays fail-closed regardless. The value is validated as a boolean when the knob is read (unconditional of code path, symmetric with `require_signed_tags`); a non-boolean value is a clean fail-closed configuration error. Lands in core as an opt-in with the default unchanged — the specific value is the adopter's overlay choice (an adopter with no CI sets `false` to publish without it). | `scripts/release-publish.sh` |
| `version_file` | `.claude-plugin/plugin.json::$.version` | Where the version of truth lives (autopilot-reflex D-13, REQ-D1.6), as `<path>::<selector>`. The selector is `$.<key>` for a top-level JSON string field (extracted with `jq`, the key passed as **data** via `--arg`, never executed) or absent for a whole-file plain `VERSION` file. The `::` separator (not `#`) avoids `config-get`'s YAML `#comment` stripping. The default reads `$.version` from the plugin manifest; override it to point at another top-level JSON string field (e.g. a `package.json`'s `$.version`) or, with no selector, a plain `VERSION` file. | `scripts/release-publish.sh`, `scripts/release-pending.sh` |
| `instruction_budget_skill_warn` | `3000` | Warn floor for a `skills/*/SKILL.md` body's word count, the secondary per-file signal (prompt-hygiene D-1, D-2). Boundary-inclusive (`>=`). A body at or over this but under the error floor is reported as a warning without failing the check. Grounded in the 500-line SKILL.md ceiling × measured `skills/` density (~8.5 words/line), recorded in prompt-hygiene D-2. | `scripts/check-instructions.sh` |
| `instruction_budget_skill_error` | `4250` | Error floor for a `skills/*/SKILL.md` body's word count (prompt-hygiene D-1, D-2). Boundary-inclusive (`>=`). A body at or over this fails the check unless a permanent exemption or a transitional `pending-diet` allowance covers it (REQ-B1.3). Derived from the 500-line ceiling × `skills/` density. | `scripts/check-instructions.sh` |
| `instruction_budget_doctrine_warn` | `2500` | Warn floor for a `doctrine/*.md` rule-doc word count (prompt-hygiene D-1, D-2). Boundary-inclusive (`>=`). `doctrine/README.md` is excluded from the per-file walk (an index, REQ-A1.1). Grounded in the 500-line ceiling × measured `doctrine/` density (~7.9 words/line). | `scripts/check-instructions.sh` |
| `instruction_budget_doctrine_error` | `4000` | Error floor for a `doctrine/*.md` rule-doc word count (prompt-hygiene D-1, D-2). Boundary-inclusive (`>=`). Over-floor fails the check unless a permanent exemption or transitional `pending-diet` allowance covers it (REQ-B1.3). | `scripts/check-instructions.sh` |
| `instruction_budget_startload_warn` | `8000` | Warn threshold for a skill's mandatory-at-start load — the SKILL.md body plus every rule doc its doctrine manifest marks `run-start` — the **primary** budget (prompt-hygiene D-1). Boundary-inclusive (`>=`). What degrades instruction-following is total instruction context per invocation, so a fat run-start doc charges every skill that front-loads it. | `scripts/check-instructions.sh` |
| `instruction_budget_startload_error` | `10000` | Error threshold for a skill's mandatory-at-start load (prompt-hygiene D-1). Boundary-inclusive (`>=`). A start-load offender can be carried only by a transitional `pending-diet` allowance, never a permanent exemption (REQ-B1.3); its fix is a diet or point-of-use reclassification (REQ-D1.1). | `scripts/check-instructions.sh` |
| `instruction_budget_closure_warn` | `15000` | Warn threshold for a skill's reachable closure — the start-load plus every rule doc its manifest defers to `point-of-use` (prompt-hygiene D-1). Boundary-inclusive (`>=`). The loose budget: point-of-use bulk is read only when its path is taken. | `scripts/check-instructions.sh` |
| `instruction_budget_closure_error` | `20000` | Error threshold for a skill's reachable closure (prompt-hygiene D-1). Boundary-inclusive (`>=`). A closure offender can be carried only by a transitional `pending-diet` allowance, never a permanent exemption (REQ-B1.3b); its fix is a content diet. | `scripts/check-instructions.sh` |
| `instruction_budget_skill_floor` | `250` | Headroom floor for a `skills/*/SKILL.md` body: the minimum word margin to its error threshold before the restoration ladder triggers (instruction-headroom D-2, REQ-A1.1). A surface whose margin is strictly below its floor is a named floor-breach **warning** on every guard run (never an error); at or above the floor but below twice it (the restoration target, D-3) is a named below-target warning. A missing or non-numeric floor knob aborts the guard fail-loud, like the budget knobs. Protective — excluded from the raise-rationale rule by suffix. | `scripts/check-instructions.sh` |
| `instruction_budget_doctrine_floor` | `250` | Headroom floor for a `doctrine/*.md` rule-doc word count (instruction-headroom D-2, REQ-A1.1). Same breach/below-target semantics and fail-loud discipline as `instruction_budget_skill_floor`. A permanently exempt doc (an `exempt` suppression entry) carries no headroom floor — its per-file budget is already waived (REQ-D1.1). | `scripts/check-instructions.sh` |
| `instruction_budget_startload_floor` | `500` | Headroom floor for a skill's mandatory-at-start load — the primary budget (instruction-headroom D-2, REQ-A1.1). Same breach/below-target semantics and fail-loud discipline as `instruction_budget_skill_floor`; restoration target is twice this (1,000 words). | `scripts/check-instructions.sh` |
| `instruction_budget_closure_floor` | `1000` | Headroom floor for a skill's reachable closure (instruction-headroom D-2, REQ-A1.1). Same breach/below-target semantics and fail-loud discipline as `instruction_budget_skill_floor`; restoration target is twice this (2,000 words), sized to absorb the full observed 80–560-word routine addition range (D-3). | `scripts/check-instructions.sh` |
| `instruction_budget_injected_warn` | `200` | Warn floor for the **static** injected prose a `hooks.json`-registered hook contributes via `additionalContext` / `hookSpecificOutput` (prompt-hygiene REQ-A1.4, REQ-B1.7, D-13). Boundary-inclusive (`>=`). Warn-only: this partly-dynamic per-session surface is always reported as an audit row but **never fails** the check (a hook whose static prose cannot be extracted is a parse-failure warning, not an error). Grounded in the ~40-word present surface plus headroom. | `scripts/check-instructions.sh` |
| `instruction_manifest_completeness_required` | `true` | When `true`, every `skills/*/SKILL.md` must declare a doctrine manifest; a manifest-less skill is an error (prompt-hygiene REQ-A1.2, D-3). Wired at Task 3, when the manifests land — it stops a future manifest-less skill from silently under-reporting its start-load. Distinct from the scoring rule (a manifest-less skill is still scored body-only) and the malformed-manifest error (a present-but-non-boolean value here is itself fail-loud, REQ-B1.8). Absent in every layer it defaults off, so an adopter not yet on the manifest convention is not forced into it. | `scripts/check-instructions.sh` |
